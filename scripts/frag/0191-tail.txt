
-- ============================================================================
-- === ПІСЛЯ НАКАТУ ===
-- ============================================================================
-- ⚠️ ПОРЯДОК: повна ревізія 37 стендів робиться на ЧИСТОМУ ДЕРЕВІ й бази не
--    потребує — тож вона йде ДО накату, а не після (канон 0190).
-- 1. select public.invariants_check(false);  → ok:true, checked:23, failed:[]
--    ⚠️ offender `attrs:…->…;acl=…` означає, що права виконання в проді НЕ ті,
--       що заміряні 13.09. Це НЕ шум: саме заради цього рядок і додано. Не
--       «підганяти» пін під базу — назвати розбіжність і зʼясувати, хто і коли
--       видав/зняв грант.
--    ⚠️ offender `body:auth_can_refer…` або `body:auth_referrer_visible_rooms…`
--       означає, що вирішувач направниківської гілки переписано.
-- 2. Тіло сторожа (обидва рецепти):
--    select md5(replace(prosrc, chr(13), '')) as raw,
--           length(replace(prosrc, chr(13), '')) as len,
--           md5(btrim(regexp_replace(replace(prosrc, chr(13), ''),
--                                    '[[:space:]]+', ' ', 'g'))) as norm
--      from pg_proc p join pg_namespace n on n.oid = p.pronamespace
--     where n.nspname = 'public' and p.proname = 'invariants_check';
-- 3. Леджер: 191 рядок, серед них '0191_fn_bodies_acl.sql'.
-- 4. ЧЕРВОНІ БАЗИСИ — у самому накаті, у транзакції з відкотом, ПʼЯТЬ штук:
--    ДВА напрямки прав і ТРИ тіла.
--    ⚠️ Тут стояло «ТРИ штуки» і перелік із трьох: проза писалась ДО того, як
--    ревʼю Н9 додало базис (а2), і не була оновлена. Число відстало від коду
--    на два і мовчало про це. Звірено з `scripts/frag/0191_apply.sql` §7.
--    (а)  ЗВУЖЕННЯ прав: `revoke execute on function public.auth_can_refer(uuid)
--         from authenticated` мусить дати `attrs:auth_can_refer(c uuid)->…;acl=…`
--         — тобто саме те, чого сторож НЕ бачив до цієї міграції. Базис дешевий:
--         грант PUBLIC лишається, тож каталог змінюється, а поведінка — ні;
--    (а2) РОЗШИРЕННЯ прав: `grant execute … room_busy_slots … to anon` —
--         небезпечний напрямок, якого (а) не перевіряє взагалі;
--    (б)(в)(г) тіла ТРЬОХ нових пінів — `auth_can_refer`,
--         `auth_referrer_visible_rooms`, `auth_referrer_clinics` — псуються
--         коментарем УСЕРЕДИНІ тіла → `body:<підпис>->…`.
--    Без цих пʼяти кроків «зелено» означає лише «пін збігся», а не «пін ловить».
-- 5. npm run db:gate   ← інакше рядок у ledger лишиться без md5.
-- 6. npm test — очікувані зміни лише в тестах, що читають ОСТАННІЙ передрук.
-- ============================================================================
-- === ВІДКАТ ===
-- ============================================================================
-- ⚠️ Відкат повертає стан, у якому права виконання 33 функцій не стереже
--    НІЩО, а ТРИ вирішувачі направниківської гілки знову не піновані №19.
--    (Тіла всіх трьох тримає ще й №22 `grant_digest` — усі три anon-досяжні;
--    без пінів №19 лишається незахищеним ЗНАЧЕННЯ їхнього `search_path`.)
--    Це відкат ДОСТУПНОЇ ПОВЕРХНІ, а не косметики.
--
-- ⚠️ ПОРЯДОК НИЖЧЕ ВИКОНАВЧИЙ, а не тематичний: кроки 1–3 роблять дерево
--    зеленим ДО того, як зникне файл. Хто піде інакше — отримає червоний
--    `npm test` і червоний `npm run build` у проміжку.
--   1. прогнати `scripts/frag/0191_rollback.sql` — дзеркало накату з тими
--      самими асертами (предстан → результат 9680c291…/112207, сторож
--      зелений, другий замір із КАТАЛОГУ). Він же знімає рядок леджера. Без
--      нього крок 4 дає «НЕМАЄ ФАЙЛА: 0191… є в migration_ledger, але
--      відсутня на диску» і валить `npm run build` (гейт симетричний);
--   2. tests/guardFnBodiesInvariant.test.ts — прибрати ТРИ сигнатури зі
--      списку PINNED (`auth_can_refer(c uuid)`,
--      `auth_referrer_visible_rooms()`, `auth_referrer_clinics()`) і ТРИ
--      (не одне!) місця про `;acl=`: тест «`;acl=` стоїть у КОЖНОМУ рядку
--      списку», поле `";acl="` у переліку НАЗВАНИХ полів і тест «ACL входить
--      в attrs: `collate "C"` і ДВІ гілки». Будь-яке залишене місце червонить
--      `npm test` на 0190, де `;acl=` немає взагалі;
--   3. стенди (інакше `latestReprint()` поїде на 0190 і три стенди стануть
--      червоними з «ЯКІР НЕ УНІКАЛЬНИЙ (0)»):
--      а) повернути ЧОТИРИ якорі списку №19 у scripts/falsify-0181.mjs (два),
--         falsify-0182.mjs і falsify-0183.mjs на текст БЕЗ `;acl=`;
--      б) прибрати мутації B1–B4 з falsify-0181.mjs і повернути
--         `EXPECTED_RED` з 12 на 8 — вони адресують рядки, яких без 0191 у
--         списку немає;
--      в) `expect` мутації A1 у falsify-0183.mjs лишити на
--         `/підпис sched_override_read/` — цей рядок у списку є й до 0191;
--   4. аж ТЕПЕР видалити цей файл і перезапустити `npm run db:gate`;
--   5. `npm test` — має бути ЗЕЛЕНИЙ. Червоний тут означає неповний крок 2
--      або 3, а не дефект відкату;
--   6. зняти згадки пінів `0a178709…` / `5f3226aa…` / `ef77618a…` з `claude/*`
--      і `docs/audit/*` або позначити їх як відкочені.
--
-- ⚠️ Цифра «Список став 33 функції.» при відкаті повертається до «30».
-- ============================================================================
