
-- ============================================================================
-- === ПІСЛЯ НАКАТУ ===
-- ============================================================================
-- ⚠️ ПОРЯДОК: повна ревізія 37 стендів робиться на ЧИСТОМУ ДЕРЕВІ й бази не
--    потребує — тож вона йде ДО накату, а не після (канон 0190).
-- 1. select public.invariants_check(false);  → ok:true, checked:23, failed:[]
--    ⚠️ offender `attrs:…->…;acl=…` означає, що права виконання в проді НЕ ті,
--       що заміряні 13.09. Це НЕ шум: саме заради цього рядок і додано. Не
--       «підганяти» пін під базу — назвати розбіжність і зʼясувати, хто і коли
--       видав/зняв грант.
--    ⚠️ offender `body:auth_can_refer…` або `body:auth_referrer_visible_rooms…`
--       означає, що вирішувач направниківської гілки переписано.
-- 2. Тіло сторожа (обидва рецепти):
--    select md5(replace(prosrc, chr(13), '')) as raw,
--           length(replace(prosrc, chr(13), '')) as len,
--           md5(btrim(regexp_replace(replace(prosrc, chr(13), ''),
--                                    '[[:space:]]+', ' ', 'g'))) as norm
--      from pg_proc p join pg_namespace n on n.oid = p.pronamespace
--     where n.nspname = 'public' and p.proname = 'invariants_check';
-- 3. Леджер: 191 рядок, серед них '0191_fn_bodies_acl.sql'.
-- 4. ЧЕРВОНИЙ БАЗИС — у самому накаті, у транзакції з відкотом, ТРИ штуки:
--    (а) `grant execute on function public.auth_can_refer(uuid) to anon` уже
--        є, тож беремо зворотний бік: `revoke execute … from authenticated`
--        мусить дати `attrs:auth_can_refer(c uuid)->…;acl=…` — тобто саме те,
--        чого сторож НЕ бачив до цієї міграції;
--    (б) тіло `auth_can_refer` псується коментарем → `body:auth_can_refer…`;
--    (в) те саме для `auth_referrer_visible_rooms`.
--    Без цих трьох кроків «зелено» означає лише «пін збігся», а не «пін ловить».
-- 5. npm run db:gate   ← інакше рядок у ledger лишиться без md5.
-- 6. npm test — очікувані зміни лише в тестах, що читають ОСТАННІЙ передрук.
-- ============================================================================
-- === ВІДКАТ ===
-- ============================================================================
-- ⚠️ Відкат повертає стан, у якому права виконання 32 функцій не стереже
--    НІЩО, а обидва вирішувачі направниківської гілки знову не піновані.
--    Це відкат ДОСТУПНОЇ ПОВЕРХНІ, а не косметики.
--
-- Виконуваний фрагмент: scripts/frag/0191_rollback.sql — дзеркало накату з
-- тими самими асертами (предстан → результат 9680c291…/112207, сторож
-- зелений, другий замір із КАТАЛОГУ).
--
-- ⚠️ ВІДКАТ У РЕПОЗИТОРІЇ (без цього дерево бреше про базу):
--   1. видалити цей файл і перезапустити `npm run db:gate`;
--   2. прибрати ДВІ сигнатури зі списку PINNED у
--      tests/guardFnBodiesInvariant.test.ts і пін поля `;acl=`;
--   3. `npm test` — упадуть саме ті тести, що читають останній передрук;
--   4. зняти згадки пінів `0a178709…` / `5f3226aa…` з `claude/*` і
--      `docs/audit/*` або позначити їх як відкочені.
--
-- ⚠️ Цифра «Список став 32 функцій.» при відкаті повертається до «30».
-- ============================================================================
