# Результаты аудита RadFlow — сводка на 22.09.2026

**Что это.** Одна страница вместо 267 КБ журнала. Полный ход аудита — в
[`RADFLOW_DEEP_TECHNICAL_FUNCTIONAL_AUDIT_2026-08-27.md`](RADFLOW_DEEP_TECHNICAL_FUNCTIONAL_AUDIT_2026-08-27.md)
(там же раздел «Вердикт 6.1»), план работ — в
[`PLAN-audit-completion-2026-09-13.md`](PLAN-audit-completion-2026-09-13.md),
список предпродакшена — в [`ToDo_Production.md`](ToDo_Production.md).
Эта сводка их **не заменяет** и в спорных местах уступает им: источник правды —
код `dev` и прод, а не документ.

**Состояние прода в этой сводке сверено запросом 22.09.2026 18:23 UTC**, а не
переписано из предыдущих редакций.

---

## 1. Вердикт

**6.1 — CONDITIONAL GO**, вынесен **18.09.2026 (сессия 75)** по журналу редакции
2026-08-27. Базовая линия вердикта — 18.09, 18:07 UTC, сверена запросами к проду;
репозиторий на тот момент `main d4f1eb3` = `dev 33c29c8`.

Формула, дословно:

> RadFlow **может принимать первый реальный центр после выполнения блокирующих
> пунктов `ToDo_Production.md` §1** — и в порядке, описанном там же в §4.

Потолок вердикта — не следствие незакрытых работ. Он **назначен владельцем ещё в
с51**: ротация service-role ключа сознательно привязана к реальному продакшену.
По плану на 18.09 **работы агента не осталось ни в одном блоке**, кроме пункта
4.3, который ждёт домена.

**Чего вердикт не утверждает.** Он не говорит, что дефектов нет, — он говорит,
что все найденные либо закрыты, либо имеют письменную диспозицию владельца с
критерием пересмотра. Он не покрывает нагрузку реального центра (Н-3 снимается
только на первом клиенте) и не покрывает пути, которые аудит не рассматривал, —
см. П-6 ниже, находку, которой в модели угроз нет вовсе.

---

## 2. Условия, снимающие «CONDITIONAL»

| # | Что | Почему блокирует | Цена |
|---|---|---|---|
| **П-1** | Ротация service-role ключа **и `CRON_SECRET` в одно окно** (решение Р74-7 от 15.09) | ключ жил в разработке — его видели логи и окружения | 30 мин + окно деплоя |
| **П-2** | Регистрация домена | блокирует I-5 и верификацию Google | вне оценки |
| **П-3** | Google OAuth `Testing` → `In production` + переподключить оба центра | в `Testing` refresh-токен живёт **ровно 7 суток** (замерено дважды) | 15 мин + верификация отдельно |
| **П-6** | 🆕 **Смена пароля и почты** — замер и закрытие (решение владельца 22.09, с76) | тихий постоянный захват аккаунта, см. §4 | замер 1–2 ч, потом решение |

**Порядок выхода** (ToDo §4): домен → П-1 в окно деплоя со сверкой `/api/build` →
П-3 с переподключением → через 8 суток замер приёмки по `important_events` и
проверке №13 → перечитать принятые риски §2 и подтвердить либо отменить каждый.
П-6 встаёт перед П-1: тумблеры Auth трогаются в том же окне, что и ключ.

**Отдельно — правила, а не разовые дела.** На **каждое** реальное восстановление:
**П-4а** вернуть `auth`-настройки руками («Auth settings & API keys» не
переносятся), **П-4б** гасить cron первым действием и только `cron.alter_job`
(`update cron.job` не работает), **П-4в** добить миграции и смотреть на
`checked`, а не только на `ok` — восстановленный клон может быть зелёным по
урезанному набору проверок.

---

## 3. Что закрыто

**Фазы аудита 0–8** (диспозиция §6.1.2 журнала, даты — по фактическому закрытию):

| Фаза | Статус | Чем закрыта |
|---|---|---|
| 0 Базовая линия | ✅ | таблица §6.1.1, 18.09 |
| 1 Архитектура и модель угроз | ✅ 15.09 (с74) | `THREAT_MODEL_2026-09-14.md` §9; T1/T8 — 0201, T2 — 0196, T3-бис — 0197, T5 — Р74-7, T7 — репетиция 3.2 |
| 2 Тулчейн, зависимости, секреты | ✅ (с50) | `PR-F2-toolchain-deps-secrets.md` |
| 3 БД и безопасность | ✅ по существу | миграции 0171–0202, сторож из 26 проверок |
| 4 Конкурентность, realtime, время | ✅ 15.09 (с73) | Г1-E/F/G и Ф4-8 живьём, `LIVE-2026-09-15-*` |
| 5 API и интеграции | 🔴 **открыта до домена** | работы агента нет; весь остаток — I-5 |
| 6 Функциональный E2E по ролям | ✅ по форме (с55) + живьём под регистратором 15.09 | `LIVE-2026-09-15-registrar.md`, 13/13 гейтов |
| 7 UX и доступность | ✅ 18.09 (с75) | живая сессия NVDA, 5/5 PASS |
| 8 Производительность и восстановление | ✅ | репетиция 15.09, миграция 0202, модель нагрузки 3.5 |

**Пакеты миграций сверх плана:** 0193 (I-8), 0194 (I-7 — сторож стал видеть
аварийно выключенное зеркало), 0195, 0196 (`search_path` значением: слабых
definer 34 → 0), 0197 (Н-6/T3-бис: `auth.users` 14 → 9, сирот 0, +проверка №24),
0198 (Н-1/М-4: самопин сторожа, +№25), 0199, 0200 (+3 подписи в №19), 0201
(+16 подписей, +№26 `role_surface`), 0202 (фаза 2 таймзон).

**Доступность.** High **W-1/W-2/W-3** — closed 18.09 живой сессией с NVDA
(eSpeak NG uk, Chrome, штамп прода `4996dccaf2db`): шаги 3, 5, 14, 16, 18 —
**5/5 PASS**. Пакет Medium **W-4…W-13, W-24** и Low **W-17, W-25** — **в коде и
на проде с 19.09**, но до живой проверки их статус «в коде», а не «closed».

**П-4 репетиция восстановления** — ✅ 15.09: отдельный проект из суточного
снимка за **5 мин 12 с**, вход владельца живым паролем, клон удалён, прод не
изменился ни на строку, стоимость **$0**, критериев приёмки 7/7.
**П-5 leaked-password** — ✅ 15.09, тумблер оказался включён (неопределённость
снята замером, а не рассуждением).

---

## 4. Что открыто

**🔴 П-6 — смена пароля и почты.** Найдено 15.09 (план §3), решение владельца
22.09: **блокирующий пункт**. `Secure email change` выкл, `Secure password
change` выкл, `Require current password when updating` выкл, минимальная длина
пароля 6. Склейка: живая сессия → смена пароля без знания старого → смена почты с
подтверждением **только на новом адресе** → молчаливый постоянный захват
аккаунта. **В модели угроз этот путь не рассматривается вовсе** — это не
«известный принятый риск», а пробел покрытия. До 22.09 пункта не было ни в
`ToDo_Production.md`, ни в `THREAT_MODEL_2026-09-14.md`.

**🔴 I-5 (Medium)** — единственная находка, пережившая вердикт как **работа**, а
не как риск: нестабильность `system` / `fullUrl` / `link.next` при смене домена.
2–3 часа после П-2.

**🟡 Р2 — правила аудит-триггеров.** Решение есть с 13.09 («таблица с ПИИ ⇒
триггер»), **не исполнено**. Замер 22.09: `fn_audit` висит на шести таблицах
(`profiles`, `queue_entries`, `waitlist_entries`, `referral_access`, `ceo_access`,
`incidents`); под критерий подпадают и остались без триггера **`patient_cases`**
(полная карточка пациента), **`doctors`**, **`referrer_private`**.
⚠️ **Критерий не покрывает случай, из-за которого его приняли:** повод — RF-03
(37 позиций `services` изменены 26.08 в одну миллисекунду, восстановить нечем), а
в `services` персональных данных нет вообще; замер 22.09 добавляет, что в
`important_events` **нет ни одного** события про каталог, услуги, цены и импорт —
следа нет в ОБОИХ журналах. Оценка **4–6 ч**, а не прежние «1,5–2 ч»: новые
`trg_audit_*` меняют именной список №17 (сегодня 23 определения триггеров),
значит это перепечатка сторожа, самопин №25 и полная ревизия стендов, чья
постоянная цена одна уже ~1,5 ч. Обратная сторона исполнения: `fn_audit` пишет
ПОЛНЫЙ снимок строки, то есть триггер на `patient_cases` копирует ПИИ пациентов
в `audit_log` (обезличивание — только через 90 дней по 0149).

**🟡 Н-3 нагрузочный профиль** — модель построена
([`LOAD-PROFILE-2026-09-15.md`](LOAD-PROFILE-2026-09-15.md)), настоящий профиль
снимается только на первом центре.

**🟡 Н-7 поштучный обход DEFINER — пересчитан 22.09.** `SECURITY DEFINER` в
`public` — **115**, подписей в списке №19 — **59** (из них на DEFINER — **55**),
значит **вне пина 60**. Из этих 60 клиентскими ролями недостижимы 54 (31 из них —
триггерные функции, у которых EXECUTE-поверхности для клиента нет по построению),
достижимы **шесть**.
⚠️ **Прежняя запись «вне списка осталось две (`search_cities`, `search_clinics`) —
гейта нет, вычищать нечего» неполная.** Без гейта действительно только эти две; а
остальные четыре — гейты, стоящие внутри одиннадцати RLS-политик:
`auth_ceo_clinics` (6 политик), `auth_is_ceo_of` (2), `auth_radiologist_case_ok`
(1), `auth_referrer_can_book_room` (2). Все четыре достижимы и `anon`, и
`authenticated`, и их тело сторож №19 не держит — тот самый класс, ради которого
0172 и завёл пин тел. **Решение владельца 22.09: дописать эти четыре в №19.**

**🟡 WCAG Medium** — ждёт живой сессии с NVDA по шагам 1, 4, 7, 8, 10–13, 15, 17,
19–21, 23 чек-листа. W-5 (размер ячейки сетки слотов, 2.5.8 для мыши) — правка
сделана и **отменена владельцем 19.09**: ячейка ≈17–19×19 px, это принятый риск
по решению владельца, а не дефект; на тач минимум блока 10.25rem держит ячейку
≥24 px.

**Названные межи, которые аудит не закрывает и не обязан:** межи 5 и 8 аудита
0191 непроверяемы (в схеме нет объекта нужной формы); `service_role` и объекты
вне `public` — вне №26; эффект `alter role authenticated set
session_replication_role` в сессиях PostgREST **не замерен**; №26 живёт без
стенда фальсификации (держат статический тест и разовая фальсификация);
`pinnedFnReprintOrder` не видит динамический SQL со склеенным именем; обратимость
и след «Аварійної зупинки» под регистратором не пройдены; прогон по мутациям под
регистратором не делался; `katottg.min.json` не в репозитории.

---

## 5. Принятые риски

Это **решения владельца с критерием пересмотра**, а не долги. Критерий у всех
один и тот же — **первый реальный центр**, если не сказано иное.

| Риск | Суть | Цена, если сработает |
|---|---|---|
| **Р6** | PITR не куплен (тариф Pro, снимки суточные) | один рабочий день записей |
| **Р74-4** | Realtime занимает 61 % времени инстанса | пересмотр при росте |
| **Р74-5** | WCAG Medium/Low как долг | часть AA не достигнута |
| **Р4** | нет E2E/Playwright; роль регистратора покрыта живым заходом, но не мутациями | регрессия ловится тестами и стендами, не сценарием |
| **I-2**, **I-6** | приняты письменно 13.09 | — |
| — | `check_no_overlap` / `check_not_in_past` вне списка №19 | подмена тела не краснит сторожа |
| — | 11 anon-функций по allowlist 0140 | — |
| **T4** | кража service-role ключа | снимается П-1 |
| **RF-07** | advisors `track='all'` — замер отложен до реального потока | — |

---

## 6. Ключевые замеры

**Таймзоны — главный перф-дефект, снят 0202.** `room_busy_slots`: 31 233 вызова,
mean **284,9 мс**, max **5 091 мс**; суммарно с трёхаргументным вариантом
≈10,05 млн мс — **≈20 % времени выполнения всего инстанса**. Причина —
`pg_timezone_names`: **409 мс на один холодный скан**. Холостой `update` одной
записи очереди стоил **795–938 мс**, из них `trg_no_overlap` — **782–833 мс**,
то есть ≈90 % стоимости записи. **После 0202 (прод, 17.09): `trg_no_overlap`
964 → 7 мс холодным, 55 → 0,6 мс тёплым, функций со сканом каталога — 0.**

**`sink_overdue_scheduled_all` max 19 с** — разобрано 17.09: из 19 174 вызовов
mean 21,1 мс, один запуск 13.09 в 20:40 UTC занял 19,17 с, следующий по длине —
0,84 с. Ждал ACCESS EXCLUSIVE на `clinics` за **собственным сухим прогоном
0192**. Зонд: холостой `update` всех 253 просроченных строк — 0,3 с. **Не дефект.**

**Инстанс (15.09):** `max_connections` 60, соединений 24 при 2 активных,
`shared_buffers` 224 МБ, БД 136 МБ. Скважность по CPU за 91 сутки — **≈0,7 %**.

**Модель первого центра** (4 кабинета × 30 исследований = 120 записей/день):
≈840 строк `queue_entries` и ≈1 040 `user_change_markers` в сутки (единственное,
что растёт нелинейно — ×9), **≈170 действий в пиковый час ≈ 0,05/с**. Вывод:
узкое место — **не пропускная способность, а задержка**.

**Google `Testing` = ровно 7 суток**, замерено дважды по `important_events`:
26.08 12:55:54 → `reauth_required` 02.09 12:56:03; 27.08 11:16:36 → 03.09 11:18:01.

**Тулчейн:** 18.09 — 3468/3468 тестов, `tsc` 0, `eslint --max-warnings 0` — 0;
19.09 после пакета Medium — **3586/3586**. Стендов фальсификации **40**, полная
ревизия на чистом клоне `main` — 40/40.

---

## 7. Состояние прода на 22.09.2026 (сверено запросом 18:23 UTC)

| Что | Значение |
|---|---|
| Реестр миграций | **202/202**, неоштампованных **0** |
| Последняя миграция | `0202_tz_kyiv_no_catalog_scan.sql`, накат **17.09 19:28:42 UTC** |
| Сторож | `invariants_check(false)` → **`ok:false`**, `checked:26` |
| Тело сторожа | `e1f1fdcfcea99906f02b0af193b814fa`, длина **165 537** (самопин №25 совпадает) |
| Список №19 `guard_fn_bodies` | **59** подписей |
| `SECURITY DEFINER` в `public` | **115**, из них EXECUTE у `authenticated` — **44**, у `anon` — **11** |
| Без `search_path` в `proconfig` | **0** |
| Таблицы `public` / с RLS | **33 / 33**, политик **63**, таблиц с политиками **21** |
| Центров / записей очереди | 2 / 330 |

**🔴 Сторож КРАСНЫЙ — и это ожидаемо.** Единственная упавшая проверка — **№13
`gcal_sync_overdue`**: оба зеркала Google в `reauth_required`, `b42134dc` — 1 825
минут, `c79588d6` — 1 861 минута (≈30–31 час на момент замера). Это **не новый
дефект, а прямое следствие незакрытого П-3**: в статусе `Testing` refresh-токен
живёт 7 суток, после чего зеркало отваливается. После миграции 0194 сторож это
**видит** (до 0194 зеркало было мёртвым 11,5 суток при `ok:true` — находка I-7),
но **вылечить не может** — лечит только смена статуса OAuth на `In production`.

Практический вывод: пока П-3 не сделан, ночной прогон `invariants` (cron в 03:50
UTC) будет краснеть примерно раз в неделю по №13, и **это красное нужно читать
как «истёк токен», а не как «сломался инвариант»**. Любое другое красное — повод
останавливаться. Ручное переподключение обоих центров возвращает зелёный ещё на
7 суток.

---

## 8. Что эта сводка поправила в документах

При сборке нашлись расхождения между документами. Поправлено в этот заход:

1. **Шапка `PRODUCT_OVERVIEW.md` отстала на три миграции.** Утверждала «прод на
   `0199`, ledger 199/199, `checked:25`, тело сторожа `d7f87fff…`/137 081, список
   №19 — 40 подписей» и отдельно — что «пакет 0200 подготовлен и **не
   накатан**». Всё это неверно с 17.09. Механизм ровно тот, против которого в
   самом документе стоит правило: коммит `fec98e0` правил §6 и §8, но не шапку и
   не §7.
2. **`ToDo_Production.md`:** Н-4 (`room_busy_slots` ≈20 %) держался открытым,
   хотя план и вердикт считают его закрытым миграцией 0202; в списке не было ни
   пакета WCAG Medium, ни отмены владельцем размеров W-5.
3. **`PRODUCT_OVERVIEW.md` §8:** пункты М-4 («24 проверки») и leaked-password
   («НЕРАЗЛИЧИМО») стояли открытыми, хотя закрыты 0198 и 15.09 соответственно.
4. **Сетка слотов описана в документе дважды и по-разному** — §4.3 говорила
   «в строке 4 получасовых слота», §4.12 — «число блоков динамическое».
   Верна вторая: `grid-template-columns: repeat(auto-fill, minmax(6.5rem, 1fr))`.
5. **`docs/USER_GUIDE.html`** (инструкция пользователя, 22.09) не упоминалась в
   обзоре продукта вовсе.
6. **Р2 и Н-7 держались на пересказе, а не на замере** (поправлено 22.09, вторая
   редакция этой сводки). Р2 стоил «1,5–2 ч» — замер даёт 4–6 ч и, что важнее,
   показывает, что выбранный критерий не покрывает инцидент, из-за которого его
   приняли. Н-7 числился «не пересчитан», а пересчёт нашёл не цифру, а **четыре
   непинованных гейта** (`auth_ceo_clinics`, `auth_is_ceo_of`,
   `auth_radiologist_case_ok`, `auth_referrer_can_book_room`), которые документы
   считали «сознательно оставленными двумя функциями без гейта».

Осталось **не поправленным сознательно**, потому что это правка чужого
документа по смыслу, а не по факту:

- **T5 в `THREAT_MODEL_2026-09-14.md` §9.2** всё ещё «диспозиции нет — развилка
  Р74-7», хотя §9.3 того же файла и `DECISIONS-2026-09-15-s74.md` фиксируют
  ответ владельца «`CRON_SECRET` с П-1». Строка таблицы просто не обновлена.
- **Статус фазы 5 / пункта 4.3** описан тремя разными формулировками в плане,
  вердикте и концовке журнала. Состояние одно и то же — ждёт домена.
