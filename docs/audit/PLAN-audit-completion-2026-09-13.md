# План окончания аудита 2026-08-27 — по пунктам и времени

**Дата:** 2026-09-13 (с67). **Язык:** русский — намеренно, по журналу
`RADFLOW_DEEP_TECHNICAL_FUNCTIONAL_AUDIT_2026-08-27.md`, частью которого этот
план в итоге станет.

> ⚠️ Всё ниже — ЗАМЕР, а не пересказ. Каждый статус подтверждён чтением файла,
> git-логом или запросом к проду. Где не подтверждён — так и написано.

---

## СТАТУС НА 15.09.2026 (конец с71) — читать ПЕРЕД тем, как называть что-то открытым

⚠️ **ЭТОТ ПЛАН СТАРШЕ РЕШЕНИЙ ВЛАДЕЛЬЦА.** Он написан 13.09 и перечисляет
развилки как открытые, потому что ответов на них тогда не было. Ответы есть —
`DECISIONS-2026-09-13-s68.md` (одиннадцать развилок) и
`DECISIONS-2026-09-14-s69.md` (Р69-1…Р69-3). В с69 шесть уже решённых пунктов
были доложены владельцу как «ждут вашего решения» — он решил их днём раньше, в
письменном виде. **Правило: прежде чем назвать что-то ждущим решения, поискать
это в файлах `DECISIONS-*`.**

⚠️ **И держать нумерации врознь:** `Р1…Р5` — РАЗВИЛКИ этого плана и пакета
решений; `Р69-1…Р69-3` — решения с69. Два разных `Р3` в одном аудите — ровно тот
расход, против которого стоит пункт 0.4.

| блок | статус на 15.09 (конец с74) |
|---|---|
| **0** — журнал | ✅ ЗАКРЫТ (с68): итоги с56–с69 внесены, противоречие фазы 6 снято, два `LIVE-*` вернулись в репозиторий, нумерации разведены, область оценки внесена |
| **1** — фаза 4 (1.1–1.4) | ✅ **ЗАКРЫТ 15.09 (с73): все пункты 1.1, 1.1-бис, 1.2, 1.3, 1.4 проверены живьём.** 1.4 — все пять путей Г1-F: `createBooking`, `rescheduleQueueEntry`, `scheduleFromWaitlist`, `createReferralBooking` дали дословный отказ при нулевых записях в базе; `updateWaitlistEntry` закрыт формулировкой «условие недостижимо в режиме правки», доказанной перехватом заявки. Метод второго захода — логгер тела Server Action; он же вскрыл, что «аномалия» первого захода была ошибкой измерения. Долги: негативный контроль звука (1.3) не изолирован; разница видимости отказа (тост в портале против текста в модалках) названа, решения не требует. ⚠️ Исторический текст ниже: **ПОЛОВИНА, 15.09 (с72):** ✅ **1.1 Г1-E** и ✅ **1.2 Г1-G** проверены живым прогоном — [`LIVE-2026-09-15-g1e-g1g-day-shift.md`](LIVE-2026-09-15-g1e-g1g-day-shift.md). Баннер называет обе даты, снимает человек; на колл-листе `moved` и `returned` оба показаны, и гейт «Всіх підтверджено» держится в обоих (кнопка исчезает из DOM). ⚠️ Сессия была АДМИНА, не радиолога: `RadiologistBoard` (второй потребитель Г1-E) не покрыт. ⚠️ **Посылка 1.3 устарела:** `in_progress` в проде не «ноль», а **один** (14.09) — создавать запись не нужно. ⚠️ **1.3 ЗАКРЫТ 15.09 (с73):** кольцо проверено с ДВУХ сторон (часы ПК `+8 мин` → сдвиг 1 с; часы базы `+8 мин` → ровно 480 с), состояния `ok`/`crit`/`over` сняты живьём, звук перевышения опознан по сигнатуре осцилляторов (`triangle` G5→E5→C5 = `playStudyOverrun`) через 1.9 с после дедлайна центра. Долг: негативный контроль «скачок часов не звучит» не изолирован. ⚠️ **1.1-бис ЗАКРЫТ 15.09 (с73):** Г1-E на `RadiologistBoard` проверен под живой сессией радиолога (Заставська М. О.) — `moved` называет обе даты, держится, снимается человеком, день остаётся; `returned` без баннера, как и на `QueueBoard`. Кажущееся расхождение двух досок снято чтением `dayShiftNoticeOf`: `fromKey` хранится от ПЕРВОГО сдвига, поэтому снятый баннер обнуляет базу и возврат становится честным новым `moved`. ⚠️ **1.4 — ЧЕТЫРЕ ПУТИ ИЗ ПЯТИ ЗАКРЫТЫ 15.09 (с73):** [`LIVE-2026-09-15-g1f-remaining-paths.md`](LIVE-2026-09-15-g1f-remaining-paths.md). `createBooking`, `rescheduleQueueEntry`, `scheduleFromWaitlist` — зелёная база + дословный отказ + ноль строк в базе; у `scheduleFromWaitlist` дополнительно подтверждено, что гард бьёт **до CAS** (кандидат остался `waiting`). `updateWaitlistEntry` закрыт формулировкой: перехват заявки показал `fromToday: false` и при сохранённой дате (приколота `pinnedKey`), и при пустой — **условие срабатывания недостижимо в режиме правки**; это не дефект, но и не «проверен живьём». **Остался один путь: `createReferralBooking`** — зелёная есть, красный требует окна направителя (подключить не удалось: и рассылка, и выбор по id ведут в админские окна). ⚠️ Метод: инструментировать САМУ заявку логгером тела Server Action; прошлая «аномалия» на `scheduleFromWaitlist` оказалась моей ошибкой — поправка не успевала примениться, признак был на экране (форма показывала 15.09 вместо 14.09) |
| **2.1** — RLS по смыслу | ✅ ЗАКРЫТО (с69): [`RLS-SEMANTIC-2026-09-14.md`](RLS-SEMANTIC-2026-09-14.md); находки С-5, С-6, С-9, С-12…С-14 заведены, С-9 закрыта решением Р69-2, С-5/С-6/С-12…С-14 остаются в очереди работ |
| **2.2** — регистратор | ✅ **ЗАКРЫТ 15.09 (с73)** — [`LIVE-2026-09-15-registrar.md`](LIVE-2026-09-15-registrar.md). Живая сессия `Реєстратор • Medicom`; **все 13 рукописных гейтов** пройдены под этой ролью впервые и **сошлись с картой без единого расхождения** (8 уводят на `/queue`, 5 пускают). ⚠️ `/setup` отдаёт 200 — проверено глазами, что это только объяснение (0 полей ввода), а не открытый мастер. Межцентровая изоляция на `/search` подтверждена **числом**: 15 из 18 строк, недостающие три — чужой центр; зелёная база снята первой. Меню сверено с гейтами — мёртвых ссылок нет. ✅ **Вопрос владельцу ЗАКРЫТ 15.09:** регистратору доступна **«Аварійна зупинка»** — ответ дословно «задумано» (стойка первой узнаёт о поломке аппарата); внесено в [`PHASE6-role-map.md`](PHASE6-role-map.md). ⚠️ Ответ закрывает **авторизацию**, но не **обратимость и след**: может ли регистратор снять свою же остановку, пишется ли она в журнал с автором, видит ли админ автора — не проверено. Не закрыто и названо: прогон по мутациям под этой ролью |
| **2.3** — Р4 (E2E) | ✅ РЕШЕНО 13.09: **НЕТ**. Из плана снято, из бюджета вычтено |
| **3.1** | ✅ **ЗАКРЫТ 15.09 (с74)**: сделанное засчитано в строке фазы 8 журнала (RF-07, PERF tz, 0188, 0189, U-62, репетиция 3.2, зонд 3.3, модель 3.5); остаток назван там же — решение Р74-3 и замеры на первом реальном центре |
| **3.2** — восстановление | ✅ **ЗАКРЫТ 15.09 (с73): РЕПЕТИЦИЯ ВЫПОЛНЕНА** — [`LIVE-2026-09-15-restore-rehearsal.md`](LIVE-2026-09-15-restore-rehearsal.md). Восстановлено в отдельный проект из суточного снимка PHYSICAL за **≈5 мин 12 с**, владелец **вошёл живым паролем** (`last_sign_in_at` 15:08:04 + метка в данных — два независимых доказательства), клон удалён, прод **не изменился ни на строку**. Стоимость по диалогу — **$0**. Критериев приёмки 7/7, из них №5 засчитан формулировкой: сторож в клоне `ok:false, checked:23`, оба красных объяснены и внешние по отношению к восстановлению. 🔴 Найдено репетицией: cron приезжает **живым** и стучит в боевой `rad-flow-tau.vercel.app` (сработало — один выстрел, эффект ноль); схема приезжает **на момент снимка** (196 против 199), и сторож может быть зелёным по УРЕЗАННОМУ набору; «Auth settings & API keys» не переносятся; документация про «расширения руками» **опровергнута** (приехало 8/8, cron 10/10, Vault 3/3); зеркало Google **живое** — документ 14.09 устарел; оба проекта зовутся `radflow` — на удалении едва не открыли прод. ⚠️ Границы: переключение Vercel, штамп деплоя, Google и RTO целиком в репетицию НЕ входили (§11 записи). ⚠️ Исторический текст ниже: 🟡 ПОЛОВИНА: рунбук написан 14.09 (`docs/RUNBOOK_RESTORE.md`). ⚠️ **Взгляд в Dashboard СДЕЛАН 15.09: тариф Pro, PITR НЕ включён** (`Database → Backups → Point in time` предлагает `Enable add-on`). Добовые бекапы идут: **8 снимков 08–15.09 подряд без дырок, PHYSICAL, 00:16–00:20 UTC ≈ 03:17 по Киеву**; Storage — 0 бакетов/0 объектов, его исключение из бекапа пустое. ⚠️ Точная цена аварии — **не «24 часа», а один рабочий день**: бекап стоит между сменами. Осталось **РОВНО ОДНО: репетиция**. ➡️ **15.09 репетиция подготовлена под ключ** — [`PLAN-3.2-restore-rehearsal.md`](PLAN-3.2-restore-rehearsal.md): эталон прода снят (13:55 UTC), способ замерен (`Restore to a New Project` доступен: Pro + PHYSICAL, **PITR не нужен**), написаны блок SQL, 7 критериев приёмки и хронометражный бланк. 🔴 Найдено при подготовке: клон с первой минуты **бьёт cron-джобами в БОЕВОЙ** `rad-flow-tau.vercel.app`, и «Auth settings & API keys» **не переносятся**. **От владельца — два клика** (выбрать снимок → `Restore`; после проверок — удалить клон) |
| **3.3 / 3.5 / 3.6** | ✅ **3.3 и 3.6 ЗАКРЫТЫ 17.09 (с75) миграцией 0202** (Р74-3(а)): прод на `Europe/Kyiv`, CHECK без алиаса, скан `pg_timezone_names` убран из шести функций; замер на проде `trg_no_overlap` 964 → 7 мс холодным, 55 → 0,6 мс тёплым — [`PR-0202-tz-kyiv-no-catalog-scan.md`](PR-0202-tz-kyiv-no-catalog-scan.md). **3.5:** модель стоит; долг `sink_overdue_scheduled_all` max 19 с разобран 17.09 — одна блокировка за сухим прогоном 0192, не дефект ([`PERF-2026-09-17-sink-overdue-19s.md`](PERF-2026-09-17-sink-overdue-19s.md)); допущение о потоке по-прежнему ждёт цифр владельца (Р74-6). ⚠️ Исторический текст: 🟡 **СДВИНУТЫ 15.09 (с74) замером.** **3.3:** гипотеза доказана прямым зондом — холостой `update` записи очереди в откаченной транзакции стоит 795–938 мс, из них триггер `trg_no_overlap` **782–833 мс** (скан `pg_timezone_names`), повторно 84/58 мс. 3.3 и 3.6 — **одна задача**, ждёт развилки **Р74-3** (`Europe/Kiev` → `Europe/Kyiv`; перед этим проверить Google Calendar API). Realtime 61 % — **Р74-4**. **3.5:** модель по замерам — [`LOAD-PROFILE-2026-09-15.md`](LOAD-PROFILE-2026-09-15.md); пропускная способность не узкое место, узкое место — задержка записи; допущение о потоке ждёт цифр владельца (**Р74-6**); новый долг — `sink_overdue_scheduled_all` max 19 с |
| **3.4** — CHECK таймзоны | ✅ ЗАКРЫТО миграцией **0192** |
| **4.1** — модель угроз | ✅ **ФАЗА 1 ЗАКРЫТА 15.09 (с74)** — ответы владельца в [`DECISIONS-2026-09-15-s74.md`](DECISIONS-2026-09-15-s74.md) (Р74-1 «все 16», Р74-2 «только клиентские роли», Р74-7 «с П-1»); очередь: ~~0201~~ (накатан 15.09) → проверка Google + 0202 → WCAG High. 🟡 **ПРОВЕРКА ПРОЙДЕНА 15.09 (с74)** — `THREAT_MODEL_2026-09-14.md` §9, леджер 200: T2, T3-бис, T7 закрыты; строка 1.6 пересверена. **Работы агента не осталось — фазу 1 закрывают три ответа владельца:** Р74-1 (T1), Р74-7 (T5), Р74-2 (T8), [`DECISIONS-PACKET-2026-09-15-s74.md`](DECISIONS-PACKET-2026-09-15-s74.md). ⚠️ Исторический текст: НАПИСАНА (`THREAT_MODEL_2026-09-14.md`), но фаза 1 ещё не закрывается: условие закрытия сформулировано, проверка по нему не пройдена |
| **4.2** — фаза 3 | ✅ **0201 НАКАТАН 15.09 (с74)** — [`PR-0201-gated-rpcs-role-surface.md`](PR-0201-gated-rpcs-role-surface.md): Р74-1 и Р74-2 исполнены — список №19 43 → **59** (все 16 definer-функций со своим гейтом), новая проверка №26 `role_surface` (членства, атрибуты и настройки клиентских ролей, ACL схем и базы, default ACL; 62 ключа), `ok:true, checked:26`; фальсификация на проде PASS (16 + 22 нарушителя, каждый своим ключом), ревизия 40/40. **Межи 0191 после 0201:** 1 и 2 — закрыты для клиентских ролей; 3 — 21 → **2** (`search_cities`, `search_clinics` без гейта, вычищать нечего); 5 и 8 — непроверяемы (в схеме нет объекта нужной формы). **Работы агента по фазе 3 не осталось** — статус фазы решает вердикт 6.1. ⚠️ Исторический текст: ✅→🟡 **0200 НАКАТАН 15.09 (с74)** — [`PR-0200-pin-desk-waitlist.md`](PR-0200-pin-desk-waitlist.md): список №19 40 → 43, `ok:true, checked:25`, ревизия 40/40. Остаток — не работа, а решения: **Р74-1** (16 definer-функций со своим гейтом: замер опроверг посылку «громкий отказ») и **Р74-2** (межи 1–2). ⚠️ Исторический текст: 🟡 **развилка «`search_path` вне №19» ЗАКРЫТА 14.09 миграцией 0196** (решение владельца: «пинить ЗНАЧЕНИЕ в проверке №2»); слабых definer-функций 34 → **0**. ⚠️ В документах с70 её звали «Р3» — это НЕ `Р3` из таблицы §3 ниже, та про 38 тригерных функций и открыта. Осталось: восемь названных меж 0191 и пять непинованных definer-функций |
| **4.3** — фаза 5 | 🟡 ждёт домена: работы агента нет, `cleanup` закрыт, I-6 принят письменно, остаётся только I-5 (2–3 ч после регистрации домена, П-2) (формулировка сведена к одной в с77) |
| **4.4** — WCAG | ✅ **ЗАКРЫТ 18.09 (с75): живая сессия владельца с NVDA — шаги 3, 5, 14, 16, 18 чек-листа 5/5 PASS, W-1/W-2/W-3 closed** (протокол — §5 [`WCAG-static-2026-09-15.md`](WCAG-static-2026-09-15.md)); попутно +W-24 (Medium: `aria-label` на `span` без роли не озвучивается) и +W-25 (Low: слипание соседних `span` в речи); на скриншоте колл-листа найден и исправлен наезд бейджа статуса на колонку «Кабінет» (`cecf29b`). Medium 11 / Low 11 — принятый риск до первого центра (Р74-5), пакет правок после вердикта. ⚠️ Исторический текст: 🟡 **HIGH ЗАКРЫТЫ В КОДЕ 17.09 (с75, Р74-5 «только High»)** — W-1 (`inert` на свёрнутых деталях трёх досок), W-2 (клавиатурный путь к правке пациента, доступу направителя и карточке центра; строки — модульные компоненты после HIGH ревью), W-3 (статус звонка глифом + текстом в колл-листе); пины `tests/wcagHigh.test.ts`; подробности — шапка §2 [`WCAG-static-2026-09-15.md`](WCAG-static-2026-09-15.md). **Осталось: живая сессия с владельцем** (клавиатура + NVDA, §4 чек-листа — шаги 3, 5, 14, 16, 18 проверяют именно эти правки). ⚠️ Исторический текст: 🟡 **СТАТИЧЕСКАЯ ПОЛОВИНА СДЕЛАНА 15.09 (с74)** — [`WCAG-static-2026-09-15.md`](WCAG-static-2026-09-15.md): 3 High / 10 Medium / 10 Low с `file:line`, чек-лист живой сессии на 25 шагов. Осталось: **живая сессия с владельцем** (клавиатура + NVDA) и объём правок — **Р74-5**. ⚠️ Исторический текст: ⏳ НЕ НАЧАТ. ⚠️ В с70 статический проход обсуждался, но **документа на диске не оставил** (`grep WCAG` по `docs/` не находит файла с датой 14.09) — считать пункт нетронутым |
| **5** — диспозиция находок | ✅ ЗАКРЫТ письменными решениями 13.09 (I-2 принята, I-5 блокирована доменом, I-6 принята) |
| **6.1** — вердикт | ✅ **ВЫНЕСЕН 18.09 (с75): CONDITIONAL GO** — раздел «Вердикт 6.1» журнала аудита: базовая линия сверена запросами, диспозиция фаз 0–8 и находок I-2/I-5/I-6/I-7, принятые риски с критериями пересмотра, условия П-1/П-2/П-3, чего вердикт НЕ утверждает; ревизия стендов на чистом клоне `main d4f1eb3` — **39/40** на `d4f1eb3` (18:05–18:40 UTC, 35 мин): `falsify-rf09` красный — протухший якорь B12 после переноса `AccessRowView` на модульный уровень (W-2, `001a38d`, пакет шёл без ревизии стендов); якорь обновлён (`36585a8`), перепрогон стенда **44/44** → **40/40**. **Работы агента по плану не осталось** (4.3 ждёт домена). ⚠️ Исторический текст: ⏳ последним. Потолок известен заранее: **CONDITIONAL GO** до ротации ключа и домена |
| **6.2 / 6.3 / 6.4** | ⏳ вынесены в [`ToDo_Production.md`](ToDo_Production.md) — они привязаны к выходу в реальный прод, а не к аудиту |

**Сверх плана сделано в с69** (в плане этих пунктов не было вообще): **0193** —
I-8, кабинетный гейт радиолога в DEFINER-чтении листа ожидания (была ЖИВАЯ);
**0194** — I-7, сторож видит аварийно выключенное зеркало Google Calendar (было
мёртвым 11,5 суток при `ok:true`); починен `gcal_pg_cron_smoke.sql`, не
проходивший с 0186.

**Сверх плана сделано в с70** (в плане этих пунктов тоже не было): **0195** —
карточка центра сужена по СТАТУСУ связи и `sink_overdue_scheduled()` снята с
`authenticated` (обе находки — из поштучного обхода Н-7; ⚠️ знаменатель обхода
исправлен: DEFINER-функций **115**, а не «~30»); **0196** — развилка
**«`search_path` вне №19»** закрыта: он пинится ЗНАЧЕНИЕМ в проверке №2, 34 функции со слабой
формой переведены на канон, слабых **0**, и заодно `audit_read_ceo` сужена по
`table_name` (замер опроверг ПОСЫЛКУ п.1 решения Р69-2; вывод Р69-2 в силе).
Класс «протухший якорь после передрука» закрыт машиной — генератор сам сверяет
якоря всех стендов. Полная ревизия на смёрженном `main`: **40/40**.

**Сверх плана сделано в с71** (этого пункта в плане тоже не было): **0197** —
закрыт пункт **Н-6** `ToDo_Production.md` (T3-бис): пять auth-аккаунтов без
профиля удалены (`auth.users` 14 → 9, сирот 0), добавлена проверка **№24**
`auth_orphan_accounts` с порогом 15 минут, `checked` 23 → **24**. ⚠️ Замер
поправил постановку: все три маршрута уже делали компенсирующий `deleteUser` —
не проверялся его РЕЗУЛЬТАТ; плюс закрыт смежный долг `/api/staff/password`
(глотал ошибку `update profiles` после уже сменённого пароля). Полная ревизия:
**40/40**.

**Сверх плана сделано в с72** (этого пункта в плане тоже не было): **0198** —
закрыт пункт **Н-1** `ToDo_Production.md` (М-4 аудита): тело САМОГО сторожа
теперь пинит МАШИНА, а не ритуал наката. Пин — в комментарии к функции; сверяет
его новая проверка **№25** `guard_self_pin`, `checked` 24 → **25**; левое звено
цепи (файл → пин) держит `planGuardPin` в гейте миграций, fail-closed на каждой
прод-сборке. Выбор места — замер на проде (`comment_survives_replace = true`,
`oid_same = true`), а не вкус. Фальсификация: тело сдвинуто на ОДИН пробел →
проверка краснеет. ⚠️ Названо прямо, чего это не ловит: у кого есть право на
`create or replace`, есть и на `comment on function` — ловится ДРЕЙФ, а не
злоумышленник с правами postgres. ⚠️ Две собственные ошибки записаны в
[`PR-0198-guard-self-pin.md`](PR-0198-guard-self-pin.md): правило сперва
положено в тесты и сломало ПОЗИТИВНЫЕ контроли двух стендов; первая редакция
гейт-правила дала четыре ложных срабатывания на живой истории (0159, 0179,
0184, 0187) и сделала бы красной каждую сборку. Полная ревизия: **40/40**.
⚠️ **Н-7 продолжается: не запинено ничем 74 функции** (было 75). Приём
«пин-в-комментарии» — для ОДНОГО случая (сторож не может пинить себя изнутри);
остальным место в списке №19, оптом не переносить.

**И там же, в с72, закрыт хвост «находки ревью-A, лично НЕ перемеренные»** (висел
с с69). Пакет **0199**, разбор — [`REVIEW-A-remeasure-2026-09-15.md`](REVIEW-A-remeasure-2026-09-15.md):
межтенантный замок в `integration_apply_status` **подтверждён зондом** (чужие
`queue`/`case` получали `xmax` нашей транзакции ДО проверки клиники — держатель
ключа центра A мог блокировать строки центра B); оракул радиолога подтверждён в
обеих RPC листа ожидания (у `schedule_from_waitlist_rpc` утекал ещё и СТАТУС), а
находка про `cancel_case_rpc` **закрыта как неверная** — гейт кабинета отвечает
за размещение, а отмена никого не кладёт. `checked` не менялся (25).
⚠️ Процессный срыв пакета записан дословно: накат не был побайтово тем, что
прогнали сухим. ⚠️ Три починенные функции **не запинены** списком №19 — это
пакет **0200** и одновременно Н-7: 74 → 71.

**Реально ждут владельца две вещи, и обе дешёвые:** развилка **Р2**
(аудит-триггеры: подтвердить цену 4–6 ч и решить про справочные таблицы) и
**leaked-password** (5 минут в дашборде Supabase). Плюс бонусом — взгляд в
Dashboard → Settings → Add-ons (тариф и PITR) — ✅ **СДЕЛАН 15.09**: тариф Pro,
PITR **не включён**, `RPO до 24 ч` подтверждён как фактический. Этот взгляд не
столько разблокировал 3.2, сколько **породил новую развилку — Р6: покупать PITR
или принять RPO 24 ч письменно** (см. §3).
Остальное — работа, а не решения.

⚠️ **И СРАЗУ ПРО ИМЯ «Р3», потому что оно означает ДВЕ РАЗНЫЕ вещи, и я это
имя путал всю сессию с70.** В таблице развилок §3 этого плана `Р3` — это
«расширять ли №19 на 38 тригерных функций расписания», и она **ЗАКРЫТА
решением владельца 13.09** (см. исправление ниже).
⚠️ **ИСПРАВЛЕНО 15.09 (с72).** До этой правки здесь и в таблице §3 стояло
«Р3 ОТКРЫТА, не тронута». Это было **неверно**: `DECISIONS-2026-09-13-s68.md`
§5 — «Р3 — **тільки два названих**»: всем 38 тригерным функциям расписания
владелец сказал **НЕТ** (основание — замер с57: тело тригерной функции трогают
8 из последних 30 миграций против 4 из 30 для нынешнего списка, полный список
удвоил бы частоту передруков навсегда), а в №19 добавлены ровно два —
`update_patient_details(uuid, jsonb, jsonb)` и `tg_change_markers_queue()`,
исполнено миграцией **0192**. Замер 15.09 подтверждает: обе подписи в списке
№19 есть. ⚠️ И это ровно та ошибка, от которой предостерегает шапка этого
плана («прежде чем назвать что-то ждущим решения, поискать это в файлах
`DECISIONS-*`») — я её здесь и допустил, распутывая коллизию имён в с70/с71, и
разнёс дальше по хендоффу, промпту с73 и журналу в с72.
А в стартовом промпте с70, в `PRODUCT_OVERVIEW` и в шапке миграции 0196 «Р3»
называлась СОСЕДНЯЯ развилка — «`search_path` вне списка №19», и закрыла её
0196. Читая любой документ с70 со словом «Р3», подставляйте `search_path`.
Это ровно тот расход, против которого стоит пункт 0.4 и предупреждение о
`Р69-*` выше, — и он повторился, потому что имя пришло из промпта, а я его
не сверил с этой таблицей.

---

## 0-бис. Область оценки: RadFlow НЕ хранит медицинских данных

⚠️ **Исправление первой редакции этого плана (13.09, по указанию владельца).**
Первая редакция дважды обосновывала приоритет блока 3.2 словами «это
медицинские данные». **Это неправда, и проверяется одним запросом.**

Замер `information_schema.columns` по `queue_entries`, `waitlist_entries`,
`patient_cases`, `case_steps`, `incidents`, `profiles`, `referrer_private`:
**колонок под диагноз, результат, заключение, снимок или анамнез не существует
ни одной.** Не «пустые» — их нет в схеме.

Что есть на самом деле (прод, 13.09, 329 записей очереди):

| класс | поля | заполнено |
|---|---|---|
| ПИБ и контакты | `patient_name`, `patient_phone`, `patient_email` | основное |
| демография | `patient_dob` · `patient_sex` · `patient_weight` | 101 · 329 · 28 |
| что за исследование | `studies`, `studies_original`, `modality` | основное |
| операционное | `status`, `scheduled_*`, `duration_min`, `room_id`, `doctor`, `cito` | основное |
| смежное со здоровьем, ТОНКИМ слоем | `indication` · `contraindications=true` · `has_contrast=true` | **13** · **2** · **18** |
| свободный текст врача | `radiologist_note` | **0** — канал есть, пуст |

**Это уже было записано, и я это пропустил.**
`docs/audit/DATA_ARCHITECTURE_AUDIT_2026-07-08.md:10` — «RadFlow — это B2B-SaaS
для управления очередью и потоком пациентов в диагностических центрах, **не**
регулируемая система медицинских записей… аудит оценивает операционную
надёжность… а не healthtech-комплаенс (HIPAA…)»; строка 52 — «immutable medical
records — вне зоны продукта (**RadFlow не хранит результаты исследований**)».

**Следствия для этого плана — не косметические:**

1. Довод «это медицинские данные» в блоке 3 и в порядке работ **снят**, а не
   смягчён. Он был ложным.
2. Оценка блока 3.2 снижена **5–7 ч → 3–5 ч**: нужна рабочая процедура
   восстановления с одной репетицией, а не комплаенс-грейд DR-программа под
   регулируемые медицинские записи.
3. Рекомендация «восстановление — следующим по важности, вне очереди»
   **отозвана**. Блок 3.2 возвращается в нормальный вес внутри блока 3.
4. Добавлен пункт **0.6**: область оценки надо внести в сам журнал. Её там нет
   — и именно поэтому в неё уехал не только я. Ср.
   `RADFLOW_TECHNICAL_AUDIT_2026-08-23.md:83` — «раскрытие медицинских и
   персональных данных»: та же вольность формулировки на два аудита раньше.

⚠️ Что при этом НЕ меняется: ПИБ, телефон, email, дата рождения и пол — это
персональные данные, и весь блок про «кто что видит» (фазы 3, 6, 7) стоит на
них, а не на медицинских записях. Связка «названный человек записан на МРТ с
контрастом» остаётся чувствительной. Просто это **не** медицинская карта.

---

## 0. Главный факт: журнал аудита отстал на 222 коммита

| что замерено | как |
|---|---|
| журнал последний раз коммитился **`7f851e3`, 2026-09-03** («стан на кінець с55») | `git log -1 -- docs/audit/RADFLOW_DEEP_…_2026-08-27.md` |
| с тех пор в репозиторий легло **222 коммита** | `git rev-list --count --since=2026-09-03 HEAD` |
| прошло **12 сессий** (с56…с67), 21 миграция (0171…0191), `checked` 16 → 23 | цепочка `PR-017*`…`PR-s66-0191` |

Это блокер сам по себе. Правило журнала — «**пакет не закрыт, пока его итог не
попал сюда**» — записано в нём же (строки 714–719) после того, как он уже
протухал на четыре сессии в с55. Вердикт выносится ПО ЭТОМУ ФАЙЛУ, значит
сегодня он выносился бы на данных десятидневной давности.

Три конкретных следствия, каждое замерено:

1. **Внутреннее противоречие.** Строка 692 — фаза 6 `[НЕ НАЧАТО]`; строка 1516
   того же файла — «Фаза 6 — ЗАКРЫТА (с55)». Таблица не обновлена.
2. **Два `LIVE-*` документа существуют ТОЛЬКО в Claude Projects.**
   `LIVE-2026-09-11-g1f-clock-guard.md` и `LIVE-2026-09-11-stop-with-case.md`
   не найдены ни на диске, ни в git (`git log --all -- <оба пути>` пуст).
   Именно они закрывают Г1-F и прогон `stop --with-case` — то есть два
   закрытия документированы вне канона.
3. **Хвост фазы 4 закрыт 10.09, а журнал этого не знает.** Сценарий `cas`
   прошёл живьём впервые за историю проекта (`LIVE-2026-09-10-race-check.md`,
   коммит `2e54dc9`).
   ⚠️ И отдельно: `cas` и `case` — **РАЗНЫЕ** сценарии. Замерено:
   `scripts/race-check.mjs:1590` перечисляет восемь команд — `plan, run, room,
   cas, waitlist, case, stop, midnight`. Журнал (и `LIVE-2026-09-10-race-check.md:118`)
   говорят «ровно три сценария» — устарело.

---

## 1. Что реально закрыто с 27.08 — по фазам

| фаза | было в журнале | фактически на 13.09 |
|---|---|---|
| 0 Базовая линия | ЗАКРЫТО | ЗАКРЫТО |
| 1 Архитектура и модель угроз | В РАБОТЕ | **не двигалась ни на строку**; и у неё НЕТ списка открытых пунктов — закрыть нельзя, потому что не сформулировано условие |
| 2 Тулчейн, зависимости, секреты | ЗАКРЫТО в с50 | ЗАКРЫТО |
| 3 БД и безопасность | В РАБОТЕ | **основной объём работы с56–с67**: 21 миграция, инварианты №17…№23, RF-09 (High, в журнале её нет вообще) |
| 4 Конкурентность, realtime, время | В РАБОТЕ, три хвоста | хвост 1 закрыт в с55; **хвост 3 (`cas`) закрыт 10.09**; хвост 2 закрыт ЧАСТИЧНО |
| 5 API и интеграции | В РАБОТЕ | +`/api/build`, +RF-05 fail-closed, +замер n8n-ветки outbox; I-5 и I-6 не тронуты |
| 6 Функциональный E2E по ролям | НЕ НАЧАТО / ЗАКРЫТА (противоречие) | закрыта по форме; по смыслу — хвосты ниже |
| 7 UX и доступность | В РАБОТЕ | +U-59/U-60, U-62, U-65, U-66, U-81, плитка кабинета, график в модалке, видимость отказа Г1-F |
| 8 Производительность и восстановление | НЕ НАЧАТО | **половина «производительность» фактически сделана** (RF-07 замер, PERF tz, 0188, 0189, U-62) и нигде не засчитана; **половина «восстановление» не начата вообще** |

Замер по фазе 8: поиск `PITR|бэкап|backup|RTO|RPO|disaster` по `docs/` даёт 42
попадания (то есть поиск не слепой), и **ни одного** в `docs/audit/` за с56–с67.
Всё живое — июльские `DATA_ARCHITECTURE_AUDIT_2026-07-08/12` и
`GOOGLE_CALENDAR_BACKUP.md` (это про календарь, не про восстановление БД).

**Закрытые долги RF (нумерация Ф6):** RF-2 (пакет 26, с57), RF-4 (пакет 26 +
0175). RF-1 — вычеркнут: описания не существовало никогда.

---

## 2. Что осталось — по пунктам и времени

⚠️ **Калибровка оценок.** Проект сам замерил, что его оценки занижены примерно
вдвое: RF-05 план 1–1,5 ч → факт 3,5 ч; RF-01 план 3 ч → факт ~4 ч; RF-02 факт
~3 ч при обещании «без передрука». Диапазоны ниже уже растянуты под это. Цифра
«с ревизией» означает, что пункт трогает `invariants_check` и тянет за собой
полную ревизию 37 стендов — **+50 мин фиксированно**.

### Блок 0 — вернуть журналу право выносить вердикт `[БЛОКЕР]`

| # | пункт | время |
|---|---|---|
| 0.1 | Внести в журнал итоги с56–с67: таблица фаз, цепочка 0171–0191, RF-09, закрытие хвоста `cas`, фактическая работа по фазе 8 | **2–3 ч** |
| 0.2 | Исправить противоречие фазы 6 (строка 692 против 1516) | входит в 0.1 |
| 0.3 | Вернуть в репозиторий два `LIVE-*` документа, живущих только в Projects | **30 мин** |
| 0.4 | Развести две нумерации RF (Ф6 RF-1/2/4 против `RELIABILITY` RF-01…RF-08) и два вердикта («6/10» против CONDITIONAL GO) — они уже путались | **1 ч** |
| 0.5 | Пометить устаревшие утверждения, не переписывая историю: «ровно три сценария» в `LIVE-2026-09-10-race-check.md:118`; §3 `PR-0183` про цену снятия политики; `LIVE-…-ui-ceo-referrer.md:178` «Г1-F вживу не перевірений» | **1 ч** |
| 0.6 | **Внести в журнал ОБЛАСТЬ ОЦЕНКИ** (раздел 0-бис): RadFlow не хранит медицинских записей, замер схемы, ссылка на `DATA_ARCHITECTURE_AUDIT_2026-07-08:10,52`. В журнале этого нет, и на этом ошибались уже дважды — аудит 23.08 (строка 83) и первая редакция этого плана | **30 мин** |

**Итого блок 0: 5–6 ч.** Без него всё остальное бессмысленно — вердикт
выносится по файлу, который врёт.

### Блок 1 — дозакрыть фазу 4

| # | пункт | время |
|---|---|---|
| ~~1.1~~ | **Живая проверка Г1-E** (баннер переноса суток на досках) | ✅ **ЗАКРЫТА 15.09** на `QueueBoard` — [`LIVE-2026-09-15-g1e-g1g-day-shift.md`](LIVE-2026-09-15-g1e-g1g-day-shift.md). ⚠️ `RadiologistBoard` не покрыт: сессия была админа |
| ~~1.2~~ | **Живая проверка Г1-G** (трёхзначный вердикт `none`/`moved`/`returned`) | ✅ **ЗАКРЫТА 15.09** на колл-листе — самом дорогом экране: `moved` и `returned` оба показаны, гейт массового действия держится в обоих |
| 1.3 | **Ф4-8 в НАЗВАННОЙ части** — кольцо таймера и звук. Проверено было другое (поправка часов на 7 мин). ⚠️ **Посылка «пациентов `in_progress` в проде ноль» УСТАРЕЛА** — замер 15.09 даёт одного (14.09, `in_progress_at` непустой), создавать запись не нужно | **1,5 ч**, записи НЕ требует |
| 1.4 | **Г1-F на остальных путях**: `rescheduleQueueEntry`, `updateWaitlistEntry`, `createBooking`, `createReferralBooking`, `scheduleFromWaitlist` — проверен только лист ожидания | **2–3 ч** |

**Итого блок 1: 6,5–8,5 ч.** После него фаза 4 закрывается честно.

### Блок 2 — дозакрыть фазу 6

| # | пункт | время |
|---|---|---|
| 2.1 | **13 таблиц с политиками из 21** читались только по ФОРМЕ (дайджест №16). Нужна проверка ПО СМЫСЛУ: под каждой ролью, запросом, с before-образом | **5–7 ч** |
| 2.2 | **Живая проверка под регистратором** — единственная роль без `LIVE-*` документа (CEO, направитель, админ, радиолог закрыты) | **1–1,5 ч** |
| 2.3 | **Развилка Р4 — Playwright / E2E по ролям.** Решение владельца НЕ принято: в `DECISIONS-2026-09-09.md` его нет, развилка висит с с56 | решение: **15 мин**; если «да» — **10–14 ч** на первый каркас |

**Итого блок 2: 6–8,5 ч** без Р4, **16–22,5 ч** с Р4.

### Блок 3 — фаза 8, самая отстающая (но уже НЕ «нулевая», см. 3.2)

| # | пункт | время |
|---|---|---|
| 3.1 | Засчитать уже сделанное как фазу 8 и назвать, что осталось (RF-07 замер, PERF tz, 0188, 0189, U-62) | **1 ч**, частично входит в 0.1 |
| 3.2 | **ВОССТАНОВЛЕНИЕ — ⚠️ статус исправлен 14.09, «не начато вообще» больше НЕВЕРНО.** Письменная процедура есть: `docs/RUNBOOK_RESTORE.md` (коммит `16ceacd`, 14.09) с замерами прода — операционно незаменимое ядро **3 168 kB**, `archive_mode=on`, `wal_level=logical`, 45 311 WAL-сегментов, разбор слоёв по восстанавливаемости. **Осталось ровно два пункта:** (а) ✅ **ЗАКРЫТ 15.09** — тариф Pro, PITR не куплен, суточные снимки PHYSICAL идут без дырок; (б) **репетиция восстановления на копии** — не проводилась ни разу, и до неё RTO в рунбуке остаётся расчётом, а не фактом. ➡️ **15.09 подготовлена под ключ:** [`PLAN-3.2-restore-rehearsal.md`](PLAN-3.2-restore-rehearsal.md) — эталон прода, блок SQL, 7 критериев приёмки, хронометраж. Способ замерен: `Restore to a New Project` доступен (Pro + PHYSICAL), **в MCP такого инструмента нет** — шаги 1–2 физически делает только владелец (два клика), остальное агент. 🔴 Две находки подготовки: клон наследует cron+Vault+URL и с первой минуты бьёт в **боевой** `rad-flow-tau.vercel.app` (гасить джобы ПЕРВЫМ действием); «Auth settings & API keys» **не переносятся** — после реального восстановления центр поднимется с выключённой защитой от утёкших паролей и новыми ключами API. ⚠️ Оценка снижена с 5–7 ч: комплаенс-грейд DR не нужен, продукт вне регулируемых медицинских записей (раздел 0-бис) | **2–4 ч** |
| 3.3 | `room_busy_slots` ≈ **20 % времени инстанса** (≈10,05 млн мс) — замерено в с63 и оставлено. Решить: лимит, кеш, или принятый риск | **2–3 ч** |
| ~~3.4~~ | ✅ **ЗАКРЫТО 13.09 миграцией 0192** — `clinics_timezone_chk` (`Europe/Kyiv`, `Europe/Kiev`, `UTC`). ⚠️ И заодно опровергнута оценка «40 мин, миграция без передрука»: CHECK меняет ключ `k:clinics` в №23, то есть **неминуемо** тянет передрук сторожа и полную ревизию стендов | факт: в составе пакета 0192 |
| 3.5 | Нагрузочный профиль под ожидаемый поток первого реального центра | **3–4 ч** |
| ~~3.6~~ | `pg_timezone_names` фаза 2 — ✅ **накатано 17.09 (0202)**; историческая оценка: заблокировано решением владельца (`Europe/Kiev` — legacy-алиас) | решение: **30 мин**; работа: **3–4 ч** |

**Итого блок 3: 8–13 ч** + развилка 3.6 (было 10–14; минус закрытая 3.4, минус
написанный рунбук в 3.2).
⚠️ **Правка статуса 14.09:** до этой даты 3.2 называлась «единственное место
аудита, где не начато НИЧЕГО» — это было верно 13.09 и НЕВЕРНО сейчас: рунбук
написан, остались взгляд в Dashboard и репетиция. Основание приоритета не
изменилось — **операционная непрерывность, а не чувствительность данных**:
потеря очереди означает, что центр завтра утром не знает, кто к нему записан, и
восстанавливать это придётся звонками. Медицинских записей RadFlow не хранит
(раздел 0-бис), и прежний довод про них снят.

### Блок 4 — довести фазы «В РАБОТЕ» до условия закрытия

| # | пункт | время |
|---|---|---|
| 4.1 | **Фаза 1 — написать модель угроз.** Активы, роли, границы доверия, что каким инвариантом закрыто. Сегодня фазу нельзя закрыть, потому что НЕ СФОРМУЛИРОВАНО, чего ей не хватает | **4–5 ч** |
| 4.2 | **Фаза 3** — восемь названных меж 0191 + пять непинованных definer-функций (пункты 1–5 очереди с68). ⚠️ **Третий пункт — `search_path` вне списка №19 — ЗАКРЫТ 14.09 миграцией 0196** (развилка Р3: пин ЗНАЧЕНИЯ в проверке №2; слабых definer-функций 34 → 0). Оценка уменьшена на его долю | **3–7 ч** |
| 4.3 | **Фаза 5** — I-6 (`Accept`/`_format`, нет 406) **2–3 ч**; `assertNoLiveDelivery` не покрывает `cleanup` **1–1,5 ч**; I-5 блокирован доменом | **3–4,5 ч** |
| 4.4 | ~~**Фаза 7** — WCAG-переаудита не было с с47; живой проверки с клавиатуры и скринридером не было ни разу~~ ✅ статика 15.09, High в коде 17.09, живая сессия NVDA 18.09 — закрыт | ~~**5–7 ч**~~ |

**Итого блок 4: 16–26,5 ч.**

### Блок 5 — диспозиция открытых находок (условие вердикта)

| находка | состояние на 13.09 | что нужно | время |
|---|---|---|---|
| **I-2** (Low) — `room_busy_slots` при отказе ACL отдаёт ноль строк, неотличимо от «свободно» | НЕ ИСПРАВЛЕНА. Рядом много работы (0189, 0190, смоук перестал быть вакуумным), но поведение то же | решение «чиним или принимаем риск письменно»; правка ломает молчаливую деградацию экрана | решение **30 мин**, правка **2–3 ч** |
| **I-5** (Medium) — `system`/`fullUrl`/`link.next` нестабильны при смене домена | НЕ ТРОНУТА, новых данных нет; домен не зарегистрирован | **блокирована доменом** | после домена **2–3 ч** |
| **I-6** (Low) — нет 406, `Accept`/`_format` игнорируются | НЕ ТРОНУТА | блокер формальной сертификации, не безопасности | см. 4.3 |

**Итого блок 5: 3–4 ч** решений и правок (без I-5).

### Блок 6 — вердикт

⚠️ **Пункты этого блока плюс всё, что привязано к выходу в прод, собраны одним
списком в `docs/audit/ToDo_Production.md` (с69).** Там же — принятые риски, у
которых критерий пересмотра звучит как «первый реальный центр», и порядок
действий в день выхода. Здесь оставлены оценки времени; там — что и в каком
порядке делать.

| # | пункт | время |
|---|---|---|
| 6.1 | ~~Написать вердикт в журнал: фазы, находки, остаточные риски, потолок~~ ✅ вынесен 18.09 (с75) | ~~**2–3 ч**~~ |
| 6.2 | **Ротация service-role ключа** — 30 мин техники + окно деплоя. Привязана владельцем к первому реальному центру. Пока не сделана, потолок CONDITIONAL GO | **30 мин** + окно |
| 6.3 | **Регистрация домена** — не сделана; блокирует I-5 и верификацию Google | вне оценки |
| 6.4 | **Google Cloud: OAuth-приложение из `Testing` в `In production`** (+ решение про верификацию). ⚠️ Решение владельца 14.09: делаем НА ЭТАПЕ РЕАЛЬНОГО ПРОДАКШЕНА, не сейчас | **15 мин** + верификация отдельно |

#### 6.4 подробно — чтобы в нужный момент не исследовать заново

**Зачем.** Пока статус `Testing`, Google выдаёт refresh-токен на **7 суток**.
Это не догадка: замерено дважды по `important_events` — подключение 26.08
12:55:54 → `reauth_required` 02.09 12:56:03 (7 сут + 9 с); 27.08 11:16:36 →
03.09 11:18:01 (7 сут + 1 мин 25 с). Документация Google формулирует это прямо:
проект с внешним типом пользователей и статусом `Testing` получает refresh-токен
со сроком 7 дней, если запрошенные scope выходят за пределы имени/почты/профиля.
Наши scope (`lib/googleCalendarClient.ts`): `calendar.events` и
`calendar.calendarlist.readonly` — выходят.

**Следствие на сегодня:** зеркала умирают каждые 7 суток после переподключения.
После 0194 это ВИДНО (проверка №13 краснеет с меткой `відвалилось(...)`), но не
лечится — лечит только смена статуса.

**Что сделать, когда дойдём до реального продакшена:**

1. Google Cloud Console → проект RadFlow → Google Auth Platform (бывший «OAuth
   consent screen») → **Audience** → `Publish app` (`Testing` → `In production`).
2. ⚠️ **Переподключить ОБА центра после переключения.** Токены, выданные в
   статусе `Testing`, свой 7-суточный срок сохраняют — новый срок получают
   только НОВЫЕ токены. Наш `buildAuthUrl` идёт с `access_type=offline` и
   `prompt=consent`, то есть переподключение гарантированно выдаёт новый
   refresh-токен (замерено в коде, `googleCalendarClient.ts:60-68`).
3. **Решить про верификацию.** Календарные scope у Google — sensitive, поэтому
   неверифицированное приложение в проде получает экран-предупреждение «Google
   hasn't verified this app» и лимит на число аккаунтов. Для пилота на 2 центра
   это терпимо; для продажи — нет. Верификация требует **верифицированного
   домена** (Search Console), политики конфиденциальности на том же домене,
   демо-видео и обоснования каждого scope; срок — единицы рабочих дней.
   ⚠️ **Домен блокирует и это (пункт 6.3), и I-5.** То есть 6.3 → 6.4-верификация
   — одна цепочка, а не два независимых хвоста.
4. ⚠️ **ПРОВЕРИТЬ ЗАМЕРОМ, А НЕ ПОВЕРИТЬ.** Документация Google описывает
   7-суточный срок как свойство статуса `Testing` и отдельного срока для
   production не называет; но на странице про верификацию среди последствий
   «неверифицированного» приложения упоминается и ограниченный срок жизни
   refresh-токена. Эти два места друг другу не противоречат явно, но и не
   складываются в гарантию. Поэтому критерий приёмки — **прожил ли токен
   больше 7 суток**: через 8 дней после переподключения посмотреть
   `select * from public.important_events where event_type ilike '%gcal%'` —
   если `reauth_required` не появился, статус сработал. Инструмент проверки у
   нас уже есть: это сама №13 после 0194.

---

## 3. Развилки владельца — минуты вашего времени, часы моего

Каждая блокирует работу и ни одна не стоит вам больше получаса.

⚠️ **ТАБЛИЦА НИЖЕ НАПИСАНА 13.09 — ДО ОТВЕТОВ ВЛАДЕЛЬЦА, и до 15.09 она
читалась как «всё это ещё открыто». Это было неверно и стоило повторных
докладов.** Статусы сверены с `DECISIONS-2026-09-13-s68.md` и
`DECISIONS-2026-09-14-s69.md` 15.09 (с72). **Реально открыта ОДНА строка —
`leaked-password`** (плюс Р2, где решение есть, но цена переоценена).

| развилка | что решить | статус (сверено 15.09) |
|---|---|---|
| ~~**Р4**~~ | Playwright / E2E по ролям | ✅ **РЕШЕНО 13.09: НЕТ** на этом этапе (s68 §1). Из бюджета вычтено |
| ~~**Р1**~~ | путь КЕЙСА для Г1-F: дублировать ли часовой гард на сервере | ✅ **РЕШЕНО 13.09: НЕТ — оставить как есть** (s68 §6) |
| **Р2** | правило «где нужен аудит-тригер» (сегодня изменения каталога `services` не пишутся в `audit_log` ВООБЩЕ) | 🟡 **решение ЕСТЬ 13.09** — критерий «таблица с PII ⇒ тригер» (s68 §4); ⚠️ не исполнено: цена переоценена, нужен повторный заход по справочным таблицам. 1,5–2 ч |
| ~~**Р3**~~ | расширять ли №19 на 38 тригерных функций расписания | ✅ **РЕШЕНО 13.09: НЕТ** (`DECISIONS-2026-09-13-s68.md` §5 «тільки два названих»). В №19 добавлены ровно два — `update_patient_details(uuid, jsonb, jsonb)` и `tg_change_markers_queue()`; исполнено в **0192**, наличие обеих подписей подтверждено замером 15.09. **Не путать со строкой `search_path` вне №19 ниже** — в документах с70 «Р3» означало ИМЕННО ЕЁ, и закрыла её 0196. ⚠️ До 15.09 эта строка говорила «ОТКРЫТА, не тронута» — неверно, исправлено |
| ~~**Р5**~~ | `user_change_markers` → `REPLICA IDENTITY FULL` | ✅ **РЕШЕНО 13.09: НЕТ** (s68 §7) |
| ~~**Пять definer-функций**~~ | `emergency_stop_rpc`, `queue_set_status_rpc`, `submit_incident_rpc`, `check_no_overlap`, `check_not_in_past` | ✅ **РЕШЕНО 13.09: пиновать ТРИ**, два тригера — принятый риск письменно (s68 §2). Исполнено в **0192** |
| ~~**`search_path` вне №19**~~ | ✅ **РЕШЕНО 14.09: расширить №2 на ЗНАЧЕНИЕ.** Исполнено миграцией 0196 | факт: в составе пакета 0196 |
| ~~**RF-03**~~ | контраст не привязывается к кабинету осознанно или это дефект импорта | ✅ **РЕШЕНО 13.09: ЗАДУМАНО** (s68 §8) — закрыто записью |
| ~~**I-2 / I-5 / I-6**~~ | диспозиция каждой | ✅ **РЕШЕНО 13.09:** I-2 и I-6 — принятый риск письменно, I-5 блокирована доменом (s68 §10) |
| ~~**3.6**~~ | `pg_timezone_names` фаза 2 | ✅ **РЕШЕНО 13.09:** CHECK **сейчас** (исполнено в 0192), фаза 2 — **позже** (s68 §9). Работа 3–4 ч остаётся, решение не нужно |
| ~~**leaked-password**~~ | ✅ **ЗАКРЫТО 15.09 замером:** владелец открыл `/auth/providers?provider=Email` — тумблер «Prevent use of leaked passwords» **ВКЛЮЧЁН**. Значит отсутствие `auth_leaked_password_protection` в advisors 13.09 и 15.09 означало «уже включено», а не «линт переехал». Неопределённость снята консолью, а не рассуждением | сделано |
| ~~**Р6**~~ — PITR | ✅ **РЕШЕНО 15.09: НЕ покупаем, риск принят письменно** ([`DECISIONS-2026-09-15-s73.md`](DECISIONS-2026-09-15-s73.md) §1) — «согласны потерять рабочий день», пересмотр перед первым реальным центром. Замер: Pro, PITR не куплен, суточные бекапы идут (8 снимков 08–15.09 подряд, PHYSICAL, ≈03:17 по Киеву), Storage 0/0. ⚠️ Цена названа в рабочих единицах: **один рабочий день записей**, а не «24 ч». ⚠️ **Принятие действительно ТОЛЬКО вместе с репетицией (П-4)**: без неё это не принятый риск, а неизмеренный | решение принято; остаётся репетиция |
| **⚠️ Смена пароля / почты** | 🔴 **НОВОЕ 15.09, решения НЕ требует до замера.** На том же экране в заводском положении: `Secure email change` выкл, `Secure password change` выкл, `Require current password when updating` выкл, минимальная длина 6, требований к составу нет. Сложенные, первые три дают: живая сессия → смена пароля без знания старого → смена почты с подтверждением только на НОВОМ адресе → молчаливый постоянный захват аккаунта. Для общего терминала регистратуры сценарий не абстрактный. ⚠️ **Это замер экрана, а не вывод о продукте:** сначала выяснить, идёт ли смена пароля через клиентский `updateUser` или через `/api/staff/password`, и есть ли там своя проверка. `THREAT_MODEL_2026-09-14.md` этот путь не рассматривает | замер: **1–2 ч**, потом решение |

---

## 4. Сколько всего и в каком порядке

| блок | часы |
|---|---|
| 0 — журнал (+0.6 область оценки) | 5–6 |
| 1 — фаза 4 | 6,5–8,5 |
| 2 — фаза 6 (без Р4) | 6–8,5 |
| 3 — фаза 8 | 10–14 |
| 4 — фазы 1/3/5/7 | 16–26,5 |
| 5 — диспозиция находок | 3–4 |
| 6 — вердикт | 2–3 |
| **ИТОГО** | **48–70 ч** |

Плюс Р4, если «да»: +10–14 ч.

По нынешнему темпу (с67 — один большой пакет за сессию, с63 — тринадцать
мелких) это **10–15 сессий**.

### Порядок, и почему именно такой

1. **Блок 0 первым, без обсуждения.** Всё остальное измеряется по журналу.
   Пока он отстаёт на 222 коммита и противоречит сам себе, любой вердикт —
   вердикт по неверным входным данным. Это ровно тот дефект, который журнал
   сам у себя нашёл в с55 и записал правило против него.
2. **Потом развилки** — одним заходом, пока я занят блоком 0. Они дешёвые для
   вас и разблокируют 20+ часов работы.
3. **Блоки 1 и 2 — сразу после блока 0.** Оба требуют живой сессии персонала,
   поэтому их выгодно сцепить в один день: Г1-E, Г1-G, Ф4-8 и проверка под
   регистратором закрываются одним заходом. После них фаза 4 закрыта честно, а
   фаза 6 — по смыслу, а не по форме.
   ⚠️ **Рекомендация первой редакции «восстановление — вне очереди, следующим
   по важности» ОТОЗВАНА.** Она стояла на доводе «это медицинские данные»,
   который оказался ложным (раздел 0-бис). Блок 3.2 остаётся важным — потеря
   очереди это операционный простой центра, — но вперёд блоков 1 и 2 не лезет.
4. **Блок 3 (фаза 8)** — после них. Внутри него 3.2 (восстановление) первым:
   это по-прежнему единственное место аудита, где не сделано ничего.
5. **Блок 4** — самый большой и самый делимый; идёт фоном между остальными.
6. **Блок 6** — последним, и его потолок известен заранее: **CONDITIONAL GO**,
   пока не сделаны ротация ключа и домен. Это решение владельца от с51, а не
   следствие незакрытых фаз.

---

## 4-бис. Как ускоряем — решение владельца 13.09, всё измерено

Сроков нет («ничто не жмёт»), поэтому темп задаёт качество, а ускорения берём
только структурные: те, что убирают ОЖИДАНИЕ, а не проверки.

### Замер: контейнер против машины владельца

Те же стенды, тот же скрипт, чистый клон из GitHub (репозиторий публичный):

| стенд | машина владельца | облачный контейнер |
|---|---|---|
| `falsify-0182` | 41 с | **30 с** |
| `falsify-0183` | 26 с | **20 с** |
| `falsify-f6srv` | 27 с | **14 с** |
| `falsify-u70` | 73 с | **61 с** |
| `npm ci` с нуля | — | **18 с** |

⚠️ **Шардирование параллельными клонами ОТБРОШЕНО по замеру.** В контейнере
2 ядра, vitest и так многопоточный: 125 с последовательно против 106 с в двух
параллельных клонах — выигрыш 15 %, не окупает сложность. У владельца 4 ядра,
но там это столкнётся с правилом «гейт гоняет один человек» и с соседней
сессией в том же дереве.

**Настоящий выигрыш не в скорости, а в том, что 48-минутная ревизия уходит с
критического пути.** 13.09 она дважды блокировала всё: 96 минут, в которые
дерево нельзя было трогать ни мне, ни владельцу.

### Что внедряем (все четыре приняты)

1. **Полная ревизия — в контейнере.** Перед прогоном ветка пушится, ревизия
   идёт фоном на чистом клоне, машина владельца свободна. ⚠️ Честная граница:
   контейнер видит ЗАПУШЕННОЕ, а не рабочее дерево — то есть это проверка
   ветки, а не working copy. Для нашей схемы (работа ветками) это не потеря.
2. **Пакет решений одним заходом.** 11 развилок одним списком с замерами и
   ценой; полчаса владельца разблокируют ~20 ч работы.
3. **Блок 0 первым.** `TASK #0` стоит ~15 мин каждую сессию, а устаревший
   журнал заставляет перемерять заново: 12 сессий × 15 мин ≈ 3 ч, которые
   окупаются однократной правкой журнала.
4. **Живые проверки одним днём.** Г1-E, Г1-G, Ф4-8 и регистратор — один заход
   с сессией персонала вместо трёх подъёмов одного и того же.

Плюс два бесплатных: **ревью идут параллельно ревизии** (13.09 шли
последовательно) и **предпроверка «трогает ли правка `invariants_check`»** —
минута, которая превращает сюрприз «нужен передрук + 50 мин» в пункт плана.
Именно на этом сюрпризе оценки проекта промахивались вдвое.

### Что НЕ делаем, и чем это доказано

Каждый пункт — с доказательством из 13.09, а не из общих соображений.

| «ускорение» | почему нет |
|---|---|
| убрать один раунд ревью | четыре раунда за день, **все четыре** нашли настоящий дефект; автор был неправ каждый раз |
| пропустить полную ревизию после передрука | она дала **34/37** — четыре мутации не применялись вообще |
| пропустить живые проверки | Г1-F **числился** проверенным и не был |
| верить диску вместо артефакта | сделано в тот же день: диагноз по инциденту `git add -A` был уверенным и неправильным, потому что мерился диск в момент чужой правки, а не коммит |
| отдать код субагентам | их правки потребуют собственных двух ревью — чистый минус |

### Новый бюджет

Часы падают умеренно: **48–70 → ≈ 42–60 ч**. Календарь сжимается сильнее —
**10–15 → 7–11 сессий**, потому что 48-минутные блоки перестают быть
последовательными, а решения перестают быть блокерами.

---

## 5. Чего этот план НЕ утверждает

- Я **не перепроверял** живую БД по каждому пункту — сверка сделана по
  репозиторию, git и точечным запросам к проду (advisors, леджер, сторож).
- Журнал прочитан не целиком: строки 1–60, 352–420, 680–740, 1380–1581.
  Разделы 60–352 и 740–1380 — история пакетов с46–с54, на план не влияют, но
  я их не читал.
- Оценки времени — **оценки**, и проект уже доказал, что они занижены вдвое.
  Диапазоны растянуты, но верхняя граница не гарантия.
- «13 таблиц из 21» взято из блоков с55 хендоффа. Сегодняшнее число политик
  могло сдвинуться (0183 сузила набор 64 → 63) — **перемерить перед началом
  блока 2.1**.
