# Модель угроз RadFlow — 2026-09-14 (блок 4.1, фаза 1)

**Зачем этот документ существует.** Фаза 1 аудита не двигалась ни на строку с
27.08 — и не потому, что её не делали, а потому что **не было сформулировано
условие её закрытия**. Нельзя закрыть фазу «архитектура и модель угроз», если
модели угроз нет: не с чем сверять. Этот документ — недостающая половина, и
последний раздел формулирует условие закрытия явно.

⚠️ **Всё, что помечено «замер», снято с прода 14.09.2026 запросом или чтением
кода.** Где замера нет — так и написано. Модель угроз, построенная на
предположениях о собственной системе, хуже отсутствующей: она создаёт
ощущение покрытия.

---

## 1. Область: что здесь защищается, а что — нет

**RadFlow — B2B-SaaS управления очередью в диагностических центрах, НЕ система
медицинских записей.** Это не смягчение, а замер (раздел 0-бис журнала):
колонок под диагноз, заключение, результат или снимок в схеме **не существует
ни одной**.

Что есть на самом деле (замер, 329 записей очереди):

| класс | поля | характер |
|---|---|---|
| **ПДн пациента** | `patient_name`, `patient_phone`, `patient_email`, `patient_dob`, `patient_sex`, `patient_weight` | основное наполнение |
| **операционное** | `status`, `scheduled_*`, `room_id`, `doctor`, `cito`, `duration_min` | расписание |
| **тонкий слой, смежный со здоровьем** | `indication` (13 строк), `contraindications=true` (2), `has_contrast=true` (18) | редкое, но чувствительное |
| **канал свободного текста** | `radiologist_note` | **0 строк** — канал есть, пуст |

⚠️ **Чувствительность здесь не в «медкарте», а в СВЯЗКЕ:** «названный человек с
этим телефоном записан на МРТ с контрастом на четверг». Раскрытие этой связки —
главный ущерб, и почти все пути ниже ведут именно к ней.

**Вне области:** сертификация HIPAA/GDPR-DPIA как процесс, безопасность
инфраструктуры Supabase и Vercel как провайдеров, физическая безопасность
центров, защита от инсайдера-владельца проекта (он имеет service-role ключ по
определению).

---

## 2. Активы — по цене раскрытия, а не по объёму

| # | актив | где | цена |
|---|---|---|---|
| **A1** | связка «ПДн ↔ исследование ↔ время» | `queue_entries`, `waitlist_entries`, `patient_cases` | раскрытие = реальный вред пациенту |
| **A2** | контакты направителей и персонала | `profiles`, `referrer_private`, `doctors` | адресный фишинг, перехват потока |
| **A3** | **service-role ключ** | env Vercel | обходит RLS полностью — это «всё сразу» |
| **A4** | операционная непрерывность очереди | БД + зеркало | центр утром не знает, кто записан |
| **A5** | аудит-след | `audit_log` (2 816), `important_events` (407) | без него инцидент недоказуем |
| **A6** | секреты интеграций | Vault (3), `integration_keys` (1), OAuth Google | подделка событий, доступ к календарю |
| **A7** | целостность расписания | гарды пересечений, CAS | двойная запись = живой конфликт в кабинете |

---

## 3. Кто действует и где границы доверия

**Роли (замер `user_role`):** `admin`, `radiologist`, `registrar`, `referrer`,
`ceo`. Живых профилей **9**: admin 2, ceo 2, referrer 3, radiologist 1,
registrar 1. `auth.users` — **14** (то есть 5 auth-юзеров без профиля).

| граница | что пересекает | чем держится (замер) |
|---|---|---|
| **Б1** аноним → приложение | 11 роутов без ролевого гейта: 4 auth, 5 FHIR-интеграционных (за ключом), 2 токенных | rate-limit на анонимных, ключ у FHIR, одноразовые токены |
| **Б2** аутентифицированный → чужие данные | 23 роута под `requireRole*`; **63 политики RLS на 21 таблице**; 12 таблиц — deny-all (RLS без политик) | RLS + `requireRole()` ДО `createAdminClient()` |
| **Б3** роль → повышение роли | `profiles.role` | **триггер** `guard_profile_privileges` (RLS разрешает владельцу UPDATE всех колонок!) |
| **Б4** приложение → БД под service-role | 4 cron-роута + админские | `CRON_SECRET` fail-closed; ключ в env |
| **Б5** БД → наружу | `pg_net` в 2 джобах, outbox → n8n, зеркало → Google | `assertNoLiveDelivery`, fail-closed список команд |
| **Б6** партнёр → FHIR-фасад | 5 роутов `/api/integrations/v1/*` | `integration_keys`, `no-store` |

⚠️ **Б3 — самая недооценённая граница.** RLS-политика `profiles_update_self`
разрешает владельцу строки UPDATE **всех** колонок, включая `role`. Отказывает
не политика, а **триггер**. Снятый или выключенный триггер = самоэскалация до
`admin`. Именно поэтому инвариант №17 пинит пару (таблица, триггер) **и**
`tgenabled` — до 0173 слова `tgenabled` в стороже не было вообще.

---

## 4. Пути атаки — и что стоит на каждом СЕГОДНЯ

Формат: путь → чем закрыт (замер) → что остаётся.

### T1. Внешний аноним читает чужую очередь
**Путь:** подобрать/угадать эндпоинт, обойти RLS через `anon`.
**Закрыто:** RLS включён на **всех 33** таблицах; `anon` не имеет политик на
данные пациентов; **11** definer-функций достижимы из `anon` — и именно их
тела пинит инвариант №22 полным сырым md5.
**Остаётся:** появление новой definer-функции с грантом `anon` — её увидит
№22; но **новая definer-функция с грантом только `authenticated`** не видна ни
№19 (список имён), ни №22 (фильтр по `anon`) — названная межа 0191 №3.

### T2. Аутентифицированный пользователь читает чужой центр
**Путь:** свой логин + подмена `clinic_id` в запросе.
**Закрыто:** политики стоят на `auth_clinic_id()`; **45** definer-функций
достижимы из `authenticated`, их решения о доступе (`auth_can_refer`,
`auth_referrer_visible_rooms`, `auth_referrer_clinics`,
`auth_can_see_slot_details`) запинены в №19 с 0191 — включая **значение**
`search_path`.
**Остаётся:** ⚠️ **`search_path` у 81 definer-функции ВНЕ списка №19 не
стережёт никто** (замер 14.09; было 85 до 0192). Для `SECURITY DEFINER` это
подручниковый вектор эскалации, и развилка 3 у владельца **открыта**.

### T3. Самоэскалация роли
**Путь:** `update profiles set role='admin' where id = auth.uid()`.
**Закрыто:** триггер `guard_profile_privileges`; №17 пинит пару и `tgenabled`;
№19 пинит тело; №20 держит, что дефолт разрешён ровно двум колонкам.
**Остаётся:** ничего названного. Это самая плотно закрытая точка системы.

### T3-бис. Забытые auth-аккаунты без профиля
**Путь:** аккаунт, который может аутентифицироваться, но роли не имеет.
**Замер 14.09:** `auth.users` — 14, `profiles` — 9, сирот-профилей **0**, но
**5 auth-аккаунтов без профиля**. Все созданы 22 и 26 июня (первые дни
проекта), все **подтверждены по почте**, трое **не входили ни разу**.
**Закрыто:** без `profiles` роли нет, а все политики стоят на `auth_role()` /
`auth_clinic_id()` — то есть такой аккаунт не видит данных.
**Остаётся:** это всё же **пять живых учётных записей** с подтверждённой
почтой, которых никто не считает пользователями. Они не дают доступа к
данным, но расширяют поверхность входа и портят любой подсчёт «сколько у нас
людей». Дёшево: удалить или задокументировать как служебные. **Раньше нигде
не было названо.**

### T4. Кража service-role ключа (A3)
**Путь:** утечка env, компрометация Vercel-аккаунта, логи.
**Закрыто:** ключ только серверный; `requireRole()` стоит **до**
`createAdminClient()` во всех роутах (проверено аудитом 12.07 — «дыр не
найдено»).
**Остаётся:** ⚠️ **ротация ключа не делалась ни разу** — сознательно привязана
владельцем к первому реальному центру (блок 6.2). Пока ключ один и вечный,
компрометация = полный доступ без следа в `audit_log` (service-role пишет от
своего имени). **Это принятый риск, а не покрытая угроза.**

### T5. Подделка вызова cron-роутов
**Путь:** POST на `/api/outbox/deliver` и др. без секрета.
**Закрыто:** 4 роута fail-closed по `CRON_SECRET`; секрет в Vault, в теле
джоба его нет.
**Остаётся:** секрет не ротируется; один секрет на все 4 роута.

### T6. Утечка через исходящие интеграции (Б5)
**Путь:** заставить систему отправить данные не туда.
**Закрыто:** `assertNoLiveDelivery` + fail-closed `N8N_FREE_COMMANDS`
(расширен на `cleanup` в с68); эмитер-триггер только на `queue_entries`.
**Остаётся:** I-5 — `system`/`fullUrl`/`link.next` нестабильны при смене
домена (блокировано регистрацией домена).

### T7. Потеря или порча данных (A4)
**Путь:** ошибка оператора, сбой провайдера, злонамеренное удаление.
**Закрыто:** 79 триггеров-гардов, CAS, `a01_no_client_delete` на трёх
таблицах; 192 миграции под симметричным гейтом; 23 инварианта ночью.
**Остаётся:** PITR не подтверждён; **репетиции восстановления не было ни разу**.
✅ **I-7 ЗАКРЫТА 14.09 ОБЕИМИ половинами:** владелец переподключил оба центра
(зеркала живые, синк ~2 мин), агент накатил **0194** — проверка №13 теперь
видит и аварийно выключенное зеркало, а не только «включённое и устаревшее».
⚠️ И замер, который меняет ожидания: refresh-токен Google живёт **ровно 7
суток**, пока OAuth-приложение в статусе Testing (две независимые точки в
`important_events`: 7 сут + 9 с и 7 сут + 1 мин 25 с). Значит зеркала будут
умирать еженедельно (следующий раз ≈ 21.09), пока consent screen не
опубликован. Проверка теперь это ВИДИТ — но лечит публикация, а не порог.

### T8. Тихая деградация защиты — **фирменный класс этого проекта**
**Путь:** не атака, а дрейф: политику правят в UI Supabase мимо репозитория,
триггер выключают, функцию переписывают.
**Закрыто:** №16 `policy_digest` (63 политики), №17 `guard_triggers` (пара +
`tgenabled`), №19 `guard_fn_bodies` (38 подписей + `attrs` + ACL), №22
`grant_digest`, №23 `schema_digest` (колонки, constraint-ы, enum-ы, уникальные
индексы). 38 стендов фальсификации доказывают, что сторожа ловят.
**Остаётся:** см. раздел 5 — это самая опасная категория, и у неё есть
свежий живой пример.

---

## 5. ⚠️ Класс отказа, который эта система порождает сама

У RadFlow очень плотная сеть сторожей. Поэтому **главная угроза здесь — не
пробитая защита, а защита, которая молчит.** Аудит ловит этот класс регулярно:

* №17 до 0173 не смотрел на `tgenabled` — гард можно было **выключить**;
* №3 смотрит `relkind='r'` — секционированная таблица без RLS невидима;
* №19 до 0191 не смотрел на `attrs`/ACL — права менялись молча;
* **I-7 (14.09):** проверка №13 фильтрует `where g.enabled`, а путь отказа
  (`failClosedTransition`) сам ставит `enabled=false`. Зеркало очереди мертво
  **11,5 суток**, `invariants_check` — зелёный, джоб — «30 успехов за час».
  ✅ **Закрыто 0194** — и причина оказалась глубже формулировки выше: дело не
  в том, что «забыли условие», а в том, что `gcal_enabled_invariant_chk`
  делает `enabled = true` НЕВОЗМОЖНЫМ без `status='ready'`. То есть отказ
  ОБЯЗАН погасить флаг — проверка стояла на поле, которое неисправность
  гарантированно перекидывает. Это усиливает правило ниже: смотреть надо не
  только «было живым и перестало», но и на то, **может ли поле, по которому
  фильтрует сторож, пережить ту самую неисправность**.
  ⚠️ И ещё одна форма того же класса, найденная попутно 14.09:
  `supabase/smoke/gcal_pg_cron_smoke.sql` **не может пройти с 0186** (падает
  на фикстуре: клиник без подключения 0 из 2; плюс оба пина тела протухли на
  значениях 0185). Проверка есть — сработать не может. Никто этого не видел,
  потому что **тело самого сторожа не пинит ни одна из 23 проверок** (в №19
  ноль подписей `invariants_check`, №7 пинит md5 ФАЙЛОВ, тесты читают файл на
  диске, а не `prosrc`). Это межа М-4 пакета 0194 и отдельный долг.

**Правило, выведенное из этих четырёх случаев и годное как критерий ревью:**

> Если проверку можно удовлетворить, **выключив то, что она стережёт**, — это
> не проверка. Сторож обязан смотреть на состояние «было живым и перестало», а
> не только на «включённое и устаревшее».

Это и есть главный вывод модели угроз для **этой** системы, и он дороже любого
из путей T1–T7.

---

## 6. Чего эта модель НЕ покрывает — названо, чтобы не создавать иллюзию

1. **Инсайдер с service-role ключом** — вне области по определению (T4).
2. **Провайдеры** (Supabase, Vercel, Google) — их компрометация не
   моделируется; следствие принимается.
3. **Социальная инженерия против персонала центра** — вне продукта.
4. **Доступность/DoS** — системного анализа доступности в аудите не было, и
   эта модель его не заменяет: нагрузочного профиля нет (блок 3.5), поведение
   под наплывом не мерено.
   ✅ **Но конкретная дыра M-13 ЗАКРЫТА — проверено 14.09, а не переписано.**
   Аудит 12.07 писал: «rate limiting только на двух анонимных роутах; без
   лимита `/api/staff`, `/api/ceo/grant`, `/api/referrers/invite` — каждый
   **создаёт auth-пользователя**». Замер сегодня: `rateLimit` вызывается в
   **13 роутах**, включая все три названных, плюс `services/import`, `search`,
   `journal`, пять роутов Google Calendar, `account/set-password`,
   `auth/login`, `auth/login-available`.
   ⚠️ Первая редакция этого раздела повторяла июльскую формулировку как
   действующую. Она устарела на два месяца. Записываю это здесь, потому что
   модель угроз, пересказывающая старый аудит вместо замера, — ровно то, от
   чего предостерегает шапка документа.
5. **Клиентская сторона** (XSS, зависимости фронта) — покрыто фазой 2
   частично; отдельного анализа не было.
6. **Мобильные/офлайн-сценарии** — их нет.

---

## 7. Условие закрытия фазы 1 — то, чего не хватало

Фаза 1 закрывается, когда выполнено **всё** нижеперечисленное. Раньше этого
списка не существовало, и именно поэтому фаза стояла.

| # | условие | статус на 14.09 |
|---|---|---|
| 1.1 | Модель угроз написана и лежит в репозитории | ✅ **этот документ** |
| 1.2 | Каждый актив A1–A7 имеет названный путь и названный сторож | ✅ раздел 4 |
| 1.3 | Каждая граница доверия Б1–Б6 замерена, а не предположена | ✅ раздел 3 |
| 1.4 | То, что модель НЕ покрывает, перечислено явно | ✅ раздел 6 |
| 1.5 | Для каждой открытой угрозы есть диспозиция: чиним / принимаем письменно / блокировано внешним | 🟡 **T2** (развилка 3 открыта), **T3-бис** (5 забытых auth-аккаунтов — диспозиции нет, находка новая), **T4** (принято письменно, привязано к первому центру), **T7** (✅ I-7 закрыта 14.09 пакетом 0194; PITR по-прежнему не подтверждён, репетиции не было) |
| 1.6 | Модель сверена с реальной архитектурой после последней миграции | ✅ замеры 14.09, ledger 192 |

⚠️ **Фаза 1 НЕ закрывается сегодня** — держит строка 1.5. Из четырёх пунктов
работа агента (I-7) закрыта 14.09 пакетом 0194; остаются ТРИ, и все три
требуют слова владельца: развилка 3 (`search_path`), PITR + репетиция
восстановления, диспозиция T3-бис. Это и есть честный ответ на вопрос «чего
фазе не хватает», которого не было с 27.08.
⚠️ И добавился четвёртый, уже агентский: **М-4 — тело сторожа не пинится
ничем** (раздел 5). Он не блокирует фазу 1, но это ровно тот класс, ради
которого фаза писалась.

⚠️ **И отдельно — чего этот документ НЕ делает.** Он не «закрывает фазу 1
написанием себя». Модель угроз — необходимое условие, а не достаточное:
строки 1.1–1.4 и 1.6 она закрывает, строку 1.5 — нет, и закрыть в одиночку не
может. Документ, который объявил бы фазу закрытой фактом своего появления, был
бы ровно тем «ощущением покрытия», от которого предостерегает его же шапка.

---

## 8. Что делать дальше — по убыванию отдачи

1. ✅ **Переподключить зеркало** (I-7) — сделано владельцем 14.09, A4 возвращён.
2. ✅ **Расширить проверку №13** — сделано пакетом 0194 (14.09).
2-бис. ⚠️ **ОПУБЛИКОВАТЬ consent screen Google** — новое, и по отдаче это
   теперь пункт №1: пока приложение в статусе Testing, refresh-токен живёт
   ровно 7 суток (замерено дважды), то есть зеркала будут умирать каждую
   неделю (следующий раз ≈ 21.09). 0194 делает это ВИДИМЫМ, но не лечит.
3. **Закрыть развилку 3** (`search_path` вне №19) — снимает T2.
4. **Репетиция восстановления** — снимает T7 и закрывает фазу 8.
5. **Разобрать 5 auth-аккаунтов без профиля** (T3-бис) — минуты, снимает
   единственную новую находку этой модели.

---

## 9. Сверка с74 (15.09.2026) — условие закрытия фазы 1, пройденное построчно

⚠️ Раздел 7 писался на леджере **192**. С тех пор легли **0193–0200**, и
строка 1.6 («модель сверена с архитектурой после последней миграции») сама
протухла. Ниже — сверка на леджере **200**, каждое число снято запросом 15.09.

### 9.1 Что сдвинулось в архитектуре (строка 1.6)

| что | 14.09 (разделы 1–6) | 15.09 (замер) | каким пакетом |
|---|---|---|---|
| проверок `invariants_check` | 23 | **25** (+№24 `auth_orphan_accounts`, +№25 `guard_self_pin`) → **26** (+№26 `role_surface`) | 0197, 0198, 0201 |
| подписей в списке №19 | 38 | **43** → **59** | 0193 (+2), 0200 (+3), 0201 (+16) |
| `auth.users` / профилей / сирот | 14 / 9 / **5** | **9 / 9 / 0** | 0197 |
| definer-функций со слабым `search_path` | 34 | **0**, значение пинит №2 | 0196 |
| тело самого сторожа | не пинит ничто (М-4) | пин в комментарии, сверяет №25 и `planGuardPin` в гейте | 0198 |
| DEFINER-чтение листа ожидания радиологом (I-8) | живая дыра | кабинетный гейт | 0193 |
| зеркало Google мёртвым при зелёном стороже (I-7) | живая дыра | №13 видит `enabled=false` | 0194 |
| карточка центра направителю | по наличию связи | по СТАТУСУ связи | 0195 |
| оракул радиолога в двух waitlist-RPC (A-1/A-2), межтенантный замок (A-4) | не было в модели | закрыто | 0199 |
| definer-функций, доступных `authenticated`, вне №19 и №22 | 21 (межа 3) | **18** → **2** (без гейта: `search_cities`, `search_clinics`) | 0200, 0201 |
| поверхность клиентских ролей: членства, атрибуты, настройки, ACL схем и базы, default ACL (межи 1–2) | не пинило ничто | **№26 `role_surface`**, 62 ключа; `checked` 26 | 0201 |
| восстановление | рунбук без репетиции | репетиция 15.09, RPO замерен в строках | с73 |

### 9.2 Диспозиция каждой угрозы (строка 1.5)

| угроза | остаток по разделу 4 | диспозиция на 15.09 |
|---|---|---|
| **T1** | новая definer-функция только для `authenticated` не видна №19/№22 | ✅ **закрыто 0201** (Р74-1 «все 16», `PR-0201-gated-rpcs-role-surface.md`): 21 → 18 (0200) → 2 без гейта; ⚠️ НОВАЯ такая функция по-прежнему видна только ревью — это ритуал №19 в `AGENTS.md`, не сторож |
| **T2** | `search_path` вне №19 не стережёт никто | ✅ **закрыто 0196** (значение в №2, слабых 0) |
| **T3** | — | ✅ без изменений |
| **T3-бис** | 5 auth-аккаунтов без профиля | ✅ **закрыто 0197** (удалены, №24 следит) |
| **T4** | ротации service-role ключа не было | ✅ диспозиция есть: **принятый риск**, П-1 `ToDo_Production.md`, первый реальный центр |
| **T5** | `CRON_SECRET` не ротируется, один на 4 роута | ✅ **диспозиция есть (Р74-7, 15.09):** ротировать `CRON_SECRET` в одно окно с service-role ключом — П-1 `ToDo_Production.md`; исполняется в день П-1. ⚠️ До 24.09 (с77) здесь стояло «диспозиции нет — развилка Р74-7», хотя §9.3 и `DECISIONS-2026-09-15-s74.md` фиксировали ответ владельца: строку таблицы не обновили вместе с вердиктом |
| **T6** | I-5 при смене домена | ✅ блокировано внешним (домен, П-2) |
| **T7** | PITR не подтверждён, репетиции нет | ✅ **закрыто с73**: PITR — принятый риск Р6 письменно; репетиция 15.09 прошла (≈5 мин, вход живым паролем) |
| **T8** | межи 1, 2 аудита 0191 (членство в ролях, `nspacl`, default privileges) | ✅ **закрыто 0201** для клиентских ролей (Р74-2): проверка №26 `role_surface`; `service_role` и объекты вне `public` — названные межи |

⚠️ **И одна поправка к разделу 6, п. 4 («доступность не мерена»).** 15.09
снята модель нагрузки по замерам (`LOAD-PROFILE-2026-09-15.md`): пропускная
способность не узкое место, а **задержка записи** — прямой зонд показал, что
~90 % стоимости записи в очередь съедает скан `pg_timezone_names` в триггере
`check_no_overlap` (795–938 мс на холодном соединении). Это не угроза
безопасности, но это та самая «доступность», которой модель не покрывала.

### 9.3 Вердикт по условию закрытия

| # | условие | 15.09 |
|---|---|---|
| 1.1–1.4 | модель, активы, границы, непокрытое | ✅ без изменений |
| 1.5 | диспозиция каждой открытой угрозы | ✅ ответы владельца Р74-1 (T1), Р74-7 (T5), Р74-2 (T8); T1 и T8 **исполнены 0201** (15.09) |
| 1.6 | сверка с архитектурой после последней миграции | ✅ этот раздел, леджер 200 |

✅ **ФАЗА 1 ЗАКРЫТА 15.09 (с74)** — ответы владельца получены
(`DECISIONS-2026-09-15-s74.md`): **Р74-1 «все 16»** (T1 → пакет 0201),
**Р74-2 «только клиентские роли»** (T8 → пакет 0201), **Р74-7 «`CRON_SECRET`
с П-1»** (T5 → принято, ротация в окно П-1). Строка 1.5 — ✅.

⚠️ **Фаза 1 закрывается ответами владельца, а не работой.** Любой из трёх
ответов — «чиним» (тогда это пакет в очереди, и фаза закрывается его
названием) или «принимаем письменно» — ставит в строке 1.5 галочку. Работы
агента, без которой фаза не закрывается, **не осталось**.

---

## 10. Дополнение с77 (24.09.2026): два пути, которых в модели не было

⚠️ Оба пути добавлены **после** закрытия фазы 1. Фазу они не переоткрывают — у
каждого есть диспозиция, — но модель, не называющая их, создавала бы то самое
«ощущение покрытия». Прод на момент записи сверен запросом (24.09 05:22 UTC):
леджер **202/202**, `checked:26`, упала только №13 (П-3), миграций в с77 нет.

### T9. Захват аккаунта из живой сессии — П-6 (⚠️ НЕ ЗАМЕРЕН)

**Путь:** атакующий получает живую сессию (незаблокированный ПК регистратуры,
украденная cookie, XSS) → меняет пароль **без знания старого** прямым
`PUT /auth/v1/user` с токеном этой сессии → меняет почту, подтверждение уходит
**только на новый адрес** → владелец аккаунта не может войти и ни о чём не
узнаёт.
**Состояние Auth (прочитано в дашборде 15.09, живьём не проверено):**
`Secure email change` — выкл, `Secure password change` — выкл, `Require current
password when updating` — выкл, минимальная длина пароля — 6.
**Что усиливает путь (чтение кода с77):**
* приложение **ни разу** не вызывает `auth.updateUser` — все смены пароля идут
  admin API на сервере. Значит, самообслуживаемый путь GoTrue не нужен ни одному
  живому сценарию, а существует только для атакующего; он же **обходит минимум 8
  символов** из `zPassword` (у GoTrue — 6);
* вход по логину резолвит адрес из `auth.users` (`resolve_login_email`), а экраны
  показывают копию `profiles.email` — **после смены почты интерфейс показывает
  старую**, захват невидим и администратору;
* админский сброс пароля почту не трогает: если почту уже сменили, атакующий
  возвращает контроль через публичный `/auth/v1/recover`.
**Что, возможно, ослабляет путь — и тоже не замерено:** служебные адреса
направителей, CEO и радиологов (`*.radflow.local`) недоставляемы; если в проекте
нет своего SMTP, встроенная почта Supabase шлёт письма только адресам команды —
тогда шаг с почтой может не пройти **случайно**, а не потому, что закрыт.
**Диспозиция:** блокирующий пункт **П-6** (решение владельца 22.09). Протокол
замера — [`PROTOCOL-P6-auth-change-2026-09-24.md`](PROTOCOL-P6-auth-change-2026-09-24.md);
живой прогон пишет в `auth` прода и идёт только по слову владельца.
**После замера:** заменить в этом разделе «не замерен» на результат каждого шага.

### T10. Вывоз ПДн файлом — экспорт поиска в Excel (с77)

**Путь:** роль в пределах своей области массово выгружает связку «ПДн ↔
исследование ↔ время» (актив A1) файлом `.xlsx` через `POST /api/search/export`.
Это новый канал: до с77 файловых выгрузок с сервера в продукте не было.
**Закрыто:**
* область, фильтры и скрытия — из того же движка, что экран
  (`lib/searchEngine.server.ts`): файл не может показать больше экрана;
  радиолог — только назначенные кабинеты, направитель — только свои записи;
* CEO — без колонки «Телефон» (колонка отсутствует, а не пустая); поиск цифрами
  телефона у CEO запрещён — до с77 номер восстанавливался подстрочным поиском
  примерно за 100 запросов;
* имя направителя — только связанного грантом с центром записи; подпись фильтра
  в файле — только из результатов: ни поиск, ни экспорт не отвечают «ПИБ по UUID»;
* след — событие `patient_data.exported` в журнал **каждого** центра, чьи записи
  ушли в файл (только количество этого центра, источник, формат; без ПДн);
* потолок 5000 строк, бюджет времени, `no-store`; формульная инъекция закрыта
  (`quotePrefix`, только `inlineStr`).
**Остаётся:**
* предел 10 файлов за 10 мин — **тормоз, а не замок**: постраничный поиск уже
  отдаёт 60 × 100 строк в минуту, а лимитер fail-open; замок — область роли;
* **Н-8:** «CEO без телефонов» держат UI и API, **не БД** — `queue_ceo_read` /
  `waitlist_ceo_read` отдают строку целиком (замер: 298 телефонов очереди и 6
  листа под сессией CEO). Прямой запрос PostgREST из браузера CEO обходит скрытие.
  ✅ **Принято владельцем письменно 24.09 (с79):** «CEO — доверенная роль,
  владелец данных центра»;
* ~~**Н-9:** персонал может выставить записи произвольный `referrer_id`, и этот
  профиль начинает читать запись~~ — ✅ **закрыто 0203 (25.09, с79)** вместе со
  вторым ключом `created_by`; вторая половина (отозванный ПОСЛЕ назначения грант
  чтение не забирает, Н-14) — **в работе: пакет 0204 (с80, решение владельца
  25.09); до наката на прод НЕ закрыта**;
* ~~**Н-10:** CSV-экспорт `CeoDashboard` (до 5000 строк с ПИБ) журнала не пишет~~ —
  ✅ **закрыто 25.09 (с79)**: `POST /api/ceo/export` с журналом;
* файл, покинувший систему, не контролируется ничем — как и скриншот; это вне
  области (раздел 6, п. 3).
**Диспозиция:** канал принят в продукт решением владельца 23.09 («экспорт для
всех ролей, весь результат»); из названного здесь Н-8 принят владельцем
(24.09), Н-9 и Н-10 закрыты в с79, Н-14 — пакет 0204 (см. выше). Разбор —
[`PR-s77-search-referrer-export.md`](PR-s77-search-referrer-export.md).

### T10б. Вывоз ПДн файлом — CSV колл-листа (с80, Н-16)

**Путь:** admin/registrar центра выгружает день обзвона — ПИБ, телефон,
процедура, кабинет, статус звонка, заметка звонка (актив A1) — через
`POST /api/call-list/export`. До с80 файл собирал браузер из состояния доски:
без события в журнал и без экранирования формул вообще.
**Закрыто:**
* доступ — позитивная форма правила страницы `/call-list`: только admin/registrar
  настроенного центра; радиолог (в т. ч. с `ceo_access`), направитель, CEO,
  персонал без центра — 403; центр — из сессии, тело — только `date` (strict);
* данные — RLS-клиентом сессии, фильтр центра/дня/статусов отсекает строки,
  видимые по `created_by`/`referrer_id`/гранту CEO; колонки файла ⊆ колонок
  доски (`note` бронирования в файл не идёт — ревью с80, L-2);
* след — `patient_data.exported {source: "call_list", rows, format: "csv",
  scheduledDate}` в журнал центра (clinic_id из строк; день файла — в заголовке
  события), fail-open с логом; keyset под `db-max-rows`, `no-store`, 10 файлов
  за 10 мин;
* `lib/csv.ts` — апостроф перед `= + - @` TAB CR LF, CR/LF/TAB в клетке → пробел,
  знак формулы сразу после запятой или `;` — тоже с апострофом (ревью с80: файл,
  открытый в LibreOffice с «Evaluate formulas» и разделителем «,» — либо «,»/«;»
  с очищенным ограничителем текста — терял границы кавычек, и хвост ПИБ после
  разделителя становился живой формулой).
**Остаётся:**
* лимит — тормоз, не замок (fail-open); те же строки персонал читает прямым
  PostgREST — замок роль и центр;
* телефон с «+» (≈99 % номеров) — с **видимым апострофом** в Excel/LibreOffice:
  цена защиты от формул в CSV; альтернатива без неё — `.xlsx` (`lib/xlsx.ts`,
  `quotePrefix`), решение владельца;
* импорт с разделителем «пробел» или любым «другим» символом тоже режет клетку
  («Тест =2+3 Три» → формула) — в CSV не закрывается без апострофа перед « + »
  в каждой многошаговой процедуре; класс целиком закрывает только `.xlsx`;
* файл вне системы не контролируется (раздел 6, п. 3).
**Диспозиция:** канал существовал и до с80 (клиентский CSV); с80 перевела его на
сервер с журналом. Разбор — [`PR-s80-call-list-export.md`](PR-s80-call-list-export.md).
