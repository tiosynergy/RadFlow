# ToDo перед реальным продакшеном

**Что это.** Один список всего, что должно быть сделано ДО того, как в системе
появится первый реальный центр с живым потоком пациентов. Собран 14.09.2026
(с69) из плана `PLAN-audit-completion-2026-09-13.md`, решений
`DECISIONS-2026-09-13-s68.md` / `DECISIONS-2026-09-14-s69.md` и находок аудита.

**Зачем отдельный файл.** В плане эти пункты разбросаны по блокам 3, 5 и 6, а в
решениях владельца они спрятаны в формулировке «критерий пересмотра — первый
реальный центр». Пока они лежат врозь, их легко не заметить именно в тот день,
когда они все становятся срочными.

⚠️ **Правило чтения.** Здесь только то, что привязано к выходу в прод. Текущая
работа аудита (блоки 0–5 плана) живёт в плане и в журнале, а не тут. Если
пункт сделан — он не удаляется, а помечается ✅ с датой и ссылкой на замер.

✅ **Вердикт аудита вынесен 18.09.2026 (с75): CONDITIONAL GO** — раздел «Вердикт
6.1» журнала `RADFLOW_DEEP_TECHNICAL_FUNCTIONAL_AUDIT_2026-08-27.md`. Условие
GO — ровно §1 этого файла в порядке §4; всё остальное — принятые риски §2 с
названным критерием пересмотра.

🆕 **22.09.2026 (с76) владелец добавил в §1 четвёртый блокирующий пункт — П-6**
(смена пароля и почты). Находка лежала в §3 плана с 15.09 и не была ни здесь, ни
в модели угроз: это **пробел покрытия**, а не известный принятый риск. Условие GO
с этой даты — П-1, П-2, П-3 **и П-6**.

🆕 **24.09.2026 (с77):** **Н-7 закрыт** — замер опроверг предпосылку: четыре гейта
RLS уже пинит №22 (ветка `f:`, полный md5 тела + владелец), дыра была в CI и
закрыта тестом (решение владельца 23.09, см. строку Н-7). В §3 заведены **Н-8,
Н-9, Н-10** — давние находки, всплывшие при разработке фильтра направников и
экспорта. **П-6:** протокол замера готов —
[`PROTOCOL-P6-auth-change-2026-09-24.md`](PROTOCOL-P6-auth-change-2026-09-24.md);
живой прогон пишет в `auth` прода и ждёт слова владельца.

---

## 1. Блокирующие — без них в прод нельзя

| # | пункт | почему блокирует | цена |
|---|---|---|---|
| П-1 | **Ротация service-role ключа** (пункт 6.2 плана) | ключ жил в разработке, его видели логи и окружения; после выхода он охраняет реальные ПИИ | 30 мин + окно деплоя |
| П-2 | **Регистрация домена** (6.3) | блокирует I-5 (`system`/`fullUrl` нестабильны при смене домена) и верификацию Google | вне оценки |
| П-3 | **Google OAuth: `Testing` → `In production`** (6.4) | в `Testing` refresh-токен живёт **ровно 7 суток** — зеркала очереди будут умирать каждую неделю | 15 мин + верификация отдельно |
| П-6 | 🆕 **Смена пароля и почты: тумблеры Auth выключены** (находка 15.09, план §3; решение владельца 22.09) | `Secure email change` выкл, `Secure password change` выкл, `Require current password when updating` выкл, минимальная длина пароля 6. Склейка: живая сессия → смена пароля **без знания старого** → смена почты с подтверждением **только на НОВОМ адресе** → молчаливый постоянный захват аккаунта. Путь дописан в `THREAT_MODEL_2026-09-14.md` (T9, с77) — пока как **незамеренный**. 📋 **Протокол замера готов (с77):** [`PROTOCOL-P6-auth-change-2026-09-24.md`](PROTOCOL-P6-auth-change-2026-09-24.md); живой прогон — по слову владельца | замер 1–2 ч, потом решение |
| ~~П-4~~ | ✅ **Репетиция восстановления БД — ВЫПОЛНЕНА 15.09** (3.2). [`LIVE-2026-09-15-restore-rehearsal.md`](LIVE-2026-09-15-restore-rehearsal.md). Восстановление из суточного снимка **≈5 мин**, вход живым паролем доказан двумя независимыми замерами, клон удалён, прод не изменился ни на строку, стоимость **$0**. Риск Р6 («теряем рабочий день») теперь **принятый**, а не неизмеренный: доказано, что единственный оставшийся путь работает. 🔴 **НО в день запуска добавились три обязательных пункта — см. ниже П-4а/П-4б/П-4в** | сделано |
| П-4а | 🔴 **После ЛЮБОГО реального восстановления — вернуть настройки `auth` руками** | «Auth settings & API keys» не переносятся (замер 15.09). Центр поднимется с **выключённой** защитой от утёкших паролей (т.е. П-5 придётся включать заново), заводской минимальной длиной пароля и **новыми** ключами API, которые надо прописать в Vercel | 15 мин |
| П-4б | 🔴 **Гасить cron ПЕРВЫМ действием в восстановленном проекте** | клон наследует джобы, секреты и боевой URL: 15.09 `outbox-deliver` выстрелил в прод через минуту после подъёма. `update cron.job` не работает — только `cron.alter_job(...)` | 2 мин |
| П-4в | 🔴 **После восстановления добить миграции и смотреть на `checked`, а не только на `ok`** | восстановление даёт схему на момент СНИМКА: 15.09 в клоне было 196 миграций против 199, и страж показывал `checked:23` вместо 25 — то есть мог быть «зелёным» по урезанному набору | 10 мин |
| ~~П-4 (исходная формулировка)~~ | ⚠️ **формулировка исправлена 14.09:** процедура **есть** — `docs/RUNBOOK_RESTORE.md` написан в этот же день (замеры: ядро 3 168 kB, `archive_mode=on`, 45 311 WAL-сегментов). Не сделано другое, и именно оно блокирует: **репетиции не было ни разу**. ⚠️ **Дашборд посмотрен 15.09, все числа теперь замерены:** тариф Pro, **PITR НЕ куплен**; добовые бекапы идут — 8 снимков 08–15.09 подряд без дырок, тип **PHYSICAL**, время **00:16–00:20 UTC ≈ 03:17 по Киеву**; Storage 0 бакетов/0 объектов. Цена аварии — **один рабочий день записей**, а не абстрактные 24 ч: бекап стоит между сменами. Владелец 15.09 **принял этот риск письменно** (Р6, см. [`DECISIONS-2026-09-15-s73.md`](DECISIONS-2026-09-15-s73.md)) — поэтому здесь остаётся РОВНО репетиция, и её вес вырос: суточный снимок теперь единственный путь. ➡️ **15.09 репетиция подготовлена под ключ** — [`PLAN-3.2-restore-rehearsal.md`](PLAN-3.2-restore-rehearsal.md): эталон прода снят, способ замерен (`Restore to a New Project` доступен: Pro + PHYSICAL, PITR не нужен), написаны блок SQL, 7 критериев приёмки и хронометражный бланк. **От владельца — два клика:** выбрать снимок → `Restore` (подтвердив показанную дашбордом сумму), после проверок — **удалить клон**. 🔴 **И новый пункт на день запуска, найденный при подготовке:** «Auth settings & API keys» при восстановлении **НЕ переносятся** — значит после реального восстановления центр поднимется с **выключённой** защитой от утёкших паролей (то есть П-5 придётся включать заново) и с **новыми** ключами API, которые надо прописать в Vercel | 2–4 ч (репетиция) |
| ~~П-5~~ | ✅ **Leaked-password protection — ЗАКРЫТО 15.09.** Владелец открыл `/auth/providers?provider=Email`: тумблер **«Prevent use of leaked passwords» ВКЛЮЧЁН**. Значит отсутствие `auth_leaked_password_protection` в advisors 13.09 и 15.09 означало «уже включено», а не «линт переехал» — неопределённость снята замером, а не рассуждением | сделано |

### Подробности П-3 (чтобы не исследовать заново)

1. Google Cloud Console → проект RadFlow → Google Auth Platform (бывший «OAuth
   consent screen») → **Audience** → `Publish app`.
2. ⚠️ **После переключения переподключить ОБА центра.** Токены, выданные в
   `Testing`, свой 7-суточный срок сохраняют; новый срок получают только новые
   токены. `buildAuthUrl` идёт с `access_type=offline` + `prompt=consent`,
   поэтому переподключение гарантированно выдаёт новый refresh-токен.
3. **Верификация.** Наши scope (`calendar.events`,
   `calendar.calendarlist.readonly`) у Google — sensitive. Без верификации
   приложение в проде работает, но показывает экран «Google hasn't verified
   this app» и имеет лимит на число аккаунтов. Верификация требует
   верифицированного домена (П-2), политики конфиденциальности на том же
   домене, демо-видео и обоснования каждого scope.
4. ⚠️ **Критерий приёмки — ЗАМЕР, а не вера.** Через 8 дней после
   переподключения: `select * from public.important_events where event_type
   ilike '%gcal%'` — если `reauth_required` не появился, статус сработал.
   Инструмент уже есть: проверка №13 после 0194 краснеет с меткой
   `відвалилось(...)`.

### Подробности П-6 (чтобы замер не начинали с нуля)

1. **Где смотреть:** Supabase Dashboard → `/auth/providers?provider=Email` и
   раздел Auth → Policies. Четыре настройки: `Secure email change`,
   `Secure password change`, `Require current password when updating`,
   `Minimum password length`.
2. ⚠️ **Замер, а не рассуждение — как с П-5.** Там тумблер оказался уже
   включённым, и неопределённость сняли пять минут в дашборде. Здесь наоборот:
   состояние прочитано, но **цепочка не проверена живьём**. Сначала воспроизвести
   на тестовом аккаунте (не на аккаунте владельца и не на реальном центре),
   потом решать.
3. **Что именно воспроизводить:** из активной сессии сменить пароль, не зная
   старого; затем сменить почту и убедиться, на какие адреса ушло
   подтверждение. Если подтверждение приходит только на новый адрес — прежний
   владелец аккаунта не узнаёт о захвате вообще.
4. **Решение после замера — за владельцем.** Включение `Secure email change` и
   `Require current password when updating` меняет поведение живых сценариев
   (приглашения персонала, восстановление пароля направителя), поэтому это не
   «просто щёлкнуть тумблер»: нужен прогон приглашения и входа после смены.
5. **Побочно:** минимальная длина 6 — отдельный вопрос; `Prevent use of leaked
   passwords` уже включён (П-5, 15.09), и он частично компенсирует короткие
   пароли, но не компенсирует смену без знания старого.
6. ⚠️ **Модель угроз это не покрывала.** ✅ Путь дописан 24.09 (с77) как **T9** в
   `THREAT_MODEL_2026-09-14.md` §10 — с пометкой «не замерено»; после замера
   строку перевести в замеренную.
7. 🆕 **Что дало чтение кода (с77) — это гипотезы для замера, а не выводы:**
   приложение **ни разу** не вызывает `auth.updateUser` — все смены пароля идут
   admin API на сервере (`/api/account/set-password` по одноразовому токену,
   `/api/staff/password` админом). Значит, тумблеры закрывают путь, которым не
   пользуется ни один живой сценарий, и включение их, скорее всего, ничего не
   сломает (проверить прогоном приглашения и входа — шаг 7 протокола). Путь
   атакующего — прямой `PUT /auth/v1/user` с токеном живой сессии; он же
   **обходит минимальную длину 8** из `zPassword` (у GoTrue — 6). Вход по логину
   резолвит адрес из `auth.users` (`resolve_login_email`), а экран персонала
   показывает копию `profiles.email` — **после смены почты интерфейс продолжает
   показывать старую**. И админский сброс пароля (`/api/staff/password`) почту
   не трогает: если почту уже сменили, атакующий возвращает контроль через
   `/auth/v1/recover`. Всё это — шаги 3–6 протокола.

---

## 2. Принятые риски, у которых критерий пересмотра = «первый реальный центр»

Это НЕ долги — это решения владельца с названной ценой. В день выхода в прод
каждое надо перечитать и подтвердить или отменить.

| риск | что принято | что перечитать в день выхода |
|---|---|---|
| **Р4 — нет E2E/Playwright** (решение 13.09) | браузерных тестов нет; аргумент «рано, а не дорого» | критерий сработал: поток появился. ⚠️ Роль **регистратора** не покрыта ни тестом, ни живым заходом |
| **`check_no_overlap` / `check_not_in_past` без пина** (решение 13.09, развилка 2) | два триггера можно переписать и даже СНЯТЬ — увидит человек в кабинете, а не CI | вторая клиника или первая жалоба на двойную запись |
| **I-2 — пустой список при отказе ACL** | «неотличимо от свободно» остаётся в продукте | первый случай, когда пустой экран приняли за свободный кабинет |
| **I-6 — нет 406** | блокер формальной сертификации, не совместимости | появление партнёра, которому нужна сертификация |
| **RF-07 / advisors `track='all'`** | замер отложен до реального потока | включить на 24 ч на реальных данных |
| **Р6 — PITR не куплен, теряем рабочий день** (решение 15.09) | восстановление только из суточного снимка (03:17 по Киеву). Авария в конце смены = потерян весь рабочий день записей; наутро центр не знает, кто записан, и обзванивает | **этот пункт и есть день выхода.** Перечитать вместе с П-4: принятие честно ТОЛЬКО если репетиция пройдена. Цена PITR — на странице Add-ons; ⚠️ он **отменяет** суточные бекапы и требует compute ≥ Small |

---

## 3. Нужно сделать, но не блокирует запуск

| # | пункт | почему сюда |
|---|---|---|
| ~~Н-1~~ | ✅ **М-4 ЗАКРЫТ 15.09 миграцией 0198** | Тело сторожа пинит МАШИНА. Пин — в КОММЕНТАРИИ к функции (`comment on function`); его сверяет новая **проверка №25** `guard_self_pin` (`checked` 24 → 25). Выбор места — замер, а не вкус: зонд на проде показал `comment_before = (NULL)`, `oid_same = true`, `comment_survives_replace = true` — `create or replace` не трогает комментарий, значит подмена тела оставляет пин старым и проверка краснеет; `drop`+`create` комментарий теряет — краснеет ветка «пин ОТСУТСТВУЕТ». Фальсификация на проде (откат транзакции): тело сдвинуто на ОДИН пробел → `тіло 1516076dc…/137082 проти піна d7f87fff…/137081`. Левое звено цепи (файл → пин) держит `planGuardPin` в гейте миграций — fail-closed на КАЖДОЙ прод-сборке. ⚠️ Чего это НЕ ловит, сказано прямо: у кого есть право на `create or replace`, есть и на `comment on function` — проверка ловит ДРЕЙФ, а не злоумышленника с правами postgres. Разбор — [`PR-0198-guard-self-pin.md`](PR-0198-guard-self-pin.md). ⚠️ Исторический текст: | сегодня `create or replace` сторожа мимо миграции не видит ни одна из **24** проверок и ни один тест. Именно этим протухли пины g/g2 в `gcal_pg_cron_smoke.sql` |
| ~~Н-2~~ | ✅ **`gcal_pg_cron_smoke.sql` починен 14.09** | фикстура больше не зависит от прода (если нет клиники без подключения — смоук создаёт свою, транзакция откатывается); два пина тела, протухавшие по построению, сняты и заменены на `raise notice` с объяснением. Прогон 14.09: `SMOKE_OK ( a b c d e f f2 g-info g2-info )` — впервые с 0186. Свойство «тело то самое» живёт в ритуале наката (предстан + результат в каждом пакете) и в №7 `ledger_md5` |
| Н-3 | **Нагрузочный профиль** (3.5) | считать по ожидаемому потоку первого центра, а не по пустой базе |
| ~~Н-4~~ | ✅ **ЗАКРЫТ 17.09 миграцией 0202** — той же, что Н-5: это была одна задача, и причина доказана замером. `room_busy_slots` ел ≈20 % времени инстанса не сам по себе, а через скан `pg_timezone_names` (409 мс холодным). После 0202 `trg_no_overlap` **964 → 7 мс** холодным и **55 → 0,6 мс** тёплым, функций со сканом каталога — **0** ([`PR-0202-tz-kyiv-no-catalog-scan.md`](PR-0202-tz-kyiv-no-catalog-scan.md)). ⚠️ Строка держалась открытой до 22.09, хотя план и вердикт считали пункт закрытым с 17.09 | — |
| ~~Н-5~~ | **Фаза 2 таймзон** (`pg_timezone_names`) | ✅ **ЗАКРЫТ 17.09 миграцией 0202** (Р74-3(а)): один центр (Medicom) переведён на `Europe/Kyiv` по явному id, CHECK сужен до `('Europe/Kyiv','UTC')`, скан убран из шести функций; Google принимает `Europe/Kyiv` (зонд в TestTioTadFlow, PASS); `trg_no_overlap` 964 → 7 мс холодным. Разбор — [`PR-0202-tz-kyiv-no-catalog-scan.md`](PR-0202-tz-kyiv-no-catalog-scan.md). ⚠️ Исторический текст: упирается в legacy-алиас `Europe/Kiev`, живущий в проде; нужно решение, сгонять ли центры на `Europe/Kyiv` |
| ~~Н-6~~ | ✅ **T3-бис ЗАКРЫТ 15.09 миграцией 0197** | Пять auth-аккаунтов без профиля удалены (`auth.users` 14 → 9, сирот 5 → 0), образы «до» — РЕДАКТИРОВАННЫЕ (id, даты, флаги; без почты и хеша пароля) в `audit_log` под `table_name='auth.users'`. За такими аккаунтами теперь следит **проверка №24** `auth_orphan_accounts` с порогом 15 минут (`checked` 23 → 24). ⚠️ Зонд под сиротой показал, что RLS держит: все таблицы дают 0 строк, `auth_role()` = null — дыра была не в доступе, а в том, что аккаунт существовал и о нём не знал никто. Разбор — [`PR-0197-auth-orphan-guard.md`](PR-0197-auth-orphan-guard.md). ⚠️ Исторический текст ниже оставлен, чтобы не терять постановку: | ⚠️ **диспозиция ЕСТЬ с 14.09 (с70): владелец решил удалить все пять.** Не исполнено — это пакет **0197**: удаление по явному списку id с образами «до», плюс починка серверных маршрутов (при неудачной записи профиля аккаунт остаётся сиротой) и смежный долг `/api/staff/password`, плюс новая проверка **№24** с ВРЕМЕННЫМ порогом, чтобы наполовину завершённая регистрация не красила сторожа те секунды, которые она законно занимает. `checked` 23 → 24 |
| ~~Н-7~~ | ✅ **ЗАКРЫТ 23.09 (с77) — тестом, а не миграцией** (решение владельца 23.09). **Замер 22.09, перепроверен 24.09:** DEFINER в `public` — **115**, подписей в №19 — **59** (из них на DEFINER — **55**), вне №19 — **60**; из этих 60 клиентскими ролями недостижимы **54** (31 — триггерные, EXECUTE-поверхности нет по построению), достижимы **шесть**: `auth_ceo_clinics` (anon+auth, 6 политик), `auth_is_ceo_of` (2), `auth_radiologist_case_ok` (1), `auth_referrer_can_book_room` (2) — **тела всех четырёх пинит №22, ветка `f:`** (полный md5 `prosrc` + владелец, с 0180; живые дайджесты совпали побайтово), а перенос в №19 ревью 0180 отклонило как дубль; `search_cities` / `search_clinics` (только `authenticated`, 0 политик) — гейта нет, пинить нечего. ⚠️ Запись с76 «четыре непинованных гейта» была ошибочной: предпосылку задачи опроверг замер. **Настоящая дыра была в CI:** миграция, правившая такой гейт без перепечатки сторожа, проходила сборку и краснила №22 только ночным кроном. Закрыто `tests/pinnedFnReprintOrder.test.ts`: имена берутся и из `f:` №22, и каждый живой `auth_*` обязан стоять в №19 или в `f:` (фальсифицировано 6 мутациями). Разбор — [`PR-s77-search-referrer-export.md`](PR-s77-search-referrer-export.md) §2. ⚠️ Межа: динамический SQL со склеенным именем и идентификаторы в кавычках детектор не видит. ⚠️ Исторический текст ниже — прежняя постановка «**Поштучный обход DEFINER-функций — знаменатель исправлен 14.09 (с70): не «~30», а 115**»: | открытый хвост I-8: вопрос «повторяет ли функция гейты политик, которые обходит» задан обходом по гейтам, а не по каждому телу. **Замер `pg_proc` 14.09:** SECURITY DEFINER в `public` — **115** (47 триггерных, 68 вызывных); доступны `authenticated` — 45, `anon` — 11; запинены списком №19 — 36, №22 — 11; **не запинены ничем — 75**. В с70 обойдены поштучно две (`referral_center_card`, `sink_overdue_scheduled` — обе вылечены пакетом 0195); в с71 проверены ещё две — `handle_new_user` (в списке №19 есть, тревога снята замером) и `invariants_check` (не пинился ничем — это был Н-1; **закрыт 15.09 пакетом 0198**: пин в комментарии, сверяет проверка №25). Итого не запинено ничем — **74**; они открыты. ⚠️ Пин-в-комментарии — решение для ОДНОГО случая, а не образец: сторож не может пинить сам себя изнутри (неподвижная точка), остальным 74 место в списке №19, который считает md5 тела и атрибуты. Не переносить приём оптом. ⚠️ Рядом названы и не запинены `unique_login` / `unique_login_from_email` — помощники `handle_new_user`, решающие ЛОГИН нового профиля. ⚠️ Слабую форму `search_path` этот пункт больше НЕ включает: 0196 перевела все 34 на канон и пинит ЗНАЧЕНИЕ в проверке №2 |
| Н-8 | 🆕 **CEO читает телефоны пациентов напрямую через RLS** (с77) | «CEO без телефонов» (`showPhone:false` с с22) держат только UI и API поиска/экспорта, а **не БД**: `queue_ceo_read` / `waitlist_ceo_read` отдают строку целиком — замер под сессией CEO: 298 телефонов очереди и 6 листа. Колоночных привилегий на уровне прикладной роли в Postgres нет. **Решение владельца:** (а) вьюха/RPC без `patient_phone` для CEO и снятие табличного чтения; (б) принять риск письменно — «CEO и так владелец данных центра». Не блокирует запуск: CEO — доверенная роль, которую заводит админ |
| Н-9 | 🆕 **Персонал может выставить записи произвольный `referrer_id`** (с77) | FK — только на `profiles`, `queue_write_staff` значение не ограничивает, `guard_referrer_doctor` стережёт лишь записи, созданные направником. А `queue_select` / `waitlist_select` пропускают `referrer_id = auth.uid()` **без проверки гранта** — значит, выбранный профиль (любой, не обязательно направитель) начинает ЧИТАТЬ запись с ПДн. Канал требует инсайдера центра (он и так видит данные) и оставляет след в `audit_log`; UI предлагает только связанных направителей, так что случайно это не происходит. Лечение — триггер/CHECK «`referrer_id` — направитель с грантом к центру записи» (миграция + перепечатка №17/№19 → полная ревизия стендов) |
| Н-10 | 🆕 **CSV-экспорт `CeoDashboard` не пишет журнал** (с77) | до 5000 строк с ПИБ, процедурой и доходом читаются в браузере напрямую под RLS, события `patient_data.exported` нет (у нового xlsx-экспорта поиска — есть, по каждому центру); защита от формульной инъекции там без TAB и CR. Дёшево: перевести на серверный роут с журналом по образцу `/api/search/export` |

---

### Сдвиги с74 (15.09.2026) по разделу 3 — замер, а не пересказ

* **Н-3 (нагрузочный профиль)** — модель по замерам:
  [`LOAD-PROFILE-2026-09-15.md`](LOAD-PROFILE-2026-09-15.md). Пропускная
  способность не узкое место; настоящий профиль снимается на первом центре
  (§6 документа — пять замеров на день выхода). Допущение о потоке ждёт цифр
  владельца (Р74-6).
* **Н-4 / Н-5 — одна задача, причина доказана.** Холостой `update` записи
  очереди стоит 795–938 мс, из них триггер `trg_no_overlap` 782–833 мс — скан
  `pg_timezone_names`. Лечение — фаза 2 таймзон, развилка **Р74-3**
  ([`DECISIONS-PACKET-2026-09-15-s74.md`](DECISIONS-PACKET-2026-09-15-s74.md)).
* **Н-7 (обход DEFINER)** — пакет **0200** запинил `auth_is_desk`,
  `schedule_from_waitlist_rpc`, `set_waitlist_status_rpc` (список №19 → 43).
  Непинованных ничем definer-функций, доступных `authenticated` и не `anon`, —
  **18**; у 16 из них свой гейт в теле (развилка **Р74-1**). ✅ **Закрыто 0201
  (15.09):** все 16 в списке №19 (59), вне — 2 без гейта; межи 1–2 для
  клиентских ролей держит новая проверка №26 `role_surface`.
* ✅ **РАЗОБРАНО 17.09 (с75):** `sink_overdue_scheduled_all()` max **19 с** —
  один запуск из 19 174 (13.09 20:40 UTC), ожидание ACCESS EXCLUSIVE на
  `clinics` за сухим прогоном 0192; скан каталога не участвует
  (`trg_no_overlap` — на колонки слота). Не дефект, чинить нечего —
  [`PERF-2026-09-17-sink-overdue-19s.md`](PERF-2026-09-17-sink-overdue-19s.md).
* ✅ **РЕШЕНО 15.09 (Р74-7):** ротировать `CRON_SECRET` в том же окне, что и
  service-role ключ (П-1) — T5 модели угроз.
* ✅ **РЕШЕНО 15.09 (Р74-4):** Realtime 61 % времени инстанса — **принятый риск**
  до реального потока; перечитать в день выхода вместе с профилем 3.5.

### Сдвиги с75–с76 (18–22.09.2026) по разделам 2 и 3

* **Доступность — пакет WCAG Medium в коде и на проде с 19.09** (`main c3ed042`):
  **W-4…W-13, W-24** и Low **W-17, W-25**. ⚠️ **Статус именно «в коде», а не
  «closed»**: High закрывали живой сессией с NVDA (18.09, 5/5 PASS), и Medium
  закроется так же — шагами 1, 4, 7, 8, 10–13, 15, 17, 19–21, 23 чек-листа. До
  этой сессии **Р74-5 в §2 остаётся принятым риском**, но уже не «на весь
  Medium», а на его непроверенную часть.
* **W-5 (размер ячейки сетки слотов) — правка ОТМЕНЕНА владельцем 19.09.**
  Ячейка ≈17–19×19 px; 2.5.8 для мыши — **принятый риск по решению владельца**,
  не дефект. На тач минимум блока 10.25rem держит ячейку ≥24 px. Число
  получасовых блоков в строке стало динамическим.
* **Н-4 закрыт** (см. таблицу выше) — 0202, вместе с Н-5.
* **П-6 добавлен в §1** — см. шапку файла.
* ⚠️ **№13 `gcal_sync_overdue` краснеет на проде примерно раз в неделю и будет
  краснеть до П-3.** Замер 22.09 18:23 UTC: `ok:false, checked:26`, оба зеркала
  в `reauth_required` (≈30 ч). Это истёкший 7-суточный токен `Testing`, а не
  сломанный инвариант. **Любое другое красное — повод останавливаться.**
  Ручное переподключение обоих центров возвращает зелёный ещё на 7 суток.

### Сдвиги с77 (23–24.09.2026) по разделам 1 и 3

* **Н-7 закрыт тестом** (см. строку выше): замер опроверг предпосылку «четыре
  гейта не пинит ничто» — их пинит №22 `f:`; дыра была в CI.
* **Н-8, Н-9, Н-10 заведены** — давние находки, всплывшие при разработке
  фильтра направников и экспорта в Excel
  ([`PR-s77-search-referrer-export.md`](PR-s77-search-referrer-export.md) §8).
  Все три ждут решения владельца, ни одна не блокирует запуск.
* **Новый канал ПДн — экспорт поиска в `.xlsx`** (с77, на проде с 24.09): область
  и скрытия — из того же движка, что экран; CEO — без колонки телефона; событие
  `patient_data.exported` в журнал КАЖДОГО центра; предел 10 файлов за 10 мин
  (тормоз, не замок). Путь внесён в модель угроз как T10.
* **П-6:** протокол замера готов, путь внесён в модель угроз как T9
  (незамеренный). Живой прогон — по слову владельца: он пишет в `auth` прода.
* ✅ **Состояние прода перепроверено 24.09 05:22 UTC:** леджер **202/202**,
  неоштампованных 0, `ok:false, checked:26`, упала только №13
  `gcal_sync_overdue` (П-3), тело сторожа `e1f1fdcf…`/165 537.

---

## 4. Порядок в день выхода

1. **П-6 (тумблеры Auth)** — замер и решение. ⚠️ Единственный пункт §1, который
   **не ждёт ни домена, ни окна деплоя**: делается в любой день, и чем раньше,
   тем короче окно, в котором захват аккаунта проходит молча. Порядок замера —
   в «Подробностях П-6» выше.
2. П-2 (домен) — он разблокирует П-3-верификацию и I-5.
3. П-1 (ротация ключа) — в окно деплоя.
4. П-3 (Google в production) + **переподключить оба центра**.
5. ~~П-5 (leaked-password)~~ — ✅ сделано 15.09, тумблер включён.
6. ~~П-4 (репетиция восстановления)~~ — ✅ **выполнена 15.09**, до первого
   реального пациента, как и требовалось.
   ⚠️ Но пересмотр **Р6** остаётся: остаёмся на суточном бекапе или покупаем
   PITR. Репетиция изменила статус риска с «неизмеренного» на **принятый** —
   доказано, что единственный путь работает, — но саму цену (рабочий день
   записей) не уменьшила. Решать перед первым реальным центром.
   ⚠️ И держать в дне запуска П-4а/П-4б/П-4в: это не разовые дела, а правила
   на КАЖДОЕ реальное восстановление.
7. Через 8 дней после п.4 — замер по `important_events` (критерий приёмки П-3):
   если `reauth_required` не появился и №13 держится зелёной — статус сработал.
8. Перечитать раздел 2 и подтвердить или отменить каждый принятый риск.
