# RadFlow — start prompt for session 68

Work **DIRECTLY** in `D:\RadFlowDev` through Desktop Commander. Production DB is
Supabase MCP, project ref `rdiqjxzibdqbhwileret`. Communication in Russian or
English; UI copy and project docs in Ukrainian (`claude/radflow-handoff.md` is
historically Russian — match each file).

You are the orchestrator and Full Task developer of RadFlow (patient flow in
MRI/CT rooms).

## There is NO unfinished package this time

s67 closed 0191 end to end: revision → dry run → apply → gate → tests → merge →
deploy → live check. No live branches, nothing half-applied. `dev` and `main`
are level and both build.

## TASK #0 — RE-MEASURE, do not believe

A discrepancy with the table below is a **FINDING**: name it out loud, do not
quietly patch the doc.

| what | expected (measured 2026-09-13, end of s67) |
|---|---|
| `main` / `dev` | take BOTH from `git ls-remote`. No live feature branch |
| prod DB | **`0191_fn_bodies_acl.sql`**, ledger **191/191**, unstamped 0 → next is **0192, FROM THE LEDGER** |
| `invariants_check(false)` | `ok:true`, **`checked:23`**, `failed:[]` |
| guard body | raw **`08014663728435627d2e993fa5ffbc77`**, length **116213**, CR **0** |
| list №19 | **33** signatures, and `;acl=` in EVERY one of the 33 rows |
| toolchain | tsc 0, eslint 0, vitest **3241/3241** (631 suites), `db:gate` **191/191**, build exit 0 |
| stands | `EXPECTED_STANDS` **37**, full revision 37/37 green, **48 min** |
| deploy stamp | compute `sha256(<40-char SHA of main>)[:12]` LOCALLY first, then fetch `/api/build`. End of s67: **`b281751d112d`** |

## What s67 did — one package, all day

**0191 is APPLIED.** `;acl=` (direct aclitems, `collate "C"`, separate branches
for `proacl IS NULL` → `<default>` and empty array → `<empty>`) plus three pins:
`auth_can_refer`, `auth_referrer_visible_rooms`, `auth_referrer_clinics`.

The live two-direction check on production, in a rolled-back probe:
```
before_ok=true | caught_search_path=t | after_restore_ok=true
offender: attrs:auth_can_refer(c uuid)->…;cfg=search_path=pg_temp, public;acl=…
```
`alter function … set search_path = pg_temp, public` — the hole s66 measured as
invisible to ALL 23 checks — now goes red by name. That was the point of the
package, and it is proven on the live DB, not in a file.

## ⚠️ Four review rounds, and the author was wrong every time

1. **The full revision on the branch gave 34/37.** Four anchors of list №19 in
   stands 0181/0182/0183 had gone stale — 0191 appended `;acl=`, the row text
   moved, and the fact was «ЯКІР НЕ УНІКАЛЬНИЙ (0)»: the mutation never applied
   and the stand proved nothing, with green baselines all along.
2. **NOT ONE mutation falsified what 0191 adds.** Measured with a green
   baseline: `acl=` across the stands — 7 hits; `auth_can_refer` — 0. Added
   B1–B4, `EXPECTED_RED` 8 → 12.
3. **B4 was vacuous in two editions, the second one invisibly.** Its closing
   marker sat BETWEEN `end` and ` as attrs`, so the test went red from a broken
   literal, not from block-comment stripping: with the Ф-1 fix removed the stand
   stayed 12/12 green — the fix itself was a guard with no red test.
4. **The numbers were fixed in the OUTPUTS, not the SOURCES.** The migration,
   `0191_apply.sql` and `0191_dryrun.sql` are GENERATED by
   `scripts/build-0191-reprint.mjs`. The first fix touched no source; the first
   rebuild would have silently restored every lie.

## Queue for session 68 — a menu, not an order

⚠️ **Ask before coding.** Compose a plan (`TaskCreate`) and **agree the first
package with me before writing code.**

1. **`search_path` value for definer functions OUTSIDE list №19** — the owner
   deferred this in s67, explicitly, until 0191 was done. It is done. Options:
   widen check №2 to pin the VALUE for every definer function (cheap, no reprint
   of №19), extend the named list (a reprint each time), or a listless catalogue
   digest (count + md5 over `prosecdef` functions — the offender names itself).
2. **`auth_referrer_clinics()` has no NAMED red test** — boundary 6 of the 0191
   audit. B3 edits exactly that row but reddens on the FIELD, not the signature.
   One more mutation of the B1 shape. Cheap.
3. **The `;cfg=` expression in `cur` is pinned by nothing** — boundary 7.
   Measured in s67: remove `|| ';cfg=' || coalesce(array_to_string(p.proconfig,
   ','), '')` and NO test goes red. Same for `secdef=` and `;vol=`. Loud on prod,
   silent in `npm test`.
4. **An aclitem with GRANT OPTION puts a `*` inside a pinned row** — boundary 8.
   Measured: `'authenticated=X*/postgres'::aclitem::text` contains star+slash.
   Zero such functions today; the first `grant … with grant option` puts a block
   comment closer inside the list, and `CODE` strips block comments with a naive
   non-greedy pair, not a tokenizer.
5. **The report path of №19 is not pinned by any test** — boundary 4, unchanged.
6. **`auth_rls_initplan` — 15 policies.** The win is NOT automatic.
7. **`docs/README.md` says «9 кронов»; `cron.job` has 10** (ids 1,3,4,5,6,10,11,
   12,13,17). Cheap; `docs/ops-cron.md` is a LIVE doc pinned by
   `invariantsCheckedPins`, so it must not lie.
8. **`assertNoLiveDelivery` does not cover `cleanup`** — that command runs before
   the guard, and DELETE of entries emits `integration.appointment.deleted`.
9. **Five definer functions that decide or return PII and are pinned by nothing:**
   `emergency_stop_rpc`, `queue_set_status_rpc`, `submit_incident_rpc`,
   `check_no_overlap`, `check_not_in_past`. Owner's decision.
10. **`pg_timezone_names` phase 2** — still blocked on the owner's decision.

## Safeguards — NOT relaxed in any degree

Two independent review rounds with **DIFFERENT lenses** on anything touching
RLS, grants, `SECURITY DEFINER`, triggers, authorization, service-role code or
production migrations — and you validate every subagent conclusion
**personally**. ⚠️ s67 is the case for that last word four times over: every
round was right and the author was wrong, and every time the proof was a
MEASUREMENT, not an argument.
Falsification: a **NAMED red test plus a GREEN baseline**. Live production
checks in **BOTH directions**. Production data only by an **explicit id list
with before-images**. Secrets and PII go **NOWHERE**.
`npm audit fix --force` is **FORBIDDEN**. Dev and prod are **ONE DB**.
**Never ask for passwords.**

⚠️ **"Success" is not proof.** After every apply verify BY QUERY: the ledger row
and count, `invariants_check(false)`, the guard body md5 (naming the recipe) and
its length, the smoke pins. And run the **full test suite**.

⚠️ **A rollback verified inside the same transaction proves nothing** — take the
md5 again with a SEPARATE query.

⚠️ **RED WINDOW.** From the second a migration registers in the ledger until its
file is in `main`, every production build fails. Named in `AGENTS.md` now. Close
it in one move: branch → `dev` (ff) → `main` (`--no-ff`) → push → deploy.

## Three habits that actually catch the recurring mistake

1. **Give every search a GREEN BASELINE.** "Found nothing" proves nothing until
   a line you know exists comes back by THAT SAME query.
2. **Ask who else writes this file.** In s67 three files fixed by hand turned
   out to be generated, and the fix would have evaporated on the next rebuild.
3. **A green 37/37 says the stands hold what they held yesterday.** Ask
   separately: which mutation shoots at what I added TODAY?

## Where the detail lives

* `claude/radflow-handoff.md` — durable state, freshest first («СОСТОЯНИЕ НА
  КОНЕЦ с67»).
* `claude/NEXT_SESSION_PROMPT.md` — the attachment: working mode, environment
  traps, rules, tools.
* `docs/audit/PR-s66-0191-fn-bodies-acl.md` — the 0191 package end to end, with
  the s67 review section, the eight named boundaries and the red-window rule.
* `AGENTS.md` — the stable rules; the time canon, the 0122 trap, the red window.
