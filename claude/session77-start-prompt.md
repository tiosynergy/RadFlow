# Стартовый промпт — сессия 77 (RadFlow)

Ты — Claude Code Cowork, оркестратор и Full Task разработчик SaaS-продукта
RadFlow. Работай непосредственно с локальным проектом: **`D:\RadFlowDev`**.

**Аудит доведён:** вердикт **6.1 CONDITIONAL GO** вынесен 18.09.2026 (с75), план
`docs/audit/PLAN-audit-completion-2026-09-13.md` исчерпан (открыт только 4.3 —
ждёт домена). Результаты аудита одной страницей —
**`docs/audit/AUDIT-SUMMARY-2026-09-22.md`** (собрана в с76, состояние прода в
ней сверено запросом).

⚠️ **Рамка с77 задана владельцем 22.09:** **П-1 (ротация ключа), П-2 (домен) и
П-3 (Google OAuth) — ПЕРЕНЕСЕНЫ на будущее, «перед чистым продакшеном».** Не
предлагай их как следующий шаг и не начинай подготовку к ним без отдельного
слова владельца: они стоят денег и необратимы. Сессия 77 — про работы, которые
можно сделать до этого окна.

Правила прежние: не спрашивать одобрения на каждый шаг; спрашивать только на
развилке владельца и там, где действие **стоит денег или необратимо**.

---

## 0. Состояние на входе — замер 22.09.2026, а не пересказ

* **Прод:** леджер **202/202**, неоштампованных 0, последняя —
  `0202_tz_kyiv_no_catalog_scan.sql` (накат 17.09 19:28:42 UTC).
  `public.invariants_check(false)` → **`ok:false, checked:26`** (замер 22.09
  18:23 UTC). Тело сторожа `e1f1fdcfcea99906f02b0af193b814fa` / **165 537**,
  самопин №25 совпадает. Список №19 — **59** подписей, №26 — **62** ключа.
  `SECURITY DEFINER` в `public` — **115**, EXECUTE у `authenticated` — **44**, у
  `anon` — **11**, без `search_path` — **0**. Таблиц `public` **33/33** с RLS,
  политик **63**. Центров 2, записей очереди 330.
* 🔴 **Сторож красный, и это ожидаемо.** Единственная упавшая проверка — **№13
  `gcal_sync_overdue`**: оба зеркала Google в `reauth_required` (`b42134dc` —
  1 825 мин, `c79588d6` — 1 861 мин). Это **истёкший 7-суточный токен статуса
  `Testing`**, то есть прямое следствие незакрытого П-3, а не сломанный
  инвариант. После 0194 сторож это видит, но вылечить не может.
  **Любое ДРУГОЕ красное — повод останавливаться.**
* **Git:** `origin/dev` = **`b4cae6e`**, `origin/main` = **`02f248a`**, штамп
  `/api/build` = **`68b4720023ed`** = `sha256(02f248a…)[:12]` — сверено 22.09.
  Перед работой возьми свежие SHA из `git ls-remote`: за время между сессиями
  могли лечь док-коммиты.
* **Тулчейн (на 19.09, `9a23101`):** `tsc` 0, `eslint --max-warnings 0` — 0,
  vitest **3586/3586**, стендов **40/40** на чистом клоне. С тех пор в код не
  входило ничего, кроме документов и `docs/USER_GUIDE.html`.
* **Сверь всё это запросом и `git log`, прежде чем что-либо делать.** «Успех» —
  не доказательство.

## 1. Что сделано в с76 — чтобы не переделывать

* **`docs/USER_GUIDE.html` — инструкция пользователя** (коммит `b4cae6e`):
  автономный HTML 0,42 МБ на украинском, разделы по ролям, 12 блоков «що робити,
  якщо…», словарь. Восемь макетов отрисованы настоящим CSS проекта
  (`styles/prototype/*`) на **вымышленных** пациентах — прод-данных в файле нет.
  Исходники — `claude/guide/` (три части, `assemble.py`, `guide-shots.mjs`,
  `optimize.py`, свой `README.md`).
  ⚠️ **Правка текста браузера НЕ требует:** каждая `<figure>` помечена
  `data-shot`, и `assemble.py` при отсутствии папки `shots/` достаёт картинки из
  предыдущей версии собранного HTML. Проверено — побайтово идентично. Править
  надо **части в `claude/guide/`, а не собранный файл**.
* **`docs/PRODUCT_OVERVIEW.md` актуализирован до с76.** Шапка пересчитана
  запросом; §7 дописан сессиями 73–76 и блоками 0200/0201/0202; §4.3 и §4.12
  приведены к одному описанию сетки слотов; битая ссылка §4.11 → §4.12;
  устаревшие «открытые» пункты §8 (М-4, leaked-password) закрыты.
* **`docs/audit/AUDIT-SUMMARY-2026-09-22.md`** — новая сводка результатов
  аудита: вердикт, условия GO, диспозиция фаз 0–8, открытое, принятые риски,
  ключевые замеры, состояние прода и список расхождений между документами.
* **`docs/audit/ToDo_Production.md`:** добавлен **П-6** (решение владельца
  22.09), закрыт **Н-4** (0202, вместе с Н-5), дописан блок сдвигов с75–с76,
  §4 перенумерован — П-6 стал первым.

## 2. Очередь работ — по приоритету

### 2.1. П-6 — смена пароля и почты (блокирующий, но НЕ ждёт домена)

Единственный пункт §1 `ToDo_Production.md`, который делается в любой день.
`Secure email change` выкл, `Secure password change` выкл, `Require current
password when updating` выкл, минимальная длина пароля 6. Склейка даёт **тихий
постоянный захват аккаунта** из живой сессии, и **в модели угроз этого пути
нет** — это пробел покрытия, а не принятый риск.

Что делает агент: готовит протокол замера (он уже набросан в «Подробностях П-6»),
пишет зонд, если он нужен, и описывает, чем именно подтверждается каждый шаг.

⚠️ **Чего агент НЕ делает сам:** живой прогон пишет в `auth` прода (нужен
тестовый аккаунт, смена пароля, смена почты). Это прод-запись → **только по
явному слову владельца и по списку id с до-образами**. Пароли не спрашивать и не
вводить: владелец логинится сам.

После замера — **дописать путь в `docs/audit/THREAT_MODEL_2026-09-14.md`**, иначе
следующий аудит найдёт его заново.

### 2.2. Р2 — аудит-триггеры на таблицы с ПИИ

Решение владельца есть с 13.09 («таблица с ПИИ ⇒ триггер»), **не исполнено**.
Замер 22.09 на проде: `fn_audit` висит ровно на шести таблицах — `profiles`,
`queue_entries`, `waitlist_entries`, `referral_access`, `ceo_access`,
`incidents`. Под критерий попадают и остались **без** триггера:

* **`patient_cases`** — `patient_name`, `patient_phone`, `patient_email`,
  `patient_dob`, `patient_sex`, `patient_weight`, `note`. Главный пробел: кейс
  — это пациент, и правка его карточки сейчас не оставляет до-образа. Дыра
  названа ещё в `claude/logging-spec-vs-reality.md` («`patient_cases` не
  аудируется вообще»);
* **`doctors`** — `name`, `phone`, `clinic_name` (врачи-направители);
* **`referrer_private`** — `email`;
* `clinic_deletion_requests` (`admin_email`, `token_hash`) и `clinics`
  (`phones`, `emails`, `address`) — данные организаций, не пациентов: включать
  или нет, решает владелец.

⚠️ **Критерий не закрывает случай, из-за которого его приняли.** Повод — RF-03:
26.08 в одну миллисекунду изменились 37 позиций `services`, и что именно
сделала операция — восстановить нечем. Но в `services` персональных данных нет
(`name`, `price`, `duration_min`, `modality`, `code` — прайс), под «таблицу с
ПИИ» она не попадёт никогда. Замер 22.09 подтверждает и вторую половину: в
`important_events` **нет ни одного** события про каталог, услуги, цены или
импорт — следа нет в ОБОИХ журналах. Поэтому первый шаг с77 — не «исполнить
Р2», а выбрать: **(а)** исполнить как записано, оставив каталог непокрытым;
**(б)** расширить критерий до «ПИИ ИЛИ невосстановимая правка» и покрыть
`services`; **(в)** закрыть каталог эмиссией в `important_events` вместо
триггера.

⚠️ **И у исполнения есть обратная цена.** `fn_audit` пишет ПОЛНЫЙ снимок строки
(`to_jsonb(old/new)`), то есть триггер на `patient_cases` **копирует PII
пациентов в `audit_log`** — ретенция 0149 обезличит их только через 90 дней.
Это не довод против, но это решение о расширении поверхности ПИИ, а не чистое
улучшение.

Оценка «1,5–2 ч» из пакета решений **ниже физического пола**: новые
`trg_audit_*` меняют именной список №17 (сейчас в нём 23 определения триггеров,
включая все шесть `trg_audit_*`), значит это перепечатка сторожа, самопин №25 и
полная ревизия стендов — одна только постоянная цена такого пакета замерена в
`plan-s60-tech-debt.md` как ~1,5 ч. Реалистично **4–6 ч**.

Это **миграция**, значит полный ритуал: предстан → сухой прогон с маркером
отката → накат → `invariants_check` → фальсификация → `npm run db:gate` →
смёржить.

⚠️ Правило из `AGENTS.md` п. 8: миграция новее последней перепечатки, трогающая
функцию из №19, краснит CI (`tests/pinnedFnReprintOrder.test.ts`).

### 2.3. Н-7 — непинованные DEFINER: замер сделан, осталось решение

Замер 22.09 по каталогу прода, без изменений схемы. DEFINER-функций в `public`
— **115**. Подписей в списке №19 — **59**, из них на DEFINER-функции — **55**
(остальные четыре пина держат не-DEFINER: `guard_no_client_delete`,
`guard_no_client_delete_incident`, `request_is_client_role`, `handle_new_user`).
Значит **вне пина 60 DEFINER-функций**. Цифра 74 относилась к с72 и устарела,
«~30» не была верной никогда.

Но абсолютное число здесь ничего не решает. Разложение этих 60 по достижимости
клиентскими ролями:

* **54** клиентом не достижимы вообще (только `service_role`/`postgres`), и
  **31** из них — триггерные функции, у которых EXECUTE-поверхности для клиента
  нет по построению. Это шум, а не долг;
* **6** достижимы клиентом — и вот это и есть результат замера:

| функция | `anon` | `authenticated` | в скольких RLS-политиках |
|---|---|---|---|
| `auth_ceo_clinics` | да | да | **6** |
| `auth_is_ceo_of` | да | да | **2** |
| `auth_radiologist_case_ok` | да | да | **1** |
| `auth_referrer_can_book_room` | да | да | **2** |
| `search_cities` | нет | да | 0 |
| `search_clinics` | нет | да | 0 |

⚠️ **Прежняя запись «вне списка сознательно осталось две функции —
`search_cities`, `search_clinics`: гейта нет, вычищать нечего» неполная.** Без
гейта только эти две. Остальные четыре — ровно гейты: их тела решают, что видит
CEO, радиолог и направитель, они стоят внутри одиннадцати политик, и их тело
сторож №19 не держит (проверено дважды: и сопоставлением подписей, и прямым
поиском имени в теле сторожа — 0 вхождений, тогда как `auth_is_admin`,
`auth_clinic_id`, `auth_is_desk`, `auth_can_refer`, `auth_referrer_clinics`,
`auth_radiologist_room_ok` в списке есть). Это тот самый класс, ради которого
0172 и завёл пин тел: подменённое тело `auth_is_admin` проходило все проверки
уровнем выше.

Задача с77 — не «посчитать», а **решить по этим шести**: дописать четыре
`auth_*` в №19 (перепечатка сторожа со всем ритуалом — самопин №25, `PINNED` в
`tests/guardFnBodiesInvariant.test.ts`, полная ревизия стендов) либо записать
мотивированный отказ. Числа 115 / 59 / 55 / 60 и таблицу — в
`ToDo_Production.md` и в §8 обзора продукта.

### 2.4. Мелочи документов

* **T5 в `THREAT_MODEL_2026-09-14.md` §9.2** всё ещё «диспозиции нет — развилка
  Р74-7», хотя §9.3 того же файла и `DECISIONS-2026-09-15-s74.md` фиксируют
  ответ владельца «`CRON_SECRET` с П-1». Строку таблицы просто не обновили.
* **Статус фазы 5 / пункта 4.3** описан тремя разными формулировками в плане,
  вердикте и концовке журнала. Состояние одно — ждёт домена; свести к одной
  фразе.

### 2.5. Что НЕ трогать в с77

* **П-1, П-2, П-3** — перенесены владельцем, см. рамку вверху.
* **Н-3 (нагрузочный профиль)** — снимается только на первом реальном центре.
* **Р6 (PITR)** — решение перед первым центром, не раньше.
* **Принятые риски §2 `ToDo_Production.md`** — пересматриваются в день выхода,
  а не по ходу.

## 3. Правила работы — не переоткрывать

**Безопасность и данные**

* **Секреты и ПИИ — никуда:** ни в отчёт, ни в лог, ни в промпт субагента.
  Значения ключей Google (`GOOGLE_OAUTH_CLIENT_ID/SECRET` в `.env.local` на
  машине владельца) **не читать и не печатать**.
* **Прод-данные — только по явному списку id и с до-образами.** До-образы
  редактировать: id, даты, флаги — да; почта и хеши — нет.
* **Дев и прод — ОДНА база.** «Попробовать на деве» не существует.
* **`npm audit fix --force` запрещён.**
* **Паролей не спрашивать и не вводить** — владелец логинится сам.

**Миграции и проверки**

* **«Сухой» прогон БЕЗ маркера отката — это НАКАТ.** Маркер обязателен.
* **«Успех» — не доказательство, проверяй ЗАПРОСОМ.**
* **Накатил — смёржил**, а не «потом»: гейт симметричен
  (`scripts/migration-gate-lib.mjs:82-88` бракует и строку леджера без файла, и
  файл без строки), поэтому от наката до пуша `main` открыто красное окно.
* **Никогда не трогай дерево, пока идёт стенд.**
* Окно `apply` → `db:gate` **не должно пересекать 03:50 UTC** — там ночной cron
  `invariants`. После `db:gate` генератор перепечатки не запускать.
* Всё, что касается **RLS, грантов, `SECURITY DEFINER`, триггеров, авторизации,
  `service_role` и прод-миграций** — **два независимых раунда ревью РАЗНЫМИ
  линзами**, и каждый вывод субагента проверять лично.

**Инструменты и перенос**

* Через мост к машине владельца — **`cmd`, а не PowerShell**. `findstr` с
  кириллицей не работает.
* `git commit` с кириллицей — **только через `-F <файл>`**, не `-m`.
* **`git add -A` запрещён** — добавлять поимённо.
* Живые проверки прода **не из контейнера**; штамп деплоя — `curl
  https://rad-flow-tau.vercel.app/api/build` **с машины владельца**, сверять с
  `sha256(git rev-parse main)[:12]`.
* Общение с владельцем — **по-русски**. Аудит-документы — по-русски. PR-доки,
  сторожевая проза, скрипты и сообщения коммитов — **по-украински**.

## 4. Уроки с76 — чтобы не наступить второй раз

* **Перенос больших файлов через мост.** Текстовые файлы пишутся на машину
  напрямую (`Desktop Commander write_file`), бинарные и очень большие — через
  `git format-patch | gzip | base64` кусками. ⚠️ **Порча приходит не от канала,
  а от переписывания**: в с76 в 441 КБ base64 было три одиночные ошибки, и один
  раз кусок оказался выдуман целиком. Лекарство одно — **md5 каждого куска
  сразу после записи** и бисекция при расхождении (`sub_b64.js`, `rng_b64.js`,
  `show_b64.js`, `patch_b64.js`, `trunc_b64.js` лежат в `D:\RadFlowDev\.git\`).
  Финальная сверка — md5 патча и `git rev-parse HEAD^{tree}` против ожидаемого.
* **Перед `git reset --hard` смотри, какая ветка выбрана.** В с76 в контейнере
  была выбрана `s75-0202`, и сброс на `origin/dev` сдвинул её указатель.
  Восстановлено из рефлога; содержимое не потерялось — оно на `origin/main`
  другими объектами. Вывод: `git rev-parse --abbrev-ref HEAD` **до** сброса.
* **В контейнере висят локальные ветки, которых нет на удалёнке** (`s74-0201`,
  `s75-0202`, `s75-b2-pin-order`, `s75-sink-19s`, `s75-wcag-high`,
  `s76-user-guide`). Это побочный эффект переноса: на машине владельца `git am`
  пересоздаёт коммиты с другими хешами. **Содержимое каждой сверено — все их
  деревья есть в `origin/main`.** Предупреждение хука про «непушенные коммиты»
  на них — ложная тревога, пушить их не нужно.
* **Документ с длинными строками правится скриптом, а не на глаз.**
  `PRODUCT_OVERVIEW.md` — 248 КБ в 856 строках; точечные замены с проверкой
  якорей надёжнее и дешевле, чем чтение файла целиком.
* **Шапка и §7 обзора продукта правятся ОДНОЙ правкой.** Правило записано в
  самом документе с 14.09 — и всё равно было нарушено: коммит `fec98e0` правил
  §6 и §8, шапка пять дней утверждала «прод на 0199, пакет 0200 не накатан».

## 5. Открытое, что владелец может решить в любой момент

1. **Переподключить зеркала Google вручную** (15 мин) — вернёт №13 в зелёное ещё
   на 7 суток. Временная мера до П-3; можно не делать, если красное №13 не
   мешает читать ночной прогон.
2. **П-1 / П-2 / П-3** — когда скажет «идём в прод». Порядок и критерии приёмки
   готовы в `ToDo_Production.md` §4.
3. **Удалить устаревшие локальные ветки в контейнере** — косметика, содержимое
   на `origin/main`.
