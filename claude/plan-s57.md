# План работ после с56 и сроки

**Составлен:** 04.09.2026, после пакета 23 (миграция 0173).
**Состояние на момент составления:** `main` = `dev` = `43c437f`, прод `0173`,
`invariants_check()` → `ok:true, checked:19, failed:[]`, ledger 173/173.
Гейт: tsc 0, eslint 0, vitest 2529/2529 (85 файлов).

---

## 1. На чём стоят оценки — замеры, а не ощущения

Все числа ниже взяты из этой сессии, а не из опыта «вообще».

| операция | замер | сколько раз мерил |
|---|---|---|
| полная ревизия 24 стендов | **34 / 37 / 38 / 43 мин** | 4 |
| гейт (tsc + eslint + vitest + db:gate) | ~2–3 мин | каждый пакет |
| деплой + отпечаток в обе стороны | ~4 мин | 5 |
| два раунда ревью субагентами | 13 и 17 мин | 1 |
| передрук `invariants_check` **с** изменением `checked` | +9 смоуков, 26 строк, 2 md5-пина | 0171, 0172 |
| передрук **без** изменения `checked` | только 2 md5-пина | 0173 |

**Постоянная цена любого пакета с передруком сторожа — около 50 минут**
(ревизия 40 + гейт 3 + деплой 4 + доки/коммиты ~5), сколько бы строк ни менялось.
Поэтому 0173 (шесть строк в списке) занял час, а не десять минут. Это не
накладные расходы, от которых стоит избавляться: ровно эта ревизия нашла в
с56 три протухших якоря, каждый из которых сделал бы соседний стенд слепым.

**Фактическая длительность пакетов этой сессии:**

| пакет | что | длительность |
|---|---|---|
| 21 | 0172 — тела функций доступа (2 раунда ревью, 4 дыры в 1-й редакции) | ~2 ч |
| 22 | Г1-F в трёх путях создания (код + тесты + 14 позиций стенда) | ~2,5 ч |
| 23 | 0173 — аудит-триггеры (передрук без смены `checked`) | ~1 ч |

Отсюда рабочая единица оценки: **пакет с миграцией — 1–2,5 ч**, пакет без
передрука — 1–2 ч.

---

## 2. Что я могу делать один (решения не нужны)

Порядок — по убыванию отношения «польза / риск», а не по номерам.

### 2.1 — 0174: обработчики исключений для остальных 18 проверок ✅ СДЕЛАНО (с57)

**Факт против оценки.** Оценивал 2,5–3 ч — вышло около того, но состав работы
разошёлся с планом: сама миграция и её накат заняли меньше, чем ТРИ находки,
которые она вытащила (метка ≠ проверка, пин годами проходил по неправильной
причине, сторож приехал без стенда). PR-док —
`docs/audit/PR-0174-invariants-fail-loud.md`, коммиты `67f8a49`, `fc4d3ac`,
`07aaec6`, `68ddf91`, `ec201ad`. Ревизия 24/24 (545 адресных, ~33 мин),
деплой измерен в обе стороны.

⚠️ **Опасность, названная заранее, оказалась не той.** Я обещал следить, чтобы
обёртка не сдвинула отступы — она и не сдвинула (проверено пином обратимости).
А сломалось другое: обёртка УДВОИЛА каждую метку в теле, и три пина, искавшие
голое имя, стали хибно-зелёными. Предсказанная опасность прошла мимо,
непредсказанная — сработала. Ловили стенды, не глаз.

---

**Что было в задаче (для истории).** Единственная оставшаяся дыра, где отказ
сторожа не виден вообще: во всём теле был ровно ОДИН `exception when` (в №18)
плюс данный 0172 для №19. Исключение в любой другой проверке откатывало
транзакцию — строка в `maintenance_runs` не появлялась, а отсутствие строки
читалось как здоровье.

---

### 2.2 — Долг с56: канал PROSE сужен ФАЙЛОМ, а не строкой ✅ СДЕЛАНО (с57)

Было: `tests/invariantsCheckedPins.test.ts` ложно сработал бы, если бы в
`clinic_people_view_smoke` появился зонд на `'checked'` («усі 7 перевірок
пройдено» — про собственные шаги смоука — стало бы «7 ≠ 19»).

**Факт против оценки: 1 ч оценивал, около часа и вышло.** Сужение переехало с
файла на строку (`ABOUT_GUARD`), замер обоих направлений: старое правило — 1
ложный нарушитель, новое — 0; на живом дереве обе редакции дают те же 7 пинов и
те же 8 строк полноты.

⚠️ **Побочная находка, которой в задаче не было:** `scripts/bump-checked-pins`
сужал так же по файлу, а его шапка ЗАЯВЛЯЛА «та сама властивість, що в
тесті-стороже». Разойдясь, они дали бы худший вид тишины: сторож краснит
строку, которой инструмент не видит, а бамп рапортует «успешно». Инструмент
переведён на то же правило, правило запинено дословно (N48).

Стенд: `falsify-0166` 45/45 → **48/48** (N46 новое покрытие, N47 позитивный
контроль, N48 расхождение инструмента). Коммит `e5fe7bf`, деплой измерен
в обе стороны: `pP5I3cEL26aZH4Db2yIfj` → `7Xz8l0tDYrSWzcyf8fW7Z`.

⚠️ **Полной ревизии тут НЕ было, и это замер, а не лень:** изменённый тест
входит в `SPEC` ровно двух стендов (`falsify-0166`, `falsify-u37` — оба
прогнаны, 48/48 и 21/21), смоуки мутируют только они же (сверено `git grep`).
Ни один другой стенд к этим файлам не привязан.

⚠️ **Названа граница:** сужение ФАЙЛОМ осталось ровно в одном месте — тест
«кожен смоук, що читає ключ 'checked', має асерт у формі». Там оно сознательное
(кто прочитал — тот пинит), но следствие названо в комментарии: смоук, который
прочитает `'checked'` по другой причине и не будет пинить число, покраснит
сторожа.

### 2.3 — RF-2 и RF-4 ✅ КОД СДЕЛАН (с57), СХЕМА — ОСТАЛАСЬ

**Факт против оценки: оценивал 1–1,5 ч на оба, вышло около двух** — и разница
целиком в RF-4. PR-док `docs/audit/PR-RF2-RF4-role-gate.md`, коммит `c33083f`,
ревизия ПОЛНАЯ 24/24 (558 адресных, ~36 мин).

⚠️ **Оценка была занижена, потому что я поверил списку из аудита.** Аудит Ф6
назвал ОДНО место fail-open (`/search`); замер по дереву нашёл ДЕСЯТЬ (одно
живое, четыре мёртвых, пять типовых значений в компонентах). Это шестой раз за
проект, когда перечень из дока оказался уже дерева, — и ровно поэтому оба новых
сторожа снимают СВОЙСТВО, а не держат список.

✅ **Вторая половина СДЕЛАНА в с57 (пакет 27, миграция 0175).** Факт против
переоценки: закладывал 1,5–2 ч — уложился примерно в них. `checked` 19 → 20,
бамп дал ровно обещанные 9 смоуков и 26 строк. Дороже плана оказалось другое:
ТРИ сторожа пришлось чинить, и ни один из них не был в смете (см. PR-док
`PR-0175-profiles-no-default.md`, §5). Самый ценный — `guardFnBodiesInvariant`
с неназванным допущением «№19 — последняя проверка»: он упал громко на честном
коде ровно тогда, когда допущение перестало быть правдой.

Ниже — оценка, как она выглядела ДО работы:

⚠️ **RF-4 закрыт наполовину, и вторая половина ДОРОЖЕ, чем «+50 мин».**
`DEFAULT 'admin'` на `profiles.role` живёт в схеме; снять его безопасно (все
четыре пути записи роль называют явно, upsert нет), но чтобы это СТЕРЁГЛОСЬ,
нужна новая проверка №20 — а она двигает `checked` 19 → 20 и тянет девять
смоуков (есть `bump-checked-pins.mjs`), константы `invariantsFailLoud`
(18/20/19) и md5-пины тела. Реальная цена — **1,5–2 ч**, а не 50 минут.
Там же найден сосед, которого аудит не называл: `approved not null default true`.

---

### 2.3-bis — что было в задаче (для истории)

* **RF-2** — на `/radiologist` ролевая ветка НИЖЕ выборки `rooms`.
* **RF-4** — `DEFAULT 'admin'` на `profiles.role` плюс `/search`:
  `(profile.role as string) || "admin"` — идиома fail-**open**, сегодня мёртвая
  (колонка `not null`), но живая как заготовка регресса.

**Оценка: 1–1,5 ч на оба.** RF-4 может потребовать миграции (снять DEFAULT) —
тогда +50 мин постоянной цены.

### 2.4 — U-81, U-62, U-65 + U-66, LOW-пачка

| пункт | что это | оценка |
|---|---|---|
| **U-81** | связка «вердикт → код возврата» не проверяется ничем | 1–1,5 ч |
| **U-62** | пакет по `useRealtimeRefetch` | 1,5–2 ч |
| ~~**U-65**~~ | ✅ **СДЕЛАНО в с57 (пакет 28)** — см. ниже | оценка 1 ч → **факт ~2,5 ч** |
| **U-66** | в ветке UPDATE нет обрезки до PK: UPDATE, вводящий строку в видимость подписчика, отдаёт ему полное прежнее состояние | 2–3 ч, вероятна миграция |
| **LOW-пачка** | U-59 (ack не знает о видимости вкладки), U-60 (бейдж при сбое чтения показывает «ничего»), U-75 (MEDIUM: `anyModalOpen` — дизъюнкция 17 состояний, сторожится текст вызова, а не состав) | 2–3 ч на все |

⚠️ Оценки U-62 и U-81 — самые слабые в этом списке: объём я знаю по одной
строке в хендоффе, кода не читал. Перед взятием в работу измерю и уточню.

#### U-65 — факт против оценки (пакет 28, с57)

**Оценивал 1 ч, вышло около 2,5.** Разница — не в сложности правки, а в
объёме: оценка была списана с одной строки хендоффа («`Sidebar.tsx:120`»),
а замер по дереву нашёл **пять** подписок без фильтра — сетка занятости,
сайдбар и **три** таблицы каталога в портале направника.

⚠️ Это **седьмой** раз за проект, когда перечень из дока оказался уже
дерева. Пять предыдущих названы в PR-доках RF-2/RF-4 и 0174. Вывод для
планирования, а не только для кода: **оценку по названному месту надо
множить на замер по дереву — до начала работы, а не после.**

Куда ушло время сверх оценки:

* +40 мин — портал: пришлось решать, входит ли он в пакет вообще. Решил,
  что входит: закрыть две подписки из пяти и написать «U-65 закрыто» —
  ровно та ложь между доком и кодом, которую этот проект ловит регулярно;
* +30 мин — стенды. `falsify-u61` пришлось переписывать: пустой словарь
  сделал две мутации недостижимыми, а одну — двухфайловой (стенд научился
  применять несколько правок как одну мутацию);
* +20 мин — **противоречие с документацией Supabase**. Она утверждает
  «Delete events are not filterable», то есть отрицает саму премису
  пакета. Разрешено замером тела `realtime.apply_rls` (фильтр на DELETE
  считается по ПОЛНОЙ replica identity, обрезка до PK — только payload).
  Разбор — в `docs/audit/PR-U65-realtime-clinic-filter.md`.

Постоянная цена (ревизия 24 стендов + гейт + деплой) — как обычно, ~50 мин
из этих 2,5 ч.

---

## 3. Что нельзя начать без вашего решения

Это не «жду ответа, чтобы не ошибиться» — это развилки, где выбор ваш, а не мой.

| # | развилка | что измерено, чтобы решать не вслепую |
|---|---|---|
| **Р1** | **Путь КЕЙСА для Г1-F** — дублировать ли гард на сервере, как сделали для трёх путей создания | Шаги кейса снимаются `buildPayload()` задолго до отправки, поэтому наивная заявка в них протухнет и гард будет отказывать ЧЕСТНОЙ работе. Нужна другая схема (заявка в момент отправки кейса), это ~2 ч |
| **Р2** | **Правило «где нужен аудит-триггер»** (граница 0173) | Сегодня список говорит «эти шесть должны быть», а не «аудит должен быть везде, где надо». НОВАЯ таблица с PII без аудита никому не заметна |
| **Р3** | **Расширять ли список №19 на 38 триггерных функций расписания** | Цена измерена: тело триггерной функции меняют 8 из последних 30 миграций против 4 из 30 для нынешнего списка — то есть вдвое чаще передрук сторожа на 1100 строк |
| **Р4** | **Playwright / E2E по ролям** | В проекте нет ни одного браузерного теста — по замыслу. Решение о заведении первого |

---

## 4. Что требует вашего участия физически

**Живые проверки** хвостов фазы 4, `cas` и самого Г1-F — им нужна сессия
персонала, а паролей я не прошу и не буду.

⚠️ **Но статус изменился, и это замер, а не догадка.** При снятии отпечатка
деплоя через коннектор Claude in Chrome я увидел: в вашем Chrome **сессия
персонала уже живая** — `/login` редиректит на `/radiologist`, страница 20014
байт вместо 96930. То есть живые проверки выполнимы **без единого пароля**: вы
уже вошли, я работаю в вашей вкладке через коннектор.

**Оценка: 30–60 мин на прогон**, если вы дадите добро на работу в этой сессии.
Что именно проверял бы (самое дешёвое и крупное первым):

1. `RescheduleModal` с выбранным слотом — один прогон закрывает узел времени;
2. Ф4-8 (таймер/звук), Ф4-2 (края окна вызова);
3. сценарий `cas`;
4. Г1-F: отказ по часам — если найдём безопасный способ разойтись часам.

⚠️ Граница, которую называю заранее: живая проверка в вашей боевой сессии — это
действия от вашего имени в проде. Ничего необратимого делать не буду и перед
каждым шагом, который что-то пишет, буду спрашивать.

---

## 5. Предлагаемая последовательность и сроки

Календарных дат не обещаю: темп зависит от того, когда ваша машина в сети.
Меряю в **часах работы сессии** — сегодня их вышло около 4,5 за два захода.

| сессия | содержание | оценка |
|---|---|---|
| **с57** | 0174 → долг PROSE → RF-2/RF-4 → 0175 → **U-65** | оценка **4,5–5,5 ч**, факт больше: U-65 приехал из с58 |
| **с58** | ~~U-65~~ (сделан в с57) → U-66 → U-81; живые проверки, если дадите вкладку | **3–4 ч** |
| **с59** | U-62 → LOW-пачка (U-59, U-60, U-75) | **4–5 ч** |
| в любую | развилки Р1–Р4 по мере ваших ответов | Р1 ~2 ч, Р2/Р3 по 1–1,5 ч, Р4 — отдельный разговор |

**Итого до пустой очереди: примерно 13–16 часов работы**, то есть три сессии
сегодняшнего размера, плюс развилки.

**Если нужен один пакет за раз** — берите верхнюю строку списка §2 и говорите
«продолжай»: порядок уже расставлен по пользе.

---

## 6. Находки самого планирования

Планирование — тоже чтение, и оно нашло две вещи.

⚠️ **Очередь в хендоффе устарела и лгала.** Блок «Очередь на с57» писался в
пакете 19 и до сих пор предлагал сделать 0172 («дайджест ТЕЛ guard-функций»),
хотя 0172 накачена ещё вчера, а 0173 закрыл её же названную границу. Живая
очередь теперь здесь, а старый блок помечен как перекрытый.

⚠️ **RF-1 — висячая ссылка.** Замер: `RF-1` встречается ровно в трёх местах, и
все три — ПЕРЕЧИСЛЕНИЯ (`claude/radflow-handoff.md:529`,
`claude/session56-start-prompt.md:26`,
`docs/audit/RADFLOW_DEEP_…_2026-08-27.md:1576`). Нигде не сказано, ЧТО это.
Для сравнения: RF-2 и RF-4 описаны в разделе «Названные долги».
**Вопрос вам:** RF-1 — это реальный долг, чьё описание потерялось, или остаток
нумерации? Планировать работу по имени без содержания я не могу.
