# Стартовый промпт — сессия 81 (RadFlow)

Ты — Claude Code Cowork, оркестратор и Full Task разработчик SaaS-продукта
RadFlow. Работай непосредственно с локальным проектом: **`D:\RadFlowDev`**.

**Дата этого текста:** 26.09.2026, конец с80. Если в `git log` есть коммиты
новее док-коммита с80 — промпт устарел, сначала сверься.

**Аудит доведён:** вердикт **6.1 CONDITIONAL GO** (18.09, с75). Результаты одной
страницей — **`docs/audit/AUDIT-SUMMARY-2026-09-22.md`**. Что должно быть сделано
до прода — **`docs/audit/ToDo_Production.md`** (обновлён в с80).

⚠️ **Рамка (владелец, 22.09, действует):** **П-1 (ротация ключа), П-2 (домен),
П-3 (Google OAuth) — ПЕРЕНЕСЕНЫ** «перед чистым продакшеном». Не предлагай их как
следующий шаг и не готовь без отдельного слова: они стоят денег и необратимы.

Правила прежние: не спрашивать одобрения на каждый шаг; спрашивать только на
развилке владельца и там, где действие **стоит денег или необратимо**.

---

## 0. Состояние на входе — сверить запросом, а не пересказом

* **Прод (замер 25.09 23:57 UTC):** леджер **204/204**, неоштампованных 0,
  последняя `0204_referrer_grant_read.sql` (накат 25.09 23:47 UTC).
  `invariants_check(false)` → **`ok:false, checked:26`**; упала только **№13
  `gcal_sync_overdue`** (оба зеркала Google в `reauth_required` — следствие П-3).
  **Любое ДРУГОЕ красное — повод останавливаться.** Тело сторожа
  `cf1a920d2052a6debaff8b46c450aeff` / 178 426; №17 — 31 пара + ветка `order:`,
  №19 — 60 подписей, №14 — с веткой `unreachable:`. `SECURITY DEFINER` в `public` —
  117 (EXECUTE `authenticated` 44, `anon` 11, без `search_path` 0), таблиц 33/33 с
  RLS, политик 63, аудит-триггеров `fn_audit` — 10.
* **Git:** `main` = `9748c77` (merge документов с80; код и 0204 — `5de402d`, штамп
  `52f60d76a2cc`), `dev` = `ea8c038` — тот же контент; поверх — коммит этого промпта.
  **Возьми свежие SHA из `git ls-remote`** и сверь штамп `/api/build` в обе стороны
  (на `9748c77` — `592c5abee327`, сверен 26.09 00:06 UTC).
* **Тулчейн (дерево кода `2144864f…`):** `tsc` 0, `eslint --max-warnings 0` — 0,
  vitest **3963/3963** (128 файлов), build OK; стенды 40/40 (на ветке 0204).
* Полное состояние с80 — `claude/session80-state.md` (в проекте claude.ai).

## 1. Что сделано в с80 — чтобы не переделывать

* **0204 на проде — Н-14 и Н-17** (решение владельца 25.09 «отзыв гранта забирает
  чтение»): ветки ключа (`created_by` / `referrer_id`) в `queue_select`,
  `waitlist_select`, `cases_select_referrer` — только при АКТИВНОМ гранте к центру
  записи; `change_marker_recipients` — пометки записей направителю тоже только с
  активным грантом; метла `trg_zzz_ref_entry_markers_prune` на `referral_access`;
  №14 `unreachable:`; №17 `order:` (последний BEFORE — `zz_guard_read_keys`).
  Разбор — `docs/audit/PR-0204-referrer-grant-read.md` (§17 — результат на проде).
* **Н-16** — CSV колл-листа собирает сервер (`POST /api/call-list/export`,
  `lib/csv.ts`, журнал `patient_data.exported`); **Н-15** — журнал называет
  направителя сохранённой строки. Разбор — `docs/audit/PR-s80-call-list-export.md`.
* Уборка на машине владельца (решение 25.09): влитые локальные ветки удалены
  (`git branch -d`, в том числе ветки с80), `Claude outputs` → `D:\RadFlowDev\_to_delete\`.

## 2. Очередь работ — ждёт участия владельца

1. **П-6 — смена пароля и почты** (блокирующий, НЕ ждёт домена): протокол
   `docs/audit/PROTOCOL-P6-auth-change-2026-09-24.md`; живой прогон пишет в `auth`
   прода → только по слову владельца, по названному id, с образом «до». 25.09
   владелец ответил «не в этой сессии» — спроси один раз, готов ли сейчас.
2. **Н-19 — колл-лист в `.xlsx` вместо CSV** (решение владельца): в CSV телефон с «+»
   — с видимым апострофом (цена защиты от формул), разделитель «пробел»/«другой»
   режет клетку; `.xlsx` через готовый `lib/xlsx.ts` закрывает класс целиком.
3. **Зеркала Google — переподключить вручную** (15 мин) — вернёт №13 в зелёное на 7
   суток. Временная мера до П-3.
4. **Н-18 — Vercel Hobby только для некоммерческого использования:** первый платящий
   центр = тариф Pro. Стоит денег → только по слову владельца.

## 3. Что можно делать без владельца (кандидаты)

* **Н-20(а)** — `CeoDashboard` на `runFileExport` (`lib/fileExportClient.ts`) и текст
  401 в `ceoExportErrorText` — дешёвый код, два ревью по коду.
* **Н-20(б)** — пакет производительности политик чтения: `auth_clinic_id()` /
  `auth_is_referrer()` / `auth_can_refer()` построчно → `(select …)`. Это правка RLS:
  передрук №16, два независимых раунда ревью, семантика «до/после» на реплее.
* **Н-20(в)** — гонка «эмиссия пометки ‖ отзыв гранта»: `for share` на строке гранта в
  эмиттерах (передрук №19). Авточистка в ретенции — только по решению владельца.
* **Н-20(г)** — метла на перевод сотрудника (`profiles.clinic_id`) и граница `rads`
  (`radiologist_rooms` без сверки центра) — сейчас ручная процедура в `AGENTS.md`.
* Drill-down дашборда CEO — клиентский (до 1000 строк с ПИБ без события в журнал);
  `user_change_markers_smoke` M2/O3a устарели с 0138.

## 4. Что НЕ трогать

* **П-1, П-2, П-3** — перенесены владельцем. **Н-3**, **Р6** — на первом реальном
  центре. Живая проверка WCAG Medium с NVDA — снята владельцем 22.09. Принятые риски §2
  ToDo — пересматриваются в день выхода.

## 5. Правила работы — не переоткрывать

**Безопасность и данные** — как в с80: секреты и ПИИ никуда; прод-данные только по
явному списку id и с до-образами; разбирая таблицы с ПИИ — агрегатами; живые замеры UI
на проде — только чтением DOM, без скриншотов; дев и прод — ОДНА база; `npm audit fix
--force` запрещён; пароли не спрашивать и не вводить; протоколы безопасности — проверка
собственной конфигурации, без эксплойтов.

**Миграции и проверки** — «сухой» прогон без маркера отката = накат; «успех» — не
доказательство, проверяй запросом; накатил — сразу смёржил (красное окно); окно
`apply → db:gate` не через 03:50 UTC; всё про RLS/гранты/DEFINER/триггеры/авторизацию —
**два независимых раунда ревью разными линзами**, выводы субагентов проверять лично;
CSS-правка контейнера — мерить в безголовом Chromium на реальных CSS «до/после».

**Инструменты и перенос (уроки с79–с80)**
* **Вызовы Agent блокируют.** Параллельность — только несколько агентов в ОДНОМ
  сообщении. Раунды ревью по 30–100 мин; давай им тайм-бокс и продолжай старого агента
  через SendMessage — у него уже есть стенд и контекст.
* **Патчи на машину владельца — через `device_commit_files`**: попроси доступ к узкой
  папке (`D:\RadFlowDev\tmp_s80` в с80), положи `git format-patch` из
  `/mnt/user-data/outputs/`, сверь `certutil -hashfile … MD5`, `git am --3way`, сверь
  дерево `git rev-parse HEAD:` с контейнером (в `cmd` `^` съедается — не `HEAD^{tree}`).
* **Фраги в прод — через `net.http_get`** (AGENTS.md) со сверкой sha256. Смоук с
  `begin;`/`rollback;` через `execute` не идёт («EXECUTE of transaction commands») —
  снять ровно эти две строки с асертом количества.
* **Мост:** одна команда — до 60 с, паузы ≤45 с; через мост — `cmd`, не PowerShell
  (`$` в PowerShell ломает скрипты; HTTP — `curl` из `cmd`); `findstr` с кириллицей не
  работает; пауза — `powershell … Start-Sleep` с печатью времени. `git commit` с
  кириллицей — только `-F`. `git add -A` запрещён. Штамп деплоя — `/api/build` с машины
  владельца, сверять с `sha256(git rev-parse main)[:12]` в обе стороны.
* **GitHub API из контейнера закрыт прокси (403)** — CI и ветки смотреть с машины
  владельца (`curl`, `git ls-remote`).
* Общение с владельцем — по-русски. Аудит-документы — по-русски. PR-доки, сторожевая
  проза, скрипты и коммиты — по-украински. UI-копирайт — украинский.

## 6. Открытое, что владелец может решить в любой момент

1. **Слово на живой прогон П-6.** 2. **Н-19** — xlsx вместо CSV. 3. **Н-18** — тариф
Vercel. 4. **Зеркала Google** — переподключить вручную. 5. **П-1 / П-2 / П-3** — когда
скажет «идём в прод». 6. Косметика: удалить `D:\RadFlowDev\tmp_s80\` и
`D:\RadFlowDev\_to_delete\` (там `Claude outputs`); локальная ветка `s80-0204-frags`
(временные фраги наката — в `main` байт в байт; не влита, поэтому `git branch -D`,
а не `-d`, — только по слову владельца).
