# Стартовый промпт сессии 62 — пакет 47: №23 `schema_digest`

Работай ПРЯМО в `D:\RadFlowDev` через Desktop Commander. Прод-БД — Supabase MCP,
project ref `rdiqjxzibdqbhwileret`. Общение по-русски, UI и доки проекта —
по-украински (`claude/radflow-handoff.md` исторически русский — держись файла).

## ЗАДАЧА №0 — ПЕРЕМЕРИТЬ, а не поверить

Таблица ниже — сводка на конец с61. **Расхождение считать НАХОДКОЙ и назвать
её вслух**, а не молча поправить.

| что | значение (замер 09.09.2026) |
|---|---|
| `main` / `dev` | `e1ffdf8` / `95d6157` |
| прод-БД | `0184_rf03b_sched_marker_fanout.sql`, ledger **184/184** → следующая **0185**, номер ИЗ ЛЕДЖЕРА |
| `invariants_check()` | `ok:true`, `checked:22`, `failed:[]` |
| тело сторожа | md5 без CR `11a297318da068f53b113c3d9120e6b8`, длина **97025**, CR 0; пин g `673861da14838e8a21c5365cb91572ce` |
| тулчейн | tsc 0, eslint 0, vitest 2983/2983, `db:gate` 184/184 |
| стенды | `EXPECTED_STANDS` **34** |
| штамп деплоя | `e1ffdf8` → считать локально ДО обращения к `/api/build` |
| засев | 228 записей, окно 10–16.09, **следующее гниение 16.09** |

⚠️ Штамп деплоя мерить ТОЛЬКО с машины владельца либо с уникальным
cache-buster: кэш `WebFetch` переживает сессию и отдаёт значение многодневной
давности (находка с61).

## Пакет 47 — миграция 0185: ветка №23 `schema_digest`

**Зачем.** Аудит (RF-05) просил schema-contract manifest. Сегодня покрыто:
№16 политики, №17 триггеры, №19 тела функций, №22 гранты. **Не покрыто —
колонки и constraints.** №23 закрывает последний кусок, и после него RF-05
закрыта в том смысле, в каком её ставил аудит.

**Что делать.**

1. Снять с прода эталон запросом: `information_schema.columns`
   (таблица, колонка, тип, nullable, default) + `pg_constraint`
   (тип, имя, определение) по схеме `public`. Нормализовать так же, как №16/№22.
2. Ветка №23 в `invariants_check`: `cur` из каталога ↔ `expd` хардкод,
   offenders `new:` / `changed:` / `missing:`, fail-loud.
3. `checked` 22→23: `node scripts/bump-checked-pins.mjs 22 23`.
4. **В ТОТ ЖЕ передрук положить дешёвые ветки**, чтобы платить ревизию один раз:
   расширить (g2) на `to_jsonb(p)` / `select *` над `profiles`.
5. Зонды ДО наката, каждый **в транзакции с маркером отката**:
   добавить колонку → красный; сменить тип → красный (`changed:`);
   снять `NOT NULL` → красный. Плюс зелёный базис: без правок — `ok:true`.
6. Накат, `db:gate`, перепин `g`/`g2` в `supabase/smoke/gcal_pg_cron_smoke.sql`,
   полная ревизия, merge, штамп в обе стороны.

⚠️ **Мина, которая уже сработала дважды (с59 №2 и с61 №6):** пин на КОЛИЧЕСТВО
в отдельном тесте. `guardTriggersInvariant.test.ts` и
`guardFnBodiesInvariant.test.ts` держат списки с числами — после передрука их
надо обновить, и покраснеют они ровно на накате, а не раньше.

⚠️ **Второй раз наступать нельзя:** «сухой» прогон БЕЗ маркера отката — это
накат. Маркер отката — единственное отличие зонда от применения (с61, урок №1).

## Дешёвая пачка, если останется время (без миграции)

- `drop column waitlist_entries.claim_token` — замер 09.09: **0** непустых
  значений и **0** упоминаний во всём коде. Формально это миграция, но
  тривиальная.
- **`tsc --noEmit` в `scripts/falsify-all.mjs`** — сейчас стенды гоняют только
  vitest. В с61 долг сработал вживую: `TS1501` поймал только tsc.
- `incidentCount` — двустановый falsy-гейт; поверхность `services` без
  `refreezeKey`; `Sidebar` читает `loadWaitCount` дважды.

## Что уже решено владельцем (не переоткрывать)

- **RF-07 — отсрочка ПРИНЯТА письменно**, критерий пересмотра: второй РЕАЛЬНЫЙ
  центр. См. `docs/audit/DECISIONS-2026-09-09.md`.
- **RF-08 закрыта**, подтверждено 09.09: `auth_leaked_password_protection`
  исчез из security-advisors.

## Что ждёт владельца (спросить, но не блокировать работу)

1. **RF-03, остаток.** Замер 09.09: 29 из 36 контрастных МРТ без кабинета (81 %)
   против 8 из 190 неконтрастных (4 %); плюс 13 услуг `active=true` на кабинете
   `DocLife` с `active=false` — их метрика `room_id is null` не видит вообще.
   Вопрос: контраст не привязывается сознательно?
2. Живой прогон 12 сценариев + `race-check.mjs cas` (никогда не запускался) —
   3–4 ч, нужен владелец на пишущих шагах.
3. Ротация service-role ключа — привязана к первому реальному центру.
4. Развилки Р1–Р5 стоят с с57.

## Правила, которые НЕ ослабляются

Два раунда ревью РАЗНЫМИ линзами на всём, что трогает RLS, гранты,
`SECURITY DEFINER`, триггеры, авторизацию, service-role код или прод-миграции;
выводы субагентов проверять лично. Фальсификация: НАЗВАННЫЙ красный тест плюс
ЗЕЛЁНЫЙ базис. Живые прод-проверки — в ОБЕ стороны. Прод-данные — только по
явному списку id с before-image. Секреты и PII — НИКУДА. `npm audit fix --force`
ЗАПРЕЩЁН. Dev и prod — ОДНА БД. Пароли не просить. «Успех» — не доказательство:
после каждого наката проверять ЗАПРОСОМ строку леджера и счёт,
`invariants_check(false)`, md5 тела сторожа без CR **и его длину**, пины смоука.
