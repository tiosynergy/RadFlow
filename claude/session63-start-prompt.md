# Стартовый промпт сессии 63

Работай ПРЯМО в `D:\RadFlowDev` через Desktop Commander. Прод-БД — Supabase MCP,
project ref `rdiqjxzibdqbhwileret`. Общение по-русски, UI и доки проекта —
по-украински (`claude/radflow-handoff.md` исторически русский — держись файла).

## ЗАДАЧА №0 — ПЕРЕМЕРИТЬ, а не поверить

Расхождение с таблицей ниже — **НАХОДКА**, назвать вслух, а не молча поправить.

| что | значение (замер 10.09.2026) |
|---|---|
| прод-БД | **`0185_schema_digest.sql`**, ledger **185/185**, unstamped 0 → следующая **0186**, номер ИЗ ЛЕДЖЕРА |
| `invariants_check()` | `ok:true`, **`checked:23`**, `failed:[]` |
| тело сторожа | md5 без CR **`8871cad05dbbee6c4cef4f4f9a3fb515`**, длина **111 592**, CR 0 |
| пин `g` | **`6eac0bb1e3c18c8d7eb5e465f9ce3b27`** |
| тулчейн | tsc 0, vitest **3007/3007**, `db:gate` 185/185 |
| стенды | `EXPECTED_STANDS` **35** |
| засев | окно 10–16.09, **следующее гниение 16.09** — проверь ПЕРВЫМ делом |

⚠️ Штамп деплоя мерить ТОЛЬКО с машины владельца либо с уникальным
cache-buster: кэш `WebFetch` переживает сессию (находка с61). Считать ожидание
локально ДО обращения: `node D:\RadFlowDev-scratch\expect-stamp.mjs`.

## Что закрыто и не надо переоткрывать

- **RF-05 закрыта** — schema-contract manifest покрыт целиком (№16, №17, №19,
  №22 и №23). См. `docs/audit/PR-0185-schema-digest.md`.
- **RF-08 закрыта** (leaked-password protection).
- **RF-07 — отсрочка ПРИНЯТА ПИСЬМЕННО**, критерий пересмотра: второй РЕАЛЬНЫЙ
  центр. См. `docs/audit/DECISIONS-2026-09-09.md`.

## Из аудита осталось ровно три пункта

1. **Живой прогон 12 сценариев + `race-check.mjs cas`** (не запускался НИ РАЗУ)
   — 3–4 ч. Требует владельца: каждый пишущий шаг — с отдельного «да».
   Сценарии: конкурентные `schedule_from_waitlist_rpc`, параллельные
   case-операции, переход через полночь, emergency stop с реальными affected
   rows, живые хвосты `RescheduleModal`, Ф4-8, Ф4-2, Г1-F, карточка CEO.
2. **Ротация service-role ключа** — решение владельца, привязано к первому
   реальному центру. Технически 30 мин + деплой, кода не трогает.
3. **RF-03, остаток — ВОПРОС ВЛАДЕЛЬЦУ.** Замер 09.09: 29 из 36 контрастных
   МРТ без кабинета (81 %) против 8 из 190 неконтрастных (4 %) — разница в
   двадцать раз, на случайность не похоже. Плюс 13 услуг `active=true`
   указывают на кабинет `DocLife` с `active=false` — их метрика
   `room_id is null` не видит вообще, то есть непригодных к записи не 37, а 50.
   Вопрос: контраст не привязывается сознательно?

## Дешёвая пачка (без миграции), если аудит ждёт владельца

- `drop column waitlist_entries.claim_token` — замер: 0 непустых значений и
  0 упоминаний во всём коде.
- **`tsc --noEmit` в `scripts/falsify-all.mjs`** — стенды гоняют только vitest.
  В с62 долг сработал дважды вживую (TS1501, TS1127): `tsc` называл точное
  место, vitest говорил лишь «файл упал».
- `incidentCount` — двустановый falsy-гейт; поверхность `services` без
  `refreezeKey`; `Sidebar` читает `loadWaitCount` дважды.
- Сужение `ack` по дате и центру — трогает общий `AckScope`, отдельный пакет
  со своим ревью.

## Мины, которые уже срабатывали — читать перед любым передруком

⚠️ **Пин за счётчиком, который мы же и двигаем, — не пин.** В с62 это убило
`falsify-0182`: ожидание держалось за `/21 пара/` в НАЗВАНИИ теста, а пакет
переименовал заголовок. Ожидания стендов писать БЕЗ чисел.

⚠️ **Канон, скопированный дословно, ломает чужие якоря.** №23 повторила форму
offenders-діфа — и якорь `falsify-0180`, уникальный годами, стал двойным.
Якорь мутации должен тянуться до имени СВОЕЙ проверки (`'check', '<имя>'`).

⚠️ **Маркер риштувань — `/* 0174 */`, а не номер вашей миграции.** Он называет
КОНВЕНЦИЮ. `tests/invariantsFailLoud.test.ts` считает строки по нему.

⚠️ **Числа, вписанные рукой, протухают.** Не дублировать md5/длину: источник
истины один — то, что вписывает генератор.

⚠️ **«Сухой» прогон БЕЗ маркера отката — это накат.** Урок с61 №1.

## Правила, которые НЕ ослабляются

Два раунда ревью РАЗНЫМИ линзами на всём, что трогает RLS, гранты,
`SECURITY DEFINER`, триггеры, авторизацию, service-role код или прод-миграции;
выводы субагентов проверять ЛИЧНО (в с62 оба раунда нашли по блокеру, и оба
пришлось подтверждать запуском). Фальсификация: НАЗВАННЫЙ красный тест плюс
ЗЕЛЁНЫЙ базис. Живые прод-проверки — в ОБЕ стороны. Прод-данные — только по
явному списку id с before-image. Секреты и PII — НИКУДА. `npm audit fix --force`
ЗАПРЕЩЁН. Dev и prod — ОДНА БД. Пароли не просить.

⚠️ **«Успех» — не доказательство.** После каждого наката проверять ЗАПРОСОМ:
строку леджера и счёт, `invariants_check(false)`, md5 тела сторожа без CR **и
его длину**, пины смоука. И **полный прогон тестов** — в с62 именно он нашёл
то, чего не увидел ни один раунд ревью.
