# Session 58 — opening prompt (English, owner's message format)

> Written at the end of session 57 (2026-09-05). The owner pastes the text
> below as the first message of session 58 and attaches `NEXT_SESSION_PROMPT.md`
> (rewritten in English in session 57). The numbers are EXPECTATIONS — the new
> session verifies them by query, exactly as the attached file says.
> `main`/`dev` tips: the last CODE commit is `1dee210` / `f7d5e0f`; the docs of
> session 57 went on top as `3c96898` / `2fd730f`, and the final docs commit is
> above that — take the hashes from `git ls-remote`, not from here. The `/login`
> fingerprint changes on every push to `main`: measured in both directions on
> 05.09 as `1KXTYxqAKBzhqunU7Gqbk` → `6B4LcEMX3j8kVL4iFwWbI`, and the final docs
> push moves it once more.

---

Continuing development and production of the RadFlow SaaS. You are the orchestrator and Full Task developer. Communication in Russian, UI copy in Ukrainian, project docs in Ukrainian. Work DIRECTLY in `D:\RadFlowDev` via Desktop Commander; prod DB — Supabase MCP (`rdiqjxzibdqbhwileret`).

AUTHORITY IS EXPANDED, and I confirm it now: you edit code/tests/SQL/docs yourself, create append-only migrations and apply them to prod, run safe prod SQL, commit, push, merge PRs, deploy, read prod logs, do live checks. The old rule "merge/push/apply is done by the owner" is CANCELLED — if you find it in the docs as a live rule, fix it in place (`docs/HANDOVER.md` and old audits keep it as history — leave those). Applying SQL is yours, but with MANDATORY verification of the Results: "Success" is not proof — verify the body md5 without CR, the ledger row, `invariants_check(false)` and the smoke pins after every apply.

SAFEGUARDS ARE NOT CANCELLED IN ANY DEGREE: two independent review rounds with DIFFERENT lenses on everything sensitive, and you validate subagent conclusions personally; falsification of guards — a NAMED red test and a GREEN baseline; live prod check in BOTH directions by measurement — if there is nothing to check on, say so plainly instead of faking a check; prod data only by an explicit list of ids with before-images, no mass deletions by a vague predicate; secrets and PII go NOWHERE — not into a report, not into a log, not into a subagent prompt; `npm audit fix --force` is FORBIDDEN; dev and prod are ONE DB; never ask for passwords. Ask me when a decision is irreversible and touches live patients, when it is a product decision rather than an engineering one, or when access is needed.

TASK #0 before any edit: `select now()`, `git ls-remote origin main dev`, `git status`, `maintenance_runs` overnight, `select max(name) from public.migration_ledger`, `select public.invariants_check()`, and the deploy fingerprint of `/login`. The prod URL is `https://rad-flow-tau.vercel.app` — it is in `AGENTS.md` and in `cron.job.command`; measure the fingerprint from a browser on my machine (the buildId `\"b\"` in the RSC payload — HTML comments are stripped by `WebFetch` from the container). ⚠️ Both browsers now carry a live RadFlow session: in my Chrome `/login` redirects to `/radiologist` (~20 000 bytes), in the built-in browser to `/queue` (~96 900) — so byte counts must not be compared between them, but the buildId is the same in both, and the built-in browser answered when Claude in Chrome had gone unresponsive. Expected: `main` = the final docs commit on top of `3c96898`, `dev` on top of `2fd730f`, clean tree; prod DB at **0177** (`0177_realtime_filter_premise.sql`), ledger **177/177**, invariants ok/**21**/[], next migration **0178** (number from the ledger, never from the folder); vitest **2714/2714** (91 files), tsc 0, eslint 0; stand revision **25/25, 588 addressed**; the nightly `invariants` run at 03:50 UTC reports `checked:21`. DO NOT TRUST THE DOCS — verify by query; a discrepancy is a finding, name it.

Session 57 ran eleven packages and four migrations, and emptied queue §2.4 completely: **0174** (18 of 19 checks wrapped in their own exception handler — before it, a failing check wrote NO row into `maintenance_runs` at all), **0175** (no privilege-granting defaults in `profiles`, RF-4), **U-65** (not a single realtime subscription without a clinic filter — one really existed on live prod), **U-66** (the fix was written and WITHDRAWN after measurement, then done properly as **0176**: "narrow → data → widen", the link written LAST, because in the UPDATE branch `realtime.apply_rls` does not trim `old_record` and delivery is decided by the NEW row), **0177** (check №21 guards the premise realtime filtering rests on), **U-81** (one single verdict→exit-code linkage, 105 tests) and **U-62 in full** (the hook's refetch policy extracted to `lib/realtimeRefetchPolicy.ts`, plus `skipInitial` for the 14 subscriptions whose `onChange` was a bare `router.refresh()` — the queue board did three idle RSC reloads on mount). `checked` 19 → 21, stands 24 → 25, tests 2553 → 2714. Read `claude/radflow-handoff.md` (top block), `claude/plan-s57.md` (§3 has the forks with their measurements) and the PR docs in `docs/audit/`.

MAIN LESSONS OF SESSION 57, all of which cost a discarded edition of my own work: a positional probe over someone else's function body is not a guard — the first edition of check №21 stayed GREEN both when the PK trim leaves the DELETE branch and when the delivery predicate is removed entirely, i.e. green on exactly the breakage it existed for (cured by a behavioural oracle plus one non-positional probe); a list without schema qualification is fail-open (`public.doctors` → `shadow.doctors` gave ZERO offenders); the guard over the guards is a suspect too — two editions of the `process.exitCode` pin were discarded and the STAND found both, because tokenizing JS with regexes lost the call in 2 files of 25 and a character scanner made it 6 of 25; a doc's list is a HYPOTHESIS — the U-81 doc was wrong in both halves and this was the EIGHTH time a doc's list turned out to be behind the tree; and a product fix rots the anchors of OTHER stands silently — it happened twice in one package, and only a FULL revision catches it.

Queue for session 58 — a menu, not an order: **the LOW batch (U-59, U-60, U-75, ~2–3 h)** is the only remaining engineering work that needs no decision from you; then the live checks (cheapest big one: one run through `RescheduleModal` with a chosen slot — and this is now possible without a single password, because a staff session is live in your Chrome; I will ask before every step that writes); then the named debts (`room_id` in `queue_reschedule_rpc` and the same class in `updateWaitlistEntry`, the missing consolidated list of visibility columns, 12 of 25 stands still not using `verdictOf`, subscriptions with a conditional refresh). **Five forks wait on you: Р1** (case path for Г1-F), **Р2** ("where an audit trigger is required"), **Р3** (extend list №19 — two named candidates now, `update_patient_details` and `tg_change_markers_queue`), **Р4** (Playwright/E2E — the project has no browser test by design), **Р5** (`user_change_markers` → `REPLICA IDENTITY FULL`: latency against a privacy side channel, both sides measured). And one question that blocks planning: **RF-1 appears in exactly three places and all three are enumerations — nowhere is it said WHAT it is.** Real debt with a lost description, or a leftover of the numbering? Compose a plan and AGREE THE FIRST PACKAGE WITH ME BEFORE CODING. If a package is product-facing, show me the texts before they land.

Order around the revision: gate → commit → falsify-all (it refuses to start on a dirty tree; NO edits in the repo while ANY stand runs — the stand snapshots the files and restores them in `finally`, so your edit silently disappears; the runner lives in `%TEMP%` as a `.cmd` launched with `start /B cmd /c <file>` — the form with an empty title breaks inside `cmd /c "…"` and silently does nothing, so give every poll a green baseline; pause with `ping -n 50 127.0.0.1 >nul` because the device bridge caps a call at ~60 s; a stand that is red with an EMPTY facts table "did not finish", it is not "guard does not hold"; the full run takes 40–45 min) → merge → push → deploy → measure the `/login` fingerprint in both directions. Commit messages with Cyrillic only via `-F .commitmsg`; PowerShell `>` writes UTF-16, so reports you need to read are written by node itself in UTF-8; nested quotes inside `cmd /c "…"` break `findstr` and `node -e` — use node scripts in `%TEMP%`. And clean up your own scratch files before the gate: eslint runs with `--max-warnings 0`, so a stray `.tmp-*.mjs` in the repo root turns the gate red for a reason unrelated to the package.
