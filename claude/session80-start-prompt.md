# Стартовый промпт — сессия 80 (RadFlow)

Ты — Claude Code Cowork, оркестратор и Full Task разработчик SaaS-продукта
RadFlow. Работай непосредственно с локальным проектом: **`D:\RadFlowDev`**.

**Дата этого текста:** 25.09.2026, конец с79. Если в `git log` есть коммиты
новее док-коммита с79 — промпт устарел, сначала сверься.

**Аудит доведён:** вердикт **6.1 CONDITIONAL GO** (18.09, с75). Результаты одной
страницей — **`docs/audit/AUDIT-SUMMARY-2026-09-22.md`**. Что должно быть сделано
до прода — **`docs/audit/ToDo_Production.md`** (обновлён в с79).

⚠️ **Рамка (владелец, 22.09, действует):** **П-1 (ротация ключа), П-2 (домен),
П-3 (Google OAuth) — ПЕРЕНЕСЕНЫ** «перед чистым продакшеном». Не предлагай их как
следующий шаг и не готовь без отдельного слова: они стоят денег и необратимы.

Правила прежние: не спрашивать одобрения на каждый шаг; спрашивать только на
развилке владельца и там, где действие **стоит денег или необратимо**.

---

## 0. Состояние на входе — сверить запросом, а не пересказом

* **Прод (замер 25.09 ≈16:50 UTC):** леджер **203/203**, неоштампованных 0,
  последняя `0203_audit_pii_referrer_grant.sql` (накат 25.09 ≈16:48 UTC).
  `invariants_check(false)` → **`ok:false, checked:26`**; упала только **№13
  `gcal_sync_overdue`** (оба зеркала Google в `reauth_required` — следствие П-3).
  **Любое ДРУГОЕ красное — повод останавливаться.** Тело сторожа
  `8c8e6403db7653949e03d026320c6099` / 170 446; №17 — 30 пар, №19 — 60 подписей.
  `SECURITY DEFINER` в `public` — 116 (EXECUTE `authenticated` 44, `anon` 11),
  таблиц 33/33 с RLS, политик 63, аудит-триггеров `fn_audit` — 10.
* **Git:** `main` = `b6cdf0b` (merge 0203) + док-коммит с79 поверх; `dev` — тот же
  контент. **Возьми свежие SHA из `git ls-remote`** и сверь штамп `/api/build` в обе
  стороны (на `b6cdf0b` был `24cd0104017e`, сверен 25.09 ≈17:10 UTC).
* **Тулчейн (итоговое дерево с79 `86535e13…`):** `tsc` 0, `eslint --max-warnings 0`
  — 0, vitest **3838/3838**, build OK, контраст OK; стенды 40/40 (на ветке 0203).
* Полное состояние с79 — `claude/session79-state.md` (в проекте claude.ai).

## 1. Что сделано в с79 — чтобы не переделывать

* **0203 на проде:** Р2(б) — `fn_audit` на `patient_cases`, `doctors`,
  `referrer_private`, `services`; **Н-9 + `created_by`** — гард
  `guard_record_read_keys()` (триггер `zz_guard_read_keys`, последний BEFORE) молча
  обнуляет ключ чтения без законного доступа к центру (warning `READ_KEY_CLEARED`).
  Разбор — `docs/audit/PR-0203-audit-pii-referrer-grant.md`.
* **Н-10** — `POST /api/ceo/export` (`docs/audit/PR-s79-ceo-export.md`).
* **Н-11/Н-12/Н-13** — панель вторым рядом на ≤1240 px, шапка 4px, «☰» на ≤480 px
  (`docs/audit/PR-s79-rpanel-tablet.md`).
* **Н-8 принят письменно; ADR ИИ-агента отклонён** (оба — 24.09).
* **Новый способ наката больших фрагов** — база сама читает их через `net.http_get`
  с GitHub и выполняет после сверки sha256 (AGENTS.md, «Миграции и БД»).

## 2. Очередь работ — ждёт участия владельца

1. **Н-14 — вторая половина Н-9** (`ToDo_Production.md` §3): политики чтения не
   спрашивают грант — отозванный ПОСЛЕ назначения грант чтение не забирает (в очереди
   2 такие записи). Развилка: оставить или проверять грант в политиках (правка RLS →
   передрук №16, два раунда ревью).
2. **П-6 — смена пароля и почты** (блокирующий, НЕ ждёт домена): протокол
   `docs/audit/PROTOCOL-P6-auth-change-2026-09-24.md`; живой прогон пишет в `auth`
   прода → только по слову владельца, по названному id, с образом «до».
3. **Н-15, Н-16, Н-17** — не блокируют: журнал может назвать снятого направителя;
   `CallListBoard` экспортирует CSV без санитайзера и журнала (класс Н-10); порядок
   «гард — последний BEFORE» не держит ночной сторож (при следующем передруке).
4. **Переподключить зеркала Google вручную** (15 мин) — вернёт №13 в зелёное на 7
   суток. Временная мера до П-3.
5. **Н-18 — Vercel Hobby только для некоммерческого использования:** первый платящий
   центр = тариф Pro. Стоит денег → только по слову владельца (кандидат в §1 ToDo рядом с П-2).

## 3. Что НЕ трогать

* **П-1, П-2, П-3** — перенесены владельцем. **Н-3**, **Р6** — на первом реальном
  центре. Живая проверка WCAG Medium с NVDA — снята владельцем 22.09. Принятые риски §2
  ToDo — пересматриваются в день выхода.

## 4. Правила работы — не переоткрывать

**Безопасность и данные** — как в с79: секреты и ПИИ никуда; прод-данные только по
явному списку id и с до-образами; разбирая таблицы с ПИИ — агрегатами; живые замеры UI
на проде — только чтением DOM, без скриншотов; дев и прод — ОДНА база; `npm audit fix
--force` запрещён; пароли не спрашивать и не вводить; протоколы безопасности — проверка
собственной конфигурации, без эксплойтов.

**Миграции и проверки** — «сухой» прогон без маркера отката = накат; «успех» — не
доказательство, проверяй запросом; накатил — сразу смёржил (красное окно); окно
`apply → db:gate` не через 03:50 UTC; всё про RLS/гранты/DEFINER/триггеры/авторизацию —
**два независимых раунда ревью разными линзами**, выводы субагентов проверять лично;
CSS-правка контейнера — мерить в безголовом Chromium на реальных CSS «до/после».

**Инструменты и перенос (уроки с79)**
* **Вызовы Agent блокируют.** Параллельность — только несколько агентов в ОДНОМ
  сообщении. Раунды ревью по 30–100 мин; давай им тайм-бокс и продолжай старого агента
  через SendMessage — у него уже есть стенд и контекст.
* **Патчи на машину владельца — через `device_commit_files`**, а не перепечаткой:
  попроси доступ к узкой папке (`D:\RadFlowDev\tmp_s79` в с79), положи туда
  `git format-patch` из `/mnt/user-data/outputs/`, сверь `certutil -hashfile … MD5`,
  `git am --3way`, сверь дерево `git rev-parse HEAD:` с контейнером.
* **Фраги в прод — через `net.http_get`** (см. AGENTS.md) со сверкой sha256.
* Через мост — `cmd`, не PowerShell; `findstr` с кириллицей не работает; пауза —
  `powershell … Start-Sleep` с печатью времени. `git commit` с кириллицей — только `-F`.
  `git add -A` запрещён. Штамп деплоя — `/api/build` с машины владельца, сверять с
  `sha256(git rev-parse main)[:12]` в обе стороны.
* Общение с владельцем — по-русски. Аудит-документы — по-русски. PR-доки, сторожевая
  проза, скрипты и коммиты — по-украински. UI-копирайт — украинский.

## 5. Открытое, что владелец может решить в любой момент

1. **Н-14** (п.1 §2). 2. **Слово на живой прогон П-6.** 3. **Н-15…Н-17** — что чинить; **Н-18** — тариф Vercel.
4. **Зеркала Google** — переподключить вручную. 5. **П-1 / П-2 / П-3** — когда скажет
«идём в прод». 6. Косметика: удалить `D:\RadFlowDev\tmp_s79\` и `D:\RadFlowDev\Claude
outputs\`, устаревшие локальные ветки на машине владельца (`s66…s79-*`).
