# Session 60 — opening prompt (English, owner's message format)

Written at the end of session 59 (2026-09-07). The owner pastes the text below as the first message of session 60 and attaches `NEXT_SESSION_PROMPT.md`. The numbers are EXPECTATIONS — the new session verifies them by query. `main`/`dev`: take the hashes from `git ls-remote`. ⚠️ The pasted text and the attachment MUST agree — session 59 opened on a pasted text one package older than its own attachment; that cost a paragraph of reconciliation.

Continuing development and production of the RadFlow SaaS. You are the orchestrator and Full Task developer. Communication in Russian, UI copy in Ukrainian, project docs in Ukrainian. Work DIRECTLY in `D:\RadFlowDev` via Desktop Commander; prod DB — Supabase MCP (`rdiqjxzibdqbhwileret`).

AUTHORITY IS EXPANDED, and I confirm it now: you edit code/tests/SQL/docs yourself, create append-only migrations and apply them to prod, run safe prod SQL, commit, push, merge PRs, deploy, read prod logs, do live checks. Applying SQL is yours, but with MANDATORY verification of the Results: "Success" is not proof — verify the body md5 without CR, the ledger row, `invariants_check(false)` and the smoke pins after every apply; and if you apply a guard reprint through a body-rebuilding DO block instead of the literal file (session 59 did), verify the WHOLE catalog row, not just `prosrc`.

SAFEGUARDS ARE NOT CANCELLED IN ANY DEGREE: two independent review rounds with DIFFERENT lenses on everything sensitive, and you validate subagent conclusions personally. ⚠️ Session 59 is one more case: on plain UI code the two rounds found six ways past fresh pins and one product defect (a hint that led the operator to a button returning 403); on the migration, one BLOCKER (a count pin that would have reddened the gate exactly when the ledger was already 179/179) and one guard branch that both rounds independently showed could be bypassed with `returns setof profiles`. Falsification of guards — a NAMED red test and a GREEN baseline; live prod check in BOTH directions by measurement; prod data only by an explicit list of ids with before-images; secrets and PII go NOWHERE; `npm audit fix --force` is FORBIDDEN; dev and prod are ONE DB; never ask for passwords. Ask me when a decision is irreversible and touches live patients, when it is a product decision rather than an engineering one, or when access is needed.

TASK #0 before any edit: `select now()`, `git ls-remote origin main dev`, `git status`, `maintenance_runs` overnight, `select max(name) from public.migration_ledger`, `select public.invariants_check(false)`, `npm run db:gate:check`, and the deploy fingerprint of `/login` (pin the instrument: `/_next/static/<buildId>/_buildManifest.js` → 200 for the current id). Expected: prod DB at 0179, ledger 179/179, invariants ok/21/[], next migration 0180; vitest 2852/2852, tsc 0, eslint 0; guard body md5 `5e468b5e…`, length 80871; `EXPECTED_STANDS` 28 — the last FULL revision was 27/27, 663 addressed after package 38 (72 min — the machine was 2× slower than on 06.09); package 39 added `falsify-rf05` (28/28, 26 addressed, 97 s) without a guard reprint, so the next full run is expected at 28/28, 689 addressed. `/login` fingerprint `uh7QxBj9ypJ-9BrDkEwu4` after the package-39 merge (the docs commit on top will move it again). ⚠️ The 08.09 03:50 nightly run is the FIRST on the 0179 body — check it. ⚠️ The fingerprint is a ONE-DIRECTION signal: changed → a build arrived; unchanged → draw no conclusion. Build latency 4–9 minutes. DO NOT TRUST THE DOCS — verify by query; a discrepancy is a finding, name it.

Session 59 ran TWO packages. Package 39 (code only, no migration) closed RF-05: the migration gate's two silent fail-opens are gone — the decision is a pure `gateEnvDecision`; on Vercel nothing bypasses (skip variable, `RADFLOW_GATE_NO_DB`, missing keys → exit 1), outside Vercel a skip is explicit and the log says «БЕЗ ЗВІРКИ» (CI sets `RADFLOW_GATE_NO_DB=1` in `gate.yml`); and `/api/staff/password` now leaves a journal event (`staff.access_changed` / `password_reset|password_set`, titles «скинув пароль керівника/направника/співробітника»). Read `docs/audit/PR-RF05-gate-fail-closed.md`. Package 38 closed RF-09 on all four channels in two deploy steps: (b) `ceo_list_for_clinic` returns `null::text as invite_token` and its body is under guard №19; (c) `fn_audit` writes `before/after` without the key and the four historical rows were cleaned by explicit ids; (d) — NEW, found by reading the route — `/api/ceo/grant` no longer reads, issues or returns a token for an EXISTING profile (owner's decision, variant A). Guard №15 got (g) and (g2); `checked` stayed 21. Read `claude/radflow-handoff.md` (top block) and `docs/audit/PR-0179-rf09-definer-audit.md`.

MAIN LESSONS OF SESSION 59: a guard branch that reads a SIGNATURE is not a guard over DATA — `returns setof profiles` carries no column name; a count pin on a list (22 functions) is a landmine for the next reprint — it reddens after the DB is already ahead of the file; the two review lenses disagree on nothing and still find different things — falsification found the untested reactivation branch, operations found the button that returns 403; a hostile double that can only READ cannot test a route that WRITES — it grew `insert/update/auth.admin` and a query log, and the query log immediately mattered (`seen` kept only the last query); and a 95 KB migration file can be produced by two independent generators (python in the container, node on the owner's machine) with one md5 — that is how you move a file you cannot paste.

Queue for session 60 — a menu, not an order. (1) The residues of the eight: `sched_referrer_read` leaks non-granted room ids/hours/closures through `rooms jsonb` (RF-03); no regression check on grants beyond profiles — one GRANT silently reopens RF-04 (package 40 in `claude/plan-s60-tech-debt.md`: migration 0180, check №22 `grant_digest`, `checked` 21 → 22, guard reprint → full stand revision); leaked-password protection still off — one switch in the Auth dashboard, the cheapest item (RF-08, needs you — the built-in browser has no Supabase session); `add_case_step_rpc` not traced to the end (RF-01). RF-05 is CLOSED (package 39). (2) The second half of U-59 (`overlayShown` → `hold`, ~10 lines). (3) The named debts: `waitlist_entries.claim_token` is a dead column since 0100 (not a secret — measured); two left by package 39 — the PII key list in `lib/importantEvents.ts` lacks `password`/`pw` (mirror of the DB CHECK, needs a migration) and `/api/staff/password` ignores the `profiles.update` error after the auth write. (4) Live checks — the CEO card under an admin has never been looked at with eyes. Five forks still wait on you: Р1–Р5. Compose a plan and AGREE THE FIRST PACKAGE WITH ME BEFORE CODING. If a package is product-facing, show me the texts before they land.

Order around the revision: gate → commit → falsify-all (28 stands; 72 min for 27 in session 59, rf05 adds ~2 min; it refuses to start on a dirty tree; NO edits in the repo while ANY stand runs; run it through a one-shot `schtasks` task — session 59 did, and the bridge never mattered; poll with `ping -n 50 127.0.0.1 >nul` and give every poll a green baseline) → merge → push → deploy → measure the fingerprint. The built-in browser holds an admin/registrar session in RadFlow and did every measurement in 59 — prefer it. Commit messages with Cyrillic only via `-F .commitmsg`; PowerShell `>` writes UTF-16; scripts in `C:\Users\Public`, not in the repo root (eslint runs with `--max-warnings 0`).
