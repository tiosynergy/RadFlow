# Session 55 — opening prompt (English, owner's message format)

> Written at the end of session 54 (2026-09-02). The owner pastes the text
> below as the first message of session 55 and attaches
> `NEXT_SESSION_PROMPT.md`. The numbers are EXPECTATIONS — the new session
> verifies them by query, exactly like the attached file says.
> `main`/`dev` tip: this file was added by the single docs commit on top of
> `71a6119` — the hash comes from `git ls-remote`, not from here. The `/login`
> fingerprint changes on every push to `main`; the last measured value before
> the docs push was `OjitZ9f5IKNhiSfEr0L6a`.

---

Continuing development and production of the RadFlow SaaS. You are the orchestrator and Full Task developer. Communication in Russian, UI copy in Ukrainian, project docs in Ukrainian. Work DIRECTLY in `D:\RadFlowDev` via Desktop Commander; prod DB — Supabase MCP (`rdiqjxzibdqbhwileret`).

AUTHORITY IS EXPANDED, and I confirm it now: you edit code/tests/SQL/docs yourself, create append-only migrations and apply them to prod, run safe prod SQL, commit, push, merge PRs, deploy, read prod logs, do live checks. The old rule "merge/push/apply is done by the owner" is CANCELLED — if you find it in the docs as a live rule, fix it in place (the handoff already marks it cancelled; `docs/HANDOVER.md` and old audits keep it as history — leave those).

SAFEGUARDS ARE NOT CANCELLED IN ANY DEGREE: two independent review rounds with DIFFERENT lenses on everything sensitive, and you validate subagent conclusions personally; falsification of guards — a NAMED red test and a GREEN baseline; live prod check in BOTH directions by measurement — if there is nothing to check on, say so plainly instead of faking a check; prod data only by an explicit list of ids with before-images, no mass deletions by a vague predicate; secrets and PII go NOWHERE — not into a report, not into a log, not into a subagent prompt; `npm audit fix --force` is FORBIDDEN; dev and prod are ONE DB; never ask for passwords. Ask me when a decision is irreversible and touches live patients, when it is a product decision rather than an engineering one, or when access is needed.

TASK #0 before any edit: `select now()`, `git ls-remote origin main dev`, `git status`, `maintenance_runs` overnight, `select max(name) from public.migration_ledger`, `select public.invariants_check()`, and the deploy fingerprint in the HTML comment at the top of `/login` (from my machine, not from the container — the domain is not in its allowlist). Expected: `main` = `dev` = the docs commit on top of `71a6119` (clean tree), prod DB at `0169` (`0169_server_now.sql`), ledger 169/169, invariants ok/15/[], next migration 0170 (number from the ledger, never from the folder), vitest 2358/2358, stand revision 21/21 (479 addressed), `/login` fingerprint changed after the last docs push (previous value `OjitZ9f5IKNhiSfEr0L6a`). DO NOT TRUST THE DOCS — verify by query; a discrepancy is a finding, name it.

Session 54 closed Г1-F (package 12: the server refuses a day derived from a skewed client clock — clock claim `{nowMs, dayKey, fromToday}` from the client, verdict with TWO conditions on the server, `clock` REQUIRED by type so tsc enumerates the call sites; no migration) and package 13 (my final wording of the five `returned` banners and the short `CLOCK_SKEW_MSG` — the cost of the short message is named and accepted). The deferred-shift window on the boards (the Г1-E leftover) is NOT a problem — closed, do not raise it. Read `claude/radflow-handoff.md`, `NEXT_SESSION_PROMPT.md`, `docs/audit/PR-G1F-server-clock-trust.md`. There are NO open questions for me.

Queue for session 55 — a menu, not an order: U-80г (recommended — addressed mutations for `f4-2`, `f4-8`, `u70`, finish `u72` (69 addressed of ~140), the Г1-G scanner neighbours, and make the "every anchor exactly once" check a permanent stand step), F3/F4/F6 (three MED in the same time node), U-76 (migration 0170: check №16 in `invariants_check()` on the ACL and type of `public.server_now()`), the named Г1-F boundary — `createBooking` / `createReferralBooking` / `scheduleFromWaitlist` have no clock guard (whether to extend the refusal there is a product decision — ask me with the measurement in hand), live checks (cheapest big one: one run on `RescheduleModal` with a chosen slot; then Ф4-8 timer/sound, Ф4-2 call-window edges), U-81, the `cas` scenario, U-62, U-65+U-66, the LOW batch. Compose a plan and AGREE THE FIRST PACKAGE WITH ME BEFORE CODING. If a package is product-facing, show me the texts before they land.

MAIN LESSON OF SESSION 54: the stand and the two review rounds found NINE holes in guards you had written the same hour — an order pin that was green identically because `indexOf` hit the IMPORT line; "the block exists" ≠ "the block is reachable"; a pin that held the LINE and not the REFUSAL (`return` → `void` stayed green); a `null` claim that removed the guard with one word; the board's claim pinned by nothing; a tolerance bounded only from below; the clinic timezone untested. Also twice you named an option before reading the write path to the end ("migration 0170" for something the DB cannot know; "keep the form open" when the PARENT closes it at 8 sites) — measure first, name options second; the handoff's own wording of Г1-F was wider than the defect (ONE silent direction, not two) — the wording of a finding is a hypothesis. And a refactor in `lib/useFollowToday.ts` staled six anchors in a NEIGHBOUR stand: the first full revision was red — re-anchor with the reason next to each position, never loosen the anchor. The second review round's lens stays "what mutation would leave this pin green".

Order around the revision: gate → commit → falsify-all (it refuses to start on a dirty tree; NO edits in the repo while ANY stand runs — the stand snapshots the files and restores them in `finally`, so your edit silently disappears; the runner lives in `$env:TEMP` as a `.cmd` with a `.done` marker, launched detached — `Start-Process -WindowStyle Hidden` held for the whole of session 54, `schtasks` if the Desktop Commander bridge restarts; wait with `sleep` in the cloud container; a stand that is red with an EMPTY facts table "did not finish", it is not "guard does not hold") → merge → push → deploy → measure the `/login` fingerprint in both directions. Commit messages with Cyrillic only via `-F .commitmsg`; PowerShell `>` writes UTF-16, so reports you need to read are written by node itself in UTF-8; large Desktop Commander reads arrive in chunks — join them with `\n`.
