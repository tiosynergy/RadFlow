# Session 56 — opening prompt (English, owner's message format)

> Written at the end of session 55 (2026-09-03). The owner pastes the text
> below as the first message of session 56 and attaches
> `NEXT_SESSION_PROMPT.md`. The numbers are EXPECTATIONS — the new session
> verifies them by query, exactly like the attached file says.
> `main`/`dev` tip: the last CODE commit is `84078d7`; this file was added by
> the docs commit(s) on top of it — the hash comes from `git ls-remote`, not
> from here. The `/login` fingerprint changes on every push to `main`; the
> last measured value before the docs push was `4CK0ANcRvG1EcmnDZuqB4`.

---

Continuing development and production of the RadFlow SaaS. You are the orchestrator and Full Task developer. Communication in Russian, UI copy in Ukrainian, project docs in Ukrainian. Work DIRECTLY in `D:\RadFlowDev` via Desktop Commander; prod DB — Supabase MCP (`rdiqjxzibdqbhwileret`).

AUTHORITY IS EXPANDED, and I confirm it now: you edit code/tests/SQL/docs yourself, create append-only migrations and apply them to prod, run safe prod SQL, commit, push, merge PRs, deploy, read prod logs, do live checks. The old rule "merge/push/apply is done by the owner" is CANCELLED — if you find it in the docs as a live rule, fix it in place (`docs/HANDOVER.md` and old audits keep it as history — leave those).

SAFEGUARDS ARE NOT CANCELLED IN ANY DEGREE: two independent review rounds with DIFFERENT lenses on everything sensitive, and you validate subagent conclusions personally; falsification of guards — a NAMED red test and a GREEN baseline; live prod check in BOTH directions by measurement — if there is nothing to check on, say so plainly instead of faking a check; prod data only by an explicit list of ids with before-images, no mass deletions by a vague predicate; secrets and PII go NOWHERE — not into a report, not into a log, not into a subagent prompt; `npm audit fix --force` is FORBIDDEN; dev and prod are ONE DB; never ask for passwords. Ask me when a decision is irreversible and touches live patients, when it is a product decision rather than an engineering one, or when access is needed.

TASK #0 before any edit: `select now()`, `git ls-remote origin main dev`, `git status`, `maintenance_runs` overnight, `select max(name) from public.migration_ledger`, `select public.invariants_check()`, and the deploy fingerprint in the HTML comment at the top of `/login`. The prod URL is `https://rad-flow-tau.vercel.app` — it is written in `AGENTS.md` now and it also sits in `cron.job.command`; measure the fingerprint from the built-in browser on my machine (`fetch(location.origin + "/login", {cache: "no-store"})`), NOT with WebFetch from the container (it strips HTML comments). Expected: `main` = `dev` = the docs commit on top of `84078d7` (clean tree), prod DB at `0170` (`0170_policy_digest_invariant.sql`), ledger 170/170, invariants ok/**16**/[], next migration **0171** (number from the ledger, never from the folder), vitest 2465/2465 (83 files), stand revision **24/24**, `/login` fingerprint changed after the last docs push (previous value `4CK0ANcRvG1EcmnDZuqB4`). DO NOT TRUST THE DOCS — verify by query; a discrepancy is a finding, name it.

Session 55 closed the phase-4 tails F3/F4/F6 (package 14) and the WHOLE of phase 6: package 1 (page auth scanner, Ф6-1…Ф6-4, `RoleNotice` with my texts AND a sign-out button), Ф6-5 (terminal screens must give an exit — `/setup` trapped a registrar in a loop), package 2 (server surface — no holes, guard on form), package 3 (migration 0170: invariant №16 `policy_digest` over the 64 RLS policies, falsified on prod without touching a policy, file and prod byte-identical). The prod URL was found in `cron.job.command` — and then in 30 places of the repository itself (`docs/HANDOVER.md`, `docs/PRODUCT_OVERVIEW.md`, the audit journal header, `supabase/cron_jobs.sql`, the FHIR tests, even the session-50 block of `claude/radflow-handoff.md` itself): the handoff line "the address is nowhere in the repository, checked with findstr" was FALSE for four sessions — `findstr` inside `cmd /c` silently returned nothing and an empty result was taken as absence. Never ask for the URL again, and give every search a green baseline (a line that is known to exist) before believing an empty result — same discipline as a guard. Read `claude/radflow-handoff.md`, `NEXT_SESSION_PROMPT.md`, `docs/audit/PR-F6-pkg3-rls-policy-digest.md` (§10 has the new finding). There are NO open questions for me except the ones listed as such.

NEW FINDING TO ACT ON (not a hole today — an uncovered surface): the correctness of RLS on the PII tables rests on BEFORE triggers (`guard_profile_privileges`, `guard_room_in_clinic`, `guard_waitlist_room`, `guard_radiologist_scope`), and NONE of them is under any invariant — `canonical_objects` knows 12 objects, `priv_drift` only the DELETE tripwire, and the word `tgenabled` does not occur in `invariants_check` at all. `profiles_update_self` plus a full-column UPDATE grant means a dropped or disabled trigger opens self-escalation to admin with all 16 invariants green.

Queue for session 56 — a menu, not an order: **0171 (recommended) = check `guard_triggers` (existence + `tgenabled = 'O'` + the right function on the right table for a NAMED list) together with U-76 (`server_now()` ACL and type) in ONE reprint of `invariants_check`** — every reprint silently breaks every stand pinned to the reprint (`falsify-0166` and `falsify-u37` went red after 0170 for exactly that reason), so two reprints cost two full revisions; then U-80г (addressed mutations for `f4-2`, `f4-8`, `u70`, finish `u72`), the named Г1-F boundary (`createBooking` / `createReferralBooking` / `scheduleFromWaitlist` have no clock guard — product decision, ask me with the measurement in hand), live checks (cheapest big one: one run on `RescheduleModal` with a chosen slot; then Ф4-8 timer/sound, Ф4-2 call-window edges, the `cas` scenario — all need a staff session, which you do not ask for), the E2E-by-role decision (Playwright — ask me, the project has no browser tests by design), U-81, U-62, U-65+U-66, the LOW batch, and the named debts RF-2 / RF-4 / RF-1. Compose a plan and AGREE THE FIRST PACKAGE WITH ME BEFORE CODING. If a package is product-facing, show me the texts before they land.

MAIN LESSONS OF SESSION 55: a scanner-derived list is a HYPOTHESIS — falsified four times in one session (28 "ungated" routes, 38 "unchecked" SECDEF functions, 9 "unchecked" policies, self-escalation); read the shared helpers before the call sites. Your own fresh guard is the prime suspect — two review rounds found four HIGH in the page scanner (pin held the predicate, not the consequence; a substring, not reachability; `hasPositive` fooled by `new Set`; "13/13 addressed" without `newFile` mutations), and one review suggestion (R6) was REJECTED after checking the source — it would have made the baseline red on eight admin-only routes. A product fix can silently de-energize a stand position (M20 inverted, G1 re-pointed) — re-read the mutations, not just the guard. The comment next to a guard describes what the guard DOES — the `RoleNotice` header would have become a lie after the button was added. A migration that reprints `invariants_check` MUST be followed by a full revision, and the monotonic threshold "≥ previous reprint" has a one-step blind spot exactly in the reprint that adds a check (16 → 15 ≥ 15 stayed green) — the fix is structural: count of `'check', '…'` labels == count of `v_n := v_n + 1;` steps. `pg_get_functiondef` renders the header differently from the file — compare only the body between `as $function$` and `$function$;`. A DO-block apply does not insert the ledger row — insert it separately, then `npm run db:gate` stamps the md5.

Order around the revision: gate → commit → falsify-all (it refuses to start on a dirty tree; NO edits in the repo while ANY stand runs — the stand snapshots the files and restores them in `finally`, so your edit silently disappears; the runner lives in `$env:TEMP` as a `.cmd` with a `.done` marker, launched detached via `schtasks /run`; poll with `ping -n 58 127.0.0.1 >nul` because the device bridge caps a call at ~60 s; a stand that is red with an EMPTY facts table "did not finish", it is not "guard does not hold"; the full run takes 35–45 min) → merge → push → deploy → measure the `/login` fingerprint in both directions. Commit messages with Cyrillic only via `-F .commitmsg`; PowerShell `>` writes UTF-16, so reports you need to read are written by node itself in UTF-8; nested quotes inside `cmd /c "…"` break `findstr` — use node scripts in `%TEMP%`.
