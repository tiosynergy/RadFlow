# Session 61 — opening prompt (English, owner's message format)

Written at the end of session 59 (2026-09-08), after packages 38, 39, 40 and 41. The owner pastes the text below as the first message of session 61 and attaches `NEXT_SESSION_PROMPT.md`. The numbers are EXPECTATIONS — the new session verifies them by query. `main`/`dev`: take the hashes from `git ls-remote`. ⚠️ The pasted text and the attachment MUST agree — session 59 opened on a pasted text one package older than its own attachment; that cost a paragraph of reconciliation.

Continuing development and production of the RadFlow SaaS. You are the orchestrator and Full Task developer. Communication in Russian, UI copy in Ukrainian, project docs in Ukrainian. Work DIRECTLY in `D:\RadFlowDev` via Desktop Commander; prod DB — Supabase MCP (`rdiqjxzibdqbhwileret`).

AUTHORITY IS EXPANDED, and I confirm it now: you edit code/tests/SQL/docs yourself, create append-only migrations and apply them to prod, run safe prod SQL, commit, push, merge PRs, deploy, read prod logs, do live checks. Applying SQL is yours, but with MANDATORY verification of the Results: "Success" is not proof — verify the body md5 without CR, the ledger row, `invariants_check(false)` and the smoke pins after every apply; and if you apply a guard reprint through a body-rebuilding DO block instead of the literal file (sessions 59 did it three times), verify the WHOLE catalog row, not just `prosrc`.

SAFEGUARDS ARE NOT CANCELLED IN ANY DEGREE: two independent review rounds with DIFFERENT lenses on everything sensitive, and you validate subagent conclusions personally. ⚠️ Package 40 is the strongest case yet: the two rounds found one BLOCKER and five MAJOR **before** the migration touched prod — and THREE of the five were me repeating, in a brand-new check, mistakes that migration 0167 had already fixed in the neighbouring check №15 (roles hardcoded instead of taken from `authenticator` members; an inner join that loses `defaclnamespace = 0`; a regex over the ACL text instead of `aclexplode`). Falsification of guards — a NAMED red test and a GREEN baseline; live prod check in BOTH directions by measurement; prod data only by an explicit list of ids with before-images; secrets and PII go NOWHERE; `npm audit fix --force` is FORBIDDEN; dev and prod are ONE DB; never ask for passwords. Ask me when a decision is irreversible and touches live patients, when it is a product decision rather than an engineering one, or when access is needed.

TASK #0 before any edit: `select now()`, `git ls-remote origin main dev`, `git status`, `maintenance_runs` overnight, `select max(name) from public.migration_ledger`, `select public.invariants_check(false)`, `npm run db:gate:check`. Expected: prod DB at **0181**, ledger **181/181**, invariants **ok / 22 / []**, next migration **0182**; vitest **2876/2876**, tsc 0, eslint 0; guard body md5 `19b47366…`, length **95038**, normalized pin g `f157d9de…`; `EXPECTED_STANDS` **30**; the last full revision was **30 stands, 712 addressed, 67 min, 30/30 green**, run through a one-shot `schtasks` task that finished AFTER the desktop bridge dropped — that is the canon. ✅ The 08.09 03:50 nightly is already checked: `maintenance_runs` id 113, `ok:true, checked:22, failed:[]` — and it was the first nightly with `checked:22` at all. ⚠️ **THE DEPLOY FINGERPRINT HAS NO WORKING INSTRUMENT.** Package 41 measured that `/login` carries no `buildId` at all (App Router; `_buildManifest` absent from the page; the id recorded after package 40 is not in what prod serves; twelve candidate tokens probed as manifest paths — all 404). Do not report a phantom incident from it; rewriting that signal is an OPEN TASK. ⚠️ And do not trust `vitest 2869` anywhere in the older docs: measured on `38ff249` itself it was **2873** — the number was taken before the docs commit landed. DO NOT TRUST THE DOCS — verify by query; a discrepancy is a finding, name it.

Session 59 ran FOUR packages. **41** closed RF-01 (0181): the `add_case_step_rpc` tail turned out to be a real hole — a radiologist could name a foreign `case_id` and copy that patient's name, phone and date of birth onto his own board, after which `auth_radiologist_case_ok` flipped and opened the case itself. The fix sits at the TRIGGER (`guard_radiologist_scope` now guards the SOURCE, not only the receiving room), because a prod probe showed a plain PostgREST INSERT bypasses the RPCs entirely; both case-RPCs got the same check as a second line, and both id-leaking journals (`queue_delay_events_read`, `schedule_exceptions_read`) were narrowed. THREE review BLOCKERs were closed BEFORE the apply — one of them a stand (`falsify-0180`) nailed to the 0180 file that would have gone green SILENTLY after this reprint. Read `docs/audit/PR-0181-rf01-case-source-scope.md`. Earlier the same session: **38** closed RF-09 on all four channels (0179). **39** closed RF-05: the migration gate's two silent fail-opens are gone — the decision is a pure `gateEnvDecision`, on Vercel nothing bypasses, and `/api/staff/password` now leaves a journal event (`staff.access_changed` / `password_reset|password_set`). **40** closed RF-04 (0180): check №22 `grant_digest` pins the WHOLE set of client-role grants **by catalog** — 69 keys in four branches (48 tables/views, 6 sequences, 4 column-only grants, 11 anon-callable SECURITY DEFINER functions with owner and full body md5); roles come from `authenticator` members, not a hardcoded pair; `WITH GRANT OPTION` is part of the digest. ⚠️ And the measurement that justified the package before a line of code: `information_schema` reports 236 privilege rows for client roles, the catalog 284 — the 48-row difference is `MAINTAIN` (PG17), a privilege the standard view **cannot represent at all**, which the PostgreSQL 17 upgrade silently granted back onto the surface 0166 had cleared of TRUNCATE. Section 1 of 0180 removes it. Read `docs/audit/PR-0180-grant-digest.md` and `claude/radflow-handoff.md` (top block).

MAIN LESSONS OF SESSION 59: an audit is only as good as its instrument — `information_schema` hid a whole privilege class for months, and the fix was to read the catalog; a new guard repeats the previous guard's mistakes unless you read that guard first (three of package 40's MAJOR findings are 0167's lessons, re-made); a check that is held up only by the `checked` counter is not a check (№22 arrived with no static test and no mutation positions — both were added before the commit); a guard branch that reads a SIGNATURE is not a guard over DATA (0179); a count pin on a list is a landmine for the next reprint; a hostile double that can only READ cannot test a route that WRITES; and the cheapest protection against a bad paste into production is an md5 assert around the rebuilt body — it caught a one-character transcription slip in package 40 with no harm done.

Queue for session 61 — a menu, not an order. **(0) NEW, and it is an instrument, not a feature:** the deploy fingerprint has no working reading — see TASK #0. Until it is rewritten, «did the build arrive» is unanswerable, and that is a hole in the safety net, not a cosmetic one. (1) The residues of the eight: RF-01 is now CLOSED (package 41), leaving one named debt behind it — the `created_by = auth.uid()` disjunct in `queue_select` is scoped by neither clinic nor room; measured 08.09, its live surface is **0 rows** (the single radiologist has created none), so re-measure before deciding it is moot. `sched_referrer_read` leaks non-granted room ids/hours/closures through `rooms jsonb` (RF-03, ~3–4 h, product decision needed); `add_case_step_rpc` not traced to the end (RF-01); leaked-password protection still **off** — one switch in the Supabase Auth dashboard, the cheapest item in the whole audit, and it needs YOU (the built-in browser has no Supabase session). RF-04 and RF-05 are CLOSED. (2) The second half of U-59 (`overlayShown` → `hold`, ~10 lines). (3) Named debts: `waitlist_entries.claim_token` is a dead column since 0100; the PII key list in `lib/importantEvents.ts` lacks `password`/`pw` (mirror of the DB CHECK — needs a migration); `/api/staff/password` ignores the `profiles.update` error after the auth write; `fn_audit` swallows every error. (4) Live checks — the CEO card under an admin has never been looked at with eyes. Five forks still wait on you: Р1–Р5. Compose a plan and AGREE THE FIRST PACKAGE WITH ME BEFORE CODING. If a package is product-facing, show me the texts before they land.

⚠️ NEW STANDING COST, and it is in `AGENTS.md` now: any migration that creates a table, view, sequence or an anon-callable SECURITY DEFINER function must update check №22's 69-key list IN THE SAME MIGRATION — otherwise the nightly guard goes red with `new:`. That means a guard reprint and a full stand revision. If the object needs no client grants, `revoke all … from anon, authenticated` in the same migration and no key appears at all.

Order around the revision: gate → commit → falsify-all (29 stands; ~75 min on this machine; it refuses to start on a dirty tree; NO edits in the repo while ANY stand runs; run it through a one-shot `schtasks` task; poll with `ping -n 50 127.0.0.1 >nul` and give every poll a green baseline) → merge → push → deploy → measure the fingerprint. ⚠️ Run git ONLY from the Windows shell (Desktop Commander): the desktop bridge's Linux shell cannot delete files, so a `git status` there leaves an `index.lock` that blocks the next commit. The built-in browser holds an admin/registrar session in RadFlow and did every measurement in 59 — prefer it. Commit messages with Cyrillic only via `-F .commitmsg`; PowerShell `>` writes UTF-16; scripts in `C:\Users\Public`, not in the repo root (eslint runs with `--max-warnings 0`).
