
> Written at the end of session 58 (2026-09-06/07), after packages 35, 36 and 37.
> The owner pastes the text below as the first message of session 59 and
> attaches `NEXT_SESSION_PROMPT.md`.
>
> Every number here is an EXPECTATION, not a fact — the new session verifies it
> by query and treats any discrepancy as a FINDING to name out loud.
>
> `main` / `dev` tips at the last CODE commit: `4c5e21e` / `700acb6`; the docs
> commit(s) carrying this file land on top of them, so take the hashes from
> `git ls-remote`, never from here.
>
> ⚠️ The `/login` fingerprint is a ONE-DIRECTION signal. Last measured
> `vQtASefO7hWs9piyM8iv9` (07.09 09:44 UTC, twice, `cache: MISS`, `age: 0`);
> the docs push carrying this file will move it again.

---

Continuing development and production of the RadFlow SaaS. You are the orchestrator and Full Task developer. Communication in Russian, UI copy in Ukrainian, project docs in Ukrainian (`claude/radflow-handoff.md` is historically Russian — match each file). Work DIRECTLY in `D:\RadFlowDev` via Desktop Commander; prod DB — Supabase MCP, project ref `rdiqjxzibdqbhwileret`.

AUTHORITY IS EXPANDED, and I confirm it now: you edit code/tests/SQL/docs yourself, create append-only migrations and apply them to prod, run safe prod SQL, commit, push, merge PRs, deploy, read prod logs, do live browser checks. Applying SQL is yours, but with MANDATORY verification of the Results — "Success" is not proof. After every apply verify, by query: the ledger row and count, `invariants_check(false)`, the body md5 without CR AND its length, and the smoke pins. Session 58 applied 0178 through a DO block that refused to run unless the OLD body matched the file's md5 and the NEW body matched the target md5 — copy that shape, it is cheap and it makes a silent divergence impossible.

SAFEGUARDS ARE NOT CANCELLED IN ANY DEGREE: two independent review rounds with DIFFERENT lenses on anything touching RLS, grants, `SECURITY DEFINER`, triggers, authorization, service-role code, integration contracts, concurrency or production migrations — and you validate every subagent conclusion PERSONALLY, because in session 58 two reviews of the same package disagreed about RF-01 and the one that was right had read a trigger the other never opened. Falsification of guards: a NAMED red test and a GREEN baseline. Live prod check in BOTH directions by measurement; if there is nothing to check on, say so plainly instead of faking a check. Prod data only by an explicit list of ids with before-images; no mass deletes by a vague predicate. Secrets and PII go NOWHERE — not into a report, not into a log, not into a subagent prompt. `npm audit fix --force` is FORBIDDEN. Dev and prod are ONE DB. Never ask for passwords. Ask me when a decision is irreversible and touches live patients, when it is a product decision rather than an engineering one, or when you need access you do not have.

TASK #0 before any edit: `select now()`; `git ls-remote origin main dev` (⚠️ NOT `git fetch` — it does not update remote-tracking); `git status`; `maintenance_runs` over the last 30 hours; `select max(name), count(*) from public.migration_ledger`; `select public.invariants_check(false)`; `npm run db:gate:check`; and the `/login` deploy fingerprint. EXPECTED: prod DB at **0178_rf09_invite_token_grant.sql**, ledger **178/178**, unstamped **0**, invariants `ok/21/[]`, guard body md5-without-CR **`6ff5dd3db1681620ff6ccef18904e7f2`** at length **77313**, next migration **0179** (the number comes FROM THE LEDGER, never from the folder); tsc 0, eslint 0, vitest **2798/2798** (94 files); stand revision **27/27, 644 addressed** (~44 min for a full run). DO NOT TRUST THE DOCS — verify by query, and if something differs, that is a FINDING: name it, do not silently patch the doc.

WHAT SESSION 58 SHIPPED — three packages and one migration. **Package 35 (U-59 + U-60 + U-75):** ack no longer clears unread marks under a background tab (`lib/ackVisibility.ts` — a pure `ackGate` and a pure `nextFreeze`; in the background the refreeze KEY is carried forward while the ids are not, so returning to the tab confirms exactly what the person left); "zero" and "we don't know" stopped being the same pixel (`lib/sidebarBadge.ts`, unknown renders as a quiet grey `—`) — measured in THREE places, not the one the doc named; and U-75 was CLOSED WITH NO CODE because its stated defect has been false since session 52. **Package 36 (fork Р6):** RF-01…RF-08 of the August audit re-measured — both High findings are dead (RF-01 by policies with `auth_radiologist_room_ok()` AND the trigger `a00_radiologist_scope` that fires inside `SECURITY DEFINER`; RF-02 by an atomic claim-first), five are partial, and re-measuring turned up a NEW High that is not in the audit at all. **Package 37 (RF-09, migration 0178):** the table-level SELECT grant on `profiles` was revoked from `anon` and `authenticated` and a column allow-list of 14 of 15 put back — without `invite_token`; the screens now take the token from the issuing route's RESPONSE (`lib/inviteLink.ts`), never from the table. Read `claude/radflow-handoff.md` (top block), `docs/audit/PR-0178-rf09-invite-token.md` and `docs/audit/PR-RF01-RF08-remeasure.md`.

⚠️⚠️ **RF-09 IS NOT CLOSED — package 37 shut one channel of three, and that is written that way everywhere on purpose.** Two remain, both confirmed by query against prod: **`ceo_list_for_clinic`** is SECURITY DEFINER with EXECUTE granted to `authenticated` and returns `invite_token` for `role='ceo'` accounts, so a column grant does not constrain it at all (escalation: a CEO holding `ceo_access` on two clinics; the admin of clinic B reads their token and takes over an account that sees clinic A) — and this function is ABSENT from check №19 `guard_fn_bodies`, so its `auth_is_admin()` gate can be removed with no invariant going red; and **`audit_log`**, where the trigger `fn_audit` writes the whole `profiles` row — 74 rows, 4 of them with a non-NULL token, 2 with a real `clinic_id`, visible to that clinic's admin and to any CEO with an active grant. Neither is exploitable today (0 live tokens; the 4 historical values are dead because `set-password` also requires `password_set = false`). ⚠️ And the comment on the new guard branch (f) claims MORE than the branch checks — it only looks at the table's column ACLs. **Migration 0179 on both channels is the top of the queue, with its own two reviews.**

MAIN LESSONS OF SESSION 58, and most of them are mine rather than inherited. **Nine ways past my own pins in one session** — four in package 35, five in package 37; the stand caught two, review caught the rest, and every single one left the WHOLE gate green. The pattern is always the same: **a pin that checks PRESENCE rather than PLACE.** Concretely: the background flag folded in at the CALL SITE; a visibility store with ZERO stand positions; `set*Ok(true)` moved above the error branch; a value pin cut off mid-expression; deleting one of two `rememberToken` calls while both strings survive in the file; deleting `referrer_id` from a route's response when the same text also appears in an `insert`; zeroing the token map inside `reload()`, which fires on every tab focus; splitting `from("profiles")` from `.select()` through a variable; and removing a hint from the JSX while leaving its constant imported. **A pin that reddens on a BENIGN refactor will be removed, so remove it yourself and replace it** — one of mine went red when a column list was extracted into a named constant, which is lesson 0141 arriving in person; and a hard-coded `toBe(2)` blocked the very FIX for a third issuance path, so count the paths instead of the number. **A green baseline validates the INSTRUMENT, not the QUERY** — I reported RF-01 as live because a search for `radiologist_rooms` came back empty, with a green baseline that proved only that the tool was reading text. **I rewrote the deploy-fingerprint rule THREE times in one session**, and the Vercel dashboard refuted my third edition too: every commit rebuilds, docs commits included, and the real anomaly is that a live read at `age: 0` once returned a twenty-hour-old buildId — an instrument question, not a pipeline one. **A reprint of the guard breaks stands pinned to the previous edition, silently** — the revision came back 26/27 because 0178 made two anchors in `falsify-0166` non-unique, one of them matching as a SUBSTRING. And the one I am least proud of: **I edited the handoff while the revision was running**, `falsify-all.mjs` read the dirty tree as "a stand failed to restore the live files", stopped on the first stand and reverted my edit with `git checkout --` — an hour of run time and the text, gone. Knowing a trap is not the same as respecting it while impatient.

QUEUE FOR SESSION 59 — a menu, not an order. **(1) Migration 0179 — the two remaining RF-09 channels**, described above; it touches a SECURITY DEFINER function and an audit trigger, so it needs its own dry-run with rollback, two reviews with different lenses, stand positions, and a decision about the 4 historical `audit_log` rows (production data — by explicit id list with before-images, or left alone with a reason). While you are in there: `ceo_list_for_clinic` belongs in check №19, and its own comment claims a `password_set` guard the code does not have. **(2) The residues of the eight:** `sched_referrer_read` leaks non-granted room ids, hours and closures to a referrer through the `rooms jsonb` column (RF-03); there is still no regression check on grants beyond the one RF-09 branch, so a `GRANT` elsewhere reopens RF-04 silently; the deploy gate has TWO fail-opens and CI relies on one of them (RF-05 — the gate itself is now verified to RUN in prod, but the ledger is not a schema-contract manifest); **leaked-password protection is still off — one switch, the cheapest item in the whole audit** (RF-08); and `add_case_step_rpc` was never traced to the end (RF-01). **(3) The second half of U-59** — a modal covering the surface; its cost is measured at about ten lines, because `anyModalOpen` already exists and its completeness is already guarded, and by frequency it is MORE common than a background tab. **(4) Live checks in the browser** — nothing in packages 35–37 was ever seen with live eyes under an admin; the cheapest big one is a single run through `RescheduleModal` with a chosen slot. **(5) The named debts:** `incidentCount` is still a two-state falsy gate; the `services` surface has no `refreezeKey`; `Sidebar` reads its counter twice on mount; the invite link carries the token in a URL that Vercel and Supabase access logs both record, and nobody has looked at that; `waitlist_entries.claim_token` is readable by `anon` and `authenticated` — the same class as RF-09, outside the package perimeter. **Five forks still wait on your decision: Р1** (the case path for Г1-F), **Р2** (the rule for "where an audit trigger is required"), **Р3** (extend list №19 to the schedule trigger functions), **Р4** (Playwright/E2E — the project has no browser test at all, by design), **Р5** (`user_change_markers` → `REPLICA IDENTITY FULL`). **Р6 is DONE. RF-1 is struck off** — it never had a description anywhere. Compose a plan (`TaskCreate`) and AGREE THE FIRST PACKAGE WITH ME BEFORE CODING; if a package is product-facing, show me the texts before they land.

ORDER OF WORK AND THE TRAPS THAT COST TIME. Gate → commit → `falsify-all` → merge → push → deploy → measure the fingerprint. ⚠️ **A migration and its file must arrive together** — the deploy gate is symmetric: a file with no ledger row is red, a ledger row with no file is red too. So a migration that changes what the LIVE code reads goes out in TWO steps: first the code alone (no migration file in the commit), then the apply, then the file. Any single-commit order leaves a window where the live old code meets the new grant and gets 42501 on the WHOLE select. ⚠️ **Run the full revision ONLY on a clean tree and touch nothing in the repo while ANY stand runs** — a stand snapshots the live files and restores them in `finally`, so your edit silently disappears, and `falsify-all` will revert it for you. ⚠️ **Claude in Chrome went unresponsive mid-session in BOTH 57 and 58** (`CDP Runtime.evaluate timed out`); the built-in browser is authorized for RadFlow and did the whole job in 58 — prefer it, and if you use both, say which instrument took which end. A Vercel deploy takes **4–7+ minutes**, so measure no earlier than 8 minutes after the push and preferably twice with a gap. Commit messages with Cyrillic only via `-F .commitmsg`. PowerShell `>` writes UTF-16, so any report you need to read should be written by node itself in UTF-8. Nested quotes inside `cmd /c "…"` break `findstr` and `node -e` — put scripts in a file, and delete scratch files before the gate, because eslint runs with `--max-warnings 0`.
