# Session 59 — opening prompt (English, owner's message format)

> Written at the end of session 58 (2026-09-06). The owner pastes the text
> below as the first message of session 59 and attaches `NEXT_SESSION_PROMPT.md`.
> The numbers are EXPECTATIONS — the new session verifies them by query.
> `main`/`dev` tips: the last CODE commit is `8382eeb` / `5a9e3e8`; this file
> arrived with the docs commit(s) on top of it — take the hashes from
> `git ls-remote`. ⚠️ The `/login` fingerprint is a ONE-DIRECTION signal — see
> the four measurements in `NEXT_SESSION_PROMPT.md`; the last value is
> `2S_QeCfEd-xbLsmKC1VOX` and the docs push carrying this file will move it again.

---

Continuing development and production of the RadFlow SaaS. You are the orchestrator and Full Task developer. Communication in Russian, UI copy in Ukrainian, project docs in Ukrainian. Work DIRECTLY in `D:\RadFlowDev` via Desktop Commander; prod DB — Supabase MCP (`rdiqjxzibdqbhwileret`).

AUTHORITY IS EXPANDED, and I confirm it now: you edit code/tests/SQL/docs yourself, create append-only migrations and apply them to prod, run safe prod SQL, commit, push, merge PRs, deploy, read prod logs, do live checks. Applying SQL is yours, but with MANDATORY verification of the Results: "Success" is not proof — verify the body md5 without CR, the ledger row, `invariants_check(false)` and the smoke pins after every apply.

SAFEGUARDS ARE NOT CANCELLED IN ANY DEGREE: two independent review rounds with DIFFERENT lenses on everything sensitive, and you validate subagent conclusions personally. ⚠️ Session 58 is the strongest case yet that this holds even on plain UI work — two rounds found FOUR ways past my own fresh pins, each leaving the whole package green, and the main one meant U-59 was not closed at all for the three busiest surfaces. Falsification of guards — a NAMED red test and a GREEN baseline; live prod check in BOTH directions by measurement, and if there is nothing to check on, say so plainly instead of faking a check; prod data only by an explicit list of ids with before-images; secrets and PII go NOWHERE; `npm audit fix --force` is FORBIDDEN; dev and prod are ONE DB; never ask for passwords. Ask me when a decision is irreversible and touches live patients, when it is a product decision rather than an engineering one, or when access is needed.

TASK #0 before any edit: `select now()`, `git ls-remote origin main dev`, `git status`, `maintenance_runs` overnight, `select max(name) from public.migration_ledger`, `select public.invariants_check(false)`, `npm run db:gate:check`, and the deploy fingerprint of `/login`. Expected: prod DB at **0177**, ledger **177/177**, invariants ok/**21**/[], next migration **0178** (session 58 shipped no migration); vitest **2763/2763** (93 files), tsc 0, eslint 0; stand revision **26/26, 619 addressed** (a full run took 52 min). ⚠️ The fingerprint is a ONE-DIRECTION signal, and session 58 got it wrong TWICE before measuring enough: **changed → a build arrived; unchanged → draw no conclusion and do not declare an incident.** Four measurements, all at `cache: MISS` — two docs merges left it unchanged for 24 h, a code merge changed it in 4 min, and then another docs merge changed it in 5. Why the first pair produced no build is unexplained and needs the Vercel dashboard, which the container cannot see. DO NOT TRUST THE DOCS — verify by query; a discrepancy is a finding, name it.

Session 58 ran one package (35) and no migrations, and emptied the LOW batch: **U-59** (ack no longer clears unread marks while the tab is in the background — the decision is a pure `ackGate`, the freeze arithmetic a pure `nextFreeze`, and in the background the refreeze KEY is carried forward while the ids are not, so returning to the tab confirms exactly what the person left); **U-60** ("zero" and "we don't know" are no longer the same pixel — measured THREE places, not the one the doc named, and unknown renders as a quiet grey `—`); and **U-75 closed WITH NO CODE**, because its stated defect has been false since session 52 — the composition of `anyModalOpen` is already computed from source and guarded by seven addressed mutations. Stands 25 → 26, tests 2714 → 2763, addressed 588 → 619. Read `claude/radflow-handoff.md` (top block) and `docs/audit/PR-U59-U60-ack-visibility.md`.

MAIN LESSONS OF SESSION 58: a guard written in the same hour is exactly as much a suspect as someone else's old code — review found the background flag folded in at the CALL SITE (`surfaceVisible: visible && documentVisible`, every pin green), a visibility store with ZERO stand positions where three one-character edits silenced it, success flags pinned by PRESENCE rather than PLACE (moving `set*Ok(true)` above the error branch brings U-60 back in all three places at once), and a value pin cut off mid-expression; the first edition of the fix closed U-59 only where it almost never fires, because `hold` kept the freeze but not the key inside it — and the comment claimed the opposite; the shorter the anchor, the less surface to go stale (re-anchoring another stand took two attempts, the first spanning six lines and breaking on a comment inserted in the middle); a doc's list is a HYPOTHESIS for the ninth and tenth time; and — my own worst one — **I rewrote the deploy-fingerprint rule twice in one session**, first refuting session 57's version, then inventing my own from two data points, which the third broke forty minutes later. Exactly the failure this project keeps catching: a plausible claim, verified in part, written down as fact. And the same shape once more in package 36: I reported RF-01 as still live because a search for `radiologist_rooms` in the policy text came back empty — with a green baseline that proved the tool was reading the text but proved nothing about my search term, since the policy calls the helper `auth_radiologist_room_ok`. **A green baseline validates the instrument, not the query.**

Queue for session 59 — a menu, not an order. ⚠️ **Р6 is DONE** (package 36, `docs/audit/PR-RF01-RF08-remeasure.md`): both High findings of the August audit are dead, three are partial, and re-measuring RF-02 turned up a NEW High that is not in the audit at all. So the top of the queue is **RF-09: `profiles.invite_token` is readable by the whole clinic** — `authenticated` and `anon` both hold SELECT on the column, `authenticated` also UPDATE, and `profiles_select` is clinic-wide, so between "the admin issues an invite" and "the person sets their password" any colleague can read the token and POST it to `/api/account/set-password`, which asks for nothing else. Account takeover including the admin's, and the atomic claim and rate limit from RF-02 do not help because the request is legitimate. Not burning right now (0 live tokens of 9 profiles), and NOT a one-liner: a plain revoke breaks `StaffManager:95` and `ReferrersManager:252`, though `CeoManager` already shows the safe pattern (security-definer RPC `ceo_list_for_clinic`) and all four invite routes already return the token in their response. After that — the residues of the eight (`sched_referrer_read` leaks non-granted room ids and closures; no regression check on grants, so one GRANT silently reopens RF-04; the deploy gate has TWO fail-opens and CI relies on one; leaked-password protection still off — one switch); then **the second half of U-59** (a modal covering the surface — ⚠️ its cost was OVERSTATED by my own first comment and is now measured at about ten lines, because `anyModalOpen` already exists and its completeness is already guarded, and by frequency it is MORE common than a background tab); then the live checks (cheapest big one: one run through `RescheduleModal` with a chosen slot — possible without a password, a staff session is live in both reachable browsers, and I will ask before every step that writes); then the named debts (`incidentCount` still a two-state gate, the `services` surface with no `refreezeKey`, `Sidebar` reading its counter twice on mount, `room_id` in `queue_reschedule_rpc` and the same class in `updateWaitlistEntry`, the missing consolidated list of visibility columns, 12 of 26 stands not using `verdictOf`). **Five forks still wait on you: Р1** (case path for Г1-F), **Р2** ("where an audit trigger is required"), **Р3** (extend list №19), **Р4** (Playwright/E2E), **Р5** (`user_change_markers` → `REPLICA IDENTITY FULL`). **RF-1 is closed as a question** — it never had a description anywhere and is struck off. Compose a plan and AGREE THE FIRST PACKAGE WITH ME BEFORE CODING. If a package is product-facing, show me the texts before they land.

Order around the revision: gate → commit → falsify-all (26 stands, ~52 min; it refuses to start on a dirty tree; NO edits in the repo while ANY stand runs — the stand snapshots the files and restores them in `finally`, so your edit silently disappears; a long run is safer through a one-shot `schtasks` task than through `start /B`, because a bridge drop kills the process tree and leaves a live file mutated; poll with `ping -n 50 127.0.0.1 >nul` and give every poll a green baseline) → merge → push → deploy → measure the fingerprint. ⚠️ Claude in Chrome went unresponsive mid-session in BOTH 57 and 58 (`CDP Runtime.evaluate timed out`); the built-in browser IS authorized in RadFlow now and did the whole job in 58 — prefer it, and say which instrument took which end. Commit messages with Cyrillic only via `-F .commitmsg`; PowerShell `>` writes UTF-16, so reports you need to read are written by node itself in UTF-8; nested quotes inside `cmd /c "…"` break `findstr` and `node -e` — use node scripts in `%TEMP%`, not in the repo root, because eslint runs with `--max-warnings 0`.
