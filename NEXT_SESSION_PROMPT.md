# RadFlow — стартовий промпт наступної сесії (після с42)

Продовжуємо розробку і продакшн SaaS **RadFlow**. Ти в ролі оркестратора і
Full Task розробника. Спілкуйся **російською**, UI-копірайт — **український**.

## РЕЖИМ РОБОТИ: НАПРЯМУ В ПАПЦІ ПРОЄКТУ

**Працюй НАПРЯМУ в `D:\RadFlowDev`** — не через завантаження файлів, не
через клони, не «підготую текст, а ти встав». Читай, пиши і правь на місці.

**Задіюй ВСЕ, що підключено:** конектори (MCP), skills, plugins. На старті
подивись, що реально доступно в сесії, і не будуй обхідних шляхів там, де є
прямий інструмент.

- **Desktop Commander MCP** — файли й процеси: `list_directory`, `read_file`
  (з `offset`/`length`, від'ємний offset = хвіст), `write_file` (чанками по
  ~30 рядків, `mode` задавати ЯВНО), `edit_block` (ЗАВЖДИ після `read_file`),
  `start_process`, `start_search` (власний рушій, кирилицю знаходить).
- **Supabase MCP** — прод-БД, `execute_sql`, ref `rdiqjxzibdqbhwileret`.
- **Claude-in-Chrome MCP** — живі перевірки в браузері. ⚠️ Кліки по
  координатах ненадійні.
- Решта підключеного (Figma, Google Drive, n8n, Lovable) — за потребою.

⚠️ **DC іноді відвалюється**: виклик висить 4 хвилини й повертає помилку
мосту. Не гатити повторно — попросити власника перепідключити сервер.
⚠️ **MCP-інструменти фіксуються на СТАРТІ сесії.** Увімкнений посеред
розмови конектор у ній не з'явиться — потрібен новий чат.

⚠️ **PowerShell/cmd б'ється об кирилицю:**
- Пиши `.cmd`/`.mjs` у ЛАТИНСЬКИЙ шлях (`C:\Windows\Temp\x.mjs`), вивід у
  `.log`, потім `read_file`. Інлайн через лапки не проходить.
- `findstr` НЕ читає UTF-16 від PowerShell-редиректу. Читай лог `read_file`
  з `offset`, не через findstr.
- `<` зарезервований — команди ВЛАСНИКУ давай з КОНКРЕТНИМИ значеннями.
- Довгий текст коміту — файлом (`git commit -F C:\Windows\Temp\...`).

## Спершу прочитай (у цьому порядку)

1. **`AGENTS.md`** — стабільні правила проєкту.
2. **`claude/radflow-handoff.md`** — durable-стан, НАЙСВІЖІШЕ (кінець с42).
3. ⛔ **`docs/HANDOVER.md` — НЕ джерело правди** (футер бреше про 0086).
4. **`docs/ops-cron.md`** (реєстр 9 задач), **`docs/audit/AUDIT_2026-08-23_RESPONSE_PLAN.md`**
   (вердикти по аудиту 23.08), `claude/pacs-fhir-integration-plan.md`.

⚠️ **І не спирайся на ЦЕЙ файл теж.** У с38 і с39 він брехав про стан; у с40
виявився чесним по хешах — але брехав про ЧАС (вимагав перевірити «нічні
прогони 25.08», хоча сесія йшла ввечері 24-го). У с41 стан звірено о 20:52
UTC 24.08 — ніч ще не наступила, тому задача №0 переїхала далі. Перевіряй і
хеші, і передумови: `select now()` коштує нічого.

⚠️ **Робочий режим с41:** Desktop Commander у сесії НЕ був доступний —
файли читались/писались через `device_stage_files`/`device_commit_files`
(перед перезаписом — stage + diff з HEAD, після — stage + md5), а грепи й
тулчейн — у клоні `/tmp/rf` (репозиторій публічний, `git clone --branch dev`).
Якщо DC є — працюй ним; якщо ні — цей шлях робочий.

## Стан на старті (кінець с42, ~16:15 Kiev 25.08)

- **Прод-БД на `0158`**, ledger 158, усі md5, сторож `ok:true checked:11`.
  Наступна міграція — `0159`.
- **`main` = `70274c6`**, **`dev` = `25f744f`** — збігаються, дерево чисте,
  хвостів немає. Усе з с41–с42 влито.
- **Live-check 25.08: `LIVE_OK 41/41`**, зонди звірки busy — `ok`
  (C-2 закрито доказово). Активних інтеграційних ключів — 0 (тимчасовий
  відкликано).
- Тулчейн: tsc 0, eslint 0, **vitest 1130/1130 (46 файлів)**, build OK.
- Cron: 9 задач; сторож — 11 перевірок. Нічні прогони 25.08 пройшли.
- Клінік 2, `queue_entries` 89.

## ✅ ЗАДАЧА №0 — нічні прогони 25.08: ЗАКРИТО (перевірено 04:13 UTC 25.08)

`audit-retention` 03:40 `{"anonymized":0,"deleted":0}`, `invariants` 03:50
`ok:true checked:9` — обидва планувальником, `succeeded`. Нижче — запит на
майбутнє, як перевіряти:

```
select now();
select job, ran_at, result from public.maintenance_runs
 order by ran_at desc limit 10;
```

Очікуємо два НОВІ рядки за ніч 24→25: `audit-retention` ~03:40 UTC
(`{"anonymized":0,"deleted":0}` — норма до середини жовтня) і `invariants`
~03:50 UTC (`ok:true checked:9` — АБО `checked:10`, якщо власник уже накотив
0156). Порожньо після 04:00 UTC = задача не відпрацювала → дивитись
`cron.job_run_details` по jobid 12/13.

## ЗАДАЧА №1 — узгодити напрямок з власником

Аудит 23.08 відпрацьовано повністю (P0–P2 + частина P3); хвостів у git
немає. Беклог нижче — на вибір власника. Спершу `select now()`, `ls-remote`,
`git status`, `maintenance_runs` за ніч 25→26 (очікуємо `invariants`
`ok:true checked:11` о 03:50 UTC).

## Беклог за пріоритетом (узгодити з власником на старті)

Повний план з вердиктами по 15 пунктах аудиту 23.08 —
`docs/audit/AUDIT_2026-08-23_RESPONSE_PLAN.md`. Що лишилось:

1. **Prune dead-рядків outbox** — `prune-outbox` чистить лише доставлені;
   кандидат `delete where dead and created_at < now() - 90 days` у
   `cron_jobs.sql` (окремий cron або міграція + реєстр `docs/ops-cron.md`).
2. **Симетричний прогін ack зі сторони Б** — потрібен другий живий користувач.
3. **Харнеси на базі `race-check.mjs`**: гонка `in_progress`, паралельний CAS.
4. P3: `lateCallClash` через абсолютний час і сусідню добу; ✅ `engines.node`
   `>=22 <25` (с42; Vercel 24.x, локально 24.15, контейнер 22 — усі в
   діапазоні); leaked-password protection — **власник відклав до продакшену**
   (с42), не пропонувати; ✅ мертві `navKey`
   (с42); зонд `f` смоуку 0155; unused_index не чіпати; дропи мертвих обʼєктів —
   лише після грепу.

## Правила, які не можна порушувати

- **Міграції накатує ВЛАСНИК** у SQL Editor, **«Run without RLS»**.
- Номер — із `select max(name) from migration_ledger`, НІКОЛИ не з папки.
- Ти готуєш файл + смоук + секцію відкату + dry-run + два ревʼю.
- **Секція `=== ВІДКАТ ===` — у КІНЦІ файлу міграції** (канон з 0147).
  У `ROLLBACK.md` нових секцій НЕ заводимо, він історичний до 0146.
- **Dry-run:** тіло в `do $$…$$` БЕЗ внутрішнього `commit`, або перевіряти
  факт відкату окремим запитом. Третій варіант, найкращий: прогнати саму
  МЕХАНІКУ на живих даних read-only, нічого не змінюючи (с39, с40).
- **Смоук: асерти лише `is distinct from`.** Фікстури мусять проходити
  форматні констрейнти.
- **Смоук звіряє ДЕЛЬТУ, а не абсолютний стан середовища** (базовий знімок
  на початку): між накатом і `db:gate` `ledger_md5` шумить законно.
- **Передрук прод-функції — звіряти по md5** нормалізованого коду. Працює і
  ДО накату: застосувати ту саму заміну до `pg_get_functiondef` через
  `replace()` і порівняти з тілом із файлу (с40). Скрипт —
  `C:\Windows\Temp\rf_fnbody.mjs`.
- ⚠️ **`select … into` всередині гілки `if` не виконається, якщо умова
  хибна**, і змінна лишить значення від попередньої перевірки. Скидати
  ЯВНО. Лічильники інкрементувати ЗЗОВНІ гілки (с40).
- **Перевірки коду функцій — з ВИРІЗАНИМИ коментарями** (інакше зонд ловить
  власний коментар — с39).
- **Новий інваріант — у `invariants_check()`**, а не лише в смоук своєї
  міграції. Інакше він замовкне назавжди після накату.
- **Правки у файл міграції — ДО `npm run db:gate`.** Гейт штампує md5 по
  вмісту файлу; допишеш коментар після — сторож заволає на розбіжність.
- **Фікстура в `queue_entries` б'ється об 15 BEFORE-тригерів.** Перевірений
  набір: кабінет `40f89c03` (Medicom, MRI, «Закревського, 9»), день +1,
  dur 20 + buf 5, `studies` з РЕАЛЬНОЇ активної позиції каталогу.
- **security_invoker=true на VIEW = ЗАМОК**, не оптимізація.
- `audit_log.action` має CHECK: лише `insert/update/delete`.
- Зміна return-сигнатури RPC: `drop function` (42P13), потім `supabase/
  types.ts` РУКАМИ (генерації типів немає).
- **`to_regclass` окремим statement-ом** (42P01/42703 до short-circuit).
- Deploy-гейт валить build, якщо міграція на диску без запису в ledger.
  **Порядок фічі з міграцією+кодом: накат → деплой (мерж) → увімкнення.**
- dev і prod — **одна БД**. PII в details заборонено. Мерж/пуш робить
  власник; ти готуєш текст коміту й PR.
- **ГЕЙТ ГОНЯЄ ХТОСЬ ОДИН.** `next build` не ізолює `.next`. Якщо власник
  делегує прогін тобі — скажи це вголос, щоб він не запустив паралельно.

## Інструменти (готові)

```
node scripts/integration-admin.mjs list --clinic <uuid>
node scripts/integration-live-check.mjs --base https://rad-flow-tau.vercel.app --read-only
node scripts/race-check.mjs plan                     # нічого не пише
node scripts/race-check.mjs run --run --n 4          # ПИШЕ в прод, прибирає
npm run db:gate                                      # штампує md5 у ledger
npm run build                                        # deploy-гейт + next build
select public.invariants_check();                    # сторож, одразу
```

⚠️ live-check **не з контейнера** (домен не в allowlist).

## Порядок ведення роботи

1. Прочитай `claude/radflow-handoff.md` — найсвіжіший.
2. Звір стан: `git ls-remote origin refs/heads/main refs/heads/dev` (⚠️
   `git fetch origin main dev` НЕ оновлює remote-tracking!), `git status`,
   БД через `execute_sql`. **Не вір цьому файлу.**
3. Узгодь із власником напрямок, склади задачник.
4. Роби напряму в `D:\RadFlowDev` — один пакет за раз, перевірка між ними.
5. Прогін гейта — і текст коміту й PR власнику.
6. Наприкінці сесії онови `claude/radflow-handoff.md` і цей файл.
