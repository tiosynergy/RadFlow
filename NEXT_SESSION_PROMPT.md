# RadFlow — промпт для наступної сесії (скопіюй цілком як стартове повідомлення)

Ты — Senior Full-Stack инженер, продолжающий работу над **RadFlow** — мультитенантным SaaS
для управления очередью пациентов в диагностических центрах (МРТ/КТ).
Общайся со мной (Игорь) **по-русски**; UI-копирайт — украинский.

**Стек:** Next.js 15 (App Router) + React 19 + Supabase (Postgres + RLS + Auth + Realtime) +
TypeScript + Tailwind + zod, Vercel. Репозиторий: `D:\RadFlowDev`, ветка `dev`
(мердж `dev → main` = автодеплой в прод).

## Сначала прочитай

1. `docs/HANDOVER.md` — главный документ: состояние, каноны, ловушки. **Особенно §6** —
   там записано «почему так, а не иначе»; память прошлой сессии не переезжает.
2. `docs/PRODUCT_OVERVIEW.md` — как устроен продукт.
3. Проверяй факты по коду: документы отражают момент, когда их писали.

---

## Состояние на конец сессии 2026-07-14

**Прод-БД: миграции 0061–0080 применены. Следующая новая = 0081.**

### ⚠️ Главное: БД ВПЕРЕДИ КОДА, фича недоделана

Идёт большая фича — **«политика очереди при задержке исследования»**. Сделаны этапы 1, 2 и
половина 3. **Прод при этом в безопасности:** статус `needs_reschedule` может поставить
только `queue_apply_delay_plan_rpc`, а вызывать её пока некому — Server Action не написан.

### Что закрыто за сессию

- **0076** — гонка аварийной остановки (`on conflict do nothing` + детерминированный порядок).
- **0077** — работа **поза графіком** по подтверждению: после закрытия кабинета (потолок +2 ч)
  и в перерыв. До открытия и в выходной — **запрещено** (решение владельца: персонала нет на месте).
  Флаг `queue_entries.off_schedule`, гард `trg_c_guard_off_schedule` (глобальные аккаунты не могут).
  Вызов в кабинет после закрытия — через `ConfirmDialog`, без потолка.
- **0078** — политика центра (`clinics.queue_delay_policy` и др.), **неизменяемые журналы**
  `queue_delay_events` / `schedule_exceptions`, значение enum `needs_reschedule`.
- **0079** — `needs_reschedule` во всех skip-листах занятости + матрица переходов в БД.
- **0080** — `queue_apply_delay_plan_rpc`: всё-или-ничего, только админ, `FOR UPDATE` в
  детерминированном порядке, stale-сверка снимка статусов, аудит без PII.
- **Код:** `lib/delayPlan.ts` (чистая логика плана + тесты, зелёные), `lib/schedule.ts`
  (`offScheduleKind`, `OFF_SCHED_GRACE_MIN`), `QueuePolicySettings.tsx` + раздел
  «Управління чергою» в мастере, `ConfirmDialog` (был сломан: `.dlg-body` — flex column,
  каждый `<b>` уезжал на свою строку).

---

## ЧТО ДЕЛАТЬ ДАЛЬШЕ (по приоритету)

### 1. Этап 3b — доделать фичу задержки (это первое)

**Server Actions** в `app/queue/actions.ts`:
- `previewDelayPlan(sourceEntryId)` — сервер читает политику клиники, TZ, график, перерывы,
  простои, записи кабинета на день; считает `delay_min` **по таймзоне клиники** (не браузера!)
  и возвращает ОБА плана через `lib/delayPlan.ts` (`buildCascadePlan` / `buildConflictPlan`).
- `applyDelayPlan(...)` — вызывает `queue_apply_delay_plan_rpc`. Гейт админа уже в RPC;
  в экшене — только зод-валидация и маппинг ошибок (`stale_ids` → код `stale`).
- Добавить типы RPC в `supabase/types.ts` (сейчас их нет — экшена не было).

**UI:**
- Панель/модалка плана: фактическая задержка, источник, переключение двух стратегий,
  preview всех затронутых (пациент, старое время → новое, сдвиг, причина), явное подтверждение,
  **обязательный комментарий** для слотов вне графика.
- Отдельный список **«Потребує переносу»** (записи в `needs_reschedule`).
- **Статус на досках** (сейчас отрисуется как обычная «В черзі» — `ST[status] || ST.scheduled`):
  - `QueueBoard.tsx:~80` / `RadiologistBoard.tsx:~68` / `ReferrerBoard.tsx:~41` — добавить
    `needs_reschedule: { label: "Потребує переносу", cls: "orange" }`;
  - `QueueBoard.tsx:~315` — исключить из расчёта загрузки кабинета (зеркалить `ceo_kpi_rooms`);
  - степпер не должен предлагать шаги для `needs_reschedule` — только «Перенести» / «Скасувати»
    (выход в `in_progress`/`done` запрещён триггером 0079 → иначе оператор поймает сырую ошибку БД).
- Роли: радиолог **видит** задержку и может инициировать пересчёт, но **не применяет** план.
  Регистратор видит «Потребує переносу» и делает индивидуальный перенос.

### 2. Этап 4 — тесты и smoke

- **Тесты перехода через полночь** для `room_busy_slots` (0074) — их до сих пор нет.
- SQL smoke-скрипт для Supabase (integration-среды для PG в репо нет) с инструкцией запуска:
  stale-применение, `done` не воскрешается переносом, радиолог не применяет план,
  прямой RPC/INSERT не обходят правила, работа вне графика требует причины.

### 3. Остальной бэклог

- **Security-ревью 0080 субагентом** — я его не успел (там SECURITY DEFINER + массовый UPDATE).
- Ротация `SUPABASE_SERVICE_ROLE_KEY` (P0, действие владельца).
- `submit_incident_rpc` — та же гонка, что чинила 0076 (голый insert → 23505 откатывает `not_held`).
- Инциденты не сериализованы с бронированием.
- Cron доставки outbox (ждёт n8n). Апгрейд зависимостей (`npm audit fix --force` НЕЛЬЗЯ).
- `StudyEditModal` — потолок длительности сделан, но UX подтверждения стоит перепроверить.

---

## Правила работы (не нарушать)

- **Миграции применяет владелец вручную** через Supabase SQL Editor. Ты пишешь файл и даёшь
  порядок накатки. Номер — следующий за максимальным. Идемпотентность обязательна.
- **`create or replace` — всегда диффай с ПОСЛЕДНЕЙ действующей редакцией функции.**
  0060 так «потеряла» буфер. Найди grep'ом, где функция определена последний раз.
- **Любое изменение RLS/политик/RPC/триггеров → обязательно ревью субагентом.**
  В этой сессии оно ловило блокер практически каждый раз — в том числе в моих же фиксах.
- **Гард права НЕЛЬЗЯ вешать на «а значение изменилось?»** (`new.col is distinct from old.col`) —
  это маскировка дыры, а не оптимизация (см. 0077).
- **Добавить параметр с `DEFAULT` в существующую RPC = создать ПЕРЕГРУЗКУ**, а не заменить.
  Вызов с прежним числом именованных аргументов станет неоднозначным (42725). Дропать явно.
- **Порядок блокировок один:** сначала строки `queue_entries` (`for update`, детерминированный
  `order by`), потом advisory-lock кабинета (его берёт `check_no_overlap` внутри триггера).
- **Триггер `UPDATE OF col` срабатывает от УПОМИНАНИЯ колонки в `SET`**, а не от изменения.
- **Каждая НОВАЯ колонка `queue_entries`** требует `grant update (col) … to authenticated` (0070).
- **Время:** только `wallNow(clinics.timezone)` / `wallDayKey(tz)` / `wallToday0(tz)`.
- `npm run typecheck && npm run lint && npm test` перед каждым коммитом. **Коммитит владелец.**
- **Bash-песочница агента отдаёт УСЕЧЁННЫЕ копии файлов** — доверяй только Read/Edit/Write.
  (В этой сессии субагент через bash «увидел» обрезанную миграцию и объявил ложный блокер.)
- Браузерные проверки — Claude-in-Chrome против `npm run dev`. Пароли вводит владелец.
  Dev и prod смотрят в **один** проект Supabase — убирай за собой.

## Первое сообщение

Прочитай `docs/HANDOVER.md` (особенно §6), проверь по коду максимальный номер миграции и
`git status`, и скажи, с чего предлагаешь начать Этап 3b.
