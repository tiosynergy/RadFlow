# RadFlow — стартовий промпт наступної сесії (після с45)

Продовжуємо розробку і продакшн SaaS **RadFlow**. Ти в ролі оркестратора і
Full Task розробника. Спілкуйся **російською**, UI-копірайт — **український**.

## ПОВНОВАЖЕННЯ: РОЗШИРЕНІ (рішення власника, діє з с46)

Власник надав розширені повноваження **постійно**, а не разово на аудит. Ти
маєш право сам: правити код, тести, SQL і доки; створювати append-only
міграції і **накатувати їх на прод**; виконувати безпечний прод-SQL; створювати
і прибирати синтетичні прод-дані; комітити, пушити, **створювати і зливати PR**,
**деплоїти**; читати прод-логи; робити живі перевірки в браузері.

**Це скасовує старе правило «мерж/пуш/накат робить власник»** — воно застаріле,
де б воно ще не траплялось у доках (у цьому файлі нижче і в `AGENTS.md` такі
рядки вже виправлені; якщо знайдеш ще — виправ).

### Повноваження НЕ скасовують інженерну обережність

Автономія в с45 була безпечною не сама по собі, а через ці страховки. Вони
обовʼязкові й далі:

- **Два незалежні раунди ревʼю** на будь-яку зміну, що торкається RLS, грантів,
  `SECURITY DEFINER`, тригерів, авторизації, service-role коду, контрактів
  інтеграцій, конкурентності або прод-міграцій. **Валідуй висновки субагентів
  сам** — у с45 з трьох перевірених особисто одна знахідка підтвердилась, одна
  знижена до Low, одна виявилась наполовину хибною.
- **Міграції:** номер із леджера (НЕ з папки), файл + смоук + секція
  `=== ВІДКАТ ===` у кінці + dry-run (`do $$` без внутрішнього commit, факт
  відкату — ОКРЕМИМ запитом). Ніколи не редагувати вже накатану міграцію.
  Порядок: накат → смоук → `invariants_check()` → `db:gate` → build → деплой.
- **Після деплою — жива перевірка на проді**, а не «тести зелені». У с45 саме
  вона довела фікс: до вставки синтетичного інциденту `free: 08:00–18:00`,
  після — `free: []`.
- **Прод-дані:** правки — за явним списком id зі свіжого знімка, з
  before-образами і репетицією відкату. **Ніяких масових видалень за нечітким
  предикатом.** Синтетику прибирати одразу і перевіряти залишок запитом.
- **Секрети і PII не виводити НІКУДИ** — ні в звіт, ні в лог, ні в промпт
  субагента. Токен пробного ключа в с45 генерувався і використовувався
  скриптом на машині власника; у сесію не потрапляв.
- `npm audit fix --force` — ЗАБОРОНЕНО. Ротація секретів — лише за
  підтвердженої потреби і з планом без простою.

### Коли все одно СПИТАТИ власника

Не «про всяк випадок», а лише коли:

- рішення **незворотне** і зачіпає живих пацієнтів або справжню клінічну
  історію (видалення/злиття записів, зміна вже наданих даних);
- це **продуктове рішення**, а не інженерне (як має поводитись фіча, що
  показувати користувачу, чи ламати контракт партнера свідомо);
- потрібні **облікові дані або доступ**, яких у тебе немає. Паролі не просити
  ніколи.

В усьому іншому — вирішуй і роби. Не проси власника копіювати код, правити
файли, запускати рутинні команди чи накатувати рутинні міграції.

## РЕЖИМ РОБОТИ: НАПРЯМУ В ПАПЦІ ПРОЄКТУ (вимога власника)

**Працюй НАПРЯМУ в `D:\RadFlowDev`** — не через завантаження файлів, не
через клони, не «підготую текст, а ти встав». Читай, пиши і правь на місці
через Desktop Commander. Клон у контейнері — лише для тулчейна, якщо прямий
прогін на машині власника недоступний; результат все одно кладеться в
`D:\RadFlowDev`.

**Задіюй ВСЕ, що підключено: конектори (MCP), skills, plugins.** ПЕРШИМ
ДІЛОМ подивись, що реально доступно в сесії (список tools + skills), і не
будуй обхідних шляхів там, де є прямий інструмент. Перевірені в бою
(с42–с44):

- **Desktop Commander MCP** — файли/процеси на машині власника:
  `read_file` (offset/length, від'ємний offset = хвіст), `edit_block`
  (ЗАВЖДИ після read), `write_file` (чанками ≤30 рядків, mode ЯВНО),
  `start_search` (свій рушій, кирилицю знаходить), `start_process`.
- **Supabase MCP** — прод-БД, `execute_sql`, ref `rdiqjxzibdqbhwileret`.
  Dry-run міграцій на живих даних — канон (SMOKE_OK у тексті помилки =
  успіх; кожен виклик — ОКРЕМА сесія).
- **Незалежні субагенти ×2** — два раунди ревʼю кожного пакета (канон).
  Субагенти читають диф прямо з `D:\RadFlowDev` через Desktop Commander —
  давай їм у промпті список DC-тулів для ToolSearch і РІЗНІ лінзи
  (у с44: «коректність/регресії» і «якість тестів і контракту»).
- **Claude Projects** (`project_read`/`project_write`) — хендофф і
  ключові доки дублюються туди.
- Також доступні: Claude-in-Chrome (живі перевірки UI; кліки по
  координатах ненадійні — тільки find/read_page + ref), Figma, Google
  Drive, n8n, Lovable; скіли engineering/* (code-review, debug,
  architecture, deploy-checklist…), product-management/*, design/*,
  docx/xlsx/pptx/pdf.

## Пастки середовища (перевірені, с43–с44)

⚠️ **DC іноді відвалюється просто ПОСЕРЕД правки.** У с44 `edit_block`
повернув «Device did not respond» — і правка НЕ лягла, хоча дві попередні
лягли. Не гатити повторно: попросити власника перепідключити, а після
цього ПЕРЕЧИТАТИ файл і звірити, що саме доїхало.
⚠️ **У DC-моста жорстка стеля ~60 с на виклик**, `timeout_ms` її не
підіймає. Довгі очікування дробити на кроки ≤50 с
(`cmd /c "ping -n 50 127.0.0.1 >nul & echo waited"`).
⚠️ **`cmd /c` — ЗАВЖДИ з лапками навколо всієї команди.** Без них
`cmd /c cd /d D:\RadFlowDev && git status` губить `cd` і дає «not a git
repository».
⚠️ **MCP-інструменти фіксуються на СТАРТІ сесії** — увімкнений посеред
розмови конектор не з'явиться, потрібен новий чат. Після переобриву моста
інструменти треба ПЕРЕзавантажити через ToolSearch.
⚠️ **PowerShell/cmd б'ється об кирилицю:** скрипти — файлом у ЛАТИНСЬКИЙ
шлях (`C:\Windows\Temp\x.cmd`), вивід у `.log`, читати `read_file` (НЕ
findstr — він не бачить ні UTF-16, ні ANSI-escape у виводі vitest).
`<` зарезервований — команди власнику з КОНКРЕТНИМИ значеннями. Довгий
коміт — файлом (`git commit -F`). `start_process` понад 60 с рветься:
довгі прогони пускай `start "" /b script.cmd` з маркером `MARKER_DONE`
у лог і читай лог потім.
⚠️ **Вивід `start_search` підміняє рядок-збіг самим патерном** — для
читання коду завжди `read_file`, а search лише щоб знайти номери рядків.
⚠️ **Протокол фальсифікації сам може бути декоративним (с46, U-11).** Скрипт
вносив дефект, гнав vitest і писав «ЧЕРВОНИЙ — назви не розпізнані»: парсер не
знімав ANSI, а `>` між іменами сьютів лежить УСЕРЕДИНІ escape-послідовності.
Формально всі 23 дефекти покраснели — фактично протокол **не відрізняв
потрібного сторожа від випадкового**, тобто не робив своєї роботи. Знімай ANSI
ПЕРШИМ (`s.replace(/\x1b\[[0-9;]*[A-Za-z]/g, "")`) і **звіряй, що почервонів
САМЕ той тест**, який мав. Червоний сигнал без імені — не результат.
⚠️ **`node -e` під PowerShell розсипається на дужках і лапках** — скрипт
завжди ФАЙЛОМ (`node scripts/x.mjs`). Так само `curl -w "%{http_code}"`:
`%` з'їдається і curl питає пароль. Пиши у файл і читай файл.

## Спершу прочитай (у цьому порядку)

1. **`AGENTS.md`** — стабільні правила проєкту.
2. **`claude/radflow-handoff.md`** — durable-стан, НАЙСВІЖІШЕ (кінець с45).
3. ⛔ **`docs/HANDOVER.md` — НЕ джерело правди** (футер бреше про 0086).
4. **`docs/GOOGLE_CALENDAR_BACKUP.md`** — runbook GCal Backup, включно з
   розділом «Статус застосунку в Google і верифікація».
5. **`docs/audit/RADFLOW_DEEP_TECHNICAL_FUNCTIONAL_AUDIT_2026-08-27.md`** —
   журнал глибокого аудиту с45: що закрито, що перевірено особисто і як саме,
   що ще НЕ перевірено. Вердикт не винесений — його виносити наступному.
6. **`docs/ops-cron.md`** (реєстр 10 задач),
   `docs/audit/AUDIT_2026-08-23_RESPONSE_PLAN.md` (⚠️ H-2, M-3, M-6 у ньому
   значаться відкритими, але в с45 перевірено: вони ЗАКРИТІ в коді).

⚠️ **І не спирайся на ЦЕЙ файл теж.** Правило підтверджено 5 сесій поспіль:
перевіряй і хеші (`git ls-remote`), і ПЕРЕДУМОВИ задач (`select now()` —
у с40 і с43 промпт вимагав перевірити ніч, яка ще не настала; у с44 промпт
брехав про хеш `dev` на один коміт).

## Стан на старті (кінець с46, ~15:35 UTC 28.08)

- **Прод-БД на `0163`**, ledger 163, усі md5, сторож `ok:true checked:13`.
  Наступна міграція — **0164**. У с45 і с46 міграцій НЕ БУЛО.
- **`main` = `843248f`**, **`dev` = `8956769`**, дерево чисте.
  **ПЕРЕВІР `ls-remote`, а не вір рядку.** (У с46 цей файл брехав про хеші на
  два коміти — саме тому правило й існує.)
- Тулчейн після с46: tsc 0, eslint **0 — жодного попередження**,
  **vitest 1523/1523 (60 файлів)**, build OK, `db:gate` 163/163.
  Було на кінець с45 — 1333/1333 (54).
- **У с46 ПʼЯТЬ пакетів:** U-5 + U-6 (екран не стверджує на недовірених даних),
  U-8 (пастка фокуса вкладених вікон), U-3 + U-4 + U-7 (проковтнута помилка
  читання), U-1 + U-2 (рядки одного зрізу під заголовком іншого) і **U-11**
  (простої приходять фідом, а не масивом). Міграцій не було.
- ✅ **Жива перевірка ЧОТИРЬОХ пакетів на проді ВИКОНАНА** (власник увійшов у
  Chrome «Pesrsonel», клініка Medicom). ⚠️ **U-11 — статус живої перевірки див.
  наприкінці цього розділу.** Метод і результати — у розділах
  «Сесія 46» у `claude/radflow-handoff.md`.
  ⚠️ **Прод-дані під час останньої перевірки мінялись — але НЕ агентом:**
  `audit_log` за 13:11 UTC показує пʼять `update` по `queue_entries` з actor
  (людина працювала в застосунку). Дії агента були read-only. Висновок: на
  ДІЮЧОМУ проді звіряй не «нічого не змінилось», а `audit_log` — ХТО змінив.
  ⚠️ `safetyErr` піднімається не миттєво: після підміни `fetch` треба 2–3 рази
  смикнути `visibilitychange`/`focus` і почекати ~5–10 с.
  ⚠️ **Жива перевірка третього пакета знайшла те, чого не знайшли ні тести, ні
  два раунди ревʼю по коду**: гейт «не показувати похідні нулі» стояв лише на
  завантаженості, а «Записи 0» і «Дохід 0 ₴» поруч лишились. Тому порядок
  «деплой → жива перевірка» не формальність.
- **Контракт партнерів змінився в с45** (пакет I-1): `busy` тепер містить
  простої кабінету. Перед будь-якою роботою в цій області перевір «завислі»
  відкриті простої — вони віддадуть `free: []` на весь діапазон:
  `select clinic_id, room_id, started_at from public.incidents
   where status in ('active','planned') and blocked_until is null;`
  (на кінець с45 — 0 рядків).
- **GCal Backup живий**: OAuth-застосунок **In production (без
  верифікації)**, обидві клініки тикають кожні 2 хв, токени безстрокові
  (Medicom → «TestTioTadFlow», titenkosmokeCLINIC → особистий
  titenkosmoke@gmail.com — рішення власника с43).
- Cron: 10 задач; сторож — 13 перевірок. Клінік 2, `queue_entries` 89.
- npm audit: 7 (лише мажори, беклог №12, `--force` ЗАБОРОНЕНО).

## ЗАДАЧА №0 — звірка ночі і живості

`select now()`, `ls-remote`, `git status`, `maintenance_runs` за останню
ніч: `outbox-retention` ~03:30 UTC, `audit-retention` ~03:40, `invariants`
~03:50 — **`ok:true checked:13`**. Якщо у failed є `gcal_sync_overdue` —
дзеркало стояло >30 хв: `select last_sync_at, last_error_code from
google_calendar_connections where enabled;` (правда — у last_sync_at,
НЕ в `cron.job_run_details`: pg_net fire-and-forget). Живість зараз:
last_sync_at свіжіший за ~4 хв.
⚠️ У `maintenance_runs` колонки — `job`, `ran_at`, `result` (НЕ job_name /
started_at / status). У `google_calendar_connections` немає `last_error_at`.

## Беклог за пріоритетом (узгодити з власником на старті)

**0. ДОВЕСТИ ГЛИБОКИЙ АУДИТ (с45 не завершив).** Журнал —
`docs/audit/RADFLOW_DEEP_TECHNICAL_FUNCTIONAL_AUDIT_2026-08-27.md`, вердикт
GO/CONDITIONAL GO/NO-GO свідомо НЕ винесений. Порядок:

  a. ✅ **U-5 + U-6 — ЗАКРИТО в с46** (merge `7e36dd9`), включно з живою
     перевіркою на проді. Правило винесене в `lib/availabilityTrust.ts` і
     `safety_unknown` у `computeCallBlock`. Ревʼю пакета додало третій екран
     (`RescheduleModal`) і четвертий канал (U-11, лишається відкритим).
  b. **Фази 2, 4, 6, 8** — жодна не починалась: тулчейн на чистому оточенні +
     залежності/секрети; конкурентність/realtime/час (`scripts/race-check.mjs`
     так і не гонявся); функціональний E2E за ролями; продуктивність і
     відновлення (restore drill).
  c. ✅ **Верифікацію решти знахідок ЗРОБЛЕНО в с46** — гіпотез більше немає, у
     кожної є вердикт (див. журнал аудиту). Підсумок: з 11 перевірених **2
     хибні** (I-4, U-9), **3 наполовину хибні** (I-5, I-6, U-2), 6 підтвердились.
     ✅ **U-8 ЗАКРИТО в с46** — і виявилось, що «правка в один аргумент» була
     оцінкою навмання: правило `active` забули в ТРЬОХ компонентах, тож пакет
     додав механізм (`lib/modalTrapStack.ts`), єдину конвенцію `nestedOpen` і
     сторож, який сам знаходить наступного порушника.
     ✅ **U-3 + U-4 + U-7 ЗАКРИТО в с46** (merge `6a85f35`, доправка `6c3f0d9`),
     разом із U-7(b): у `reload()` дашборда зʼявився guard покоління. Сторож —
     **сканер** читань `rooms`/`schedule_overrides` у шести екранах. PR:
     `docs/audit/PR-U3-U4-U7-read-error-trust.md`.
     ✅ **U-1 + U-2 ЗАКРИТО в с46** (merge `a288df4`) двома правками різної
     природи: структурною (підпис дати береться з САМОГО запису, тож рядок не
     може стверджувати чужу дату) і свіжістю (спінер на кожен зріз + guard
     покоління з виходом протухлого замикання + ключ зрізу). Ревʼю знайшло в
     тому ж файлі ще два проковтнутих `error` класу U-3/U-4 — обидва закриті.
     ✅ **U-11 ЗАКРИТО в с46** (merge `843248f`). Проп `incidents` став ФІДОМ
     `{rows, failed}` і ОБОВʼЯЗКОВИМ — повноту правки у 18 місцях перелічив
     `tsc`, а не память автора. Особиста перевірка знайшла на три споживачі
     більше, ніж було у формулюванні: `BreakdownModal`, `computeRoomLoad` і
     `ReferralPortal`, який пропа не передавав **узагалі** (для направника
     кабінет у простої виглядав робочим ЗАВЖДИ). Рішення «невідомо →
     заблоковано» винесене в `lib/incidents.ts` (`slotBlockedByFeed`), бо
     component-тестів у проєкті немає. Два раунди ревʼю дали **пʼять
     регресій самої правки** — усі полагоджені в тій же гілці; 33 фальсифікації,
     жодної мовчазної. PR: `docs/audit/PR-U11-incident-feed.md`.

     **Брати першою — U-16 (High):** канал `schedule_overrides` — той самий
     клас, що U-11, і містить ЄДИНИЙ знайдений fail-open, наслідок якого
     ЗАПИСУЄТЬСЯ в БД, а не показується: `BreakdownModal` рахував «До кінця дня»
     з непрочитаних override'ів і клав чужий кінець у `incidents.blocked_until`.
     Точково вже закрито (проп `overridesFailed`), але канал відкритий:
     `RoomDayOverviewModal` отримує `overrides` голою мапою, і день, закритий
     ЛИШЕ через `schedule_overrides`, малюється повною сіткою вільних слотів
     (кнопка «▦ Зайнятість кабінету» під `safetyErr` не гасне). Лікується тим
     самим механізмом: фід замість мапи, проп обовʼязковий.
     **U-15 (нова, Medium):** `StudyEditModal` не отримує простої ЖОДНОГО з
     трьох батьків, а міняє ТРИВАЛІСТЬ — може розтягнути дослідження у вікно
     простою. Перед правкою зʼясувати: `check_not_during_incident` перевіряє
     весь інтервал `[start, end)` чи лише початок (від цього Medium чи High).
     **U-17 / U-18 (нові, Low):** проковтнуті `error` в `app/queue/actions.ts`
     (`:1059` — губляться події аудиту `incident.emergency_stop`; `:511` —
     `studiesRoomMismatch`, підстраховано тригером 0088).
     **U-12 (нова, High для направника):** `ReferralPortal` рендерить
     `<StudyEditModal>` БЕЗ пропа `offSchedule` → запис, що легально стоїть поза
     графіком, для направника незбережуваний назавжди (той самий провал, від
     якого писалась 0077); плюс `doEditStudies` кличе `editQueueEntryStudies`
     чотирма аргументами, губить пʼятий (`offSchedule`).
     **U-13 (нова):** порожній рядок `maybeSingle()` = «не знаємо» — закрито в
     `StudyEditModal` і `RescheduleModal`, лишається в `BookingModal` і
     `ReferralPortal` (треба окремо звірити RLS-видимість кабінетів, 0139).
     **U-14 (Low, уточнена в с46):** сканер `readErrorTrust` не покриває
     `.from("incidents")`. Ревʼю пакета U-11 звірило **всі 9** продуктових
     читань цієї таблиці — помилку дивляться скрізь, тож зараз дірки немає;
     лишається відсутність сторожа на майбутнє (наступне читання ніхто не
     спіймає). Додати `incidents` у `TABLE_RE` сканера.
     **I-5 звʼязана із задачею «свій домен»**: канонічні FHIR `system`/`fullUrl`
     нестабільні при зміні домену, партнерський RIS втратить привʼязку послуг.
  d. **Другий пакет БД:** REVOKE TRUNCATE на решті ~18 таблиць; поверхня
     DELETE у `incidents` (грант + політика `incidents_desk_write ALL`, хоча
     застосунок ніколи не видаляє); перевірка дрейфу привілеїв в
     `invariants_check()`.

1. **Власний домен RadFlow** — блокер верифікації Google (Search Console
   не приймає *.vercel.app) і правильний крок перед продом. Ланцюг:
   домен → Vercel → `GOOGLE_OAUTH_REDIRECT_URI` → Search Console →
   consent screen (+Privacy Policy/Terms на цьому ж домені) →
   верифікація → зникає екран «не перевірено» і стеля 100 акаунтів.
2. **Симетричний прогін ack зі сторони Б** — протокол готовий:
   `docs/qa-unread-ack-symmetry.md` (8 кроків, два акаунти, два профілі
   браузера). Лишилось виконати.
3. **Хвости с44 у `PatientEditModal`** — гейт автовибору лікаря після
   «＋ Додати» і складання списку опцій (нормалізація, дедуп, початковий
   ключ, fail-closed `refUnresolved`) досі в JSX і не покриті. Це КОРІНЬ
   інциденту с31; кандидати в `lib/referrerField.ts`.
4. **Частковий відказ `save()` у майстрі** — `throw` між інсертами губить
   уже накопичені `assigned`: кабінети в БД є, у формі без `roomId`, на
   наступному «Зберегти» — блокер с33 і осиротілий кабінет. Лікується
   видачею id у `catch` до тоста про помилку.
5. Негативні сценарії GCal §12 (revoke токена, зняття ACL — fail-closed +
   журнал) на ТЕСТОВОМУ акаунті — за бажанням власника.
6. P3 хвости: `saveAndExit` повертає `true` навіть при `stillDirty`
   (сьогодні прикрито оверлеєм, тобто тримається на CSS); компонентні
   тести (jsdom) — рішення «лишаємо статичні сторожі назавжди» ухвалити
   явно; `activeSaved` у `app/setup/page.tsx` — мертве поле; `keepIds` у
   `save()` тільки пишеться; ✅ спільний хелпер зачистки коментарів для
   тестів — **зроблено в с46** (`tests/helpers/codeOf.ts`, межі поведінки
   запиновані в `tests/codeOf.test.ts`); leaked-password protection —
   **власник відклав до продакшену, не пропонувати**.
7. Планові мажори (беклог №12): vitest ≥3.2.6 / next 16 — тільки окремою
   задачею, повний прогін.

## Правила, які не можна порушувати

- **Міграції накатуєш ТИ** (Supabase MCP `execute_sql`) — розширені
  повноваження, див. шапку. Власник у SQL Editor більше не потрібен.
- Номер — із `select max(name) from migration_ledger`, НІКОЛИ не з папки.
- Файл + смоук + секція `=== ВІДКАТ ===` у КІНЦІ файлу + dry-run + два
  незалежні ревʼю. Порядок: накат → смоук → invariants → db:gate →
  build → деплой → **жива перевірка на проді**.
- **Dry-run:** тіло в `do $$…$$` БЕЗ внутрішнього commit; факт відкату
  перевіряти ОКРЕМИМ запитом. `ledger_md5` шумить між накатом і db:gate —
  це законно.
- **Смоук: асерти лише `is distinct from`**; «RLS мовчки зʼїв» ловиться
  ТІЛЬКИ через `get diagnostics row_count`. Фікстури мусять проходити
  констрейнти. Смоук звіряє ДЕЛЬТУ, не абсолютний стан.
- Передрук прод-функції — звіряти по md5 нормалізованого коду
  (`C:\Windows\Temp\rf_fnbody.mjs`).
- `select … into` в гілці if НЕ виконується при хибній умові — скидати
  ЯВНО; лічильники ЗЗОВНІ гілки.
- Новий інваріант — у `invariants_check()`, не лише в смоук.
- Правки у файл міграції — ДО `npm run db:gate`.
- Фікстура в `queue_entries` бʼється об 15 BEFORE-тригерів — перевірений
  набір у хендоффі; для `doctors` тригерів НЕМАЄ, фікстури вільні.
- security_invoker=true на VIEW = ЗАМОК. dev і prod — ОДНА БД. PII в
  details заборонено. **Мерж/пуш/деплой робиш ти** (розширені повноваження);
  ⚠️ памʼятай, що dev і prod — одна БД, тож міграція діє на прод одразу після
  накату, ДО деплою коду.
- **ГЕЙТ ГОНЯЄ ХТОСЬ ОДИН** (`.next` спільний).
- **Вкладені модалки**: батьківський `useModalA11y` глушити параметром
  `active`, стейти дітей — ДО виклику хука (с43).

## Правила тестів (уточнено в с45)

- Компонентних тестів НЕМАЄ: `vitest.config.ts` — `environment: "node"`.
  Правило, ціна помилки якого висока, виноситься з JSX у `lib/` — інакше його
  не покрити взагалі.
- **АЛЕ РОУТИ ТЕПЕР ТЕСТУЮТЬСЯ ПОВЕДІНКОВО (с45).**
  `tests/fixtures/fakeSupabase.ts` — двійник PostgREST у памʼяті; роут
  викликається по-справжньому і перевіряється HTTP-відповідь
  (`tests/slotsRoute.test.ts`, `tests/fhirSlotRoute.test.ts`). Мережі й
  Postgres там немає. **Двійник мусить лишатись злим:** він КИДАЄ на будь-якому
  фільтрі, якого не реалізує, і звіряє імена колонок із фікстурою. Мʼякий
  двійник гірший за відсутність — доданий у роут `.gte(...)` був би no-op у
  тесті й справжнім фільтром у проді.
- **Regex по вихідному коду роута — НЕ сторож.** У с45 обидва раунди ревʼю
  зняли саме такі перевірки: `wallInstant(` на місці, а «+ доба» у верхній
  межі загублена — тест зелений, дірка відкрита. Роут перевіряється викликом
  роута; статичний regex лишається хіба як детектор ВИДАЛЕННЯ рядка.
- **Фікстура з самих цифр робить перевірку регістру порожньою.** uuid у
  фікстурах — з БУКВАМИ, інакше `toUpperCase()` нічого не змінює і тест
  проходить за будь-якої реалізації (реальний випадок с45).
- **Фальсифікація обовʼязкова:** внеси дефект НАЗАД і назви, який саме тест
  почервонів. У с45 це показало, що одна з двох «захисних» правок насправді
  вторинна, і сторожить її зовсім інший тест, ніж здавалося.
- **Тавтологічний тест виглядає як сторож.** Перед тим як лишити тест,
  мисленно ВІДКОТИ фікс і назви, що почервоніє. Якщо нічого — тест
  декоративний (у с44 обидва раунди ревʼю знайшли саме це).
- **Сторож, що шукає ІМЯ, ловить рядок імпорту (с46).** `toContain("slotDataTrusted")`
  лишався зеленим при `const availTrusted = true` — імпорт на місці. Сторожити
  треба ВИКЛИК (`fn(state)`), привʼязку до живих прапорців і ЗАБОРОНЯТИ
  розмивання: `availTrusted = … || …`, `safetyErr = a && b` замість `a || b`,
  `flag: x && false`. Ослабити гейт завжди дешевше, ніж прибрати.
- **Тест порядку перевірок з ВИМКНЕНИМИ конкурентами порядку не перевіряє (с46).**
  `{ safetyUnknown: true, roomBlocked: false }` — конкурент інертний, перенос
  гейта нижче лишався зеленим. Той самий клас, що «фікстура з самих цифр».
- ⚠️ **У блочному коментарі `/*…*/` не можна писати glob із двома зірочками і
  слешами** — послідовність закриває коментар достроково. Так упав перший прогін
  тулчейна в с46.
- **Статичний сторож на один рядок перевіряє наявність виклику, а не його
  зміст.** Обходиться підстановкою констант в аргументи, присвоєнням
  `null` праворуч і виносом тіла з блоку. Правильно: вирізати БЛОК і
  перевіряти всередині, рахувати входження по файлу, сторожити ІМПОРТ і
  сторожити ПОРЯДОК ланок ланцюга через `indexOf`.
- Статичний сторож читає КОД, не коментарі: `codeOf()` у тестах с44.
- Кожна чиста функція, що годує React-стейт, має тест «не мутує вхід» і
  тест на ідентичність посилань в обидва боки.

## Інструменти (готові)

```
node scripts/integration-admin.mjs list --clinic <uuid>
node scripts/integration-live-check.mjs --base https://rad-flow-tau.vercel.app --read-only
node scripts/race-check.mjs plan / run --run --n 4
npm run db:gate        # штампує md5 (лікує ledger_md5 у сторожа)
npm run build          # deploy-гейт + next build
select public.invariants_check();
```

⚠️ live-check **не з контейнера** (домен не в allowlist).

## Порядок ведення роботи

1. Прочитай `claude/radflow-handoff.md` — найсвіжіший.
2. Звір стан: `git ls-remote origin refs/heads/main refs/heads/dev`
   (⚠️ `git fetch origin main dev` НЕ оновлює remote-tracking!),
   `git status`, БД через `execute_sql`, `select now()`. **Не вір цьому
   файлу.**
3. Узгодь із власником напрямок, склади задачник (TaskCreate).
4. Роби напряму в `D:\RadFlowDev` — один пакет за раз, тулчейн і два
   ревʼю між ними.
5. **Комітиш, мержиш і деплоїш сам** (розширені повноваження). Після
   деплою — жива перевірка на проді, а не «тести зелені». Текст PR все одно
   пиши: він лишається в `docs/audit/` або в описі PR як слід рішення.
6. Наприкінці сесії онови `claude/radflow-handoff.md` (+копію в Claude
   Projects) і цей файл.
