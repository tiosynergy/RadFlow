# RadFlow — стартовий промпт сесії 50 (після с49)

Продовжуємо розробку і продакшн SaaS **RadFlow**. Ти в ролі оркестратора і
Full Task розробника. Спілкування — **російською**, UI-копірайт — **український**.
Робота — **НАПРЯМУ** в `D:\RadFlowDev` через Desktop Commander.

> ## ПОВНОВАЖЕННЯ В ЦІЙ СЕСІЇ — РОЗШИРЕНІ, ТІ САМІ, ЩО В с45–с49
>
> Власник підтвердив їх при передачі роботи в с50 окремо і явно. Це не опис
> минулого — це твої повноваження ЗАРАЗ. Повний текст із страховками — у
> розділі «ПОВНОВАЖЕННЯ» нижче; він чинний без змін.
>
> Коротко: сам правиш код/тести/SQL/доки, створюєш і **накатуєш міграції на
> прод**, виконуєш безпечний прод-SQL, створюєш і прибираєш синтетичні
> прод-дані, комітиш, пушиш, **зливаєш PR**, **деплоїш**, читаєш прод-логи,
> робиш живі перевірки в браузері. Старе правило «мерж/пуш/накат робить
> власник» скасоване — виправляй його всюди, де ще трапиться (у с49 так
> виправлені шапка і §8 `docs/HANDOVER.md`).
>
> **Страховки не скасовані жодною мірою:** два незалежні раунди ревʼю на все
> чутливе (RLS, гранти, `SECURITY DEFINER`, тригери, авторизація, service-role,
> контракти інтеграцій, конкурентність, прод-міграції) з особистою валідацією
> висновків субагентів; фальсифікація сторожів з іменем червоного тесту і
> ЗЕЛЕНОЮ базовою лінією; жива перевірка на проді в ОБИДВА боки вимірюванням;
> прод-дані — лише за явним списком id; секрети і PII — нікуди;
> `npm audit fix --force` заборонений.

## ЗАДАЧА №0 (перед будь-якою правкою)

`select now()`, `git ls-remote origin main dev`, `git status`, `maintenance_runs`
за ніч, `select max(name) from public.migration_ledger`. **Цьому файлу не
вірити — звіряти запитом.** Точний хеш HEAD у файлі назвати неможливо в
принципі: коміт, який його записує, сам його й міняє. Тому нижче зафіксована
**межа коду**, а не HEAD.

**Стан на кінець с49** (звірено запитом, не з памʼяті):
`main` = `dev` = межа коду **`61b07bc`** (U-57). Прод-БД на **`0168`**,
ledger 168, непроштампованих 0, наступна міграція — **0169**. Сторож:
`ok:true`, **`checked:15`**, `failed:[]`. Тулчейн: tsc 0, eslint 0,
vitest **2023/2023**, `db:gate` 168/168, build OK.

## ЩО БУЛО В с49 — сім пакетів, три з них у самому кінці

U-37 · U-33 · U-13 · **поверхня привілеїв (0166 + 0167)** · U-17/U-18/U-14 ·
**U-55** · **U-56 (міграція 0168)** · **U-57**.

Три останні закривали чергу боргів, яку власник назвав явно («закривай по черзі
U-55, U-56, U-57»), і кожен дав урок, що коштує дорожче за сам пакет:

- **U-55** (`fec6954`) — «не прочитали» ≠ «немає доступу». Пʼять місць у
  `app/queue/actions.ts` вели `null` від `maybeSingle()` в одну гілку відмови;
  найдорожче — `setQueuePriority`, де збій читання профілю казав **адміну, що він
  не адмін**. Ревʼю спіймало **мою власну регресію** (гейт ролі стояв ПЕРЕД
  обчисленням власника). Стенд знайшов, що **`statementEnd` не пінив жоден
  тест**: зсув вікна робить сканер лише поблажливішим, а поблажливість завжди
  зелена.
- **U-56** (`3026a95`, міграція **0168**) — `emergency_stop_rpc` віддає
  `stopped_incidents`, і журнал більше не залежить від другого читання.
  **ПАСТКА 0122 заміряна до написання міграції:** `drop`+`create` знімає ACL, і
  дефолт схеми `public` повертає EXECUTE **і `anon`, і PUBLIC**. Два раунди
  ревʼю дали **два BLOCKER — обидва в моєму коді**: гейт журналу стояв на
  нерозібраному `stopped_rooms` (той самий fail-open, переставлений на сусіднє
  поле), і головний пін не бачив багаторядкового `.from("incidents")`, який
  лежить у ТОМУ Ж файлі.
- **U-57** (`61b07bc`) — довіра сканера `readErrorTrust` переведена з тексту
  чужого файла на ПОВЕДІНКУ, і перелік `RULES` тепер звіряється з переліком
  доведених правил. **Половина стенда навмисно перевіряє ЗЕЛЕНЕ**: правка без
  дефекту не сміє червоніти — саме це доводить, що борг закрито, а не
  переставлено.

## ПЕРШИЙ ПАКЕТ с50 — узгодити з власником

Черга боргів U-55/U-56/U-57 **вичерпана**. Кандидати за пріоритетом — у
`claude/radflow-handoff.md`, розділ «Очередь работ». Найвагоміше:

1. **Довести глибокий аудит** — фази 2, 4, 6, 8 не починались жодного дня
   (тулчейн на чистому оточенні + секрети; конкурентність/realtime/час —
   `scripts/race-check.mjs` так і не гонявся; функціональний E2E за ролями;
   продуктивність і restore drill). **Вердикт GO/CONDITIONAL GO/NO-GO не
   винесений** — і це головне, що лишається відкритим уже пʼять сесій.
2. **U-54** — default-ACL грантора `postgres` у схемі `storage` роздає TRUNCATE
   ролям `anon`/`authenticated` (заміряно). Окремий пакет БД. ⚠️ Розширювати
   сторожа на інші схеми **не можна навпростець**: `graphql`/`graphql_public`
   мають ті самі записи від `supabase_admin`, і перевірка стала б вічно
   червоною (= знятою, урок 0141).
3. **Низькопріоритетний борг** U-26…U-29, U-31/U-32, U-34…U-36, U-48…U-51 —
   список і причини в хендоффі.
4. **U-53** — чи має кабінет із простоями в історії видалятись. Питання
   **продуктове**, не інженерне: спитати власника, а не вирішувати самому.

---

> ⚠️ **Урок с49 №1: снисходительность всегда зелёная.** Мутація, що робить
> сторожа МʼЯКШИМ, не червонить жоден тест — якщо саму межу не запінено
> ВИКЛИКОМ. Так у U-55 виявилось, що `statementEnd` (межі вікна сканера) не
> пінив ніхто: зсунеш вікно — сканер стане поблажливішим, і все лишиться
> зеленим. Правило: після кожного сторожа питати не лише «що почервоніє, якщо
> зламати», а й «що почервоніє, якщо ПОСЛАБИТИ».
>
> ⚠️ **Урок с49 №2: fail-open уміє переїжджати на сусіднє поле.** У U-56 я
> розібрав `stopped_incidents` суворо — і лишив гейт усього блоку на
> `res?.stopped_rooms ?? []`, полі, яке НЕ розбирається. Зникни воно — журнал
> вимкнувся б мовчки, а користувачеві й далі писали б «зупинено 3». Питати: на
> чому стоїть ВХІД у полагоджену гілку.
>
> ⚠️ **Урок с49 №3: пін по тексту помиляється в ОБИДВА боки.** Він сліпий до
> іншої форми з тим самим змістом і червоний на правці БЕЗ дефекту. Тому в
> стенді має бути половина мутацій, які мусять лишитись ЗЕЛЕНИМИ
> (`res.error != null`, перенос рядка, перейменування параметра). Саме вони
> доводять, що борг закрито.
>
> ⚠️ **Урок с49 №4: `drop`+`create` РОЗШИРЮЄ права.** Дефолтний ACL схеми
> `public` роздає EXECUTE `anon` і PUBLIC; явний `revoke` зі старої міграції
> зникає разом зі старою функцією. Після кожного `drop`+`create`: `revoke` +
> **явний** `grant` + **ассерт у тій самій транзакції**. Смоук — другий рубіж,
> бо його запускають руками.
>
> ⚠️ **Урок с49 №5: стенд фальсифікації — артефакт свого коміта.** Після мержу
> він не підтримується: якорі описують код, якого вже немає, перезапуск нічого
> не доводить, а переписування знищило б запис. Інвентар застарілого
> дописується в шапку стенда — і сам інвентар буває неповним (ревʼю знайшло три
> пропущені мутації в `falsify-u17-u18.mjs`).
>
> ⚠️ **Урок с49 №6 (про ревʼю).** У U-56 **обидва BLOCKER-и були в моєму
> власному коді**, у U-55 — регресія, яку я вніс тим самим пакетом. Плануй
> бюджет пакета як «написати фікс + знайти пʼять внесених ним дефектів +
> полагодити їх». Пакет без знайдених регресій — привід не радіти, а перевірити,
> чи ревʼю взагалі дивилось.

## ПОСТІЙНА ЧАСТИНА ПРОМПТА

(Повноваження, страховки, режим роботи, пастки середовища. Переноситься з
сесії в сесію без змін, поки власник не вирішить інакше.)

## ПОВНОВАЖЕННЯ: РОЗШИРЕНІ — ДІЮТЬ І В ЦІЙ СЕСІЇ

(Рішення власника від 27.08, підтверджене при передачі роботи в с47, с48, с49
і **с50**. Це НЕ опис того, що було раніше, — це твої повноваження ЗАРАЗ.)

Власник надав розширені повноваження **постійно**, а не разово на аудит. Ти
маєш право сам: правити код, тести, SQL і доки; створювати append-only
міграції і **накатувати їх на прод**; виконувати безпечний прод-SQL; створювати
і прибирати синтетичні прод-дані; комітити, пушити, **створювати і зливати PR**,
**деплоїти**; читати прод-логи; робити живі перевірки в браузері.

**Це скасовує старе правило «мерж/пуш/накат робить власник»** — воно застаріле,
де б воно ще не траплялось у доках. У `AGENTS.md` і `docs/HANDOVER.md` такі
рядки вже виправлені; якщо знайдеш ще — виправ.

### Повноваження НЕ скасовують інженерну обережність

- **Два незалежні раунди ревʼю** на будь-яку зміну, що торкається RLS, грантів,
  `SECURITY DEFINER`, тригерів, авторизації, service-role коду, контрактів
  інтеграцій, конкурентності або прод-міграцій. **Валідуй висновки субагентів
  сам.** Давай раундам РІЗНІ лінзи (у с49 для U-56: «міграція і привілеї» проти
  «чи справжні сторожі») — і не змішуй: ревʼюер, який дивиться все одразу,
  дивиться поверхнево.
  ⚠️ **Ревʼю знаходить не стільки старі дефекти, скільки ті, які щойно вніс ТИ
  САМ.** У U-56 — обидва BLOCKER; у U-55 — регресія в авторизації; у с47 в U-11
  таких було 5 із 5.
  ⚠️ **Окремий клас: «закрив те, що закривати не треба».** Питай не лише «що я
  зламав», а й «що з доданого зайве».
- **Міграції:** номер із леджера (НЕ з папки), файл + смоук + секція
  `=== ВІДКАТ ===` у кінці + dry-run + **сторож попередника** (`do $ledger$`).
  Ніколи не редагувати вже накатану міграцію.
  Порядок: накат → смоук → `invariants_check()` → `db:gate` → build → деплой →
  жива перевірка.
  ⚠️ **Після `drop`+`create`** — `revoke execute … from anon, public` + **явний**
  `grant … to <ролі>` + **ассерт ACL у тій самій транзакції** (`do $$ …
  has_function_privilege … $$`). Причина заміряна в с49 (пастка 0122).
  ⚠️ **У секції ВІДКАТ** `delete from migration_ledger` тримати закоментованим
  ДРУГИМ рівнем: гейт збірки валить «файл є, запису немає», тож оператор,
  який зніме один рівень `--`, заблокує всі деплої посеред інциденту.
  ⚠️ **Передрук сторожа міняє md5 його тіла** — пін у `gcal_pg_cron_smoke.sql`
  (крок g) перезнімати ЖИВИМ запитом.
- **Після деплою — жива перевірка на проді**, а не «тести зелені». Перевіряй
  ОБИДВА напрямки. Якщо пакет не міняв КЛІЄНТСЬКОГО коду — сказати це прямо, а
  не зображати перевірку бандла: у серверних дій ознаки «новий бандл доїхав»
  немає в принципі.
  ⚠️ **Спершу зʼясуй, ЩО перезапускає потрібний завантажувач.**
  ⚠️ **`supabase-js` захоплює `fetch` при створенні клієнта** — перехоплювач,
  поставлений пізніше, не спрацює (с49, три невдалі спроби заміру).
- **Сторожа теж треба фальсифікувати.** Вносиш дефект назад і звіряєш, що
  почервонів **ІМЕННО ТОЙ** тест. «Червоний» без імені — не результат. І
  навпаки: **зелена мутація — не привід зняти мутацію, а привід переписати
  сторожа.**
  ⚠️ **БАЗОВА ЛІНІЯ обовʼязкова:** перед мутаціями стенд один раз ганяє
  немутований набір і вимагає ЗЕЛЕНОГО. Зламаний заздалегідь сторож дав би
  бездоганний звіт, нічого не довівши.
  ⚠️ **Якір мутації перевіряти на УНІКАЛЬНІСТЬ** — у с49 стенд двічі чесно
  відхилив неунікальний якір (той самий рядок стояв ще й у секції ВІДКАТ).
  ⚠️ **Половина мутацій має лишитись ЗЕЛЕНОЮ** — «правка без дефекту не сміє
  червоніти». Без цієї половини не видно, що сторож не надто чутливий.
  ⚠️ **Клас, що ловиться найгірше: текст цілий, гілка мертва.** `{true` замість
  умови; ранній `return`; `const flag = false;`; `stateOf={() => "free"}`.
  Пінь те, що робить гілку ДОСЯЖНОЮ, і ПРИВʼЯЗКУ.
  ⚠️ **Скрипт фальсифікації править БОЙОВІ файли** — `try/finally` + обробники
  `SIGINT`/`SIGTERM`/`uncaughtException` обовʼязкові.
- **Коментар у коді — не джерело істини про сервер.** Читай функцію з живої БД
  (`pg_get_functiondef`) — і **цілком**.
- **Прод-дані:** правки — за явним списком id зі свіжого знімка, з
  before-образами і репетицією відкату. **Ніяких масових видалень за нечітким
  предикатом.** Синтетику прибирати одразу і перевіряти залишок **ОКРЕМИМ**
  запитом.
- **Секрети і PII не виводити НІКУДИ** — ні в звіт, ні в лог, ні в промпт
  субагента.
- `npm audit fix --force` — ЗАБОРОНЕНО.

### Коли все одно СПИТАТИ власника

Не «про всяк випадок», а лише коли:

- рішення **незворотне** і зачіпає живих пацієнтів або справжню клінічну
  історію;
- це **продуктове рішення**, а не інженерне (як має поводитись фіча, що
  показувати користувачу, чи ламати контракт партнера свідомо) — наприклад
  U-53;
- потрібні **облікові дані або доступ**, яких у тебе немає. Паролі не просити
  ніколи.

В усьому іншому — вирішуй і роби.

## РЕЖИМ РОБОТИ: НАПРЯМУ В ПАПЦІ ПРОЄКТУ (вимога власника)

**Працюй НАПРЯМУ в `D:\RadFlowDev`** — не через завантаження файлів, не
через клони, не «підготую текст, а ти встав». Читай, пиши і правь на місці
через Desktop Commander.

**Задіюй ВСЕ, що підключено: конектори (MCP), skills, plugins.** ПЕРШИМ
ДІЛОМ подивись, що реально доступно в сесії. Перевірені в бою:

- **Desktop Commander MCP** — файли/процеси на машині власника:
  `read_file` (offset/length, відʼємний offset = хвіст), `edit_block`
  (ЗАВЖДИ після read), `write_file` (чанками ≤30 рядків, mode ЯВНО),
  `start_search` (свій рушій, кирилицю знаходить), `start_process`.
- **Supabase MCP** — прод-БД, `execute_sql`, ref `rdiqjxzibdqbhwileret`.
  Dry-run міграцій на живих даних — канон (SMOKE_OK у тексті помилки =
  успіх; кожен виклик — ОКРЕМА сесія).
- **Незалежні субагенти ×2** — два раунди ревʼю кожного пакета (канон).
  Субагенти читають диф прямо з `D:\RadFlowDev` через Desktop Commander —
  давай їм у промпті список DC-тулів для ToolSearch і РІЗНІ лінзи.
- **Claude Projects** (`project_read`/`project_write`) — хендофф і
  ключові доки дублюються туди.
- Також доступні: Claude-in-Chrome (живі перевірки UI), Figma, Google
  Drive, n8n, Lovable; скіли engineering/*, product-management/*, design/*,
  docx/xlsx/pptx/pdf.

## Пастки середовища (перевірені, с43–с49)

⚠️ **Повний гейт на цій машині йде 15–25 хвилин** (2023 тести). Точковий
прогін двох-трьох спеків — ~30 с; стенд фальсифікації з 28 мутацій — ~20 хв.
Це нормальна швидкість, а не зависання: плануй очікування маркером, а не
повторними запусками.
⚠️ **Гейт одним фоновим прогоном:** `start /b cmd /c scripts\<файл>.bat`, далі
опитувати маркер `*.done` і читати червоні імена через `node scripts/chk.js`
(повний гейт, `.full.json`) або **`node scripts/vtred.mjs`** (точковий прогін,
`.vt.json`). ⚠️ `chk.js` на точковому прогоні покаже СТАРИЙ звіт.
Скільки тестів дав КОНКРЕТНИЙ файл — `node scripts/chkfile.js tests/<файл>`.
У `.gitignore` заборонено КЛАС кореневих маркерів (`/*.done`, `/*.log`,
`/falsify-*.md`).
⚠️ **`head` і `tail` у цьому шелі ВІДСУТНІ**, і конвеєр із ними роняє весь
ланцюжок — команда виглядає виконаною, але не виконалась. Ніяких unix-фільтрів.
⚠️ **`git commit -m` з кирилицею і дужками під PowerShell розсипається:**
`(0166+0167)` приїхало як `333` (PowerShell порахував арифметику). Довге
повідомлення — **тільки файлом** (`-F`), після мержу звіряти `git log --oneline`.
⚠️ **Блочний коментар не терпить `*/` усередині** — cron-вираз «зірочка слеш 5»
і glob із двох зірочок його містять; **будь-яка пара «зірочка + слеш» усередині
шляху** (`app/*` + `/page.tsx`) закриває коментар, і файл розсипається помилкою
парсера НИЖЧЕ за місце правки. Писати словами.
⚠️ **`{/* … */}` не можна ставити одразу після `{cond && (`**.
⚠️ **Значення в React-інпут** писати через нативний сеттер плюс події
`input`/`blur`.
⚠️ **Вбудований браузер (Browser pane) НЕ авторизований у RadFlow.** Жива
перевірка йде через Claude-in-Chrome; deep-link
`/queue?date=YYYY-MM-DD&entry=<uuid>` одразу розгортає потрібний рядок.
`location.reload()` усередині `javascript_tool` роняє виклик.
⚠️ **`started_at` простоїв на проді НЕ вирівняний на хвилину** — арифметику
хвилин округляти.
⚠️ **DC іноді відвалюється просто ПОСЕРЕД правки.** Не гатити повторно:
попросити власника перепідключити, а після цього ПЕРЕЧИТАТИ файл і звірити,
що саме доїхало.
⚠️ **Міст може повернутись НЕ ПОВНІСТЮ:** повертається `Claude_Browser`, а
Desktop Commander — ні. Перевіряй наявність КОНКРЕТНОГО проксійованого сервера.
⚠️ **У DC-моста жорстка стеля ~60 с на виклик**, `timeout_ms` її не підіймає.
Довгі очікування дробити (`timeout /t 290 /nobreak >nul` у `.bat`, або
`ping -n N 127.0.0.1` в неінтерактивному шелі).
⚠️ **`cmd /c` — ЗАВЖДИ з лапками навколо всієї команди**, інакше губиться `cd`.
⚠️ **MCP-інструменти фіксуються на СТАРТІ сесії.**
⚠️ **PowerShell/cmd бʼється об кирилицю:** скрипти — файлом, вивід у `.log`,
читати `read_file` (НЕ `findstr` — він не бачить ні кирилиці в частині файлів,
ні ANSI-escape у виводі vitest).
⚠️ **Вивід `start_search` підміняє рядок-збіг самим патерном** — для читання
коду завжди `read_file`; результати ТІЛЬКИ через `get_more_search_results`;
`filePattern` через кому мовчки дає 0; перша сторінка обрізається на 100.
⚠️ **`node -e` під PowerShell розсипається на дужках і лапках** — скрипт
завжди ФАЙЛОМ. ⚠️ `String.replace(str, str)` трактує `$&` у заміні — при
склейці SQL використовувати функцію-заміну.
⚠️ **Vercel деплоїть ~2–3 хв.** Ознака живого бандла — рядок, унікальний для
пакета; для пакетів БЕЗ клієнтського коду такої ознаки немає в принципі.

## Спершу прочитай (у цьому порядку)

1. **`AGENTS.md`** — стабільні правила проєкту (розділ «Миграции и БД» уже
   містить пастку 0122).
2. **`claude/radflow-handoff.md`** — durable-стан, НАЙСВІЖІШЕ (кінець с49).
   Копія в Claude Projects — там же посесійні доки `claude/session49-*`.
3. ⛔ **`docs/HANDOVER.md` — НЕ джерело правди** (його шапка і §8 виправлені в
   с49, але сам файл лишається історичним; §6 цінний — там «чому так»).
4. **`docs/PRODUCT_OVERVIEW.md`** — продукт і еволюція схеми (§7 доповнений
   0166/0167/0168, §8 — U-55 і U-56).
5. **`docs/audit/RADFLOW_DEEP_TECHNICAL_FUNCTIONAL_AUDIT_2026-08-27.md`** —
   журнал глибокого аудиту; **вердикт не винесений**.
6. **`docs/ops-cron.md`** (реєстр 10 задач).
7. PR-доки останніх пакетів: `PR-U55-queue-entries-read-trust.md`,
   `PR-U56-emergency-stop-incident-ids.md`,
   `PR-U57-room-schedule-trust-by-behaviour.md`.

⚠️ **І не спирайся на ЦЕЙ файл теж.** Правило підтверджено шість сесій поспіль:
перевіряй і хеші (`git ls-remote`), і ПЕРЕДУМОВИ задач (`select now()`).

## Правила, які не можна порушувати

- **Міграції накатуєш ТИ** (Supabase MCP `execute_sql`).
- Номер — із `select max(name) from migration_ledger`, НІКОЛИ не з папки.
- Файл + смоук + секція `=== ВІДКАТ ===` у КІНЦІ + dry-run + сторож попередника
  + два незалежні ревʼю.
- **Dry-run:** тіло в `do $$…$$` БЕЗ внутрішнього commit; факт відкату
  перевіряти ОКРЕМИМ запитом. `ledger_md5` шумить між накатом і db:gate — це
  законно.
- **Смоук: асерти лише `is distinct from`**; «RLS мовчки зʼїв» ловиться ТІЛЬКИ
  через `get diagnostics row_count`. Смоук звіряє ДЕЛЬТУ.
  ⚠️ Смоук проти ЖИВОГО прода бере AccessExclusiveLock — `set local
  lock_timeout` обовʼязковий на грантах і політиках.
- Передрук прод-функції — звіряти по md5 нормалізованого коду.
- `select … into` в гілці if НЕ виконується при хибній умові — скидати ЯВНО.
- Новий інваріант — у `invariants_check()`, не лише в смоук.
- Правки у файл міграції — ДО `npm run db:gate`.
- security_invoker=true на VIEW = ЗАМОК. **dev і prod — ОДНА БД**, тож міграція
  діє на прод одразу після накату, ДО деплою коду.
- **ГЕЙТ ГОНЯЄ ХТОСЬ ОДИН** (`.next` спільний).
- **Вкладені модалки**: батьківський `useModalA11y` глушити параметром `active`.

## Правила тестів

- Компонентних тестів НЕМАЄ: `vitest.config.ts` — `environment: "node"`.
  Правило, ціна помилки якого висока, виноситься з JSX у `lib/`.
- **Роути тестуються ПОВЕДІНКОВО** (`tests/fixtures/fakeSupabase.ts` —
  двійник PostgREST). **Двійник мусить лишатись злим:** кидає на будь-якому
  фільтрі, якого не реалізує.
- **Regex по вихідному коду — НЕ сторож.** Перевіряй ВИКЛИКОМ; статичний regex
  лишається хіба як детектор ВИДАЛЕННЯ рядка. І пам'ятай, що він помиляється в
  обидва боки (урок U-57).
- **Фікстура з самих цифр робить перевірку регістру порожньою.** Фікстура мусить
  РОЗРІЗНЯТИ дві реалізації: у с49 зразок «неповної відповіді» не відрізняв
  перевірку по ключах від перевірки по довжині, і мутація лишалась зеленою.
- **Тавтологічний тест виглядає як сторож.** Перед тим як лишити тест, мисленно
  ВІДКОТИ фікс і назви, що почервоніє.
- **Сторож, що шукає ІМЯ, ловить рядок імпорту.** Сторожити треба ВИКЛИК і
  ЗАБОРОНЯТИ розмивання.
- **Сторожити треба ПРОВОДКУ, а не лише тип пропа.**
- **Два тести з ОДНАКОВИМ імʼям знімають вимогу «назви, який саме почервонів».**
- **Фальсифікацію читай JSON-репортером**, не текстом.
- **Списки файлів у сторожі пишуться руками — додай сканер.** І питай про
  ОБСЯГ сканера: у с49 виявилось, що він роками дивився лише в `components/`.

## Інструменти (готові)

```
node scripts/integration-admin.mjs list --clinic <uuid>
node scripts/integration-live-check.mjs --base https://rad-flow-tau.vercel.app --read-only
node scripts/race-check.mjs plan / run --run --n 4
npm run db:gate        # штампує md5
npm run build          # deploy-гейт + next build
select public.invariants_check();
```

⚠️ live-check **не з контейнера** (домен не в allowlist).

## Порядок ведення роботи

1. Прочитай `claude/radflow-handoff.md` — найсвіжіший.
2. Звір стан: `git ls-remote origin refs/heads/main refs/heads/dev`
   (⚠️ `git fetch origin main dev` НЕ оновлює remote-tracking!),
   `git status`, БД через `execute_sql`, `select now()`. **Не вір цьому файлу.**
3. Узгодь із власником напрямок, склади задачник (TaskCreate).
4. Роби напряму в `D:\RadFlowDev` — один пакет за раз, тулчейн і два ревʼю
   між ними.
5. **Комітиш, мержиш і деплоїш сам.** Після деплою — жива перевірка на проді.
   Текст PR усе одно пиши: він лишається в `docs/audit/`.
6. Наприкінці сесії онови `claude/radflow-handoff.md` (+копію в Claude
   Projects) і цей файл.
