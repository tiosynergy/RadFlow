# RadFlow — стартовий промпт наступної сесії (після с39)

Продовжуємо розробку і продакшн SaaS **RadFlow**. Ти в ролі оркестратора і
розробника: керуєш агентами, конекторами, skills, plugins. Спілкуйся
**російською**, UI-копірайт — **український**.

## РЕЖИМ РОБОТИ: НАПРЯМУ В ПАПЦІ ПРОЄКТУ

**Працюй НАПРЯМУ в `D:\RadFlowDev`** через **Desktop Commander MCP**:
`list_directory`, `read_file`, `write_file` (mode=append чанками по ~30
рядків), `edit_block` (завжди після `read_file`), `start_process`,
`start_search` (власний рушій, кирилицю знаходить). Прод-БД — **Supabase
MCP** `execute_sql`, ref `rdiqjxzibdqbhwileret`.

⚠️ **PowerShell/cmd б'ється об кирилицю:**
- Пиши `.cmd` файлом у ЛАТИНСЬКИЙ шлях (`C:\Windows\Temp\x.cmd`), вивід у
  `.log`, потім `read_file`. Інлайн через лапки не проходить.
- `findstr` НЕ читає UTF-16 від PowerShell-редиректу. Читай лог `read_file`
  з `offset`, не через findstr.
- `<` зарезервований — команди ВЛАСНИКУ давай з КОНКРЕТНИМИ значеннями.

## Спершу прочитай (у цьому порядку)

1. **`AGENTS.md`** — стабільні правила проєкту.
2. **`claude/radflow-handoff.md`** — durable-стан, НАЙСВІЖІШЕ (кінець с39).
3. ⛔ **`docs/HANDOVER.md` — НЕ джерело правди** (футер бреше про 0086).
4. **`claude/pacs-fhir-integration-plan.md`**, **`docs/ops-cron.md`**.

⚠️ **І не спирайся на ЦЕЙ файл теж.** ДВІЧІ поспіль він брехав про стан
(с38 і с39). Перевірка займає одну команду.

## Стан на старті (кінець с39)

- **Прод-БД на `0154`**, ledger 154. Наступна — `0155`.
- ⚠️ **У `0154` НЕМАЄ md5** — `npm run db:gate` не ганявся.
- ⚠️ **Файли `0154` (міграція + смоук) НЕ закомічені** — untracked у
  робочому дереві. У проді міграція Є, у репозиторії НЕМАЄ.
- **`main` = `9e6e989`** (PR #43: звук, 0151, 0152, 0153).
  **`dev` = `fd71bfb`** (+ UI-фікс формату дати). **PR не проведено.**
- Тулчейн: tsc 0, eslint 0, **vitest 1088/1088 (43 файли)**, build OK.
- Cron: **9 задач** (нова `invariants`, jobid 13, `50 3 * * *`).
- Клінік 2, `queue_entries` 89, активних ключів 0.

## ⚠️ ЗАДАЧА №0 — нічні прогони 25.08

Перевіряти БЕЗ вікна в часі, одним запитом:

```
select job, ran_at, result from public.maintenance_runs
 order by ran_at desc limit 10;
```

Очікуємо ДВА нових рядки за ніч: `audit-retention` (03:40, перший прогін
прямим викликом після 0152) і `invariants` (03:50, перший прогін сторожа).
Порожньо — задача не відпрацювала.

## ЗАДАЧА №1 — `0155`: сторож ловить сам себе

Перший прогін дав `{"check":"cron_daily_ran_48h","offenders":["invariants"]}`
— перевірка 5 не розрізняє «задача протухла» і «задачу щойно створили».
Готовий рецепт і обґрунтування — в handoff, секція «Задача №1». Коротко:
розділити на `cron_daily_stalled` і `cron_daily_never_ran`, `checked` стане
9, зонд `d` у смоуці підняти з 8 на 9. Передрук — приймом звірки по md5.

## Беклог за пріоритетом (узгодити AskUserQuestion)

1. **`0155`** — див. вище.
2. **Комміт + PR** для `0154` і UI-фіксу (два хвости в дереві й у dev).
3. **Код `stale` покриває ДВІ ситуації** — «хтось випередив» і «перехід
   заборонений `p_allowed`»: наступна правка матриці дасть хибне повідомлення.
4. **Винести `fmtOrigin` у `lib`** — дубль у QueueBoard і ReferrerBoard,
   у с39 правили обидві копії руками.
5. **Симетричний прогін ack зі сторони B** — у с39 перевірено лише ack
   регістратора, повний цикл заморозки з її боку не ганяли.
6. **Нові харнеси на базі `race-check.mjs`**: гонка `in_progress` (0129) і
   паралельний CAS `queue_set_status_rpc`.
7. **Мертві `navKey` в `SURFACE_BY_NAV`** (кандидат — `calls`): правка TS.
8. **unused_index** — 15 з `idx_scan=0`. ⚠️ Рекомендація с39: НЕ чіпати
   (більшість по 16 КБ, найбільші — триграмні індекси пошуку).

## Правила, які не можна порушувати

- **Міграції накатує ВЛАСНИК** у SQL Editor, **«Run without RLS»**.
- Номер — із `select max(name) from migration_ledger`, НІКОЛИ не з папки.
- Ти готуєш файл + смоук + секцію відкату + dry-run + два ревʼю.
- **Dry-run:** тіло в `do $$…$$` БЕЗ внутрішнього `commit`, або перевіряти
  факт відкату окремим запитом. Третій варіант (добре працював у с39):
  прогнати саму МЕХАНІКУ на живих даних read-only, нічого не змінюючи.
- **Смоук: асерти лише `is distinct from`.** Фікстури мусять проходити
  форматні констрейнти.
- **Перевірки коду функцій — з ВИРІЗАНИМИ коментарями** (інакше зонд ловить
  власний коментар — с39).
- **Смоук звіряє ДЕЛЬТУ, а не абсолютний стан середовища** (базовий знімок
  на початку): між накатом і `db:gate` `ledger_md5` шумить законно.
- **Передрук прод-функції — звіряти по md5** нормалізованого коду (с39).
- **Порожня таблиця ≠ мертва** (`doctors` порожня, але BookingModal її
  ВСТАВЛЯЄ). Перед дропом — грепати код і `pg_proc`.
- **Новий інваріант — у `invariants_check()`**, а не лише в смоук своєї
  міграції. Інакше він замовкне назавжди після накату.
- **Фікстура в `queue_entries` б'ється об 15 BEFORE-тригерів.** Перевірений
  набір: кабінет `40f89c03` (Medicom, MRI, «Закревського, 9»), день +1,
  dur 20 + buf 5, `studies` з РЕАЛЬНОЇ активної позиції каталогу.
- **security_invoker=true на VIEW = ЗАМОК**, не оптимізація.
- `audit_log.action` має CHECK: лише `insert/update/delete`.
- Зміна return-сигнатури RPC: `drop function` (42P13), потім `supabase/
  types.ts` РУКАМИ (генерації типів немає).
- **`to_regclass` окремим statement-ом** (42P01/42703 до short-circuit).
- Deploy-гейт валить build, якщо міграція на диску без запису в ledger.
  **Порядок фічі з міграцією+кодом: накат → деплой (мерж) → увімкнення.**
- dev і prod — **одна БД**. PII в details заборонено. Мерж/пуш робить
  власник; ти готуєш текст коміту й PR.
- **ГЕЙТ ГОНЯЄ ХТОСЬ ОДИН.** `next build` не ізолює `.next`.

## Інструменти (готові)

```
node scripts/integration-admin.mjs list --clinic <uuid>
node scripts/integration-live-check.mjs --base https://rad-flow-tau.vercel.app --read-only
node scripts/race-check.mjs plan                     # нічого не пише
node scripts/race-check.mjs run --run --n 4          # ПИШЕ в прод, прибирає
npm run db:gate                                      # машина власника, md5
select public.invariants_check();                    # сторож, одразу
```

⚠️ live-check **не з контейнера** (домен не в allowlist).

## Порядок ведення роботи

1. Прочитай `claude/radflow-handoff.md` — найсвіжіший.
2. Звір стан: `git ls-remote origin refs/heads/main refs/heads/dev` (⚠️
   `git fetch origin main dev` НЕ оновлює remote-tracking!), `git status`,
   БД через `execute_sql`. **Не вір цьому файлу.**
3. Узгодь із власником напрямок (AskUserQuestion), склади задачник.
4. Роби напряму в D:\RadFlowDev — прогони гейт — давай текст коміту й PR.
5. Наприкінці сесії онови `claude/radflow-handoff.md` і цей файл.
