Продовжуємо розробку і продакшн SaaS **RadFlow**. Ти в ролі оркестратора і
Full Task розробника. Спілкування — **російською**, UI-копірайт — **український**.
Робота — **НАПРЯМУ** в `D:\RadFlowDev` через Desktop Commander.

> ## ПОВНОВАЖЕННЯ В ЦІЙ СЕСІЇ — РОЗШИРЕНІ, ТІ САМІ, ЩО В с45–с52
>
> Власник підтвердив їх при передачі роботи в **с53** окремо і явно і повторив
> дослівно на старті **с55**. Це не опис
> минулого — це твої повноваження ЗАРАЗ. Повний текст зі страховками — нижче,
> у розділі «ПОВНОВАЖЕННЯ».
>
> Коротко: сам правиш код/тести/SQL/доки, створюєш і **накатуєш міграції на
> прод**, виконуєш безпечний прод-SQL, створюєш і прибираєш синтетичні
> прод-дані, комітиш, пушиш, **створюєш і зливаєш PR**, **деплоїш**, читаєш
> прод-логи, робиш живі перевірки в браузері.
>
> Старе правило «мерж/пуш/накат робить власник» **скасоване**. Якщо натрапиш на
> нього в доках — виправ на місці.
>
> **Страховки не скасовані жодною мірою:** два незалежні раунди ревʼю на все
> чутливе з РІЗНИМИ лінзами і особистою валідацією висновків субагентів;
> фальсифікація сторожів з іменем червоного тесту і ЗЕЛЕНОЮ базовою лінією;
> жива перевірка на проді в ОБИДВА боки вимірюванням; прод-дані — лише за явним
> списком id; секрети і PII — НІКУДИ (ні в звіт, ні в лог, ні в промпт
> субагента); `npm audit fix --force` заборонений; паролі не просити ніколи.

---

## ЗАДАЧА №0 (перед будь-якою правкою)

`select now()` · `git ls-remote origin main dev` · `git status` ·
`maintenance_runs` за ніч · `select max(name) from public.migration_ledger` ·
`select public.invariants_check()` · відбиток деплою в HTML-коментарі на
початку `/login` — **URL прода `https://rad-flow-tau.vercel.app`** (записаний в
`AGENTS.md`; лежить і в `cron.job.command`); знімати з вбудованого браузера на
машині власника (`fetch(location.origin + "/login", {cache: "no-store"})`), не
`WebFetch` з контейнера — той вирізає HTML-коментарі.

⚠️ **ЦЬОМУ ФАЙЛУ НЕ ВІРИТИ — звіряти запитом.** Нижче — що МАЄ вийти, а не
джерело правди. У с50 цей файл на старті сесії брехав про власний стан
(казав «кінець с50», а описував кінець с49) — тому перевірка обовʼязкова.

**Стан на кінець с55** (пакети 14–18 — хвости фази 4 і ВСЯ фаза 6; звірено
запитом і командою 03.09.2026, останній замір БД **15:45 UTC**):

| що | значення |
|---|---|
| `main` / `dev` | рівні; **останній КОД — `84078d7`** (переякорення після 0170; історія дня: `cb5ffd3` → `cc6ef29`/`35e76e7` → `d568999` → `d8214f4` → `d993954` → `3486ac4` → `84078d7`); зверху — docs-коміт цієї правки, звіряти `git ls-remote` |
| деплой | ✅ ВИМІРЯНО В ОБИДВА БОКИ, тричі за сесію: `11WYebxt_zkW1tkRsrdhQ` → `XehL2Gj_TG9i2UeDjf6rt` (пакет 2) → **`4CK0ANcRvG1EcmnDZuqB4`** (після `84078d7`), HTTP 200. **URL — `https://rad-flow-tau.vercel.app`** (тепер в `AGENTS.md`); після docs-пушу цієї правки відбиток знову інший |
| прод-БД | **`0170`** (`0170_policy_digest_invariant.sql`), ledger **170/170**, 0 незаштампованих — **накачено в с55** (звірено 15:45 UTC) |
| `invariants_check()` | `ok:true`, **`checked:16`**, `failed:[]` (15:45 UTC) |
| **наступна міграція** | **0171** — номер брати з леджера; кандидат — `guard_triggers` + U-76 ОДНИМ передруком |
| тулчейн | tsc 0, eslint 0, vitest **2465/2465** (83 файли), `db:gate` 170/170 |
| ✅ ревізія стендів | **24 із 24 ЗЕЛЕНІ** після `84078d7` (`EXPECTED_STANDS` 22 → 24: +`f6auth` 30/30, +`f6srv` 8/8; `f346` 32/32), **549 адресних**, ~39 хв |
| нічні джоби | 03.09: `outbox-retention` 0/0/0 (03:30), `audit-retention` 0/0 (03:40), `invariants` ok/**15** (03:50 — ДО накату 0170; денні прогони після накату — ok/16). Завтра о 03:50 має бути `checked:16` |

## ЩО БУЛО В с55 — пакети 14–18: хвости фази 4 і ВСЯ фаза 6 — ЧИТАТИ ПЕРШИМ

**(PR-доки: `docs/audit/PR-F3-F4-F6-time-node-tails.md`,
`PR-F6-auth-surface.md`, `PR-F6-5-terminal-screens.md`,
`PR-F6-pkg2-server-surface.md`, `PR-F6-pkg3-rls-policy-digest.md`; карта ролей —
`PHASE6-role-map.md`. Коміти: `cb5ffd3` F3/F4/F6 → `cc6ef29`/`35e76e7` сканер
сторінок → `d568999` Ф6-4 → `d8214f4` Ф6-5 → `d993954` пакет 2 → `3486ac4`
пакет 3 (міграція 0170) → `84078d7` переякорення після 0170.)**

### ⚠️ URL ПРОДА ЗНАЙДЕНО — і хендофф чотирьох сесій про нього БРЕХАВ

`https://rad-flow-tau.vercel.app`. Спершу знайдений у проді — в
`cron.job.command` (джоби `outbox-deliver`, `gcal-backup-sync`). Потім
`git grep -I rad-flow-tau` дав **30 місць у самому репозиторії**:
`docs/HANDOVER.md`, `docs/PRODUCT_OVERVIEW.md`, шапка журналу аудиту,
`supabase/cron_jobs.sql`, міграції 0152/0161, `scripts/integration-admin.mjs`,
`tests/fhirContract.test.ts` і блок с50 самого `claude/radflow-handoff.md`.
Запис «адреси немає НІДЕ в репозиторії, перевірено findstr» був **неправдою**:
пошук повертав порожньо (ймовірно, пастка лапок/кодової сторінки `findstr`
усередині `cmd /c`), і порожній результат прийняли за відсутність. **Урок: у
пошуку, як у сторожа, має бути зелена базова лінія — завідомо наявний рядок;
«нічого не знайшов» без неї нічого не доводить.** Відбиток `/login` знято В
ОБИДВА БОКИ вперше за фазу; URL записано в `AGENTS.md`. **Більше не питати
власника про URL.**

### Фаза 6 — ЗАКРИТА (чотири пакети + Ф6-5)

* **Пакет 1, сторінки** — `tests/authSurface.test.ts` (72) + `falsify-f6auth`
  (34 мутації, 30/30 адресних, 15 сторожів, 0 без мутації). Ф6-1…Ф6-4 закриті.
  Ф6-4 — екран-відмова `components/RoleNotice.tsx` з текстом власника
  («Доступ обмежено» / «У вашій ролі цей розділ недоступний. Зверніться до
  адміністратора центру.»), З КНОПКОЮ виходу (власник передумав після Ф6-5).
* **Ф6-5** — `/setup` рендерив відмову БЕЗ виходу: реєстратор у ненастроєному
  центрі замикався в петлі `/queue → /setup → /login → /queue`. Живе за
  побудовою, не за даними (ненастроєних центрів у проді 0). Сторож —
  властивість із дерева: будь-яка панель `minHeight: "100vh"` мусить давати
  вихід (`<SignOutButton/>` або `<Link href=…>`). Мій же `RoleNotice` того
  самого дня повторив помилку — його спіймав цей сторож.
* **Пакет 2, серверна поверхня** — `tests/serverAuthSurface.test.ts` (8) +
  `falsify-f6srv` (10, 8/8). **Дір НЕМАЄ:** 22 роути через `requireRole`,
  15 через ключ інтеграції/FHIR, 4 через `CRON_SECRET`, 8 pre-auth (кожен
  прочитаний, з причиною в інвентарі); усі 48 server actions під сесією (RLS),
  `createAdminClient` ніде не раніше гейта; зі 111 `SECURITY DEFINER` без
  перевірки викликача і доступних `authenticated` — рівно 2 (довідники).
  ⚠️ Перший регекс-скан позначив 28 роутів «без авторизації» — гіпотеза,
  спростована читанням (FHIR → `requireFhirKey`).
* **Пакет 3, RLS — міграція 0170:** інваріант №16 `policy_digest` — md5-дайджест
  64 політик хардкодом усередині `invariants_check` (канон `canonical_objects`;
  таблиця отримала б дефолтні GRANT-и). **Дір НЕМАЄ.** Відфальсифіковано на
  проді БЕЗ зміни політик (підміною очікуваного списку): `changed:` /
  `missing:` / `new:` — усі три гілки. Прод і файл збігаються ДОСЛІВНО
  (md5 тіла `22f8bbbd…`, 29932 байти з обох боків).
  Правильність знято ЧИТАННЯМ для PII-таблиць (`profiles`, `queue_entries`,
  `waitlist_entries`, `patient_cases` + звʼязкові) — шість гіпотез «тут дірка»,
  усі спростовані (§10 PR-доку пакета 3).

### ⚠️ НОВА ЗНАХІДКА (не дірка сьогодні — непокрита поверхня): правильність RLS тримається на BEFORE-тригерах, і ЖОДЕН з них не під інваріантом

`profiles_update_self` дозволяє власнику рядка `UPDATE` усіх 15 колонок, разом
із `role` (GRANT `authenticated` UPDATE на всі колонки — звірено); відмовляє
ТРИГЕР `guard_profile_privileges`. Те саме: `guard_room_in_clinic` для
направника, `a00_radiologist_scope` для рентгенолога. У тексті
`invariants_check` ці функції **не згадані**, слова `tgenabled` **немає
взагалі** — `drop trigger` або `disable trigger` в UI Supabase відкриває
самоескалацію при зелених 16 інваріантах. Кандидат на **0171: перевірка
`guard_triggers`** (існування + `tgenabled = 'O'` + функція на таблиці) —
зібрати ОДНИМ передруком разом із **U-76** (`server_now()` ACL), бо кожен
передрук ламає стенди, прибиті до передруку (нижче).

### ⚠️ Ревізія після 0170 дала 2 червоних — обидва стенди були ПРИБИТІ до передруку

* `falsify-0166`: N15–N24 стріляли в `0167`, а сторожі читають ОСТАННІЙ
  передрук (тепер 0170) — 10 «дивиться не туди» + 6 «якір не знайдено»
  (літерал `15`). Це ДОСЛІВНО дефект, знайдений і полагоджений у с51 у
  `falsify-u37`, чий коментар обіцяв «наступна ж міграція знову зробить стенд
  сліпим» — так і сталось, у СУСІДНЬОМУ стенді. Тепер ціль — `latestReprint()`,
  число для пінів — з нього.
* `falsify-u37` N10: **сліпа зона монотонності.** Поріг «не менше за
  попередній передрук» має дірку в один крок рівно в тому передруку, який
  ДОДАЄ перевірку: 16 → 15 ≥ 15 — зелено. Вилікувано структурою в
  `tests/unreadChanges.test.ts`: число міток `'check', '…'` == число кроків
  `v_n := v_n + 1;`.

**Правило: міграція, що передруковує `invariants_check`, зобовʼязана прогнати
ПОВНУ ревізію — вона ламає ВСІ стенди, прибиті до передруку, і робить це
мовчки.** Ціна 0170 поза функцією: число перевірок у ДЕВʼЯТИ смоуках 15 → 16
(19 замін; сторож `invariantsCheckedPins` підтвердив), md5-пін тіла в
`gcal_pg_cron_smoke` крок g перезнято живим запитом.

### Хвости фази 4 (пакет 14, `PR-F3-F4-F6-time-node-tails.md`)

Рішення власника: дошки СЛІДУЮТЬ за поправкою (пін дип-лінка знято); банер
зміни центру — «У цьому центрі зараз інша дата». F3 — виміряно зондом, що пін
міняв результат РІВНО в одному стані і там же шкодив; F4 — чисте
`dateOnCenterSwitch`, ревʼю знайшло, що перенесення стало МОВЧАЗНИМ, додано
скидання часу і банер; F6 — `observeWallStep` + `markClockStale` (друга
вартова, без якої перша була б декорацією). Новий стенд `falsify-f346`
(32/32). Живої перевірки цих правок і `cas` не було — потрібен токен сесії
персоналу.

### Уроки с55 (кожен виміряний, не відчутий)

1. **Список зі сканера — гіпотеза, не результат** — спростовано ЧОТИРИ рази за
   сесію (28 «незахищених» роутів, 38 «неперевірених» SECDEF, 9 «неперевірених»
   політик, самоескалація/чужий центр).
2. **Мій свіжий сторож — такий самий підозрюваний:** два раунди ревʼю дали
   чотири HIGH у сканері сторінок (пін тримав предикат, а не наслідок; підрядок,
   а не досяжність; `hasPositive` ловився на `new Set`; «13/13 адресних» без
   `newFile`-мутацій). Одну пораду ревʼю (R6) ВІДХИЛЕНО після перевірки по
   джерелу — вона зробила б базу червоною на 8 admin-only роутах.
3. **Правка продукту може тихо знеструмити позицію стенда** (M20 інвертовано,
   G1 перенаправлено на `/staff`) — перечитувати мутації, не лише сторожа.
4. **Коментар поруч зі сторожем описує те, що сторож РОБИТЬ** — шапка
   `RoleNotice` після додавання кнопки стала б брехнею, переписана.
5. **`pg_get_functiondef` рендерить шапку інакше, ніж файл** — порівнювати
   лише тіло між `as $function$` і `$function$;` (регістр!).
6. DO-блок накату не вставляє рядок леджера — вставляти окремо, потім
   `db:gate` штампує md5.
7. **Порожній результат пошуку — не доказ відсутності.** Чотири сесії
   «не знаходили» URL, який лежав у 30 файлах репозиторію: `findstr` у
   `cmd /c` мовчки повертав порожньо. Пошук перевіряти на завідомо наявному
   рядку так само, як сторож — на червоній мутації.

## ЩО БУЛО В с54 — пакет 12 (Г1-F)

**(`docs/audit/PR-G1F-server-clock-trust.md`; коміти `74419ab` код, `fa0caaa`
переякорення `falsify-u72`; мерж `fe2a93b`. МІГРАЦІЇ НЕМАЄ.)**

Рішення власника: **сервер не приймає добу, виведену з розбіжного годинника**,
і відмова торкається ТІЛЬКИ дат, ВИВЕДЕНИХ із «сьогодні» (дата, обрана
людиною, проходить завжди).

⚠️ **Спершу ВИМІРЯНО, як і обіцяв власнику в с53.** Зонд по реальному ядру,
вісім позицій, в обидва боки: під `busy: saving` `apply = null`; та сама
подача без `busy` — `apply` є (зелена база), тобто різницю дає РІВНО `busy`;
на гілці ПОМИЛКИ перенесення застосовується, на гілці УСПІХУ — ніколи, ключ
помирає з хуком; у БД їде дата ДО поправки. Серверний бік теж виміряно на
проді: `check_not_in_past` ловить лише напрямок «у минуле», «у майбутнє» не
ловить ніщо. **Хендофф с53 називав Г1-F ширше, ніж є: мовчки їде ОДИН
напрямок, не два.**

⚠️ **МІГРАЦІЯ НЕ ЗНАДОБИЛАСЬ, і це та сама помилка, що Ф4-2 в с50.** Я назвав
варіант «міграція 0170», не дочитавши шлях запису. Обидва шляхи — Server
Actions (лист очікування пише в таблицю ПРЯМО, без RPC), а головне: БД не може
знати, що дата ВИВЕДЕНА з клієнтського «сьогодні», поки клієнт цього не
скаже — «останній рубіж у тригері» тут неможливий ЗА ПОБУДОВОЮ.

**Що зроблено.** `lib/clockTrust.ts` — чисте правило `clockClaimVerdict`
(`ok`/`skew`/`malformed`), `serverClockNow`, один текст відмови.
`lib/useFollowToday.ts` — з `followedDay` винесено предикат `derivedFromToday`
(без зміни поведінки) і доданий `clockClaimOf`. Гард — у трьох місцях запису;
у патчі листа лише коли патч ВЕЗЕ `desired_date_from`, і умова читається з
САМОГО патча.

⚠️ **ДВІ УМОВИ ВЕРДИКТА, і друга не косметична.** Порівнювати самі доби не
можна: правило `useFollowToday` СВІДОМО не реагує на справжню північ, тож гард
«доби не збіглись» відмовляв би опівночі чесній роботі. Розбіжність МОМЕНТІВ
відрізняє «годинник бреше» від «настала північ».

⚠️ **СТОРОЖ ПОВНОТИ — ВИМОГА ТИПУ, а не перелік місць.** `clock` обовʼязковий
у `RescheduleInput` / `WaitlistInput` / `WaitlistFormOut`; у рантаймі поле
необовʼязкове (стара вкладка не ляже в мить деплою). tsc сам перелічив
**11 точок виклику в 6 файлах**, зокрема інлайн-переноси дошки повз модалку.

### Дев'ять дірок у МОЇХ ЖЕ свіжих сторожах (стенд — дві, ревʼю — сім)

1. **Пін порядку зелений ТОЖДЕСТВЕННО:** `s.indexOf("clockClaimVerdict")`
   влучав у рядок ІМПОРТУ. Плюс `isPastSlot(...)` є у файлі ТРИЧІ.
2. **«Є блок» ≠ «блок ДОСЯЖНИЙ»:** обгортка гарда в `if (…) {` лишала пін
   зеленим, а гард мертвим.
3. **HIGH — пін тримав РЯДОК, а не ВІДМОВУ:** `return CLOCK_SKEW_ERR;` →
   `void CLOCK_SKEW_ERR;` лишало все зеленим.
4. **HIGH — `null` замість заявки знімав гард одним словом** (тип дозволяє
   `null` заради патчів без дати). Тепер на патчі З ДАТОЮ `null` — відмова.
5. **HIGH — заявка дошки не пінувалась нічим.**
6. **MED-HIGH — допуск був закріплений лише знизу:** усі межі задавались через
   саму константу, тож її можна було мовчки РОЗШИРИТИ до 33 хв.
7. **MED — зона клініки не перевірялась** (лише формат ключа).
8. **MED — другий `clockClaimOf`** (гілка «інший кабінет») не покритий.
9. **LOW — якорі в регулярці ключа доби** (`$` без `m` пропускає `\n`).

### Ревізія спіймала ШІСТЬ протухлих якорів у СУСІДНЬОМУ стенді

Винесення `derivedFromToday` зробило M8, M10, M30, M122, T1, T2, T5 у
`falsify-u72` неробочими: пʼять — «ЯКІР НЕ УНІКАЛЬНИЙ (0)» (рядок переїхав і
змінив `null` на `false`), M30 — «(2)»: у `WaitlistModal` зʼявився ДРУГИЙ
`pinnedKey: …`, і якір із чотирма пробілами став ПІДРЯДКОМ рядка з десятьма.
Переякорено (`fa0caaa`), кожна позиція несе поруч причину.

### ⚠️ Пастка середовища, додана в с54

**Правки в репозиторії під час прогону СТЕНДА (не лише `falsify-all`) мовчки
відкочуються.** Стенд знімає знімок файлів на старті і відновлює їх у
`finally`. Я правив `lib/clockTrust.ts`, поки стенд ганявся, — правку відкотило,
і виявилось це лише тому, що я звірив файл після прогону.

### Названі межі Г1-F (усі — у шапці `lib/clockTrust.ts`)

* `fromToday` рахує КЛІЄНТ проти СВОГО «сьогодні». Якщо поправка приземлилась
  РАНІШЕ кліка, а поле лишилось старим, гард промовчить. У двох формах пакета
  цей стан недосяжний (`busy` тримається рівно на час запиту), крім вузької
  щілини між приземленням поправки і прогоном ефекту.
  ⚠️ Перша редакція шапки цю межу ПРИХОВУВАЛА фразою «сервер знає правду
  завжди»; знайшло ревʼю.
* `createBooking` / `createReferralBooking` / `scheduleFromWaitlist` гарда НЕ
  мають — це межа узгодженого обсягу.
* Зсув, менший за ~32 с, гард не ловить: там відрізнити його від справжньої
  півночі неможливо В ПРИНЦИПІ.
* Серверний момент — годинник Node на Vercel (той самий, що в `isPastSlot`);
  розбіжність Node ↔ Postgres не вимірювалась.

⚠️ **ЖИВОЇ ПЕРЕВІРКИ САМОГО ГАРДА НЕ БУЛО, і це не відписка.** Він спрацьовує
лише коли годинник ПК розійшовся з сервером І поправка перетинає північ; для
цього потрібні креденшели персоналу і переведення годинника через північ під
живим центром. Креденшелів агент не просить. Виміряно натомість: відбиток
деплою в обидва боки, схема прода (ledger лишився 0169), інваріанти, джоби.

## ЩО БУЛО В с53 — пакет 11 (Г1-G)

**(`docs/audit/PR-G1G-one-day-shift-rule.md`; коміти `9c0bcaa` код, `c22865a`
док, `c74ee6c` + `2a0ef0f` бюджет ревізії; мерж `1f9fa0b`.)**

Шість екранів зведено на спільні правила `lib/useFollowToday.ts` замість шести
рукописних пар умов банера; ще пʼять копій формату ключа доби замінено на
`dateKeyOf` (усього **вісім** місць на спільній функції).

⚠️ **ГОЛОВНЕ — ПОМИЛКА ПЕРШОЇ РЕДАКЦІЇ ЦЬОГО Ж ПАКЕТА (ревʼю А, HIGH), і це
той самий урок, що в шапці:** свій свіжий сторож — такий самий підозрюваний.
Вердикт «чи є що показати» був зведений до була, і «туди-назад» (01→02→01)
давало `false` — тишу. А поправок було ДВІ: слот стерто двічі, на дошці
обдзвону між ними стояв інший день, і **гейт «Всіх підтверджено» ВІДКРИВАВСЯ**
саме тоді (я перевів його з `!!dayShifted` на той самий бул). Виправлено
**тризначним `dayShiftNoticeVerdict`** — `none` / `moved` / `returned`; гейти
дивляться на «не `none`»; що показувати на `returned`, вирішує екран за тим, що
зруйнував `onShift` (форми і карта дня — так, колл-лист — так і гейтить, дошки
та лист очікування — мовчать, кожне рішення запінене). ⚠️ **Тексти `returned` —
мій вибір слів, не рішення власника.**

⚠️ **Друге HIGH (ревʼю Б) — сліпа зона ВСІХ пінів по джерелу:** `src()`
схлопує пробіли, а JSX на переносі перед `<b>{дата}</b>` пробіл зʼїдає
(«змінено з1 вересня»). Зонд T14 стенда стояв зеленим і брехав. Тепер це
червона M139, пін по `raw()`, перенос з явним `{" "}` дозволений (T15), піни
на РЕЧЕННЯ банера — по `text()`.

Що перевірено і НЕ правлено (з причиною) — у PR-доці.

### Пастки середовища, додані в с53 (Г1-G)

* **Убитий таймаутом стенд не виконує `finally`** → мутований файл у дереві.
  `falsify-all` тепер відновлює дерево сам і лишається червоним.
* **Бюджет стенда 45 хв, виміряно двічі** (u72: 901 с при 15, 1501 с при 25,
  1510 с на швидкій машині). ~4 с/мутація вільна машина, ~10 с зайнята.
* **Машина сповільнюється в 2–4 рази посеред сесії** → сторонні стенди
  червоніють з ПОРОЖНІМИ фактами. Це «не встиг», не «не тримає» — ганяти
  окремо.
* **Жодних правок у репозиторії під час `falsify-all`** (навіть доків) і
  раннер планувальника — ПОЗА репозиторієм (`$env:TEMP`).
* **PowerShell калічить кирилицю** — звіти читати node-скриптом. Разова
  перевірка «кожен якір рівно один раз у своєму файлі» ловить протухлі якорі
  за секунди (борг: зробити кроком стенда).

✅ **ПРО ДЕПЛОЙ.** Відбиток розгортання лежить у HTML-коментарі на початку
публічної сторінки `/login` (`<!--<deploy-id>-->`), і в с53 він змінювався
після кожного пушу в `main` (Г1-E: `9R3eomm2x8K7A_5SGn3vG` →
`ulJy_sXMKS1MLhG0lmyp6`; Г1-G: `xYAZdThUFmVLQXx9_hqxo` → `qPPOsJcCoGpvTOK2b6sXK`
→ `ESXGQdS2G1D03O8q2kuND`). Нова збірка на проді — вимір, а не слова.
⚠️ **НЕ виміряно, що саме в ній лежать нові рядки екранів:** їхні чанки живуть
за автентифікацією, `_buildManifest.js` в App Router по шляху
`/_next/static/<id>/` віддає 404, а креденшелів агент не просить. Живої
перевірки банерів (потрібна поправка годинника через північ — і назад — під
персоналом) не було.

⚠️ **«452 адресних» проти «437» — це ЗРОСТАННЯ, а не перерахунок.**
`falsify-u72` пішов з 54 до **69**: 8 позицій Г1-G плюс 7 позицій Г1-G-fix
(зокрема M139 — колишній хибно-зелений зонд T14). Решта стендів дали ті самі
числа, що в пакеті 10.

⚠️ **Разовий `ok:false` у журналі 31.08 15:55:02 UTC — НЕ дефект.** `ledger_md5`
на `0169_server_now.sql` у вікні накатки; прогін через 23 с уже зелений, усі інші
теж. Записано, бо при читанні журналу цей рядок виглядає як провал інваріанта.

⚠️ **Число «адресних» УПАЛО з 478 до 459 — це виправлення рахунку, не втрата.**
Пʼять стендів родини `verdictOf` перестали рахувати рефакторні рядки
(`green: true`) за адресні. Чотири стенди без адресності рахують по-старому.

⚠️ **Ревізія тепер вимагає чистого дерева.** Якщо є незакомічені правки —
`node scripts/falsify-all.mjs --allow-dirty`: перевірка між стендами лишається,
а звіт скаже, що міряли робочу копію, а не `HEAD`.

## ЩО БУЛО В с53 — пакет 10 (Г1-E)

**(`docs/audit/PR-G1E-board-day-shift.md`, коміт `afd618f`, мерж `28cd234`.)**
Рішення власника: банер, як у форм; гейти дня не чіпати.

`QueueBoard` і `RadiologistBoard` були ЄДИНИМИ споживачами `useFollowToday`, які
не брали `onShift`: доба переставлялась МОВЧКИ. Тепер про це говорить банер, що
називає ОБИДВІ доби і стоїть ПЕРШИМ у стовпці — вище банерів простоїв, CITO і
помилок завантаження (їх там накопичується до півдесятка).

* **Стан банера — у КЛЮЧАХ доби, а не в підписах.** `fmtShort` друкує
  «1 вересня» без року, тож на порівнянні підписів поправка рівно на рік
  вирішила б, що казати нема чого, — і банер зник би там, де він найпотрібніший.
* **Два чисті правила** в `lib/useFollowToday.ts`: `dayShiftNoticeOf`
  (накопичення ПЕРШОЇ доби) і `dayShiftNoticeVisible`
  (`fromKey !== toKey && toKey === curKey`). Друга умова — ВЛАСТИВІСТЬ, а не
  перелік місць, зобовʼязаних гасити банер.
* **Власні копії формату ключа доби в обох дошках** замінено на спільну
  `dateKeyOf`: дві копії розійшлися б мовчки і вимкнули б банер ЦІЛКОМ.
* **Гейти дня НЕ чіпали**, і це вимір: `isToday` рахується від ВИМІРЯНОГО
  «сьогодні», тож поки дошка на іншій добі, «не сьогодні» — правда.

⚠️ **НАЙЦІННІШЕ В ПАКЕТІ — знову знахідки ревʼю ПО НЬОМУ Ж.**

**Ревʼю А (лінза «поведінка»), HIGH: банер про ВІДКЛАДЕНЕ перенесення був
МЕРТВИМ UI.** Ядро віддає непорожній `pending` ЛИШЕ під `busy`; `busy` на дошках
— це рівно «відкритий оверлей»; кожен оверлей обох дощок малюється як `.overlay`
(`position: fixed; inset: 0; z-index: 200; rgba(0,0,0,0.55); blur(6px)`). Тобто
банер існував би рівно тоді, коли дошку не видно, і зникав би в мить, коли її
відкривають; у єдиному кадрі, де він потрапляє на пікселі, його текст «доки
відкрите вікно» вже неправда. Це спростувало й ПРЕМІСУ продуктового рішення
(«оператор бачить виродження дошки» — не бачить, вона під оверлеєм). Єдина
реально спостережувана втрата в цьому вікні — вимкнений звук «пацієнт готовий».
Рішення власника після виміру: банер зняти; правильне місце для пояснення —
САМЕ ВІКНО, і це окремий пакет.

**Ревʼю Б (лінза «яка мутація лишить цей пін зеленим»), пʼять дірок у моїх
власних свіжих сторожах:**
1. усі фікстури банера рухали добу ВПЕРЕД → `fromKey !== toKey` → `fromKey <
   toKey` лишалось зеленим, а це вимикає банер у напрямку «ПК спішить»;
2. зустрічний пін на `isToday` сторожив ВЛАСНЕ НАПИСАННЯ — гейт послаблюється в
   місцях СПОЖИВАННЯ (`notToday: !isToday && !anyModalOpen`);
3. спільне правило обходилось локальною копією під тим самим імʼям — гілка
   рендера при цьому байт у байт та сама;
4. зонд T3 після зняття `pendingShift` став ОБОВʼЯЗКОВО червоним (його `to`
   містив заборонене присвоєння) — стенд у такому стані не доводив нічого;
5. заборона «не показувати відкладене перенесення» обходилась перейменуванням і
   тернарником; `role="status"` пінувався «десь у вікні 200 символів»; а
   M116/M117 підставляли копію `dateKey`, ПОСИМВОЛЬНО рівну `dateKeyOf`, тобто
   нічого не ламали — при тому що `what` і повідомлення піна стверджували, що
   банер зникне.

⚠️ **Названі межі пакета:** рукописний ДРУГИЙ споживач `dayShifted` у самому
екрані сканером не ловиться; треті шляхи зміни дати (сьогодні їх два, обидва
через `pickDate`) безпеку не зламають, але «намір» — так; `MiniCalendar` не
синхронізує видимий місяць із пропом (дефект ПЕРЕДІСНУВАВ); фокус після
«Зрозуміло» падає в `body`.

## ЩО БУЛО В с52 — пакет 9 (Г1-B + Г1-C + Г1-D)

**(`docs/audit/PR-G1-BCD-silent-day-shift.md`, коміт `2137293`, мерж `77f8eb4`.)**
Рішення власника: саме ці три, а не U-80г / вузол часу / міграція 0170.

Спільний клас: правило `useFollowToday` переносить добу екрана, а три місця про
це мовчали — і в кожному **коментар поруч стверджував протилежне**.

* **Г1-B `RoomDayOverviewModal`** — виклик без `onShift`: РУЧНА зміна дати
  скидала обраний слот, автоматична ні. Слот від іншої доби лишався підсвіченим
  і підписаним «Обрано …» на новій карті. Екран нічого не пише, тож помилка
  виходить ГОЛОСОМ і їй не заперечить жоден гард продукту.
* **Г1-C `WaitlistModal`** — вікно має два кінці, слідує один. Порожній «по» →
  `desired_date_from` мовчки в БД; вузьке вікно → форма відповідає «виправте
  діапазон», звинувачуючи оператора в діапазоні, якого він не вводив. Гарди
  лишились дослівно: бракувало не гарда, а ПРИЧИНИ.
* **Г1-D `QueueBoard`** — `CALL_SITES` пінував ІМʼЯ прапорця `busy` і мовчав про
  склад. Інвентар оверлеїв, заповнений АПРІОРІ з дерева, знайшов дірку:
  `openCaseId` (CaseModal) не входив у `anyModalOpen`, хоч коментар вимагав
  «покривати УСІ оверлеї». Під кейсом «N» відкривало бронювання поверх нього,
  «R» перезавантажувало дошку, цифри міняли кабінет, j/k наводили фокус позаду.

⚠️ **Сторож Г1-D — не перелік, а властивість.** Рахуються ОБИДВІ сторони з
джерела: гейти оверлеїв (`{x && <XModal>`, `{x ? <XModal>`, рукописний
`<div className="overlay">`) і операнди `busy` з одним кроком розіменування.
Умова: **як тільки екран почав перелічувати оверлеї в `busy` — мусить
перелічити їх УСІ**; перелік «кого перевіряти» звідси виводиться сам, тож два
законні задуми `busy` (дошки «щось відкрито» / форми «дія в польоті»)
розрізняються без рукописного списку файлів.

⚠️ **Найцінніше в пакеті — знахідки ревʼю ПО НЬОМУ Ж.** Обидва раунди дали
справжні дефекти в моїй уже написаній правці, і всі виправлені тут же:
банер міг сказати «змінено з 1 вересня на 1 вересня» (поправка туди-назад при
накопиченому `from`); хвіст про «по» називав ПРИЧИНУ, якої екран знати не може;
пін `role="status"` у вікні 200 символів лишався зеленим, коли роль належала
сусідньому вузлу; піни на `const badRange/pastWindow/valid` доводили лише
існування рядків, а не те, що `valid` доходить до кнопки і до `handleSave`;
вкладеність хвоста в банер **вимірювання показало недоведеною** (винесений
назовні хвіст лишався в межах вікна, пін був зелений). Найгірше — сканер Г1-D
не бачив тернарника і **КРАВ гейт у сусіда**: `CaseModal` отримував `modalOpen`
від `BookingModal` трьома рядками вище, дір не зʼявлялось, і зняття
`openCaseId` було ПОВНІСТЮ зеленим. Тобто сторож рапортував про покриття,
якого немає.

⚠️ **Борги, названі явно (не «зробимо потім», а «ось де воно живе»)** — перший
із них, **Г1-E, закрито в с53** (пакет 10), решта жива:
* **Г1-E** — дошки (`QueueBoard`, `RadiologistBoard`) не передають `onShift` і
  викидають `pendingShift`: коли `busy` знімається, доба застосовується МОВЧКИ.
  Плюс поки перенесення відкладене, `isToday` хибний — за оверлеєм дошка
  малюється архівом і звук «пацієнт готовий» вимкнено. Це властивість самого
  `busy` (діяла й для решти 17 оверлеїв), але кейс робить вікно найдовшим.
  **Що показувати на головній дошці — рішення продуктове, за власником.**
* **Г1-F** — Г1-A на «простих» формах (`WaitlistModal`, `RescheduleModal` з
  `busy: saving`): поправка під час збереження відкладається, модалка
  закривається, хук розмонтовується — `onShift` не викликається НІКОЛИ.
* **Г1-G** — умова `from !== to` є лише у двох нових банерах; у `BookingModal`,
  `RescheduleModal`, `ReferralPortal`, `CallListBoard` та сама вада жива.
* **U-80г** — у `falsify-u72` адресні 26 зі 104; решта на рівні спек-ФАЙЛУ.
  Лишились без адресності `f4-2`, `f4-8`, `u70`.
* **Названа межа сканера:** лексичне згадування ≠ причинна залежність
  (`busy: a || (FLAG && (b || c))` при `FLAG = false`). Аналізу потоку даних по
  джерелу немає — це межа, а не пропуск.

## ЩО БУЛО В с52 — пакети 7 і 8

**Пакет 8 — U-80б (`docs/audit/PR-U-80b-named-guard.md`).** Мутація тепер
називає ІМʼЯ тесту-сторожа, який зобовʼязаний її спіймати. Досі позиція
зараховувалась за фактом «набір почервонів» — байдуже який тест, тобто доводила
«щось зламалось», а не «спрацював гвард, заради якого її писали».

112 позицій у восьми стендах. Дві форми: окрема таблиця `EXPECT` (`u20`, `u30`,
`u15`) із симетрією в обидва боки, і поле `expect` у рядку мутації (`u61`,
`u67`, `f4-portal`, `f4-affected`, `f4-incident-window`) з передпольотною
перевіркою.

⚠️ **Інвентар заповнювався НЕ з прогону.** Для кожної позиції спершу названо,
який тест ЗОБОВʼЯЗАНИЙ її спіймати, і лише потім звірено вимірюванням.
Розбіжність «чекав А, почервонів Б» — це ЗНАХІДКА, а не привід переписати
очікування. Так знайшлись три дефекти:

1. `realtimeSubscriptionSurface`: тест «PII-таблиця не може прикинутись no-pii»
   на цю підміну НЕ реагує — його тіло охороняє регулярку `SENSITIVE`, а не
   список підстав. Перша половина назви описувала СУСІДНІЙ тест. Назву
   виправлено.
2. `queueStatus`: тест «inProgressBlockReason має гілку clash із причиною» мав
   ОДИН ассерт — на наявність літерала `r.code === "clash"`. Лишити гілку і
   повернути з неї `null` не бачив жоден із 57 тестів набору. Додано ассерт
   «гілка ПОВЕРТАЄ рядок» + позиції M5/M6 на обох дошках.
3. Три позиції рухали КЛЮЧІ списку `UNFILTERED_ALLOWED`, а ЗНАЧЕННЯ не чіпала
   жодна — тоді як саме воно вирішує, чи перевірятиме машина рядок («no-pii» йде
   в схему, «accepted» — рішення людини). Словник пінимо цілком, додано S9.

**Три сторожі проти обходу самої адресності:** пін `EXPECTED_RED` (правило
«`expect` заборонений у зеленого рядка» саме ж і дає спосіб погасити червону
позицію — перевести в `green: true` і зняти сторожа, без жодного сліду); власний
рядок «N/N адресних» замість завищеного числа з `verdictOf`; окремий діагноз
«СТОРОЖА З ТАКИМ ІМЕНЕМ НЕМАЄ» замість «ЧУЖИЙ спек» для опечатки в стенді.

**Чотири стенди перейшли з `a.title` на `a.fullName`:** у `describe.each` обидві
дошки дають ОДНАКОВІ `title`, тобто мутація в `QueueBoard` доводилась би червоним
від `RadiologistBoard`.

⚠️ **Борг звідси:** `f4-2`, `f4-8`, `u70`, `u72` перевіряли ФАЙЛ спека
(`redInNew`), а не імʼя тесту — «почервоніло в моєму спеку», байдуже що саме.
Це наступний зріз U-80б. **У с52 пакет 9 частково закрив `u72`**: там завелось
поле `expect` і чесний рядок «ПІДСУМОК: N/M адресних», але лише для 26 мутацій
зі 104. Лишились цілком неадресними `f4-2`, `f4-8`, `u70`.

## Пакет 7 — U-74 ч.2 / U-80в

**(`docs/audit/PR-U-74-part2-stand-revision.md`.)**
Ревізія стендів фальсифікації: 19 із 26 мертвих позицій переякорено, 7 заморожено.

Три сторожі самої ревізії, кожен із названим червоним тестом і зеленою базою:

1. **Дерево між стендами звіряється ПО ВМІСТУ.** У с51 `falsify-u15` помер із
   кодом 7 і лишив `StudyEditModal.tsx` мутованим — наступні стенди чесно
   показали ✅ на отруєному файлі. Перша моя редакція перевірки порівнювала
   список шляхів із `git status` — і була сліпа рівно там, де найнебезпечніше:
   якщо стенд мутував файл, який і БЕЗ НЬОГО стояв « M », рядок статусу не
   мінявся. Зонд підтвердив: список «після отруєння» збігався з базовим
   поіменно. Тепер порівнюються посекційні хеші `git diff HEAD`.
2. **Запасний сторож бачить ПІДСУМКОВИЙ ВИРОК стенда.** Він розбирає текст на
   випадок знятого `process.exitCode`, і над ним стояв мій коментар, що цей
   режим закритий. Я цього не перевіряв. Коли прогнав вивід усіх двадцяти проти
   регексів — ЖОДЕН стенд не ловився за текстом свого вироку.
3. **Підлога на кількість адресних мутацій.** «0/0 адресних» і «все заморожено»
   були зеленими: `verdictOf(lines, 0)` вважає 0/0 нормою, а процедура для
   протухлого якоря — дописати позицію в інвентар заморозки. Тепер нуль адресних
   або нерозпізнаний підсумок = ЧЕРВОНИЙ, число видно окремою колонкою.

**Заморозка тепер називає спадкоємця.** Доказ «імені немає в цих файлах»
видавався за доказ «дефекту немає». `incidentGapCode` справді зникла з
`lib/studies.ts`, але робота жива — переїхала в `stoppedIncidentsGap`
(`lib/incidents.ts`) з U-56. Кожна заморожена позиція тепер несе `movedTo` (де
живе робота) і `guardedBy` (хто її стереже), і стенд перевіряє обидва.

⚠️ **Борг звідси, HIGH:** `expectRe` («почервонів САМЕ названий сторож») немає в
`falsify-u20`, `falsify-u30`, `falsify-u15`. Для `u20` це не теорія: у `SPECS`
є і `tests/readErrorTrust.test.ts`, який пінить ТІ САМІ рядки, що мутують N01,
N02 і N10 (`readErrorTrust.test.ts:684`, `:685`, `:716`). Приберіть із
`offScheduleConsent.test.ts` перевірки U-20 — мутації все одно почервоніють, і
стенд зарахує це собі в успіх.

## ЩО БУЛО В с51 — ШІСТЬ пакетів

**Пакет 6 — Г1-A (`docs/audit/PR-G1A-case-day-stop.md`).** HIGH, знахідка
ревʼю Г; рішення власника — «зупинити збереження, вирішує людина».

Поки оператор набирає КЕЙС (до 12 досліджень), `busy` тримається кроками і
перенесення доби навмисно відкладається — це правильно. Смертельним було інше:
відкладений ключ жив ЛИШЕ в ref-і хука, тож після `createCase` форма
закривалась, хук розмонтовувався, і ключ помирав. `onShift` не викликався
НІКОЛИ — до 12 записів ішли зі старою датою мовчки, і при «ПК спішить» не
заперечував жоден гард.

Зроблено: `stepClockShift` віддає `pending: {from,to}`; чиста `pendingShiftOf`
перекладає доби КЛІНІКИ в доби ПОЛЯ через той самий `followedDay` і ним же
відсікає випадки, де показувати нічого; у двох формах із кейсом — СТОП:
збереження заблоковане, банер називає обидві доби і велить переспитати
пацієнта, дві кнопки («Перенести кейс на X» рухає І поле, І кожен крок;
«Залишити X» — свідоме підтвердження, без нього оператор був би замкнений).

⚠️ **Стенд знову знайшов дві мої помилки:** пін гейта був «десь є» замість «є
там, де треба» (у кожному файлі ДВА `disabled={!!dayStop ||`, тож мутація, що
знімає гейт із КЕЙСА, лишалась зеленою через другий збіг — той самий клас,
проти якого в пакеті 5 заведено сканери, відтворений через годину), і протухлі
якорі CALL_BM/CALL_RP.

## ЩО БУЛО В с51 — попередні пʼять пакетів

**Пакет 5 — F2/F7 (`docs/audit/PR-F2-F7-silent-shift.md`).** Виконання ДВОХ
рішень власника по знахідках ревʼю Б із пакета 4.

- **F2 (MED-HIGH).** `CallListBoard` був ЄДИНИМ екраном, де дата одночасно
  вимовляється пацієнту голосом, задає цілі НЕЗВОРОТНОГО масового «Всіх
  підтверджено» і мінялась МОВЧКИ. Тепер: банер над списком з обома днями +
  гейт на масову кнопку, який знімає лише людина.
- **F7 (MED).** Банер називав лише НОВУ дату і інструктував про ПОЛЕ ФОРМИ.
  `onShift` став `(nextDay, prevDay)`; другою добою їде `value` — те, що
  оператор БАЧИВ У ПОЛІ. Текст велить назвати нову дату ПАЦІЄНТУ.

⚠️ **Два ревʼю після лікування знайшли ПʼЯТЬ дефектів самого лікування** —
усі виправлені в пакеті. Найпоказовіший: `setConfirmAllAsk(false)` у `onShift`
виявився МЕРТВИМ викликом (`confirmAllAsk` входить у `anyBusy`), а мій
коментар стверджував «діалог закривається», і пін ЗАКРІПЛЮВАВ мертвий рядок.

**СКАНЕРИ ПОВНОТИ — головне в пакеті.** `CALL_SITES`/`ON_SHIFT_SITES` були
рукописними переліками, і саме цим механізмом `CallListBoard` півроку міняв
день мовчки. Заведено три перевірки, що знімають ВЛАСТИВІСТЬ із дерева:
заморозив «сьогодні» без правила; звірка переліку з деревом в обидва боки;
кожен `onShift` бере ДВІ доби (тип не рятує — TS дозволяє меншу арність).

## ЩО БУЛО В с51 — попередні чотири пакети

**Пакет 4 — U-77/78/79 (`docs/audit/PR-U-77-78-79.md`).** Дірки в сторожах часу.
Закрито ЧОТИРИ класи, і ТРИ з них знайшли ревʼю, а не я:

- **U-77, HIGH.** Проводку хука не сторожив НІХТО (нуль згадок `prevOffsetRef`,
  `pendingKeyRef`, `applyRef` у `tests/`). Я закрив її пінами по джерелу і
  написав, що поведінкового тесту «тут бути не може». **Ревʼю А спростувало
  рахунком:** піни тримали 4 факти з 10, а мутація
  `nowOffsetMs: nowOffset` → `nowOffsetMs: prevOffsetRef.current` вбиває U-70 і
  U-72 ЦІЛКОМ і лишає зеленими всі чотири піни, tsc і eslint. Проводку винесено
  в чистий `stepClockShift`; пінами лишились рівно 4 факти, перелічені у файлі
  тестів поіменно, кожен із мутацією.
- **Фантомне перенесення, HIGH (ревʼю Б).** `applyFrom` беруть на момент
  відкладання, `applyTo` — на момент звільнення, рівності не перевіряв ніхто.
  Поправка, що з'їхала і повернулась під модалкою, давала `onShift` з ТІЄЮ
  САМОЮ датою: форма стирала обраний слот і брехала банером. **Мій власний тест
  звався «no-op за смислом» і закріплював протилежне.** Закрито перевіркою
  `if (to === pending)` + зустрічний зонд + M47/M48.
- **U-78, MED.** TZ прогону прибита `Europe/Kyiv`, і всі тести передавали ту
  саму зону як зону центру — тож «загубити `clinicTz`» нічого не міняло в
  жодному з ТРЬОХ викликів `wallDayKeyAt`. Закрито тестами з
  `America/New_York`; третій виклик (той, що будує `applyTo`) знайшло ревʼю А.
- **U-79, MED.** Поправка < 1 хв через північ; **відʼємний зсув не подавався в
  ядро ЖОДНОГО разу** (ревʼю Б) — при тому, що саме «ПК спішить» є причиною
  існування пакета.

⚠️ **Стенд знайшов ДВІ мої помилки в тій самій сесії:** протухлий якір M44
(фікс фантома виніс вираз у `const to` через годину після написання мутації) і
маскування мутації M39 власним фіксом (правильна поведінка збіглася зі
зіпсованою — довелось шукати НОВИЙ вхід, де зіпсована зона не вигадує
перенесення, а глушить справжнє). Обидві описані в PR.

## ЩО БУЛО В с51 — попередні три пакети

**Пакет 3 — U-80 (`docs/audit/PR-U-80-false-green-stands.md`).** U-74 зробив
вердикт стендів ГУЧНИМ, але у семи стендів ВХІДНІ дані вердикту були невірні:
чотири не мали гілки `crashed` (мутація, що зламала збірку, друкувалась як
«сторож спіймав»), три першого покоління читали звіт ПОПЕРЕДНЬОЇ мутації, не
мали базової лінії і зараховували «звіт не прочитано» в успіх. Полагоджено.
⚠️ Ревʼю знайшло чотири HIGH, і ТРИ з них були дефектами самого лікування —
усі виправлені в пакеті. Ревізія після лікування дала ТІ САМІ цифри: пакет
профілактичний, нових дефектів не виявив.

**Пакет 2 — U-74 (`docs/audit/PR-U-74-falsify-verdict.md`).** Правило
«відхилений якір = ЧЕРВОНИЙ вердикт» введено у ВСІ 20 стендів (спільний
`scripts/lib/falsify-verdict.mjs`, 13 тестів на сам вердикт, новий
`scripts/falsify-all.mjs` — один прогін усіх стендів, ≈21 хв, НЕ гейт).
Під протухлим якорем знайшлись ДВІ справжні діри — закриті поведінковими
тестами і засторожені мутаціями M32/M33 у falsify-u72.

⚠️ **Перша ж ревізія знайшла ще 26 мертвих позицій** (див. таблицю вище) — це
**U-74 частина 2**. І ще пʼять номерів завело ревʼю: **U-77** (проводка
`useClockDayShift` не сторожиться нічим — три однорядкові мутації вбивають
U-70 і U-72 цілком), **U-78** (`clinicTz` не закріплений), **U-79** (немає
переносу доби хвилинною поправкою), **U-80** (сім стендів дають ЛОЖНЕ зелене),
**U-81** (звʼязка «вердикт → код повернення» не перевіряється).

**Пакет 1 — живі перевірки вузла часу**

Розбір — `docs/audit/PR-live-checks-s51.md`. Коротко: вузол часу с50 на проді
працює (виміряно в обидва боки, обидва споживачі правила, відкладення під
модалкою, контроль «поправка без перетину доби дату не рухає»). Але прогін
коштував **трьох нових знахідок**, і вони важливіші за галочки:

- ⚠️ **U-74 (HIGH)** — у `scripts/falsify-u70.mjs` пʼять мутацій відхиляються
  по протухлих якорях (M14, M15, M16, T3, T4), серед них сторож РІВНО на
  помилку, заради якої стенд писався. Стенд не червоніє від відхиленого якоря.
- ⚠️ **U-76 (MEDIUM)** — `server_now` не покрита `invariants_check()`; будь-яке
  майбутнє пере-створення тихо повертає прод на годинник ПК. Міграція 0170.
- **U-75 (MEDIUM)** — склад `anyModalOpen` (17 станів) не сторожиться.

**Рішення власника в с51:** branch protection на `main` — НЕ вмикати поки;
ротація ключа — під час реального продакшену і реєстрації домену RadFlow
(тобто вердикт аудиту свідомо лишається не вище CONDITIONAL GO).

---

## ЩО БУЛО В с50 — одинадцять пакетів, одна міграція (0169)

Повний перелік із розборами — `claude/radflow-handoff.md` (блок «СОСТОЯНИЕ НА
КОНЕЦ с50» — нижче за блоками с54…с51) і `docs/audit/PR-*.md`.

1. **Ф2** — фаза 2 аудиту закрита (тулчейн, залежності, секрети).
2. **Початок фази 4** — харнес конкурентності прогнаний УПЕРШЕ за проєкт
   (існував із с32 і жодного разу не ганявся). Гонка за слот і за кабінет —
   **PASS** на живому проді. `cas` — чесний SKIP.
3. **Ф4-1/Ф4-4/Ф4-6** — «постраждалі» від простою.
4. **Ф4-3/Ф4-10/Ф4-11** — портал направника, лист очікування.
5. **Ф4-5/Ф4-7/Ф4-9** — вікно простою: клієнт як дзеркало живого гарда.
6. **U-61** — поверхня realtime-підписок; міграція написана і **скасована до
   накату** (перевела б систему в гіршу конфігурацію).
7. **Ф4-2** — секунди в дзеркалі `lateCallClash` ↔ гард 0129. **Міграція НЕ
   знадобилась: права виявилась серверна сторона.**
8. **Ф4-8** — годинник СЕРВЕРА для таймера і звуку. **Міграція 0169.**
9. **U-67(б)** — мертвий діалог «Викликати все одно» знято (рішення власника).
10. **U-70** — настінний канон на годинник бази + правило «дошка слідує за
    сьогодні». Заразом закрито пункт (а) U-67.
11. **U-72** — те саме правило на СЕМИ формах, зокрема тих, що пишуть
    `scheduled_date`.

### Вузол «час» — головний результат сесії, і його треба тримати в голові

Браузер більше НЕ авторитет часу: ні для порівняння з серверним моментом, ні
для показу користувачеві. Зсув міряється проти `public.server_now()` (0169),
живе в `lib/serverClock.ts`, вимірювач — `components/ServerClockSync.tsx`
(**один** на застосунок, у кореневому layout).

⚠️ **Правило «стан, виведений із сьогодні, слідує за поправкою» —
`lib/useFollowToday.ts`, ОДИН екземпляр на дев'ять екранів.** Своєї копії не
заводити. Форма, що пише дату в БД, зобовʼязана при перенесенні ще й скинути
обраний слот і сказати про це (`onShift`): тихе перенесення гірше за протухлу
дату — оператор уже назвав пацієнту день і час уголос.

---

## ВІСІМ РАЗІВ ЗА с50 Я ПОМИЛИВСЯ ОДНАКОВО — це головне, що варто нести далі

Щоразу: зробив твердження, прочитавши ЧАСТИНУ картини.

1. **Ф4-5** — перебільшив власну знахідку (кламп робив обидві гілки однаковими).
2. **U-61** — прочитав рішення про ДОСТАВКУ події і не дочитав до збирання
   PAYLOAD: «витік ПІБ» виявився неіснуючим, а написана міграція — шкідливою.
3. **Ф4-2** — заявив включення множин «клієнт блокує ⊇ сервер відхилить» БЕЗ
   умов, забувши про затримку між рішенням клієнта і серверним `now()`.
4. **Ф4-8(а)** — скопіював форму міграції 0168 і загубив її СЕНС: ассерт ACL у
   тій самій транзакції, введений ОДНУ МІГРАЦІЮ ТОМУ як MAJOR.
5. **Ф4-8(б)** — міряв RTT тим самим годинником, який перевіряю: зіпсована
   проба виглядала ШВИДШОЮ і вигравала відбір за мінімумом.
6. **U-70(а)** — вивів слак виклику з ОДНОГО джерела помилки (`rtt/2`), забувши,
   що зсув менший за `CLOCK_MIN_APPLY_MS` ОБНУЛЯЄТЬСЯ.
7. **U-70(б)** — правило слідування порівнювало добу з минулим запуском ефекту,
   тож справжня північ лишала протухлий ключ.
8. **U-72** — коментар у `WaitlistModal` стверджував те, що правда лише в
   частині випадків; **і окремо — СПИСОК ЕКРАНІВ у самій знахідці U-72 був
   невірним**: два екрани з шести дефекту не мали, три постраждалі в списку
   були відсутні.

**Правила, виведені з цих восьми:**

- дочитувати до кінця гілки;
- не формулювати включення множин, не перелічивши, що лежить МІЖ сторонами;
- не копіювати форму сусідньої міграції, не перечитавши її обґрунтування;
- **формулювання САМОЇ знахідки перевіряти так само, як код.** Ф4-2 була
  заведена як «потрібна міграція» (права була серверна сторона), U-72 — з
  невірним списком екранів. **Список зі знахідки — гіпотеза, а не результат.**

### Чотири правила про СТОРОЖІВ (виведені в U-70 і U-72)

⚠️ **Тест, який ПОВТОРЮЄ перевіряну формулу, не перевіряє нічого.** Обидві
помилки U-70 були зеленими саме тому. Перевіряє поіменний розбір доданків плюс
поведінкове підтвердження КОЖНОГО доданка окремо.

⚠️ **Тест, що порівнює результат САМ ІЗ СОБОЮ, — не тест.** У U-72 два виклики
`decideShift` поспіль дають однакову відповідь і при справному, і при зламаному
коді. Розводити треба ВХОДИ, а не повторювати виклик.

⚠️ **Протухлий якір у стенді фальсифікації виглядає майже як успіх.** Три якорі
в `falsify-f4-2.mjs` писали «ЯКІР НЕ УНІКАЛЬНИЙ (0)» — мутації відхилялись,
сторожі не перевірялись. Карта `FILES` стенда мусить покривати ВСЮ ланцюжок, а
не лише «свої» файли: у U-72 стенд першої редакції фізично не міг зламати
розсилку годинника, від якої обидва пакети живі або мертві цілком.

⚠️ **Зелений зонд, який почервонів, частіше правий, ніж зонд.** У U-72 стенд
двічі знайшов дефект не в коді, а в самому стороже. Загалом за сесію стенди
знайшли **чотири** дефекти в моїх же сторожах.

### Три уроки с51 — усі про ПРИЛАД, а не про продукт

⚠️ **Прилад треба калібрувати ДО висновків, і калібрування треба повторити.**
Я оголосив фазу вимірювача сталою (504 мс) за ОДНИМ заміром, а вона гуляла
0,34–1,21 с — і на цій «сталій» побудував висновок про точність 10 мс там, де
сам модуль обіцяє 2125 мс (`CLOCK_WORST_ERROR_MS`). Знайшло ревʼю А.

⚠️ **Імітація має названі межі, і вони бувають усередині перевіряного модуля.**
Підміна `Date` зсувом > 5 хв робить `stale` в `applyClockEstimate` завжди
істинним — тобто ціла гілка («гірша оцінка не заміняє кращу») цим методом
непіддослідна В ПРИНЦИПІ. Я записав це в «руки не дійшли» замість «інструмент
не може». Знайшло ревʼю А.

⚠️ **Живу перевірку теж треба фальсифікувати — питанням «а при СТАРІЙ редакції
вона була б зеленою?».** Перевірку переносу доби я поставив ПІСЛЯ справжньої
півночі — стані, у якому чинне правило і та його редакція, яку ревʼю А визнало
дефектом у с50, дають ОДНАКОВЕ спостереження. Знайшло ревʼю Б. Різничний
дослід описаний у `PR-live-checks-s51.md` §4.

---

## ПЕРШИЙ ПАКЕТ с55 — УЗГОДИТИ З ВЛАСНИКОМ ПЕРЕД ПРАВКОЮ

⚠️ **Спитай, перш ніж кодити.** Перелік нижче — не черга «згори вниз», а меню.
У с52 власник тричі обирав НЕ те, що стояло першим.

**Моя рекомендація на с55 — U-80г** (доробити прилад: адресність для `f4-2`,
`f4-8`, `u70` плюс сусіди Г1-G), або **F3/F4/F6** (три MED в одному вузлі
часу). Г1-E, Г1-G закриті в с53, Г1-F — у с54.

Стартовий промпт для с55 (англійською, у форматі повідомлення власника з
с54) — **`claude/session55-start-prompt.md`**; цей файл він додає як вкладення.
Числа в ньому — очікування, звіряти запитом так само, як цей файл.

### Відкритих питань власнику НЕМАЄ — обидва закриті пакетом 13 (с54)

* **Тексти `returned`** у пʼяти екранах затверджені власником у новій редакції
  («🕐 Годинник центру уточнено — дата лишилась X, але обраний час скинуто.
  Оберіть час заново»; дошка обдзвону — «день обдзвону лишився X, але на дошці
  встиг постояти інший день»). Механізм («уточнювався двічі») прибрано. Піни по
  `text()` тримають те, чим `returned` відрізняється від `moved`.
* **`CLOCK_SKEW_MSG`** — власник обрав КОРОТКУ редакцію з одним виходом
  («Перевірте дату і збережіть ще раз»). ⚠️ Названа ціна, прийнята свідомо:
  коли вимірювач годинника мовчить зовсім, поле не переїде і повтор дає ту саму
  відмову по колу; вихід «обрати дату вручну» лишається в поведінці, але не в
  підказці. Записано в шапці константи і в PR-доці.

Мерж пакета 13 — `f2ae492`; ревізія 21/21, 479 адресних; деплой виміряний.

### Закрите в с54 — не піднімати

Вікно ВІДКЛАДЕНОГО перенесення на дошках (залишок Г1-E) — **власник визнав, що
це не проблема; питання знято з черги.** Виміряна втрата в цьому вікні —
лише вимкнений звук «пацієнт готовий».

Борги з пакета 9 (деталі — у `docs/audit/PR-G1-BCD-silent-day-shift.md`):

* ~~**Г1-E (HIGH, продуктове)**~~ — **ЗРОБЛЕНО в с53, пакет 10.** Див.
  `docs/audit/PR-G1E-board-day-shift.md`.
* ~~**Г1-F (MED)**~~ — **ЗРОБЛЕНО в с54, пакет 12.** Див.
  `docs/audit/PR-G1F-server-clock-trust.md`. Лікували НЕ в формі (її на успіху
  закриває батько — 8 місць у 4 файлах), а на сервері: заявка про годинник +
  гард у Server Actions. Міграції не знадобилось. Лишились названі межі —
  `createBooking` гарда не має, зсув менший за ~32 с не ловиться.
* ~~**Г1-G (MED)**~~ — **ЗРОБЛЕНО в с53, пакет 11.** Див.
  `docs/audit/PR-G1G-one-day-shift-rule.md`. ⚠️ Вердикт став ТРИЗНАЧНИМ
  (`dayShiftNoticeVerdict`: `none`/`moved`/`returned`) — перша редакція з булем
  відкривала гейт масової дії на «туди-назад». Лишилось: затвердити тексти
  `returned` у власника; борг U-80г-сусіди (сліпота `not.toMatch` до `!=`,
  `===`, псевдонімів; піни без мутацій; `FROZEN_TODAY` не бачить
  `useState<T>(`; `busyOperands` міряє склад, а не оператор; `overlayGates`
  знає чотири суфікси) — усе назване в PR-доці, нічого з цього не закрито.
* **Названа межа сканера Г1-D (LOW, документована)** — лексичне згадування ≠
  причинна залежність: `busy: a || (FLAG && (b || c))` при `FLAG = false`
  лишається зеленим. Аналізу потоку даних по джерелу немає і не планувалось.

Решта кандидатів, від найбільшого до найменшого:

1. ~~**U-74 частина 2 + U-80в (HIGH)**~~ — **ЗРОБЛЕНО в с52, пакет 7.**
   Ревізія: 20/20 зелених, 478 адресних мутацій. Див.
   `docs/audit/PR-U-74-part2-stand-revision.md`.
1б. ~~**U-80б (HIGH) — `expectRe` у восьми стендах**~~ — **ЗРОБЛЕНО в с52,
   пакет 8.** 112 позицій називають імʼя тесту-сторожа. Див.
   `docs/audit/PR-U-80b-named-guard.md`.
1в. **U-80г (HIGH) — адресність для решти трьох.** `f4-2`, `f4-8`, `u70`
   перевіряють ФАЙЛ спека (`redInNew.length > 0`), а не імʼя тесту:
   «почервоніло в моєму спеку», байдуже що саме. Механізм готовий і
   відпрацьований у девʼяти стендах — переносити прямо (`expect` у рядку +
   передпольотна перевірка + `EXPECTED_RED` + власний рядок «N/N адресних» +
   `fullName`).
   ⚠️ **`u72` закритий ЧАСТКОВО:** там уже є `expect`, `fullName`,
   `EXPECTED_ADDRESSED` і чесний рядок «ПІДСУМОК: N/M адресних»; адресних тепер
   **69** (`EXPECTED_ADDRESSED = 69`; у с52 було 26 — приріст дали Г1-G у с53
   і Г1-F у с54), але стенд виріс до ~140 позицій, і решта червоних (≈60) далі
   тримаються спек-файлом. Дочистити `u72` до кінця — окремий шматок цього ж
   пункту, і зразок уже лежить у самому файлі (`scripts/falsify-u72.mjs`).
   ⚠️ У `f4-8` шість рядків `green: true` із 23 — після переносу число
   «адресних» у ревізії впаде з 23 до 17, і це буде виправлення рахунку.
1г. **Борги ревізії, менші.**
   - `red.some(expectRe)` доводить «названий сторож СЕРЕД червоних», а не
     «червоний лише він». Побічні червоні невидимі — приклад: N01 у `u15`
     червонить і `incidentDurCap`, і `offScheduleConsent`, обидва законно.
   - `green: true` доводить лише «набір лишився зеленим»; що правка справді
     нейтральна — не доводить ніщо. `EXPECTED_RED` лише не дає ТИХО переводити
     червоні позиції в зелені.
   - Чотири рефакторні позиції зелені ЗА ПОБУДОВОЮ (правлять коментар, а спеки
     читають код через `codeOf`). У самих рядках це вже написано.
   - `falsify-f4-8` кличе `spawnSync` без таймауту.
   - пін `labelFor` у `readErrorTrust.test.ts` має лінивий `[\s\S]{0,140}?`,
     який не тримає клаузулу U-15 р2-A4. ⚠️ Сам клауз стереже
     `incidentDurCap` → «підпис межі має власну гілку простою» (звірено в с52),
     тож дірки покриття тут немає — є зайво слабкий пін.
2. ~~**U-77 / U-78 / U-79**~~ — **ЗРОБЛЕНО в с51, пакет 4.** Див. вище і
   `docs/audit/PR-U-77-78-79.md`.
2а. **ЗНАХІДКИ РЕВʼЮ, НЕ ЗАКРИТІ.** Не «на потім за бажанням» — у кожної
   названа ціна. ~~F2, F7~~ — **ЗРОБЛЕНО в с51, пакет 5.**
   - ~~**Г1-A (HIGH)**~~ — **ЗРОБЛЕНО в с51, пакет 6.** Власник обрав
     «зупинити збереження, вирішує людина». Див. `PR-G1A-case-day-stop.md`.
     ⚠️ Лишилась НАЗВАНА МЕЖА: «Перенести кейс» не перевіряє зайнятість тих
     самих слотів на новій добі — розрахунок на `caseBusyWindows` і серверний
     `CASE_PATIENT_OVERLAP`. Якщо колись знадобиться закрити — це окремий крок.
   - **F3 (MED)** — `pinnedKey: initialDate` глушить правило на дошках, коли
     оператор прийшов deep-link'ом на запис СЬОГОДНІШНЬОГО дня: `curKey ===
     pinnedKey` з першого рендера, `followedDay` вертає `null` завжди. Пін ще й
     пере-взводиться, коли оператор повернувся календарем на ту саму дату.
     Пін мусить означати «користувач ще не йшов із цієї дати» (прапорець), а не
     збіг значень.
   - **F4 (MED)** — `ReferralPortal.tsx:203–211`: після вибору центру з іншим
     календарним днем `bookDate` лишається від попереднього центру, а
     `clinicTz` уже новий — `followedDay` вирішує, що дату обрав користувач, і
     мовчить до кінця сесії. Тобто на ЄДИНОМУ мультицентровому екрані правило
     вимикається саме в цільовому випадку.
   - **F6 (MED)** — застряглий зсув не має терміну придатності (`stale` вирішує
     лише, чи ПРИЙНЯТИ нову оцінку, і ніде не анулює чинну), а стрибок СИРОГО
     годинника ПК правило не бачить узагалі: `wallNow()` рухають дві величини,
     а епоха реагує на одну. Вікно розриву — до `RESYNC_MS` (10 хв). ⚠️ Шапка
     `serverClock.ts:79–82` стверджує протилежне — доку теж правити.
   - ~~**Г1-B / Г1-C / Г1-D (MED)**~~ — **ЗРОБЛЕНО в с52, пакет 9.** Див.
     `docs/audit/PR-G1-BCD-silent-day-shift.md`. Г1-D дав справжню знахідку:
     `openCaseId` (CaseModal) не входив у `anyModalOpen`, тобто під відкритим
     кейсом хоткеї стріляли в дошку позаду. Похідні борги — Г1-E, Г1-F, Г1-G
     угорі цього розділу.
   - **Побудовані `setCall` (MED)** — банер `dayShifted` гейтить лише масову
     кнопку; статуси по одному оператор може ставити далі. Пара до F2 неповна,
     обговорити разом із Г1-A.
3. **U-76 (MEDIUM) — міграція 0170:** перевірка №16 в `invariants_check()` на
   ACL і тип `public.server_now()`. Єдина знахідка ревʼю, що працює без людини.
5. **Довести глибокий аудит до вердикту.** Фази 6 і 8 не починались.
   ⚠️ Вище CONDITIONAL GO вердикт бути не може — власник свідомо привʼязав
   ротацію ключа до реального продакшену і реєстрації домену.
6. **Дорешта живих перевірок.** Найдешевший великий крок — **один прогін на
   `RescheduleModal` з обраним слотом**: закриває `offsetDays: 1`, `onShift`
   (скидання слоту) і `busy` разом. Далі — Ф4-8 п.1/п.2 (кільце таймера і звук,
   потрібен пацієнт `in_progress`) і Ф4-2 (межі вікна виклику).
7. ~~**U-75 (MEDIUM)** — склад `anyModalOpen` звести в літерал `OVERLAYS` + пін
   на кількість ключів.~~ — **ЗАКРИТО ІНАКШЕ в с52, пакет 9, і це варто
   прочитати.** Літерал `OVERLAYS` був би ДРУГИМ екземпляром переліку «що зараз
   відкрито» — рівно тим, від чого застерігає коментар над `anyModalOpen`.
   Замість переліку заведено ВЛАСТИВІСТЬ: сторож рахує гейти оверлеїв прямо з
   дерева і звіряє з операндами `busy`. Напруга «суворий пін ловить підміну, але
   падає від перейменування» знята сама: перейменування стану міняє обидві
   сторони однаково, а нова модалка — лише ліву.
8. **U-81 (MEDIUM)** — тест, що читає всі `scripts/falsify-*.mjs` і вимагає
   наявності `process.exitCode`: зараз звʼязка живе у 20 незалежних копіях.
9. **`cas`-сценарій** `scripts/race-check.mjs`. Потрібен токен живого персоналу
   в `RADFLOW_USER_JWT`; **у переписку токен не потрапляє**.
10. **U-62** — пакет по `useRealtimeRefetch` (MEDIUM/LOW).
11. **U-65 + U-66** — міжтенантний оракул видалень. ⚠️ U-65 у с51 **підтверджено
    на живому проді**: підписка без фільтра реально є в `realtime.subscription`.
12. **Дрібні LOW пачкою:** U-59, U-60, U-63, U-68, U-69, U-71, U-73.
13. **Протухлий ФЕТЧ** (`CeoDashboard`, `SearchScreen`).

## Інструмент, що зʼявився в с51

```
node scripts/falsify-all.mjs                # ревізія ВСІХ 21 стендів (≈31–32 хв у с54, НЕ гейт)
node scripts/falsify-all.mjs u70 u72        # лише названі
node scripts/falsify-all.mjs --allow-dirty  # є незакомічені правки
```
Виходить одиницею, якщо хоч один стенд червоний; **двійкою** — якщо ревізія
взагалі не почалась (брудне дерево без `--allow-dirty`, зламаний `git diff`).

Всередині (с52): пін на кількість стендів (`EXPECTED_STANDS` = 21 з с54),
таймаут **45 хв** на стенд (с53; спочатку було 15 — `u72` у нього не вкладався),
звірка дерева ПО ВМІСТУ після КОЖНОГО стенда, розбір підсумкового вироку
стенда за текстом і **підлога на кількість адресних мутацій** — стенд, який
прогнав нуль або чий підсумок не розпізнано, ЧЕРВОНИЙ.

**Відкриті питання власнику:** закритих у с51 двох (branch protection, ротація
ключа) більше не піднімати — рішення записані вище.

✅ **F2 і F7 закриті в с51 (пакет 5)** — власник обрав «банер + скидання, як у
формах» і «назвати обидві дати + веліти переспитати пацієнта». Зроблено.

✅ **Г1-A закрито в с51 (пакет 6)** — власник обрав «зупинити збереження,
вирішує людина». Зроблено.

**Відкритих питань власнику НЕМАЄ.** Наступний пакет узгоджувати з нуля.

Два `queue_entries` від 31.08 («тест мрт 0109», «тест кт 0109») власник
підтвердив як свої — питання закрите.

## ⚠️ УРОК с51, який дорожчий за обидва пакети

**Ревʼю знайшло дві МОЇ помилки в пакеті, який САМЕ ПРО ЦЕ.** Знімаючи мутацію
M15, я послався на чуже покриття, не звіривши, що це той самий дефект. І
оголосив зонд T4 «полагодженим», побачивши ЗЕЛЕНЕ, — а зелене було тому, що
стенд не ганяє спек, який на цю правку реагує.

Тобто: **правило, яке я щойно записав великими літерами в коді, я порушив на
першому ж застосуванні — двічі з чотирьох.** Не тому, що забув, а тому, що
перевірив частину картини і зупинився. Це той самий механізм, що дав вісім
однакових помилок у с50.

Практичний висновок для наступної сесії: **«зелене» від власного інструмента —
це вхідні дані, а не вердикт.** Перед тим як записати ✅, спитати: який спек
мусив би почервоніти, він у SPECS цього стенда взагалі є?

### Продовження уроку в пакеті 4 — той самий механізм, третій раз

У пакеті 4 я зробив це знову, і знову двічі. Обидва рази — **написав про свій
інструмент більше, ніж виміряв**:

1. **«Поведінкового тесту тут бути не може».** Написав це поруч із чотирма
   пінами по джерелу як «названу межу». Ревʼю А спростувало РАХУНКОМ: несучих
   фактів десять, піни тримали чотири, і одна однорядкова мутація вбивала обидва
   пакети, лишаючи всі чотири зеленими. Правда була «я не став виносити далі» —
   і винесення зайняло двадцять хвилин.
2. **Тест, який назвав властивість, якої немає.** «Поправка туди-назад: доба
   повернулась, перенесення — **no-op за смислом**» закріплював `applyFrom ===
   applyTo` — тобто фіксував як правильне те, що no-op НЕ БУЛО: форма стирала
   слот і брехала банером. Назва тесту стверджувала висновок, а тіло закріплювало
   спостереження, і вони суперечили одне одному ЦІЛУ СЕСІЮ.

Що з цього виносити конкретно:
- **Коли пишеш «тут інакше не можна» — це гіпотеза, і вона перевіряється
  спробою.** Один із двох HIGH цієї сесії був захований саме за такою фразою.
- **Назва тесту — теж твердження.** Якщо вона каже «no-op», а `toEqual`
  закріплює непорожній результат, то один із двох бреше. Перечитати назви своїх
  тестів як твердження, які треба довести, — дешева і, як виявилось, врожайна
  вправа.
- **Кількісний прийом ревʼю А варто привласнити:** перелічити несучі факти
  ПОІМЕННО і порахувати, скільки з них має сторожа. «Закрито пінами» без
  знаменника — не вимір.

### Пакет 5 — той самий механізм учетверте, і нарешті ліки від класу

У пакеті 5 ревʼю знайшло ПʼЯТЬ дефектів мого власного лікування. Найгірший —
знову той самий: `setConfirmAllAsk(false)` у `onShift` виявився МЕРТВИМ
викликом (`confirmAllAsk` входить у `anyBusy`, тож при відкритому діалозі
`onShift` не виконується взагалі), мій коментар стверджував «діалог
закривається», а пін ЗАКРІПЛЮВАВ мертвий рядок. Тобто я втретє за сесію
написав про інструмент більше, ніж виміряв.

**Але головне в пакеті 5 — не це, а знахідка ревʼю Г: ЛІКИ ВІД КЛАСУ.**
`CALL_SITES` і `ON_SHIFT_SITES` були РУКОПИСНИМИ переліками файлів. Вони
доводили, що перелічене підключено, і не знали нічого про неперелічене — саме
цим механізмом `CallListBoard` півроку міняв день мовчки. Заведено три
СКАНЕРИ, що знімають ВЛАСТИВІСТЬ із дерева (`readdirSync` + регекс + звірка в
обидва боки + нижня межа на кількість, щоб протухлий регекс не дав тихого
зеленого).

**Правило на майбутнє, дорожче за всі окремі фікси:** щоразу, коли сторож — це
СПИСОК, спитати «а що станеться з тим, чого в списку немає?». Якщо відповідь
«нічого» — список не сторож, а перепис. Ідіома в проєкті вже є чотири рази:
`roomScheduleRead`, `unreadChanges`, сканер U-37 і тепер `followToday`.

### Пакет 6 — правило порушено ЧЕРЕЗ ГОДИНУ після того, як його записано

Це найкорисніше спостереження сесії, і воно неприємне. У пакеті 6 я запінував
гейт виразом `disabled={!!dayStop ||` — одним на файл. У кожному з двох файлів
таких місць ДВА (кнопка кейса і кнопка збереження), тож мутація, що знімає гейт
із КЕЙСА — тобто повертає рівно той HIGH, заради якого пакет писався, — лишалась
ЗЕЛЕНОЮ через другий збіг.

Це буквально те саме «десь є» замість «є там, де треба», проти якого годиною
раніше я завів сканери повноти. Знання правила не спрацювало; спрацював СТЕНД.

Практичний висновок, вужчий і корисніший за попередній: **регекс без якоря на
МІСЦЕ — не сторож.** Якщо в файлі може бути більше одного збігу, пін мусить
називати, ЯКИЙ саме, і на кожен потрібна своя мутація. Перевірка проста: скільки
разів цей підрядок трапляється у файлі? Якщо більше одного — пін уже бреше.

### Пакет 7 (с52) — той самий механізм, ушосте, і цього разу в КОМЕНТАРІ

Три знахідки другого ревʼю виявились одним і тим самим: **я описував сторожа
ширше, ніж його доказ.** Не помилка в коді — помилка в тому, що біля коду
написано, і саме вона робить дірку невидимою для наступного читача.

1. Над запасним сторожем `falsify-all` стояв мій коментар: режим «зняли
   `process.exitCode`» закритий. Перевірка (прогнати вивід усіх 20 стендів
   проти регексів) зайняла хвилину і показала: не ловиться ЖОДЕН.
2. Заморожена позиція друкувала «знято з коду разом із ДЕФЕКТОМ», а перевірялось
   «немає ІМЕНІ у цих файлах». `incidentGapCode` під це підпадає, хоча робота
   жива під іншим іменем в іншому файлі.
3. Перевірка чистоти дерева порівнювала СПИСОК ШЛЯХІВ і називалась «дерево таке
   саме, як до стенда». Зонд показав: після отруєння список збігається з
   базовим поіменно.

Правило, вужче за попередні і перевіряється механічно: **коментар біля сторожа
мусить описувати те, що сторож РОБИТЬ, а не те, заради чого його ставили.**
Якщо в коментарі стоїть «дефект», а в коді — `indexOf(name)`, це вже розходження.
Перед тим як написати «цей режим закритий», прогнати ОДИН зонд, який його
відтворює: якщо зонд зелений — закритий, якщо ні — коментар бреше голосніше,
ніж мовчав би його брак.

### Пакет 8 (с52) — правило спрацювало вперше, і воно окупилось одразу

Пакет 8 будувався за правилом із пакета 7 навмисно: інвентар «мутація → її
сторож» заповнювався НЕ з прогону, а з формулювання кожної позиції — тобто
спершу твердження, потім вимір. Це коштувало кількох годин читання чужих
спеків замість «прогнати і записати, що почервоніло».

Окупилось на першому ж прогоні. Три рази вимір розійшовся з твердженням, і
щоразу винним був НЕ інвентар:

* тест, у якого перша половина назви описує СУСІДНІЙ тест
  (`PII-таблиця не може прикинутись «no-pii»` не реагує на цю підміну);
* тест, у якого назва обіцяє «із причиною», а тіло перевіряє лише наявність
  літерала — і повернути з гілки `null` не бачив жоден із 57 тестів набору;
* цілий напрямок без мутації і без сторожа (ЗНАЧЕННЯ списку дозволених, які
  вирішують, чи перевіряє машина рядок узагалі).

Якби `expect` заповнювався «з фактичних червоних», усі три пройшли б як
успіх — інвентар просто зафіксував би те, що є. **Розходження виміру і
твердження — це знахідка, а не привід поправити твердження.** Записую окремо,
бо спокуса «підігнати очікування під результат» виникає щоразу і виглядає як
економія часу.

### Пакет 9 (с52) — усьоме, і цього разу механізм спрацював на МЕНІ САМОМУ

Той самий клас, але з новим поворотом: у пакеті 9 «коментар описує намір, а не
дію» знайшлось **тричі в чужому коді** — і **ще шість разів у моєму власному,
щойно написаному**, причому знайшов не я, а два раунди ревʼю.

Що знайшлось у чужому коді (усе — твердження, яке було неправдою в момент,
коли його читали):
* коментар над `anyModalOpen` дослівно вимагав «покривати УСІ оверлеї» — а
  `openCaseId` там не було;
* шапка `useFollowToday` називала `onShift` обовʼязковим для форм — а два
  екрани його не передавали;
* `CALL_SITES` називався сторожем підключення — а пінував ІМʼЯ прапорця `busy`
  і мовчав про його склад.

Що знайшлось у моєму власному коді, вже після того, як я вважав пакет готовим:
* банер, який я щойно написав, міг сказати «змінено з 1 вересня на 1 вересня»;
* хвіст підказки називав ПРИЧИНУ, якої екран знати не може;
* мій пін `role="status"` лишався зеленим, коли роль ішла до сусіднього вузла;
* мій пін скидання слота не був звʼязаний із сіткою, яка цей слот малює;
* мої три піни на гарди доводили існування рядків, а не те, що гард доходить
  до кнопки;
* **вкладеність хвоста в банер я вважав доведеною — вимір показав, що ні**:
  винесений назовні хвіст лишався в межах вікна `[\s\S]{0,400}`, і пін був
  зелений.

І окремо найгірше — **мій сканер не просто слабшав, він РАПОРТУВАВ ПРО
ПОКРИТТЯ**: не знаючи тернарника, пошук гейта вгору крав гейт у сусіда, тож
`CaseModal` отримував `modalOpen` від `BookingModal` трьома рядками вище.
Кількість гейтів лишалась 18, дір — жодної, і зняття `openCaseId` з `busy` було
ПОВНІСТЮ зеленим.

**Висновок для наступної сесії, одним рядком: власний свіжий сторож — такий
самий підозрюваний, як чужий старий.** Два раунди ревʼю по ВЖЕ НАПИСАНІЙ правці
— не формальність і не витрата часу; у цьому пакеті вони дали більше, ніж сама
правка. І перевіряти треба питанням «яка мутація лишить цей пін зеленим», а не
«чи ловить він те, заради чого я його ставив».

---

# ПОСТІЙНА ЧАСТИНА ПРОМПТА

(Повноваження, страховки, режим роботи, пастки середовища. Переноситься з сесії
в сесію, поки власник не вирішить інакше.)

## ПОВНОВАЖЕННЯ: РОЗШИРЕНІ — ДІЮТЬ І В ЦІЙ СЕСІЇ

(Рішення власника від 27.08, підтверджене при передачі роботи в с47–с52.
**Власник підтвердив їх окремо і явно при передачі роботи в с53** — це не опис
минулого, а твої повноваження ЗАРАЗ.)

Ти маєш право сам: правити код, тести, SQL і доки; створювати append-only
міграції і **накатувати їх на прод**; виконувати безпечний прод-SQL; створювати
і прибирати синтетичні прод-дані; комітити, пушити, **створювати і зливати PR**,
**деплоїти**; читати прод-логи; робити живі перевірки в браузері.

### Повноваження НЕ скасовують інженерну обережність

- **Два незалежні раунди ревʼю** на будь-яку зміну, що торкається RLS, грантів,
  `SECURITY DEFINER`, тригерів, авторизації, service-role коду, контрактів
  інтеграцій, конкурентності або прод-міграцій. **Валідуй висновки субагентів
  сам.** Давай раундам РІЗНІ лінзи і не змішуй їх. Перевірені пари лінз:
  «що зламається у наступного» × «чи не переоцінив автор свої заміри» (с50,
  пакети Ф4); **«операційний ризик і досвід оператора» × «фальсифікація: де
  правило невірне і де сторож лишився б зеленим»** (с50, U-72 — дала найбільший
  улов за сесію, і обидві лінзи незалежно знайшли одну й ту саму дірку).
  ⚠️ **Ревʼю знаходить не стільки старі дефекти, скільки ті, які щойно вніс ТИ
  САМ.** У с50 практично всі HIGH були мої власні.
  ⚠️ **Окремий клас: «закрив те, що закривати не треба»** (U-61).
- **Міграції:** номер із леджера (НЕ з папки), файл + смоук + секція
  `=== ВІДКАТ ===` у кінці + dry-run + **сторож попередника** (`do $ledger$`).
  Ніколи не редагувати вже накатану міграцію — гейт штампує md5 ФАЙЛУ.
  Порядок: накат → смоук → `invariants_check()` → `db:gate` → build → деплой →
  жива перевірка.
  ⚠️ **Після `drop`+`create`** — `revoke execute … from anon, public` + **явний**
  `grant … to <ролі>` + **ассерт ACL у ТІЙ САМІЙ транзакції**. Пастка 0122.
  ⚠️ **У секції ВІДКАТ** `delete from migration_ledger` тримати закоментованим
  ДРУГИМ рівнем.
- **Після деплою — жива перевірка на проді**, а не «тести зелені». Обидва
  напрямки, вимірюванням. Якщо пакет не міняв КЛІЄНТСЬКОГО коду — сказати прямо.
- **Сторожа теж треба фальсифікувати.** Базова лінія ЗЕЛЕНА; якір мутації
  перевірений на УНІКАЛЬНІСТЬ; частина мутацій мусить лишитись ЗЕЛЕНОЮ; мутація
  мусить ПОТРАПИТИ в артефакт; кожна червона мутація названа СПЕКОМ, який
  зобовʼязаний почервоніти («щось десь упало» вердикту не дає).
- **Коментар у коді — не джерело істини про сервер.** Читай функцію з живої БД
  (`pg_get_functiondef`) — і **цілком**.
- **Прод-дані:** правки — за явним списком id зі свіжого знімка, з
  before-образами. **Ніяких масових видалень за нечітким предикатом.**
- **Секрети і PII не виводити НІКУДИ.** `npm audit fix --force` — ЗАБОРОНЕНО.

### Коли все одно СПИТАТИ власника

- рішення **незворотне** і зачіпає живих пацієнтів;
- це **продуктове рішення**, а не інженерне (як U-53 і U-67);
- потрібні **облікові дані або доступ**, яких у тебе немає. Паролі не просити.

## РЕЖИМ РОБОТИ: НАПРЯМУ В ПАПЦІ ПРОЄКТУ (вимога власника)

**Працюй НАПРЯМУ в `D:\RadFlowDev`** — не через завантаження файлів, не через
клони, не «підготую текст, а ти встав». Читай, пиши і правь на місці через
Desktop Commander.

**Задіюй ВСЕ, що підключено: конектори (MCP), skills, plugins.** ПЕРШИМ ДІЛОМ
подивись, що реально доступно в сесії. Перевірені в бою:

- **Desktop Commander MCP** — файли/процеси на машині власника:
  `read_file` (offset/length, відʼємний offset = хвіст), `edit_block`
  (ЗАВЖДИ після read), `write_file` (чанками, `mode` ЯВНО — інакше відмова на
  наявному файлі), `start_search`, `start_process` (шелл, git/npm/node).
- **Supabase MCP** — прод-БД, `execute_sql`, ref `rdiqjxzibdqbhwileret`.
  Dry-run міграцій на живих даних — канон (SMOKE_OK у тексті помилки = успіх).
- **Хмарний контейнер (`Bash`)** — чисте оточення: клон із GitHub + `npm ci` +
  повний гейт за **~2 хвилини**. Також зручний для `sleep` під час очікування
  довгих фонових прогонів на машині власника.
- **Незалежні субагенти ×2** — два раунди ревʼю кожного пакета (канон).
  Субагенти читають код прямо з `D:\RadFlowDev` через Desktop Commander — давай
  їм у промпті список DC-тулів і РІЗНІ лінзи. ⚠️ Вимагай від них формат із
  полями «СЦЕНАРІЙ / ВРЕД / ЛІКУВАННЯ» і явне «НЕ ПЕРЕВІРЕНО» там, де не
  дочитали: у с50 це відсікло кілька правдоподібних, але хибних знахідок.
- **Claude Projects** (`project_read`/`project_write`) — хендофф і ключові доки.
- Також: вбудований браузер, Claude-in-Chrome (живі перевірки UI), Figma,
  Google Drive, n8n, Lovable.

## Пастки середовища (перевірені, с43–с54)

⚠️ **Правки в репозиторії під час прогону БУДЬ-ЯКОГО стенда мовчки
відкочуються (с54).** Стенд знімає знімок бойових файлів на старті і відновлює
їх у `finally` — не лише `falsify-all`, а кожен `falsify-*.mjs`. Правка,
зроблена, поки стенд ганяється, зникає без сліду; помітити можна лише звіркою
файлу ПІСЛЯ прогону. Поки стенд іде — не торкатись дерева взагалі.
⚠️ **Робочий запуск довгого прогону в с54 — окремий `.cmd` у `$env:TEMP`
(`cd /d D:\RadFlowDev`, `node scripts/x.mjs > x.out 2>&1`, наприкінці
`echo done > x.done`), запущений як `Start-Process -FilePath
"$env:TEMP\x.cmd" -WindowStyle Hidden`;** чекання — `sleep` у хмарному
контейнері (Cowork), опитування — `Test-Path` маркера, читання результату —
`read_file`. Три повні ревізії і кілька гейтів поспіль без жодного падіння
моста. ⚠️ Але це та сама форма, що НЕ переживає перезапуск Desktop Commander
(див. нижче, с53): якщо міст падав протягом сесії — брати `schtasks`.
⚠️ **PowerShell `>` пише UTF-16, а `node -e` з кирилицею у виводі розсипається:**
звіт/лог, який треба ЧИТАТИ, писати з node самим (`writeFileSync(…, "utf8")`) і
читати `read_file`, а не через `Get-Content`/`type` після `>`.
⚠️ **Хмарна сесія (Cowork), с54:** великий результат `read_file` Desktop
Commander міст зберігає у файл контейнера і віддає ЧАСТИНАМИ — при склеюванні
частин між ними треба ставити `\n`, інакше губляться байти на межах (виявлено
md5). `project_write` з `local_path` бере файл лише з робочої теки контейнера
(`/home/claude/...`); виклик може впертись у 30-секундний таймаут і ВСЕ ОДНО
записати — перед повтором звірити `project_read`/md5, а не писати вдруге.
⚠️ **`falsify-all.mjs` ВІДМОВЛЯЄТЬСЯ стартувати на брудному дереві** — і це
правильно, але легко з'їдає 20 хвилин (перевірено с52: я запустив ревізію з
незакоміченими правками, вирішив, що вона працює, і чекав марно; лог у
`%TEMP%` уже містив відмову). **Порядок такий:** гейт → комміт → ревізія на
чистому дереві. Або `--allow-dirty`, якщо свідомо міряєш робочу копію.
⚠️ **Фоновий запуск, який НЕ впирається в 60-секундний міст:**
`start "" /B cmd /c "node scripts\<файл>.mjs > %TEMP%\<лог>.log 2>&1"` —
повертається миттєво. Далі опитувати `dir` по вихідному звіту і `git status`
(видно, який бойовий файл стенд мутує ЗАРАЗ). ⚠️ Вивід node у файл
блок-буферизований, тож `type` по логу довго показує порожнечу — це НЕ ознака
того, що прогін не йде.
⚠️ **Пауза між опитуваннями: `ping -n 50 127.0.0.1 > NUL &`** — тримається в
межах мосту (55 с), на відміну від `timeout /t`. Два `ping` поспіль в одній
команді вже дають «Device did not respond within 60s».
⚠️ **Один прогін `falsify-u72` ≈ 15–25 хв (с53), повна ревізія 21 стенда
≈ 31–32 хв (с54, тричі на чистому дереві).** Плануй це як фонову роботу, не як
крок.

⚠️ **Повний гейт на цій машині 3–6 хвилин** (2358 тестів у с54), на чистому
клоні в хмарі ~2 хв. Стенд із 37 мутацій — **~3 хвилини**. Точковий прогін двох-трьох
спеків — ~1 с. Плануй очікування маркером, а не переопитуванням.
⚠️ **Гейт одним фоновим прогоном:** `start /B cmd /c scripts\full-check.bat`,
далі опитувати маркер `chk.done`.
⚠️ **`start "" /b` всередині `cmd /c "…"` НЕ спрацьовує** через вкладені лапки —
працює форма БЕЗ порожнього заголовка: `start /B cmd /c scripts\<файл>.bat`.
⚠️ **Екранування лапок у `cmd /c "… \"…\" …"` розсипається** — довгі команди з
кирилицею і лапками ЗАВЖДИ файлом (`.bat` або `-F`).
⚠️ **`%ERRORLEVEL%` у ланцюжку через `&` розкривається ДО запуску** — перевіряй
через `&&`/`||`.
⚠️ **`git commit -m` з кирилицею і дужками розсипається** — повідомлення
**тільки файлом** (`-F .commitmsg`, у `.gitignore`). ⚠️ `echo` пише не-UTF-8:
файл повідомлення створювати `write_file`, а не `echo`.
⚠️ **`git merge --ff-only` падає**, коли в `main` є мерж-коміти — `--no-ff -F`.
⚠️ **`node -e` під cmd/PowerShell розсипається на лапках** — скрипт ЗАВЖДИ
файлом.
⚠️ **`head`, `tail` у цьому шелі ВІДСУТНІ**; `findstr` не бачить кирилиці;
`powershell -Command` через `cmd /c` з лапками теж розсипається. Хвіст файлу —
`read_file` з відʼємним `offset`.
⚠️ **Міст DC не витримує очікування > 60 с** у передньому плані. Довгі паузи —
`sleep` у хмарному контейнері або `powershell -NoProfile -Command "Start-Sleep 50"`.
⚠️ **ТАЙМАУТ ІНСТРУМЕНТА НЕ СКАСОВУЄ ЗАПУЩЕНУ КОМАНДУ** (перевірено с52,
коштувало зіпсованого виміру). `start_process` повернув «Device did not respond
within 60s» — процес на машині ПРОДОВЖУЄ працювати. Якщо після цього запустити
те саме вдруге, два стенди почнуть мутувати ті самі бойові файли, і другий
побачить чужу мутацію як «базова лінія червона». Після будь-якого таймауту:
`Get-Process node` / `wmic process where "name='node.exe'"` — і ТІЛЬКИ потім
вирішувати, чи перезапускати.
⚠️ **ПЕРЕЗАПУСК DESKTOP COMMANDER УБИВАЄ ВЕСЬ ДОЧІРНІЙ ПРОЦЕС-ТРЕЙ** (с53,
коштувало 15 стендів прогону і отруєного `lib/incidents.ts`). Фоновий запуск
через `Start-Process ... -WindowStyle Hidden` із шела DC НЕ переживає падіння
моста: коли DC піднімається наново, усі його нащадки зникають, а стенд лишає
бойовий файл МУТОВАНИМ. **Надійний спосіб — одноразова задача планувальника:**
`schtasks /create /tn <імʼя> /tr "<bat>" /sc once /st <час> /f` і `schtasks /run
/tn <імʼя>` (сам bat пише `write_file`; попередження «/ST передує поточному
часу» нешкідливе, `/run` стартує задачу негайно). Перевірено на трьох довгих
прогонах поспіль — переживає і таймаути моста, і перезапуск DC. Після прогону
задачу прибрати: `schtasks /delete /tn <імʼя> /f`.
⚠️ **`Start-Sleep` довше ~55 с у переднім плані валить виклик моста** — пауза
або 50 с, або `sleep` у хмарному контейнері (там ліміт значно більший).
⚠️ **`taskkill /F` по РАННЕРУ вбиває і дочірній стенд** (с52). Обробники
сигналів у стенда при цьому НЕ відпрацьовують, тож бойовий файл лишається
МУТОВАНИМ — рівно те отруєння, проти якого писався сторож дерева. Після
зупинки прогону ОБОВʼЯЗКОВО `git status` і, якщо в дереві бойовий файл,
`git diff` (переконатись, що це мутація стенда) + `git checkout --` саме по
ньому. Коректніше — не вбивати ранер, а дочекатись кінця поточного стенда.
⚠️ **`timeout /t N` НЕ спить, коли ввід перенаправлено** («перенаправление
ввода не поддерживается», миттєвий вихід) — а через `cmd /c` він завжди
перенаправлений. Пауза: `powershell -NoProfile -Command "Start-Sleep -Seconds N"`.
⚠️ **`%TIME%` у `cmd /c "… & …"` розкривається ПРИ РОЗБОРІ рядка**, тож
`echo %TIME% & sleep & echo %TIME%` друкує два однакові числа і створює
враження, що пауза не спрацювала. Потрібне `cmd /v:on /c "… !TIME! …"`.
⚠️ **Вивід ноди в файл через `>>` блочно буферизований** (~4 КБ): лог відстає
від дійсності на кілька стендів. Прогрес довгого прогону дивитись по
`git status` (який файл зараз мутований), а не по хвосту логу.
⚠️ **Вивід тестів може перевищити ліміт токенів** — писати в лог-файл і читати
хвіст, а не тягнути в контекст.
⚠️ **Блочний коментар не терпить `*/` усередині** — будь-яка пара «зірочка +
слеш» у шляху закриває його, і файл падає з помилкою НИЖЧЕ за місце правки.
⚠️ **`{/* … */}` не можна ставити одразу після `{cond && (`**.
⚠️ **DC відвалюється просто ПОСЕРЕД правки.** Не гатити повторно: після
повернення ПЕРЕЧИТАТИ файл і звірити, що доїхало.
⚠️ **`start_search`** віддає результати ТІЛЬКИ через `get_more_search_results`;
рядок-збіг у виводі підміняється самим патерном; `filePattern` через кому не
працює.
⚠️ **Vercel деплоїть ~2–3 хв.** Для пакетів БЕЗ клієнтського коду ознаки «новий
бандл доїхав» немає в принципі.
⚠️ **`supabase-js` захоплює `fetch` при створенні клієнта.**
⚠️ **MCP-інструменти фіксуються на СТАРТІ сесії.**
⚠️ **Вбудований браузер НЕ авторизований у RadFlow**; жива перевірка продукту —
через Claude-in-Chrome; deep-link `/queue?date=YYYY-MM-DD&entry=<uuid>`.

## Спершу прочитай (у цьому порядку)

1. **`AGENTS.md`** — стабільні правила. Розділ «Конвенції коду» містить канон
   часу (пастка 0122 — у розділі про міграції).
2. **`claude/radflow-handoff.md`** — durable-стан, НАЙСВІЖІШЕ (кінець с54).
   Починається блоком «СОСТОЯНИЕ НА КОНЕЦ с54»; нижче — блоки с53, с52, с51,
   с50 у зворотному порядку. Копія в Claude Projects (`project_read
   claude/radflow-handoff.md`, md5 звірено з репо) — там же посесійні доки
   `claude/session*` (для с54 — `claude/session54-g1f-server-clock-trust.md`).
3. ⛔ **`docs/HANDOVER.md` — НЕ джерело правди** (місцями просто бреше, напр.
   «vitest — 59 тестів»); §6 цінний — там «чому так».
4. **`docs/PRODUCT_OVERVIEW.md`** — продукт і еволюція схеми. §7 доповнений
   0169 і вузлом часу, §8 — U-70/U-72, U-73 і протухлим фетчем.
5. **`docs/audit/RADFLOW_DEEP_TECHNICAL_FUNCTIONAL_AUDIT_2026-08-27.md`** —
   журнал аудиту. Фаза 2 закрита; фаза 4 закрита ПО ПУНКТАХ Ф4-1…Ф4-11, але
   `cas`-сценарій і живі перевірки лишаються відкритими хвостами. Фази 6 і 8 не
   починались, **вердикт не винесений**.
6. **PR-доки с50:** `PR-F4-concurrency-realtime-time.md`,
   `PR-F4-2-late-call-mirror.md`, `PR-F4-8-server-clock.md`,
   `PR-U-70-wall-canon-server-clock.md`, `PR-U-72-forms-follow-today.md`.
   **PR-доки с51–с54** (усі в `docs/audit/`): `PR-live-checks-s51.md`,
   `PR-U-74-falsify-verdict.md`, `PR-U-80-false-green-stands.md`,
   `PR-U-77-78-79.md`, `PR-F2-F7-silent-shift.md`, `PR-G1A-case-day-stop.md`,
   `PR-U-74-part2-stand-revision.md`, `PR-U-80b-named-guard.md`,
   `PR-G1-BCD-silent-day-shift.md`, `PR-G1E-board-day-shift.md`,
   `PR-G1G-one-day-shift-rule.md`, `PR-G1F-server-clock-trust.md`.
7. **`docs/ops-cron.md`** (реєстр 10 задач).

⚠️ **І не спирайся на ЦЕЙ файл теж.** Перевіряй і хеші (`git ls-remote`), і
ПЕРЕДУМОВИ задач (`select now()`).

## Правила, які не можна порушувати

- **Міграції накатуєш ТИ** (Supabase MCP `execute_sql`).
- Номер — із `select max(name) from migration_ledger`, НІКОЛИ не з папки.
- Файл + смоук + секція `=== ВІДКАТ ===` у КІНЦІ + dry-run + сторож попередника
  + два незалежні ревʼю.
- **Dry-run:** тіло в `do $$…$$` БЕЗ внутрішнього commit; факт відкату
  перевіряти ОКРЕМИМ запитом.
- **Смоук: асерти лише `is distinct from`**; «RLS мовчки зʼїв» ловиться ТІЛЬКИ
  через `get diagnostics row_count`. Смоук звіряє ДЕЛЬТУ.
  ⚠️ Смоук проти ЖИВОГО прода бере AccessExclusiveLock — `set local
  lock_timeout` обовʼязковий на грантах і політиках.
- Новий інваріант — у `invariants_check()`, не лише в смоук.
- Правки у файл міграції — ДО `npm run db:gate`.
- `security_invoker=true` на VIEW = ЗАМОК. **dev і prod — ОДНА БД.**
- **ГЕЙТ ГОНЯЄ ХТОСЬ ОДИН** (`.next` спільний).
- **Вкладені модалки**: батьківський `useModalA11y` глушити параметром `active`.
- **Час:** нове порівняння з серверним моментом або новий показ часу —
  `serverNow()`, а не `Date.now()`. Нова заморожена дата, виведена з «сьогодні»,
  — через `lib/useFollowToday.ts`.

## Правила тестів

- Компонентних тестів НЕМАЄ: `vitest.config.ts` — `environment: "node"`,
  TZ прибита `Europe/Kyiv` (навмисно НЕ UTC — половина часових багів проєкту
  про зсув доби, і в UTC вони не відтворюються).
- **Роути тестуються ПОВЕДІНКОВО** (`tests/fixtures/fakeSupabase.ts`).
  **Двійник мусить лишатись злим:** кидає на будь-якому нереалізованому фільтрі.
- **Regex по вихідному коду — НЕ сторож.** Перевіряй ВИКЛИКОМ. Статичний пін
  лишається легітимним лише там, де поведінкового інструменту немає в принципі
  (місця вживання в компонентах) — і тоді це треба НАЗВАТИ як межу.
- **Фікстура мусить РОЗРІЗНЯТИ дві реалізації**, інакше перевірка порожня.
- **Тавтологічний тест виглядає як сторож.** Мисленно ВІДКОТИ фікс і назви, що
  почервоніє.
- **Сторож, що шукає ІМЯ, ловить рядок імпорту.** Сторожити треба ВИКЛИК.
- **Два тести з ОДНАКОВИМ імʼям** знімають вимогу «назви, який почервонів».
- **Фальсифікацію читай JSON-репортером**, не текстом.
- ⚠️ **`npm run lint` із `--max-warnings 0`** — будь-яке попередження червоне.

## Інструменти (готові)

```
scripts\full-check.bat                  # повний гейт одним фоновим прогоном
node scripts/falsify-<стенд>.mjs        # один із 21 стендів (перелік — у falsify-all.mjs)
node scripts/falsify-g1f.mjs            # стенд Г1-F (с54): 32 позиції, 27 адресних
node scripts/falsify-u72.mjs            # стенд U-72: ~140 позицій, 69 адресних, 15–25 хв
node scripts/secret-scan.mjs            # скан історії на секрети
node scripts/secret-scan.mjs --selftest # перевірка самого сканера
node scripts/integration-admin.mjs list --clinic <uuid>
node scripts/race-check.mjs plan / run --run --n 4     # cas ще НЕ ганявся
npm run db:gate        # штампує md5
npm run db:gate:check  # звіряє без штампування
npm run build          # deploy-гейт + next build
select public.invariants_check();
```

⚠️ live-check **не з контейнера** (домен не в allowlist).

## Порядок ведення роботи

1. Прочитай `claude/radflow-handoff.md` — найсвіжіший.
2. Звір стан: `git ls-remote origin refs/heads/main refs/heads/dev`
   (⚠️ `git fetch` НЕ оновлює remote-tracking!), `git status`, БД через
   `execute_sql`, `select now()`. **Не вір цьому файлу.**
3. Узгодь із власником напрямок, склади задачник (TaskCreate).
4. Роби напряму в `D:\RadFlowDev` — **один пакет за раз**, тулчейн і два ревʼю
   між ними.
   ⚠️ **Ревʼю — по ВЖЕ НАПИСАНІЙ правці, і власний свіжий сторож у ньому такий
   самий підозрюваний, як чужий старий** (урок пакета 9: ревʼю знайшло шість
   дефектів у моєму щойно написаному коді, серед них сканер, який рапортував
   про покриття, якого немає). Лінза другого раунду — завжди «яка мутація
   лишить цей пін зеленим», а не «чи ловить він те, заради чого я його ставив».
5. **Комітиш, мержиш і деплоїш сам.** Після деплою — жива перевірка на проді.
   Текст PR усе одно пиши: він лишається в `docs/audit/`.
   ⚠️ **Порядок навколо ревізії:** гейт → комміт → `falsify-all` (вона
   відмовляється стартувати на брудному дереві) → мерж → пуш → деплой.
6. Наприкінці сесії онови `claude/radflow-handoff.md`, `docs/PRODUCT_OVERVIEW.md`
   (§7/§8), `AGENTS.md` (якщо зʼявився новий інваріант), цей файл — і копії в
   Claude Projects.
