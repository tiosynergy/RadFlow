-- ============================================================
--  RadFlow — Міграція 0021: авто-розблокування простою (auto_unblock)
--  Запускати у Supabase → SQL Editor ПІСЛЯ 0020_no_booking_during_incident.sql.
--
--  Прапорець на інциденті (поломка/ТО):
--    • auto_unblock = true  (типово) — кабінет автоматично розблокується,
--      коли настане blocked_until (час завершення). Очерга відновлюється сама.
--    • auto_unblock = false — розблокування ЛИШЕ вручну («🔓 Розблокувати»),
--      blocked_until трактується як орієнтовний; до зняття кабінет лишається
--      заблокованим (для бронювань — недоступним без обмеження в часі).
--  Старі рядки отримують true (поведінка за замовчуванням).
-- ============================================================

alter table public.incidents
  add column if not exists auto_unblock boolean not null default true;
