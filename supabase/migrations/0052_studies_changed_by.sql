-- ============================================================
--  RadFlow — Міграція 0052: хто змінив дослідження (studies_changed_by)
--  Запускати у Supabase → SQL Editor ПІСЛЯ 0051_waitlist_room.sql.
--
--  Навіщо: у дошці направника зміни складу досліджень підписувались жорстко
--  «змінено клінікою» — навіть коли їх зробив сам направник. Тепер зберігаємо
--  автора останньої правки складу досліджень:
--    'clinic'   — правив персонал центру (адмін/реєстратор/радіолог);
--    'referrer' — правив лікар-направник.
--  UI підписує зміну відповідно (клініка бачить «змінено направником», направник —
--  «змінено клінікою»), обидві сторони синхронізуються realtime.
--  NULL = склад не змінювали від початкового (studies_original).
--
--  Вільний text (без enum) — значення виставляє server action editQueueEntryStudies
--  за роллю викликача. Безпечна для повторного запуску.
-- ============================================================

alter table public.queue_entries
  add column if not exists studies_changed_by text;
