-- 0186_claim_token_marked_dead.sql
-- ============================================================================
--  RadFlow — Міграція 0186: `waitlist_entries.claim_token` оголошена МЕРТВОЮ.
--  Запускати ПІСЛЯ 0185. Даних не змінює. Схеми не змінює. Тіл не передруковує.
--  Змінює РІВНО ОДНЕ: коментар до колонки.
-- ============================================================================
--
--  ЩО ЦЕ ЗА КОЛОНКА І ЧОМУ ВОНА ВВОДИТЬ В ОМАНУ
--  --------------------------------------------
--  0089 завела `claim_token` як транзієнтну мітку застовплення: клієнт
--  генерував uuid, ставив його рядку листа очікування, і далі rollback/link
--  чіпали ЛИШЕ власний claim. Механізм захищав від ДВОХ операторів, що
--  розбирають одного кандидата.
--
--  0100 цей механізм ЗАМІНИЛА цілком: увесь перенос став ОДНІЄЮ транзакцією
--  всередині `schedule_from_waitlist_rpc` (CAS-застовплення `where status =
--  'waiting'` → вставка запису → `scheduled_entry_id`). Проміжного стану інші
--  транзакції більше не бачать, тож окремий токен став непотрібним — і сама
--  0100 це прямо каже: «але тут чистимо в null».
--
--  ⚠️ ПРОБЛЕМА НЕ В КОЛОНЦІ, А В КОМЕНТАРІ. У проді досі стоїть текст 0089 —
--  він описує МЕХАНІЗМ, якого немає шість років міграцій. Той, хто читає схему
--  (DBA, наступний агент, аудит), бачить живий claim-токен і резонно вважає,
--  що на нього щось спирається. Це рівно той клас дефекту, який ця сесія вже
--  ловила в коді: коментар, що описує неіснуючий рубіж, дорожчий за відсутній.
--
--  ЗАМІР 11.09.2026 (запитом, не переказом):
--    • `select count(claim_token) from public.waitlist_entries` → **0** із 6;
--    • обʼєктів БД, що згадують колонку, — РІВНО ДВА, обидва SECURITY DEFINER,
--      і обидва тільки ПИШУТЬ у неї `null`, жоден не читає:
--        `schedule_from_waitlist_rpc` — `set status='scheduled', claim_token = null`;
--        `set_waitlist_status_rpc`    — `set status='waiting', …, claim_token = null`;
--    • індексів, політик, constraint-ів і вʼюх із нею — НЕМАЄ (звірено по
--      pg_indexes, pg_policies, pg_constraint, pg_get_viewdef);
--    • у репозиторії колонку не пише і не читає жоден рядок застосунку —
--      лише `supabase/types.ts` (оголошення) і два коментарі в тестах.
--
--  ЧОМУ НЕ DROP — РІШЕННЯ ВЛАСНИКА 11.09.2026
--  ------------------------------------------
--  DROP COLUMN потребує ПЕРЕДРУКУ обох definer-тіл (з них треба прибрати
--  `claim_token = null`), а `schedule_from_waitlist_rpc` — це гарант гонки за
--  кандидата (55000 WAITLIST_STALE), єдиний у проєкті, написаний РУКАМИ, а не
--  породжений двигуном. Плюс ledger, `supabase/types.ts`, перепін md5 у
--  `guardFnBodiesInvariant` і смоук-пінів. Функціональної вигоди — нуль:
--  колонка nullable, порожня, нічим не читана, грантів на неї 0102 не давала.
--  Тобто ціна — blast radius на найкритичнішому RPC листа очікування, вигода —
--  мінус одна мертва колонка. Обмін поганий, і його не роблять.
--
--  ⚠️ ЩО ЛИШАЄТЬСЯ НЕЗАКРИТИМ, названо вголос: колонка існує, і теоретично в
--  неї можна записати значення прямим запитом службової роллю. Це НЕ дірка:
--  0102 зняла табличний UPDATE у `authenticated`/`anon` і видала поколоночні
--  гранти лише на редаговані колонки, `claim_token` серед них немає. Клієнт до
--  неї не дістанеться; службова роль і так може все.
--
--  ⚠️ ЧОМУ МІГРАЦІЯ, А НЕ ПРОСТО КОМЕНТАР У `types.ts`. Коментар у репозиторії
--  побачить той, хто читає репозиторій. Схему ж читають і повз нього — через
--  Supabase Studio, `\d+`, дампи. Брехня стоїть У БАЗІ, і виправляти її треба
--  в базі. Відтворюваність цього кроку і дає міграція.
-- ============================================================================

begin;

do $ledger$
begin
  if not exists (select 1 from public.migration_ledger
                  where name = '0185_schema_digest.sql') then
    raise exception '0186 потребує 0185 (накатуйте по порядку)';
  end if;
  if exists (select 1 from public.migration_ledger
              where name = '0186_claim_token_marked_dead.sql') then
    raise exception '0186 вже накатана';
  end if;
end
$ledger$;

-- ============================================================================
-- 1. Єдина зміна міграції: коментар більше не описує живий механізм.
--    ⚠️ `comment on` не чіпає ні `pg_attribute`, ні constraint-и, тож перевірка
--    №23 `schema_digest` (0185) від цього не рухається — вона будується на
--    каталозі колонок і констрейнтів, а не на `pg_description`. Звірено
--    прогоном `invariants_check(false)` до і після: 23/23, failed [].
-- ============================================================================
comment on column public.waitlist_entries.claim_token is
  'МЕРТВА з 0100 (позначено 0186). Механізм claim-токена 0089 замінено однією '
  'транзакцією в schedule_from_waitlist_rpc: CAS waiting→scheduled, вставка '
  'запису і scheduled_entry_id роблять атомарно, проміжного стану ніхто не '
  'бачить. Колонку НІХТО НЕ ЧИТАЄ; два SECURITY DEFINER RPC лише пишуть у неї '
  'null. Заміряно 11.09.2026: 0 рядків зі значенням, жодного індексу, політики, '
  'constraint-а чи вʼюхи. НЕ покладайтесь на неї і не починайте писати — '
  'гарантію неперетину дає CAS у тілі RPC, а не цей токен. DROP свідомо НЕ '
  'зроблено: він вимагає передруку обох definer-тіл, одне з яких — гарант '
  'гонки за кандидата (55000 WAITLIST_STALE).';

-- ============================================================================
-- Самореєстрація (канон 0142) — ОСТАННІЙ statement перед commit
-- ============================================================================
insert into public.migration_ledger (name)
values ('0186_claim_token_marked_dead.sql')
on conflict (name) do nothing;

commit;

-- ============================================================================
-- === ПІСЛЯ НАКАТУ ===
-- ============================================================================
-- 1. select col_description('public.waitlist_entries'::regclass, a.attnum)
--      from pg_attribute a
--     where a.attrelid = 'public.waitlist_entries'::regclass
--       and a.attname = 'claim_token';
--    → текст мусить починатись на «МЕРТВА з 0100».
-- 2. select public.invariants_check(false);  →  ok:true, checked:23, failed:[]
-- 3. npm run db:gate   ← інакше рядок у ledger лишиться без md5, і deploy-гейт
--    завалить build (перевірка №7 `ledger_md5`).
-- ============================================================================
-- === ВІДКАТ ===
-- ============================================================================
-- ⚠️ Відкат повертає в базу коментар, який описує неіснуючий механізм.
--
-- begin;
--   comment on column public.waitlist_entries.claim_token is
--     'Транзієнтний токен застовплення (scheduleFromWaitlist): щоб rollback/link '
--     'чіпали лише власний claim. null у стані спокою.';
--   delete from public.migration_ledger where name = '0186_claim_token_marked_dead.sql';
-- commit;
--
-- ⚠️ ВІДКАТ У РЕПОЗИТОРІЇ (без цього дерево бреше про базу):
--   1. supabase/types.ts: прибрати коментар про мертву колонку;
--   2. видалити цей файл і перезапустити `npm run db:gate`.
-- ============================================================================
