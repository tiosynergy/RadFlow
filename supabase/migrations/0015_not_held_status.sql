-- ============================================================
--  RadFlow — Міграція 0015: окремий статус «Не відбулося» (not_held)
--  Запускати у Supabase → SQL Editor ПІСЛЯ 0014.
--
--  Розділяємо три негативні результати запису:
--    • no_show   — «Неявка» (пацієнт не прийшов);
--    • not_held  — «Не відбулося» (НОВЕ; не відбулось з інших причин);
--    • cancelled — «Скасовано» (запис скасовано).
--  Додаємо лише нове значення enum — наявні дані не змінюються (безпечно).
-- ============================================================

alter type public.queue_status add value if not exists 'not_held';
